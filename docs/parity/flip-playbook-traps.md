# Parity flip-playbook trap index

> **Status:** `reference` — the durable in-repo index of the parity-flip
> playbook's numbered traps (1–37). Promoted from the team-memory note
> `superbot-next-parity-flip-playbook` per inbox ORDER 015 (2026-07-12).
> The playbook note stays the full prose home; this index is the
> reachable in-repo map so `trap N` citations in code, goldens, session
> cards and `control/status.md` resolve without team-memory access.
> Lettered cites (e.g. trap 14d, 15f) are sub-items of the parent entry.

Sibling docs: [flag-13-disposition-2026-07-10.md](flag-13-disposition-2026-07-10.md)
(the three ruled corpus-red classes); `docs/decisions.md` for the D-NNNN
rulings the traps cite.

## The index

1. **Ratchet writes** — `tools/check_parity_depth.py --write-ratchet` splices ONLY the `depth.ratchet` block since #199 (merge `029b4001`; pre-#199 it wiped parity.yml's ~129-line comment header). Flip+ratchet still lands as the deliberate LAST commit of a flip PR.
2. **Sim-gate rows via overlays, never hand-edits** — new manifest commands need `manifest/layout/<name>.lock.json` legacy-seed rows then `check_sim_gate --write-baseline`; AMEND lock files additively — overwriting strands old pins.
3. **Oracle access** — menno420/superbot denies direct `get_file_contents` in worker sessions; `mcp__github__search_code` with a `repo:` qualifier works (see 15f for the phrase-not-path recipe).
4. **DeferMode** — shipped slash surfaces that answered type-4 direct need `DeferMode.NONE` or the resolver auto-defers (type-5 + followup, red goldens). Inverse case: trap 14a.
5. **Session-lifecycle panels** — `_mint_ephemeral` (#117) mints `<cid:N>`-shaped ids and `_record_anchor` (#118) skips anchors; no disposition class needed for `<cid:N>` (the `<cid:N>` analysis, `docs/decisions.md`).
6. **Q-0194 ride-along** — session card + telemetry row in the SAME PR or CI holds the merge.
7. **A-16 R2 is not always vacuous** — declared stores/events/settings the goldens never touch red `check_parity_depth` at flip; the sanctioned move is `depth.exemptions` rows under an EXISTING reason class (closed vocabulary), never a new class.
8. **Prefix subcommands** — land as `group="<parent>"` CommandSpecs; qualified_name joins with a space and the parity boot's 3-token prefix lookup dispatches them.
9. **renderer_override scope** — may adjust more than the footer (inline fields, per-component `disabled`) if each adjusted surface is named in the justification; match components by CANONICAL `<panel_id>.<action_id>` ids (overrides run before `_mint_ephemeral` on open and before the refresh remap).
10. **Cleanup-flip lessons (#140)** — (a) goldens can pin PROCESS-MEMORY leakage from the shipped bot (the `_word_cache` precedent; the tell is an alphabetically-earlier sweep case that wrote the value; graduates to a seeded trajectory under trap 20); (b) Discord-READ effects need a domain read port + capture twin recording fake_http's wire shape; (c) A-16 R2's SETTINGS dimension is dead code today — never hunt setting exemptions; (d) every new home hub needs its `HUB_NAV_LABELS` entry in `sb/kernel/panels/render.py`; (e) rebase races: `manifest.snapshot.json` via `manifest_compile.py --write`, `telemetry/model-usage.jsonl` keep BOTH lines — a local rebase now does that automatically (`.gitattributes` `merge=union`; GitHub's server-side merge still shows the conflict — runbook: `docs/operations/telemetry-merge-conflicts.md`).
11. **xp-flip lessons** — (a) CDN/asset goldens need the capture twin to record fake_http's ALIAS LITERAL; (b) goldens can pin capture-environment failure artifacts (the shipped global on_command_error copy) — port as a handler-owned golden-pinned literal + in-code note; (c) attachment-bearing goldens pin only `{"_files": [filename]}` — delegate render + ADD the RenderedAttachment; (d) `session_lifecycle=True` also kills an unexpected `panel_anchors` db_delta row; (e) shipped optional read seams port with the oracle's own degradation posture.
12. **proof_channel-flip lessons (#145)** — (a) the composition-parity burn-down list cuts BOTH ways: registering an ensure-only ref requires PRUNING its `_KNOWN_ENSURE_ONLY` row; (b) `table:<name>` covered-elsewhere has a no-golden-carries-the-table flavor citing named sibling sweeps; (c) state-dependent embed DESCRIPTIONS are renderer_override territory; (d) reshapes with no custom_id_overrides and no new commands need ZERO sim-gate/compat rows; (e) check `STYLE_TOKEN_COLORS` before assuming a new token.
13. **CI/merge mechanics (#145)** — (a) Actions can silently DROP pull_request synchronize events for one branch (~50 min observed); diagnose via branch-filtered run listing + a sibling check, burn at most one empty commit; (b) `get_check_runs` total_count 0 means "no suites yet for this head SHA", not "no CI"; (c) when main moves mid-flip: merge origin/main (never rebase pushed history), parity.yml keep-both, burn-down keep-both-PRUNES, snapshot via `manifest_compile --write`, rerun the ladder.
14. **economy-flip lessons (#152)** — (a) trap 4's INVERSE: a shipped deferring slash panel needs `DeferMode.AUTO` + `ReplyVisibility.EPHEMERAL` declared; (b) the capture twin's `_embed_payload` may lack a field the live presenter serializes (thumbnail) — check transport.py against panel_view.py first; (c) delegation-style override: `render_panel` for components then `dataclasses.replace` the embed; (d) `--write-ratchet` counts are RAW covered-side counts, not declared∩covered; (e) shipped "reads that write" port as idempotent ensures — VERIFY per-surface; (f) "no golden can carry the table" has three grounds with three homes (env-keyed-integration ×2, input-vocabulary: the modal-driven and select-driven ledger classes); (g) codex can be usage-limited — merge on green, record the non-review (Q-0258); (h) PanelRef-routed overrides read author lines via the guild_directory port or degrade to none; (i) selector option dicts pass through the render verbatim.
15. **ticket-flip lessons (#154)** — (a) goldens can pin a wire `emoji` field NEXT TO the label — check the wire shape before assuming glyphs-in-labels; (b) all-guard-path rows flip with empty declarations and ZERO exemptions — never mint dead tables; (c) the `invoking-message-deletion` disposition already absorbs the shipped delete-the-invoker tails; (d) a brand-new subsystem needs the full birth kit in ONE PR (see also 34); (e) `HUB_NAV_LABELS` grows one golden-cited label at a time, `home_hub` pinned explicitly; (f) oracle reconstruction: search_code exact PHRASES — full-filename `path:` returns 0; (g) prefix GROUP subcommand aliases ride `CommandSpec(aliases=...)`.
16. **karma-flip lessons (#157)** — (a) never `except TypedRefusalError` around `engine.run` — the K7 engine classifies leg exceptions; the sanctioned refusal lane is a `ctx.params` side-channel; (b) the shipped red error envelope ports as a zero-component session-lifecycle panel with renderer_override (`karma.error_card`); (c) don't "improve" shipped cooldown semantics; (d) any new ledger store with the S14 `mutation_id` key joins the kernel-surface-drift `columns:` disposition; (e) `run_golden_parity.py` has no `--only` — import and call `_replay_corpus({"<subsystem>"})`; (f) declared `ReplyVisibility.EPHEMERAL` already type-5-defers; (g) `STYLE_TOKEN_COLORS` carries magenta/dark_red.
17. **The setup create-channel wall (HISTORICAL — resolved by #245, see 37)** — the leaked-workspace flavor: a golden sending into a `<msg:N>` channel with no create in its own calls means an earlier capture case created it and the gateway cache carried it across the per-case truncate; parked setup whole (A-16 full-corpus door) until the trap-37 re-home + `CAPTURE_WORLD_CHANNELS` seeding resolved it.
18. **moderation-flip lessons (#163)** — (a) effect-leg failure cannot suppress the op's domain event — a "call recorded, no row, no event" golden forces call-Discord-first INTO the record leg (the effect-leg boundary ruling, `docs/decisions.md`); (b) capture twins may need to RAISE AFTER RECORDING (CaptureArtifact); (c) capture-world guild config IS reseedable world state (`CAPTURE_WORLD_SETTINGS`); (d) the shipped did-you-mean matched SYNONYM tokens, not bare names; (e) a design-wins ConfirmationSpec on a golden-exercised command cannot survive its flip; (f) ratchet counts raw covered-side (14d confirmed); (g) removing a command: manifest row out, seed row removed, `--write-baseline`, `check_compat_frozen --write`.
19. **K1 claims panel action_ids BARE and cross-subsystem (#167)** — internal tokens collide across subsystems; rename the internal action_id and pin the shipped wire byte via `custom_id_override`.
20. **Process-local render state is mode-dependent (#167)** — gate/report/isolation replay different case sets, so accumulated counters/caches render differently per mode: RUNNER-SEED the trajectory (`CAPTURE_WORLD_COUNTERS`), never accumulate.
21. **`SelectorSpec` has no `visible_when`** — state-keyed select visibility takes the renderer_override component-drop lane (the component-drop wording, `docs/decisions.md`), not grammar gating or schema growth.
22. **"Let the K7 op raise" is a proven red class (#170)** — a golden pinning a ❌/ℹ️-prefixed plain-content reply that the port renders as `Missing/invalid argument: …` means the guard belongs in the handler, shipped-cog style.
23. **Depth class `guard-only-capture`** — fits pending rows whose capture sweeps drove every subcommand argument-less; cite the ledger class rather than re-arguing (but see 35b for its honesty boundary).
24. **Capture-sha drift (#173)** — search_code reconstructs the oracle's DEFAULT branch, which can be ahead of the corpus sha; diff reconstructed fragments against the golden FIRST, pin the corpus, ledger the drift.
25. **Serial ladder** — report/pytest/isolation-replay must never run concurrently on the shared Postgres; raced runs produce phantom diffs (#178/#179).
26. **type-4 empty components (#178)** — discord.py omits the `components` key on type-4 responses with no view, so the twin must too; slash+PanelRef resolver defaults to `DeferMode.NONE`; `_replay_corpus` is a coroutine — wrap in `asyncio.run`.
27. **Trap 25 extends to FILE mutation (#183)** — nothing backgrounded while ratchet files are in a scratch state.
28. **Grep `parity/goldens/_sweep_skips.json` FIRST on every new row** — a listed capture-skip means the subcommand is deliberately undeclared; but the verdict is per-surface, not automatic undeclare (35a).
29. **`mergeable_state: dirty` spawns ZERO check runs (#186)** — presents exactly like trap 13a; check mergeable_state first; the fix is the forward-merge, not empty commits.
30. **CLOSED by #197 (`2d65739`)** — `check_sim_gate` originally missed VALUE drift on existing [A] pins (#190); it now reds overlay-masks-manifest drift and `--write-baseline` refuses.
31. **LayoutSpec gains mint the 3 arrangement lock rows** (12d nuance); emoji wire shape has TWO shipped forms (15a); a dropped FieldsBlock call site makes its provider ensure-only — register at module import (#111 doctrine).
32. **Minted goldens (the mint-strip ruling, `docs/decisions.md`)** — corpus growth inverts the honesty problem: mint via `capture_case`, STRIP the kernel-spine surfaces, verify every user-facing byte against the oracle BEFORE committing, record `minted_goldens: N` + grow the count pins.
33. **ticket-admin re-home (#200)** — options-source-less `SelectorKind.ROLE` maps to native RoleSelect (wire type 6); new engine store markers need `store.ensure_refs()` in ENSURE_REFS or clear_ref_table order-flakes; oracle-wins on bare DELETE misses (unconditional ack, no invented miss branch).
34. **starboard birth (#211)** — the birth kit ALSO needs a `verification/verified_live.yml` roster row (`<key>: unverified`, V4 gate); `check_symbol_shadowing` reds duplicate public read names across store/service — use `get_*_row` renames.
35. **wave-6 games-family (#219/#224/#226/#227/#230)** — (a) skip-listed-but-declared is NOT always a contradiction — per-surface verdicts when golden-pinned bytes depend on the lane existing; (b) exemption-class honesty: guard-only-capture needs an actually-captured bare invocation, otherwise env-keyed-integration or time-driven; covered-elsewhere may cite NAMED `_unmapped` siblings; never undeclare a surface a reviewer calls dead without source; (c) content-triggers firing on the command message arm LIVE in the shared message feed AND in the harness boot; (d) static-id hubs ride `custom_id_override` with NO `_mint_ephemeral`; (e) trust the golden's flags byte over recon "public" labels.
36. **wave-7 money-race family (#233/#236/#240)** — (a) date-seeded daily surfaces seed the capture-day value via `CAPTURE_WORLD_*` runner maps, never a datetime monkeypatch; (b) money-race domains flip cleanly when the diff never touches the store/ops locking legs; (c) forward-merge migration-number collisions: renumber to the next free slot, keep BOTH checksums.json rows; (d) read the golden's anchor block BEFORE choosing the panel lifecycle; (e) loot/RNG determinism rides the runner-reseeded module-global stream; (f) `pytest tests/unit tests/integration` in ONE invocation flakes 4 money-race cases on pristine main — run the suites separately.
37. **setup/quicksetup endgame (#245/#246)** — trap-17 walls resolve via re-homing the create-carrying `_unmapped` golden onto the row + `CAPTURE_WORLD_CHANNELS` find-branch seeding; **re-evaluate old park claims against the CURRENT tree before honoring them** — trap 17's wall justification was recorded true in wave 6 and falsified by #242 before wave 8 picked it up.
