# Merge-automation verification probe (2026-07-15)

> **Status:** `historical`

This file is a one-time, obviously-inert probe created by an audit session to
confirm that ordinary (non-workflow-file) code/doc PRs in this repo land on
green CI with zero human click, via the repo's native `auto-merge-enabler`
mechanism. It touches no `.github/workflows/**` files and carries no
functional change. Safe to delete once the audit has read its outcome.
