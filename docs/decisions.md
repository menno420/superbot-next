# superbot-next — decisions

> **Status:** `living-ledger`
>
> Generated by substrate-kit. Append-only decision ledger — entries are
> superseded, never deleted. Rule docs cite entries as bare [D-NNNN] ids;
> this file holds the provenance so rules never narrate it inline.

<!-- Grammar: ## [D-NNNN] <title> / - status: decided|superseded|retired / - date: YYYY-MM-DD / - supersedes: D-NNNN (opt) / - superseded-by: D-NNNN (opt) / - verdict: <one line> / - why: <2-3 lines> / - provenance: <ref> -->

## [D-0001] Adopt the substrate-kit workflow

- status: decided
- date: 2026-07-08
- verdict: superbot-next runs on the substrate-kit agent workflow.
- why: A repo-resident working agreement, decision ledger, and session
  discipline let agents work correctly with little steering; adopting the
  kit starts superbot-next governed instead of accreting rules ad hoc.
- provenance: substrate-kit adoption interview

## [D-0002] Pin substrate-kit adoption provenance
- status: decided
- date: 2026-07-08
- verdict: superbot-next adopted dist/bootstrap.py from menno420/substrate-kit main @ 3d303a0f2ab9da1dd6e008479cc8778d6419e637 (blob 7d2b85c740b952aa8c52d013fdcfa182f395b509); superbot-next is a kit consumer, never a copy of superbot (Q-0247).
- why: Canonical plan section 5 step 7b: superbot-next adopts FROM the extracted kit repo so both superbot and superbot-next later pin the same versioned dist; recording the exact source SHA makes the adoption reproducible and auditable.
- provenance: menno420/substrate-kit@3d303a0f2ab9da1dd6e008479cc8778d6419e637 dist/bootstrap.py; rebuild-kickoff-steps-6-8-brief-2026-07-07 (Q-0247)

## [D-0003] Reserve sb/ as the rebuild code root; S1 lands K0 config+observability
- status: decided
- date: 2026-07-08
- verdict: Code root sb/ (sb/spec, sb/kernel, sb/namespace, sb/adapters, sb/app, sb/manifest) is reserved per the frozen L0 layer map; S1 (K0) lands sb/spec/config.py (44 CONFIG_FIELDS: 36 harvested verbatim from shipped disbot/ at 7f7628e1 + 8 new operational fields from spec 05 s3.1), sb/spec/observability.py (46 metric families verbatim from disbot/services/metrics.py), sb/kernel/config (preflight/parse_bool/parse_dsn, RC-10 one-attribute-per-field Config), sb/kernel/observability/metrics.py (render()), sb/kernel/db/data_plane.py (4th rail, pure-stdlib so it lands at K0 with preflight; S4 adds pool/migrations/idempotency), checkers check_config_usage + check_metric_cardinality.
- why: Spec 05 s3.2 requires preflight() to run assert_data_plane + assert_intents, so the data-plane rail must exist at K0 even though its file home is sb/kernel/db (S4 band); spec counts (39 env vars / 47 fields) were call-site counts at spec time - current shipped source wins per Q-0120, yielding 36 harvested + 8 new = 44.
- provenance: frozen L0 spec 05 (ops-kernel rails) s2/s3.1-3.5/s11; phase-b-l0-build-order S1 row; canonical plan A-21 (EXTRA_OWNER_USER_IDS at K0)

## [D-0004] S2 lands the K1 namespace registry as a stdlib-only leaf; seed compat core, full harvest deferred to Stage-2
- status: decided
- date: 2026-07-08
- verdict: sb/namespace/{kinds,records,validate,index,triggers}.py built verbatim to frozen L0 spec 03: 16 kinds, (kind,value,scope) collision key with CommandScope(surface,parent_group), one pure validate(snapshot)->NamespaceReport oracle shared by CI/merge-tree/boot, is_reserved(value,kind,*,surface,parent) point query, check_trigger set-time gate, tombstone/ban-as-reservation. legacy_reservations.json ships as a SEED (RESERVED_PREFIX_OWNERS map + the 3 owner-keyed subsystem entries); tombstones.json seeds the ActionSpec custom_id ban only. tools/compute_corpus.py implements the spec 03 s3.6 algorithm over an exported expanded-corpus JSON (nav-nodes-first, shared-verb walk, cap budget, deterministic demotion).
- why: The full compat core (43 subsystem keys, legacy custom_ids, settings-key vocab, event names, common-word blocklist) is Stage-2 harvest DATA produced by walking shipped bot.commands in the superbot repo - spec 03 s9 explicitly defers the per-subsystem naming result to Stage-2; the mechanism (schema, loader, owner map, oracle) is what K1 owns and is fully built. compute_corpus takes the walk export as input because superbot-next never imports old-bot code (brief: superbot is a read-only oracle).
- provenance: frozen L0 spec 03 (k1-namespace-registry) s2-s11; phase-b-l0-build-order S2 row; RC-7

## [D-0005] S3 lands the K2 manifest compiler (the linchpin): 9-pass pipeline, snapshot+stable_hash, boot-gate leg A; A-2/A-19 gates armed; S0 registry re-hosted
- status: decided
- date: 2026-07-08
- verdict: tools/manifest_compile.py runs P1-P9 + _project exactly per frozen L0 spec 01: P3 is a thin call into K1's validate (RC-7), P4 authority dormant until sb/spec/authority.py lands at S7/K6 (armed-later, spec 01 s11), P7 store-completeness dormant until a baseline exists (arms at K3), P8 layout-lock overlays [A]-only, P9 recompile-parity with hash body excluding stable_hash/compiler_version/manifest_count. sb/spec/refs.py (7 *Ref types, @handler family raising RefRedefined on dup, two-form PredicateRef carve-out), sb/spec/roles.py (S/A/O registry), sb/spec/manifest.py (MINIMAL SubsystemManifest root - facet grammar minted by Gate-0 fold/amendments, compiler duck-types facet fields), sb/app/boot_gate.py (leg A live; legs B/C dormant until K8). Empty manifest.snapshot.json (0 subsystems) committed as the parity anchor. A-2 schema-growth ledger (baseline = the 8 s2.1 seed fields) + check_schema_growth; A-19 check_escape_hatches (ViewRef occurrences = tier-3 units, per-subsystem count ratchet vs committed baseline, --tighten one-way) inside the manifest-validate gate. S0 re-host: rebuild-amendments.yml + check_amendments.py copied from superbot @ 7f7628e1 with spec_corpus roots prefixed superbot: (external oracle) - rules 1-3 enforced locally, rule 4 corpus resolution advisory-skipped.
- why: never_strand admits WorkflowRef routes in addition to PanelRef/HandlerRef because audit_completeness REQUIRES mutating specs to carry a WorkflowRef - the two P6 predicates would otherwise be jointly unsatisfiable for mutating commands; SF-a option A is armed for never_strand button-binding + action_cooldown_parity per spec 01 s3.2 dependency flag (owner-gated fork, not re-decided).
- provenance: frozen L0 spec 01 (compiler-snapshot-amendments) s3.1-3.7/s5/s11; canonical plan A-2/A-19; build-order S0+S3 rows

## [D-0006] S4 lands the K3 DB seam: pool+transaction, fresh-chain checksum migrations, idempotency-key contract
- status: decided
- date: 2026-07-08
- verdict: sb/kernel/db/pool.py ports shipped utils/db/pool.py with spec-05 hardening: command_timeout=cfg.DB_COMMAND_TIMEOUT_S, max_inactive_connection_lifetime=cfg.DB_IDLE_LIFETIME_S, checked_acquire() validate-or-reacquire (ping after 30s idle, reacquire once), transaction() as THE sanctioned txn seam, DBUnavailable(ConnectionError) as the one typed signal (routes through spec 02's existing ConnectionError transient row - fork 5b, no seam edit), refuse-with-notice centralized in fetchone/fetchall/execute (T2-14); query-level PostgresError passes through unmasked. migrations.py is a FRESH chain NNNN 0001+ (4-digit, no legacy bootstrap DDL ported), schema_migrations gains checksum TEXT NOT NULL, verify_applied_checksums() at boot (MigrationDrift = refuse boot, never repair), pg_advisory_lock preserved under a new sb-next lock key; tools/check_migrations.py is the CI twin (numbering contiguous from 0001 + immutability vs committed migrations/checksums.json). idempotency.py: IdempotencyKey (colon-free namespace, natural-key dedup_token, render/parse - parse splits at most twice so spec-08 suffix tokens roundtrip), PriorOutcome, once()/record_outcome()/read_outcome() over migration 0001 idempotency_keys; OUTCOMES tuple carries the frozen 2.7 vocab early (record_outcome must validate) - the K6 outcomes.py leaf (S7) finalizes the grammar. asyncpg import-guarded throughout (not installed in this container). active_registry() added to sb/kernel/observability/metrics.py so the seam observes db_query_seconds without holding a registry reference.
- why: Spec 05 s3.4-3.7 verbatim; the shipped pool/migrations are the port base per s2 retirement map; 4-digit NNNN chosen over shipped 3-digit because spec 05/08 write the fresh chain as 0001+/000N and the rebuild chain restarts at 1; OUTCOMES lives in idempotency.py until S7 because spec 02's outcomes.py leaf lands at K6 and K3 must not reach upward.
- provenance: frozen L0 spec 05 (ops-kernel rails) s3.4/s3.5/s3.6/s3.7/s5/s6/s11; shipped disbot/utils/db/{pool,migrations,codec}.py at 7f7628e1; build-order S4 row

## [D-0007] S5 lands the K4 event outbox: DeliveryClass canonical home, in-txn capture, relay/reaper PollLanes, delivery_declared fence
- status: decided
- date: 2026-07-08
- verdict: sb/spec/events.py is the CANONICAL DeliveryClass home (RC-17 - K7 EventEmitSpec.delivery imports it, no local copy) with EventSpec = design-spec 2.8 frozen fields + delivery (default BEST_EFFORT, spec 08 fork A), FieldSpec, and KNOWN_EVENTS (name->EventSpec registry, kernel-seeded with the audit.action_recorded canary at audited=True delivery=AT_LEAST_ONCE from birth). sb/spec/versioning.py is spec 09 s3.2 verbatim (CheckpointClass/VersionPolicy/VersionedRow + version-extended StoreSpec) - authored at S5 because spec 08 s5.1 declares OUTBOX_STORE against it; the resolve/compile primitives stay S10. sb/kernel/outbox/{store,enqueue,relay,metrics}.py: exactly-once capture (INSERT ON CONFLICT dedup_key, IdempotencyKey.render()), atomic SKIP LOCKED claim with lease-as-visibility-timeout, two counters split (claims vs delivery_attempts - MAX_ATTEMPTS=12 gates only the latter), enqueue_audit_action as the durable TWIN (namespace=audit, dedup_token=mutation_id, occurred_at pre-serialized isoformat), enqueue_all two-call protocol returning BestEffortBatch.emit_after_commit(), to_jsonable codec (spec 08 s6.5), backoff min(5*2^(n-1),300). sb/kernel/scheduler/poll.py authors the PORT half only (LaneTickResult + PollLane Protocol) - lanes AUTHORED at K4, REGISTERED at K5 (F-1/RC-20); PollSupervisor lands S6. delivery_declared fence added to P6 duck-typed on the delivery field (all 3 rules; rule 3 inspects registered handler signatures for **kwargs/_outbox_dedup_key, unresolvable refs skip - P2 owns those). Compiler gains P0 _import_spec_grammar so snapshot field_roles is import-order independent; snapshot regenerated (hash 2172f662). A-2: 24 ledger entries minted (EventSpec 8, FieldSpec 3, StoreSpec 13) citing kernel/tools consumers until port bands mint manifests. Migration 0002_event_outbox.sql + checksums. Four outbox MetricSpecs homed in sb/kernel/outbox/metrics.py; check_metric_cardinality now validates METRICS+OUTBOX_METRICS (50 families).
- why: enqueue_all K7-lane dedup tokens are duck-typed until S8 finalizes WorkflowResult: result.dedup_key (DURABLE_ONCE once()-key) -> render():emit_index, else result.mutation_id:emit_index, namespace from result.op_key/ctx.op_key - S8 MUST honor these attribute names or update enqueue_all in the same PR; the relay takes an injected bus (Protocol) because the EventBus lands with K8, and findings is an injected callable because the findings engine folds at K5 (S6 provides the minimal sink); kernel stores (idempotency_keys, event_outbox) live as migrations + module-level StoreSpec constants, not manifest facets, until a kernel manifest exists - P7 baseline unaffected.
- provenance: frozen L0 spec 08 (event-outbox) s3.1-3.5/s5/s6/s11/s12; spec 09 s3.2/s3.6 (grammar + poll port); build-order S5 row; F-1/RC-20

## [D-0008] S6 lands K5: 7-phase lifecycle, RUNNING-only /ready + drain gate, the ONE PollSupervisor, findings seam, A-8 alert sink spec
- status: decided
- date: 2026-07-08
- verdict: sb/kernel/lifecycle ports shipped core/runtime/lifecycle.py near-verbatim (7 phases, coalescing request_shutdown/request_restart first-intent-wins, 128-event ring, diagnostics_snapshot, one-shot startup-duration observation) with metrics through the guarded active_registry and the diagnostics_service self-registration NOT ported (findings engine folds minimally instead). sb/adapters/http/health.py implements the spec 05 s3.8 state table as a PURE readiness_decision(gateway_ready, phase, db_up) core + aiohttp-guarded server on cfg.HEALTH_HOST/HEALTH_PORT: RC-9 semantics change applied (STARTING => 503 still_starting vs shipped 200; can_accept_commands stays {STARTING,RUNNING} for its own callers), DB-aware RUNNING gate (bounded SELECT 1 via checked_acquire, wait_for 2s, ~1s cache), /metrics = render() with full Prometheus content-type via raw header, /lifecycle diagnostic dump. HEALTH_PORT/HEALTH_HOST declared as harvested ConfigSpecs (S1 sweep missed disbot/healthserver.py:64,70 - source wins; CONFIG_FIELDS 44->46). sb/kernel/scheduler/poll.py gains PollSupervisor: ALWAYS-ON, waits for RUNNING before the FIRST reconcile_on_boot (terminal-before-RUNNING exits), skips claiming when not can_accept_commands (drain gate), per-lane exception isolation, duplicate lane names rejected; SYSTEM_CLOCK = aware-UTC-now. sb/app/poll_host.py build_poll_supervisor registers OutboxRelayLane+OutboxReaperLane (the REGISTERED-at-K5 half of F-1/RC-20); bus is injected (EventBus lands K8). Findings engine folds minimally as sb/kernel/observability/findings.py: record_operator_finding(*, source, severity, summary, detail, correlation_id) - the exact spec 08 s4 seam - with redact_text scrub + 256-ring + recent_findings. A-8: sb/kernel/observability/redaction.py ports redact_text/redact_payload VERBATIM from disbot/core/runtime/ai/redaction.py, HOISTED out of the ai namespace (K10 imports from here); sb/kernel/observability/alerts.py is the operator-alert sink spec - AlertKind (the named shipped WebhookReporter feed), OperatorAlert, OperatorAlertSink port, prepare_alert as the ONE redaction choke point, LoggingAlertSink/NullAlertSink
- why: The Discord WebhookReporter twin is an adapter over this port that ports at the operator band (needs discord embeds + owner-provisioned DISCORD_WEBHOOK_URL, already a SecretSpec) - the K5 obligation is the SPEC + redaction chokepoint, which is what A-8 pins; the health server takes gateway_ready as an injected callable because the gateway lands at K8; the full findings ENGINE (persisted store, operator projection) remains F-3.4 growth at this same module home.
- provenance: frozen L0 spec 05 (ops-kernel rails) s3.8/s6; spec 09 s3.6; shipped disbot/{healthserver.py,core/runtime/lifecycle.py,core/runtime/ai/redaction.py,services/webhook_reporter.py} at 7f7628e1; canonical plan A-8; build-order S6 row; RC-9

## [D-0009] S7/K6: outcomes re-home, R-16 fold shape, read ports, owner install seam
- status: decided
- date: 2026-07-08
- verdict: outcomes.py owns the frozen five with SHIPPED lowercase values (source-wins) and idempotency.OUTCOMES re-points to it; R-16 lands Lane.ROLE_SET (role:<binding> prefix-classified before the dot rule, fail-closed allow-set = deny-until-role) + per-channel role-set constraint (detail=role_not_held); R-2 rides only insofar as TIER+ROLE_SET express its role/permission-floor legs — the resource-owner clause has no frozen grammar and defers to the ticket/moderation port bands; revoke-overlay + role-binding reads are installable async ports defaulting to no-row; owner identity installs from Config at the composition root (fail-closed before install); is_bootstrap_command ports verbatim to sb/namespace/bootstrap.py (K1 owns bootstrap-ness); validate_authority_ref's reservation corpora are optional params so P4 arms format-only until manifests exist
- why: spec 04 + shared-vocab 7.1 + canonical plan A-12 win over spec 02 (RC-2/3/6/13/14/15); the S4 uppercase OUTCOMES tuple was a pre-S7 mirror, shipped contracts.py:48-52 values are the golden-harness grounding; A-14 join-verification needs deny-until-role so an unconfigured binding must not admit
- provenance: S7 kernel worker 2026-07-08; frozen L0 spec 04-authority-engine.md, 02-resolver-error-envelope.md, shared-vocabulary.md 7.1, rebuild-canonical-plan-2026-07-06.md 11b A-12

## [D-0010] S8/K7: engine entries, error mirror, WorkflowResult outbox carriers, settings read engine, Q-D24 fence
- status: decided
- date: 2026-07-08
- verdict: Three entries over one _execute core per spec 07 (run self-txn; run_ref/apply external-conn skip guard+record+EFFECT+BEST_EFFORT and return committed_at=None); WorkflowResult adds op_key property + engine-populated dedup_key runtime field to honor outbox enqueue_all's duck-typed §3.5 carriers (D-0007); K7-local _classify_exception mirrors spec 02's transient/bug rows until S9 lands from_exception — S9 MUST re-point it; ConfirmationSpec homes at sb/spec/confirmation.py with challenge enum (test conftest's typed_challenge stand-in remains test-local); workflow spec types register field roles but live outside sb.spec so the A-2 ledger tracks only ConfirmationSpec's 5 fields; Q-D24 ships-until-ruled option A = session_transition [S] flag + fence session_transition=>NATURAL_KEY; settings read engine (F-3.4) is kernel-registered SettingDeclaration + installable store/binding/secret ports — the Gate-0 SettingSpec facet is NOT minted, band-1 registers specs into this registry; audit_log retention=permanent pending owner retention ruling; central trace default AT_LEAST_ONCE via enqueue_audit_action (spec 07 §9.7, rides 08 OD-1)
- why: spec 07 + design-spec §2.7/§4.1-4.4 win; PIN-2/F-2 by caller type (draft=run per-op, fence scoped to external-conn only); the S4-S6 handoff mandated honoring enqueue_all's attribute names; Q-D24 packet says ship option A until owner rules
- provenance: S8 kernel worker 2026-07-08; frozen L0 spec 07-workflow-engine.md, rebuild-design-spec-2026-07-02.md §2.7/§4, owner-decision-packet.md Q-D24, canonical plan F-3.4/A-5

## [D-0011] S9/K8: resolver seam, RC-12 batch, dormant ports, S9b panel-runtime split
- status: decided
- date: 2026-07-08
- verdict: resolve() lands the full spec-02 order with the absorption edits (04's 10-field decision + Lane consumed; owner_override threaded to channel-access; TransparencySink named at step 4; denial copy = decision.denial_message); RC-12 CLOSED: ActorRef gains member_tier (trailing default per Gate-0 Group-2) + A-12 role_ids + already-applied actor_type, adapters compute both via member_tier_from_member/role_ids_from_member fresh per interaction; policy/visibility/panel-engine/target-index reads are installable ports defaulting unconfigured-allow (panel engine default raises => classified bug — the panel/presentation runtime is the explicit S9b follow-up); K7 _classify_exception re-pointed to from_exception(MAINTENANCE, target=None) via lazy import (kernel/interaction imports kernel/workflow at module level, the reverse edge stays function-level); cooldown is in-memory with the fork-D refund rule and a fixed 5/60s AI-throttle axis pending the AI band's policy; confirm = second dispatch with request_id in-memory dedup; boot-gate leg B armed by build_runtime(snapshot) realizing sets from the installed dispatch index, leg C armed by tree_sync.sync_remote (shipped SyncOutcome/_remote_paths lifted, non-fatal); command.dispatched EventSpec registered from birth (owner=kernel carve-out) with override_applied/base_allowed derived per RC-2/RC-5; minimal in-process EventBus (per-handler isolation) is the composition bus for relay/BestEffortBatch/trace; tools/check_no_skip.py arms the AST no-skip fence (surface-registration + discord-import containment)
- why: spec 02 + the S9 absorption task win (build-order row S9); the RC-12/A-12 batch was the hard predecessor gate; F-3.4 allows the S9b split for the panel runtime; discord/aiohttp absent so every adapter is duck-typed guarded
- provenance: S9 kernel worker 2026-07-08; frozen L0 spec 02-resolver-error-envelope.md, 04 seam notes, phase-b-l0-build-order.md S9 row, canonical plan A-12/F-3.4

## [D-0012] S9b/K8: the panel/presentation runtime — grammar leaf, ONE kernel engine, presenter port
- status: decided
- date: 2026-07-08
- verdict: PanelSpec family lands as sb/spec/panels.py per design-spec §2.3/§2.4/§2.6 with the L-24 riders folded (alt_text on EmbedFrameSpec at declared-field depth — the non-empty fence sequences with the render-layer build; ModalSpec/ModalFieldSpec = amendment G-10 in-spec, a form BODY not a dispatch surface; the locale CopyResolver seam with IDENTITY_COPY_RESOLVER default in sb/kernel/interaction/locale.py); ONE kernel engine (sb/kernel/panels/{compile,registry,router,context,render,engine,projections}.py) renders a pure RenderedPanel model behind an installable presenter port — discord materialization (PanelRuntimeView invoker-lock/timeout-disable, DiscordPanelPresenter) is adapter-side (sb/adapters/discord/panel_view.py, guarded); the §3.4 custom-id router ships with the fixed precedence (static table → g<N>: parse [g1 parser + deprecation-window registration] → polite expiry), and the compile fence keeps a legacy custom_id_override out of the scheme-token family; nav ids nav:help / nav:hub:<hub> preserved verbatim (views/navigation.py:58-59; row 4), FOLLOW_PARENT resolved through an installable hub resolver at render/click time, parents rebuilt fresh; layout coverage exhaustive+exclusive over the union of pages + Discord caps + never-strand + destructive⇒danger+never-row-0 + persistent⇒timeout None + escape-hatch justification fences at registration; settings + help panels are PROJECTIONS (sb/kernel/panels/projections.py) — read-view v1, per-setting edit workflows are the band-1 scalar-lane port; component adapter falls through target-index → panel router (ComponentBinding re-enters resolve(); NavBinding → engine.handle_nav; DynamicRoute → installable games-band dispatcher; else polite expiry)
- why: K8's PROVIDES explicitly includes the panel runtime (F-3.4; canonical plan D-6 "panel runtime → K8/S9b") and S10's draft/due-queue surfaces do not need it — but OPEN_PANEL was port-stubbed, so S9b lands first as its own PR. Deviations: PanelActionSpec.handler admits PanelRef (open-child actions dispatch through resolve()'s OPEN_PANEL terminal) beyond §2.6's WorkflowRef|HandlerRef; the destructive "never adjacent to a hot action" half is DEFERRED until §2.10.4's deterministic hot/adjacent definitions are minted (row-0 fence live); Q-0216 multi-select-default is not yet checkable (no BindingSpec facet) — rides the band-1 SettingSpec/BindingSpec cut; the compiler-test PanelActionSpec.destructive stand-in re-tagged O to match the real grammar (roles are frozen metadata, the real grammar wins); the A-3 navigation-completeness golden lands at layer V (step 11) per the canonical plan, not here
- provenance: design-spec §2.3/§2.4/§2.6/§3.4; gate-0/l24-presentation-riders.md §1/§2/§4; canonical plan D-6/F-3.4; S9 worker deferral note (progress log)

## [D-0013] S10/K9: the durability band — due-queue + version policy + draft pipeline (peers over K3)
- status: decided
- date: 2026-07-08
- verdict: Spec 09 leg — sb/spec/scheduler.py (durability-extended ManagedTaskSpec, NO idempotency field per §12#2; + A-13 QuietHours(skip|defer, defer pinned for one-shots — a one-shot never silently loses its only fire) + R-17 ConditionTrigger(condition, poll_interval_s)); sb_due_queue migration 0004 with the COALESCE(guild_id,0) partial unique slot index (GLOBAL double-arm closed); sb/kernel/db/scheduler.py (SKIP-LOCKED claim_due = the uuid4-defeated claim_run fix; mark_fired advances INSIDE the fire txn); misfire.py pure (COALESCE default; one-shots policy-exempt; croniter = guarded bounded deferral); due_queue.py DueQueueLane (deterministic once() key task_id:fire_epoch under the task_key colon→dot-folded namespace — K3's namespace is colon-free, ledgered fold; ONE fire txn: once + run_ref(conn) + record_outcome + mark_fired; §3.8 failure routing with MAX_FIRE_ATTEMPTS=12→dead and error_policy LOG/DISABLE_AFTER_N(3)/ESCALATE; bounded boot reconcile via the same claim path; arm_declared_tasks idempotent; SYSTEM_ACTOR sentinel); **check_atomic_db_only runs for every armed WorkflowRef handler at declare/arm (the S10 obligation)**; user_automation.py = the A-13 producer (user_automation:* namespace, interval floor 3600s, per-user cap 5 via an installable PER_ACTOR_QUOTA-socket reader, category-B ACTION arm = SEMANTIC_VIOLATION pending Q-0243, creator-ActorRef fires via the _creator_actor payload channel — never SYSTEM bypass). Versioning: sb/kernel/versioning/{resolve,compile}.py against the S5-authored grammar (NOT re-minted) — resolve_versioned_load rows 0-4 (broken upcast rung QUARANTINES, never falls into REJECT; value-REJECT compensation refund-then-retire once()-guarded under {table}.version_reject; comp failure ⇒ row NOT retired), run_recovery generated sweep, check_version_policy 5-rule fence (DROP-on-value unbuildable — the RPS-forfeit shape). Spec 06 leg — sb/spec/draft.py (ConfirmChallenge = ALIAS of §2.7 Challenge, no dup enum), sb_drafts/sb_draft_operations migration 0005 ((draft_id, op_seq) PK — the L-7 collapse fix), db/draft.py (NULL-safe IS NOT DISTINCT FROM listing; CAS update_status; conditional reap_stuck_applying TTL=900), store/registry (fail-closed OpKindRegistry)/preview (T2-5 requires_confirmation + kernel verify_confirmation + preview_hash pin)/accept (AND-over-distinct-refs gate, derived ref = display floor only)/apply (per-op run(spec,ctx) PIN-2, _draft_dedup_token through ctx.params for K7's DedupKeySpec tuple form, per-op heartbeat, APPLIED CAS honoring a reaper win)/pipeline facade + janitor (ExpiryJanitorLane hosting expiry + stuck-APPLYING reap). poll_host registers DueQueueLane + ExpiryJanitorLane (roster now 4 lanes; with_durability_lanes=False keeps the pre-S10 roster for test hosts).
- why: frozen L0 specs 06 + 09 win; 06 §12#4/PIN-2 resolves F-2 by caller type (EFFECT-bearing draft ops → run() per-op; pure-DB scheduler/versioning → run_ref(conn)); deviations ledgered: once() namespace colon-fold (K3 constraint), quiet-hours one-shot DEFER pin, apply_draft takes the actor explicitly (AuthorityDecision carries no actor field — spec 06 assumed decision.actor), OD-1 built default REJECT_AND_PRESERVE stands (owner-gated default un-decided), croniter not installed in this container (Cron arming raises until the pinned dep lands — flagged), EventTrigger+DURABLE declare is fenced as a category error (bus-armed tasks are not due-queue rows).
- provenance: frozen L0 06-draft-pipeline.md; 09-scheduler-state.md; canonical plan A-13/R-17/Q-0243; S7-S9 successor notes (S10 obligations)

## [D-0014] S11: rubric classes 11/12/13 mechanics — cost grammar, data lifecycle, egress port
- status: decided
- date: 2026-07-08
- verdict: The LENS (rubric v2 classes 11/12/13 + the 13>12>11 victim precedence + the adversarial-abuse pass procedure) lands as a PROPOSAL doc (docs/planning/rubric-v2-classes-11-12-13.md — owner-gated T-1/T-2/T-3, never self-applied per Q-0233); the MECHANICS land live: sb/spec/cost.py (CostPosture; CommandSpec.cost_posture+quota_ref role-registered against the duck-typed facet; FAIL_CLOSED = the L-16 media default-OFF rule) + tools/check_cost_posture.py (Phase 1 declaration-presence; Phase 2 live-binding sequences after T2-15); StoreSpec grows data_class/erasure_ref/is_cache/cache_scope (additive-not-closed, spec 10 §8.2) + a store REGISTRY (register_store/registered_stores in sb/spec/versioning.py — the machine-walkable inventory) + tools/check_data_lifecycle.py (retention+erasure_ref on every member store; WorkflowRef-only hooks; member caches GUILD-scoped); the five kernel stores are honestly tagged MEMBER_ID with DECLARED erasure refs (event_outbox scrub_subject, audit_log tombstone_subject — TOMBSTONE keeps the forensic skeleton, idempotency_keys prune_subject, sb_due_queue erase_subject_timers, sb_drafts discard_subject_drafts; bodies land with each store's owning band); sb/kernel/privacy/erasure.py = run_erasure (enumerate the registry slice → per-store once("privacy.erasure")-guarded run_ref(conn) external-conn legs → one terminal ErasureLegResult per store or complete=False+unreached, resumable) + the A-15 run_export read-only twin (installable per-store export readers, account-level cross-guild iteration, zero writes); sb/kernel/interaction/egress.py = RC-21 (TrustLevel default-deny UNTRUSTED, OutboundContent, ChannelEmitter port, kernel-side neutralize_untrusted, loud not-installed default) + sb/adapters/discord/egress.py (DiscordChannelEmitter — the ONLY AllowedMentions constructor, guarded); tools/check_egress.py = the AST send-fence WIDENED per A-5 to raw Discord state mutations (banned method set outside sb/adapters; sb/app/error_handlers.py exempted as the sanctioned C-1 error shims; kernel-port receivers allowlisted).
- why: frozen L0 spec 10 wins (§2.A/§8.1/§8.3); the checkers are its own 🔧 tags. Interpretations: is_cache minted as an explicit [S] marker (the spec's "the store is a cache" predicate needs a declared carrier); erasure disposition reported through the workflow result's `after` mapping with a value-class fallback; run_export readers are installable per store (a store without a reader is REPORTED unreached, never silently skipped); sb_due_queue/sb_drafts classed AGGREGATE self-managed (boot-reconcile/janitor own cross-deploy resume — not run_recovery sweeps); N-1/N-2 (credential/supply-chain) and N-3 (permission census) remain lenses here — fixes at S13/S15 per the spec's reciprocal pointers.
- provenance: frozen L0 10-security-abuse-rubric.md; canonical plan RC-21/A-5/A-15; build-order S11 row

## [D-0015] S12: data-integrity invariants — declared grammar, report-only sweep, quarantine, verify-import
- status: decided
- date: 2026-07-08
- verdict: sb/spec/invariants.py (InvariantKind{ROW_PREDICATE,RECONCILIATION,UNIQUENESS,REFERENTIAL,TERMINAL_ONCE}, Severity{REPAIRABLE,QUARANTINE_ONLY,ALERT_ONLY}, SweepCadence+CADENCE_SECONDS, InvariantSpec, Violation) + SubsystemManifest.data_invariants facet (sibling to stores — a reconciliation spans TWO stores); declare_invariant runs the fence at registration. sb/kernel/invariants/compile.py = the invariant_coverage fence (bears_value coverage keyed on checkpoint_class: AGGREGATE/LEDGER⇒RECONCILIATION|TERMINAL_ONCE, SESSION escrow⇒REFERENTIAL|TERMINAL_ONCE; REPAIRABLE⇒WorkflowRef repair_ref, atomic_db_only-checked when registered; RECONCILIATION⇒baseline_ref — bare aggregate==ledger equality would flood quarantine on pre-ledger history; value-bearing repairable RECONCILIATION/TERMINAL_ONCE ⇒ ground_truth_store REQUIRED else QUARANTINE_ONLY only — Q3/Q-D13, the money direction is never guessed). sb/kernel/invariants/sweep.py = InvariantSweepLane, a PollLane PEER on 09's supervisor (NOT a due-queue fire, NOT a second claim loop): epoch once() guard {id}.sweep:{epoch} (at-most-once per window across instances), report-only DEFAULT with the operative enforce state settings-backed at invariants.enforce.<id> (the one-way door, flipped live), REPAIRABLE repairs = the 09 _fire_one pattern verbatim (once({id}.repair:{row_id}:{fingerprint}) + run_ref(conn) + record_outcome in ONE txn) under SWEEP_ACTOR(actor_type="backfill") (RC-18), failures classified via from_exception(MAINTENANCE, None) (RC-19) and QUARANTINED never half-fixed, max_actions_per_run circuit breaker bounds BOTH postures + escalates mass_corruption; soft-quarantine = evidence snapshot into sb_quarantine (owner-signed disposition; v1 visible-but-flagged — read-exclusion is the labeled deferral) + sb_invariant_sweep_log kernel-internal bookkeeping (migration 0006). run_verify_import = CUT-2 stage 3.5 / CUT-3 verified-restore: the SAME sweep dry-run FORCED, machine-readable stop-codes invariant_violation/unrepaired_quarantine + the per-id violations read-off.
- why: frozen L0 spec 11 wins; Q4 (MAINTENANCE) and the run_ref seam were already CLOSED upstream and are consumed. Interpretations: guild enumeration is an installable port (no gateway yet — default no-guild pass, tests install fakes); ON_BOOT invariants ride reconcile_on_boot with an hourly epoch guard against crash-loop re-sweeps; sb_quarantine tagged MEMBER_PII (snapshots preserve full row payloads) with a declared scrub ref; Q1 posture built = (C) permanent report-only lane (the recommendation — flagged for ratification), Q2 (hard-vs-advisory cut gates) and Q3 per-invariant money directions stay OWNER; Q5 = (A) reuse backfill.
- provenance: frozen L0 11-data-integrity-repair.md; build-order S12 row; canonical plan RC-18/RC-19/Q-D13

## [D-0016] S13: credential lifecycle + supply-chain posture — sibling leaf, durable rotation, hash-pinned lock
- status: decided
- date: 2026-07-08
- verdict: sb/spec/credentials.py = the CredentialSpec SIBLING leaf (spec 12 §2.A — SecretSpec untouched, the ⑪ InvariantSpec discipline): RotationPosture{MANAGED,AUTONOMOUS,OWNER_PROMPT,ON_COMPROMISE}, CLOSED RevocationRef kill-path vocabulary, BlastTier total order, CredentialStore partition discriminator (store==WORKER_ENV ⟺ config_ref is not None), and the ONE flat CREDENTIAL_REGISTRY (13 rows). tools/check_credential_lifecycle.py = rules 1-3 (+partition +uniqueness), machine-complete BOTH directions against the credential-bearing CONFIG_FIELDS subset. Rotation = detector/executor split (§2.B(1a)): sb/kernel/credentials/cadence.py rotation_due (registry cadence × ledger last_rotated_at; horizon_epoch = days//cadence period index) + arm_due_rotations (leaf → DURABLE OneShot arm_one_shot; root → operator prompt); sb/kernel/credentials/rotation.py = the distinguished externally-effecting fire (HandlerRef, exempt from the pure-DB fence per §2.B(1b)) with horizon-stable once() (credential.rotation:0:{name}:{horizon}) + the RESERVED/ISSUED_PENDING_VERIFY/VERIFIED/FAILED phase ledger (sb_credential_rotation, migration 0007; fingerprint = NON-SECRET identity only) + the §2.B(1c) re-run/resume rule (issued-pending-verify re-fires complete the verify, never re-issue; RESERVED resumes adopt an orphan); RotationProvider = the installable CUT-1 ops port, un-installed = loud FAILED+finding. Supply chain (§2.C/Q-D18): requirements.txt = human constraints with <next-major ceilings (CL-6), requirements.lock = pip-compile --generate-hashes output (17 dists, 762 hashes; croniter pinned per the S10 flag), tools/check_lockfile_fresh.py (static + --regen legs), .github/workflows/ci.yml = tests + the 14-checker fleet + manifest verify + kit check --strict + lockfile-fresh(--regen) + pip-audit — the FIRST CI workflow files; required-check designation stays OWNER. Runbook docs/operations/credential-lifecycle.md; CL-1/CL-2/CL-3/CL-5b routed as an open question-router block.
- why: frozen L0 spec 12 wins. Interpretations (source wins Q-0120): the "SecretSpec subset" read as credential-bearing type ∈ {SECRET, DSN} (DATABASE_URL is DSN-typed, same class); 4 harvested worker secrets the spec's §2.A table predates got rows (DISCORD_WEBHOOK_URL, PARAGON_API_KEY, YOUTUBE_API_KEY, CLAUDE_ROUTINE_TOKEN) with 3 additive RevocationRef members naming their real kill mechanisms (discord.webhook_delete, google.cloud_console, paragon.dashboard — rule 1 demands a real kill-path per row); CONTROL_API_TOKEN row DEFERRED to the control-api band (its SecretSpec doesn't exist; a row would dangle rule 3); the phase-ledger TABLE built now (the §6 deferral covers only provider API bindings — the state machine needs its home); never-rotated cadence rows count DUE (conservative; providers install at CUT-1); rotation registered as a DURABLE OneShot HandlerRef task — the sanctioned externally-effecting fire, its own multi-txn protocol committing UNDER the scheduler's outer guard so a mid-fire restart resumes via reconcile_on_boot; CI pytest runs WITHOUT runtime deps (the guarded-import discipline is itself under test).
- provenance: frozen L0 12-credential-lifecycle.md; build-order S13 row; Q-D18; S9b-S12 successor notes (croniter/lockfile pointer)

## [D-0017] S14: backup/DR/rollback — ported proof, derived rollback_class, narrow reverse importer
- status: decided
- date: 2026-07-08
- verdict: .github/workflows/backup-db.yml PORTED with the §2.1 five-item de-repo-bind (repo-identity guard → vars.BACKUP_ENABLED; DATABASE_PUBLIC_URL re-creation + the 400-day retention raise = named OWNER cutover steps; UNVERIFIED graduated into the continuous proof; the >=10 CREATE TABLE floor re-commented non-empty-NOT-restorable; daily/monthly tiers + failure-issue + PGDG install verbatim). .github/workflows/restore-verify.yml = the weekly side-effect-free verified-restore proof: latest artifact → fresh Postgres service container → SB_VERIFY_BOOT boot (sb/app/verify_boot.py: preflight → boot-gate leg A → db.init(+migration checksums) → RUNNING with NO gateway/token/PollSupervisor/relay) → S12 run_verify_import dry sweep — verified := boots AND sweep-clean (dossier 11's definition consumed verbatim); asserts the monthly artifact is not retention-clamped; emits last_verified_restore_age; uploads NO restored-data artifact (the §2.2 privacy posture). SB_VERIFY_BOOT = the 47th ConfigSpec (spec 13 §8-5 additive; brief said 48th, actual count is 46→47 — source wins) with the preflight rail SB_VERIFY_BOOT=true ⇒ SB_DATA_PLANE=test else RefuseBoot. Grammar: ForwardMapKind{NAME_STABLE,RENAME,COLLAPSE,NEW_ONLY,DROP} + RollbackClass{REVERSE_IMPORTABLE,DECLARED_LOSS,REPLAY_INTENT} on 09's version-extended StoreSpec — rollback_class is DERIVED (derive_rollback_class: invertible ∧ bears_value ⇒ REVERSE_IMPORTABLE; SESSION ⇒ COLLAPSE short-circuit always wins; retirement ⇒ DROP; replay_intent = the ONE hand-set OWNER narrowing, role [O]); tools/check_rollback_disposition.py = the rollback_class_resolved fence + replay-intent-narrows + reverse-importer coverage == derived set BOTH directions. tools/importer/reverse/ = the Q-D15 posture-B narrow importer: LEDGER re-insert by mutation_id ON CONFLICT DO NOTHING, AGGREGATE absolute-value natural-key upsert, REPLAY_INTENT human list, M1 owner-signed + M2 per-subject loss manifest, stop-codes cutover_flip_ts_unset/reverse_importer_coverage_gap/store_import_failed; CUTOVER_FLIP_TS_KEY = "platform.cutover_flip_ts" (global settings key, written once at the flip through the audited seam). All 8 kernel stores DECLARE forward_map_kind=NEW_ONLY. Playbook docs/operations/rollback-playbook.md; Q1/Q2(i)/Q3 routed as a question-router block.
- why: frozen L0 spec 13 wins. Interpretations: NO silent absence-of-source ⇒ NEW_ONLY fallback — until the CUT-2 alias map exists every store must DECLARE (or derive via session/retirement); a silent default would misclass a renamed value store as unrecoverable; the derivation lives in sb/spec/versioning.py beside bears_value (spec §8-1 homing) with the fence as a standalone checker over the S11 store registry (the manifest-validate twin arms when manifests exist); per-guild config backup/restore is NOT here (a band-1 draft-lane consumer, A-15); rollback_class not registered as a field-role (it is derived, never declared — only forward_map_kind [S] + replay_intent [O] registered + A-2 ledgered); restore-verify installs the S13 hash-pinned lock (--require-hashes) — the two steps compose.
- provenance: frozen L0 13-backup-dr-rollback.md; build-order S14 row; Q-D14/Q-D15; superbot .github/workflows/backup-db.yml @7f7628e1

## [D-0018] S15: platform governance — IntentPosture DEGRADE seam-correction, survivability gates, guild-cap alert, permission census
- status: decided
- date: 2026-07-08
- verdict: The F-3/PG-2 adjudication BUILT AS RULED: Q-D5 = DEGRADE per the canonical-plan F-2 ruling (+Q-0246/A-22 Full-vs-Lite product tier) — the older build-order "open, PG-2" line loses (newer plan wins; ledgered as the known doc conflict). sb/spec/config.py: IntentPosture{REQUIRED,DEGRADE} (mirrors ConfigPosture); IntentSpec += posture (authoritative) + degrades (capability classes); INTENT_CONTRACT required True→False + posture=DEGRADE + degrades sets (message_content: prefix/fuzzy/triggers/nl_message/passive_onmessage; members: member_join/member_leave/member_cache — invoker member_tier is INTERACTION_CREATE-payload-computed, RC-12, so authority is unaffected by denial). sb/kernel/config: assert_intents enforces the mirror invariant required==(posture is REQUIRED) as a config-COMPILE error, fail-closes only REQUIRED-posture intents (none today), and intent_degradations(cfg) yields DegradedCapability markers — the composition root does NOT register a degraded intent's capability classes (not-registering removes the empty-content footgun at the source); the pre-S15 fail-closed test updated in the same PR. sb/kernel/platform_governance.py: emit_degrade_notices = the once-per-STATE-CHANGE latch (platform.degrade_state; a merge=deploy under the same denial never re-fires; a cleared denial fires "restored"); evaluate_guild_cap = the ~75/90 latched lead-time alert (fire-once per threshold via platform.guildcap.<t>, one-way, guild_count gauge set) — both over an installable state store (in-memory default until the settings band installs settings-row persistence — honest v1). guild_count gauge = the 47th metric family. sb/spec/governance.py: the D-5 slash_common tag role-registered on CommandSpec (duck-typed facet, cost.py pattern) and declared on PanelActionSpec/SelectorSpec (sb/spec/panels.py — inherently interaction-delivered, presence = survivable); check_manifest_survival + slash_cap_violations cores; tools/check_intent_survival.py + tools/check_slash_cap.py = the CI gates (green over 0 manifests; the invariant arms as port bands declare). tools/permission_census.py = the CUT-2 census shapes (per-guild all-command sweep schema; the GET is bot-token-readable, the PUT is NOT — no automated replay exists), the PRESERVED/RENAMED/DROPPED partition (same-app-id preservation; --new-application-id shows PG-5(b)'s total loss), carry-verify (copy-fidelity on the PRESERVED set, cutover-blocking exit), admin-notice lines with the exact re-apply overlay. PG-1/PG-3/PG-4/PG-5 routed as a question-router block.
- why: frozen L0 spec 14 wins. Interpretations: the module is platform_governance (a "platform" package shadows the stdlib module — same reason the test dir is tests/unit/platform_governance); IntentSpec fields are NOT role-registered (config grammar is not manifest grammar — S1 precedent) so only the three slash_common carriers minted A-2 entries; capability grouping in check_intent_survival duck-reads an optional `capability` field falling back to the spec's own name (the frozen command facet has no capability field yet — re-binds when band-1 mints it); the census live GET wiring + rename-map production are CUT-2 ops build (partition/verify logic is what CI can hold now); the degrade-notice + guild-cap latches persist in-memory until the settings band installs the durable store (flagged in module docstring, not silent).
- provenance: frozen L0 14-platform-governance.md; canonical plan F-2/Q-0246/A-22/PG-2 adjudication (build brief §9.6); build-order S15 row; RC-11/RC-12

## [D-0019] Layer-V V-1: parity corpus imported verbatim; golden-parity born-red as its own two-leg workflow
- status: decided
- date: 2026-07-08
- verdict: The Phase-0.5 golden corpus imported BYTE-IDENTICAL from menno420/superbot @ 7f7628e12f3b89c5c2a1fbdcfb039787df269e20 — 482 files (465 goldens + _sweep_skips.json + the harness/cases/run/coverage/README/COVERAGE.md), per-file sha256 manifest digest 2783d0032f90465fc378e77096284655babb3c8c6da449c30092791eaca14dcf pinned in parity/parity.yml `source`. parity/parity.yml minted at birth with the design-spec §6 gate-5 semantics (49 subsystem rows = the goldens dirs, ALL `pending`; ported is a one-way door; the file doubles as the owner's port dashboard) + the A-16 depth section FROM BIRTH (closed reason-class vocabulary, exemptions, count-ratchet) + the A-16 clause-3 `kernel` coverage home (kernel-owned events + the 8 kernel store tables as data rows, checker stays DB-free). tools/check_parity_depth.py = the A-16 floor INSIDE the golden-parity named gate (R1 schema/both-direction roster, R2 100% declared-surface-or-exempt at every flip, R3 count-ratchet + mandatory row at the flip, R4 structural one-way-door guard; --write-ratchet upward-only regen). .github/workflows/golden-parity.yml = its OWN workflow, deliberately NOT folded into ci.yml: `gate` job (Postgres-18-serviced per gate 5, restore-verify.yml service-block precedent; check_parity_depth + tools/run_golden_parity.py --gate: ported-must-replay-green, pending reported-not-failing, vacuously green at birth — the job the owner designates REQUIRED) + `report` job (run_golden_parity.py --report: full-corpus red-until-parity, BORN RED BY DESIGN, never to be marked required). The replay-binding probe actually attempts Harness.start() (the harness's disbot imports are lazy — a module-import probe false-greens); the new bot's replay adapter is port-band work and a `ported` flip without it goes loudly red in the gate.
- why: canonical plan §5 step 11 ("import the parity goldens into a Postgres-serviced required workflow", born red) wins over the older design-spec §6 goldens-live-in-the-old-repo integrity clause (Q-0120 newer-plan precedence); the integrity rule itself carries over — goldens change only via explicit reviewed PR with the diff explained, and the source pin makes drift auditable. Born-red isolated from the green fleet per the S13-S15 successor directive; the two-job split reconciles "born red by design" with gate 5's "required from day one" (job-level required checks). `_unmapped` (239 sweep cases) kept as an explicit pending row — port bands re-home cases as they flip.
- provenance: canonical plan §2.2 V-1 + §5 step 11 + §11b A-16; design-spec §6 gate 5; parity/README.md integrity rule; superbot parity/ @7f7628e1

## [D-0020] Layer-V V-3: sim runner + oracles + check_sim_gate; A-3 navigation golden; V-2 procedure
- status: decided
- date: 2026-07-08
- verdict: sim/ built FRESH to design-spec §2.10 (the §5 ~L992/L1029 contract — not re-derived): sim/space.py machine-derives the write surface from the grammar's own S/A/O roles (a_tagged_fields over snapshot_field_roles — today LayoutSpec.pages/PageSpec.rows/PanelSpec.layout/Table+ListSpec.default_sort; band-1 arrangement fields join automatically), assignment keys by namespace id, the §2.10.4 telemetry sidecar (sim/usage.snapshot.json SEEDED-EMPTY — the sim never runs on invented data; neutral prior + confidence=low + stays-Exempt until real signal), §2.10.5 hard constraints (5-row/25-component caps; destructive never-row-0 + never-adjacent-to-hot with hot = top usage quartile, id-sorted ties; nav row 4 engine-owned outside the search space) and candidate generation (exhaustive ≤ limit else fixed-seed sampling + annealing neighbours). sim/oracles/ = the pluggable registry hosting the three NAMED oracles (canonical plan step 11: one shared runner, distinct oracles): navigation (Q-0235 instruction-driven engine — deterministic label-match user, task-success-rate/ABSOLUTE-path-hops/wrong-turns, optional naive-user port advisory-only per §8 Q9, corpus minted from graph labels INDEPENDENT of the NL-router eval corpus per the #1701 Goodhart caution), settings_grouping (scroll-to-coverage + intra-group co-edit pair-mass cohesion, neutral 0.5 without signal), dense_panel (positional interaction cost + co-use pair distance). sim/run.py --space <sim_id> = deterministic records (winner, per-term breakdown, top-5 alternatives, input hashes, seed — bit-for-bit reproducible) into sim/records/; spaces are REGISTERED by port bands, unknown/empty = loud exit. sim/apply.py = the SOLE machine [A]-writer (manifest/layout/<subsystem>.lock.json; SimRef(record_id,input_hash)|Exempt(reason) provenance MANDATORY per entry; loader rejects non-[A] keys both on write and load — structural semantic-corruption immunity). tools/check_sim_gate.py = sim-reviewed-or-exempt (§2.10.6): every [A] assignment pinned in sim/sim-gate-baseline.json with validated provenance (sim_ref must name a real record whose snapshot hash matches), encoded thresholds on SEMANTIC size (panel ≤4 declared actions+selectors pre-layout; settings ≤6 per subsystem never per group; nav slots permanently exempt), stale-pin red both directions, --write-baseline refuses without provenance; added to ci.yml's green fleet (18th gate). A-3 navigation-completeness golden: sim/navigation_walk.py drives the REAL S9b panel engine (reachability from hub roots, direct-entry semantic-parent fallback per verification-review §3.4, Back/Home rendered AND static-table-bound, parent re-render population stability) + tests/unit/navigation_golden/ (green over the empty registry, proven non-false-greening by fixture negatives; presets_checked=False LOUD until band-1 preset grammar). V-2: docs/planning/grammar-spike-classification-procedure.md (unit vocabulary + tier semantics frozen from the spike, 6-step per-band procedure, check_escape_hatches reconciliation, retires at cutover per A-19), linked from repo-navigation-map.
- why: the sim-gate "diffs [A]-fields against the merge base" is implemented as the proven committed-pinned-baseline pattern (A-2/A-19/escape-hatch precedent) — equivalent enforcement (any [A] change must update the pin in the same PR, and the pin update itself demands provenance), CI-shape-compatible with fetch-depth-1 checkouts; a git-ref diff mode is a labeled follow-up, not a gap. The navigation oracle scores ABSOLUTE hop depth (a topology-relative shortest-path term would hide deeper topologies — caught by the shallow-vs-deep test). Overlay loading is exposed for the compiler but NOT yet wired into manifest_compile (overlays arm when band-1 manifests exist — labeled follow-up). sim-reviewed-or-exempt REQUIRED-check designation stays OWNER (build-brief §8.1).
- provenance: design-spec §2.10 (~L992/L1029) + §6 gate 4; canonical plan §2.2 V-2/V-3 + §5 step 11 + §11b A-19; Q-0235; verification-review §3.4; superbot tools/sim/ (8) + tools/game_sim/ (2) + tools/grammar_spike/ precedent fleet @7f7628e1

## [D-0021] Layer-V V-5: verified_live registry — §3.3 schema, A-18 tiers, Q-0244 encoded, debt-list model
- status: decided
- date: 2026-07-08
- verdict: verification/ = the V-5 home OUTSIDE the boot chain (nothing under sb/ imports it — the parity/sim precedent). verification/verified_live.py = the verification-review §3.3 schema carried FIELD-COMPLETE (surface kind+id under test, subsystem, scenario steps, test guild/channel/persona, expected visible result, expected DB/audit/event effects, signer, signed_at + build SHA, evidence rows with links, rollback/cutover status, notes) + the A-18 tier as ONE schema field (automated | human_required) + closed vocabularies (SurfaceKind incl. FLOW for Q-0234 judgment walks; EvidenceKind prefix_twin_live/pipeline_replay/parity_golden/human_walk/owner_judgment/transcript/screenshot_video; CutoverStatus pre_cut1..cut3/rolled_back). VERIFIED is a SIGNED FACT (signer+signed_at+build_sha+linked evidence mandatory — "a live sign-off should not be just a checkbox"), with the Q-0244 ruling ENCODED as the automated-tier evidence rule: slash/component surfaces verify via prefix-twin live pass + in-process pipeline-true replay (both required), prefix surfaces via the lane-A live agent test; human_required verifies via human-walk/owner-judgment evidence. debt_list() = the A-18(3) CUT-2/CUT-3 reaction-window publication: unsigned human-tier rows + explicit DEBT rows — published, NEVER blocking (an unverified human row is schema-valid by construction). verification/verified_live.yml = the registry data: the parity.yml roster + kernel as the dashboard (ONE port spine, two lenses — V4 cross-checks both directions), EVERY SUBSYSTEM BORN unverified, records empty (port bands mint per-surface rows; CUT-1 arms the live lanes). tools/check_verified_live.py = the gate (V1 schema/uniqueness/membership, V2 signed-fact + tier evidence, V3 dashboard honesty: subsystem verified needs >=1 record + zero unverified AUTOMATED rows — human debt never blocks the flip, V4 roster spine; --debt-list exit-0 publication); ci.yml 19th fleet gate.
- why: A-18 package items (1)+(3) built as frozen, RESOLVED by Q-0244 (2026-07-07) — the human tier shrinks to optional Q-0234 judgment walks; item (2) (batched lane-B walks per band) is band-execution not schema; the ⚑ owner options (human-tier hard-block CUT-3; signer delegation Q-0222-vs-Q-0234) routed to the question-router, built per the ruling meanwhile. The A-10 capability FACT is honored in the evidence vocabulary itself: the only live slash/component driver is the human lane, so the automated lane's slash evidence is replay-shaped by construction. Subsystem-level dashboard chosen over per-surface seeding: the ~150-250 surface rows need manifests to enumerate honestly (A-16 lesson: denominators come from manifests, not memory) — minting them empty now would fabricate the census.
- provenance: rebuild-verification-review-2026-07-03 §3.3/§4.6; canonical plan §2.2 V-5 + §5 steps 11-12 + §11b A-18; owner ruling Q-0244; A-10 frozen constraint

## [D-0022] K10 PR-1: AI invocation kernel core — task registry replaces the closed AITask enum; provider port; the never-raises gateway pipeline
- status: decided
- date: 2026-07-08
- verdict: sb/kernel/ai/ minted as the K10 band home (canonical plan §2.1 K10 row + §2.4 B-1). tasks.py = the domain-registered task registry CUTTING B-1 contamination #1: an AI task is a registered AITaskSpec (task_id, owner_subsystem, realtime hint, knowledge_domain marker for the A-17 harness), the 17 shipped AITask VALUES frozen verbatim in LEGACY_TASK_IDS (K1 ai_task-kind compat discipline — legacy ids claimable only byte-identical; near-miss normalisation collisions refused; collision-on-differing-spec refused), the kernel seeds only general.nl_answer — every other shipped id waits for its owning band (setup/settings/help=1, logs/moderation=2, platform/code_context=5, btd6/projmoon/video=7). contracts.py = the shipped provider-neutral grammar ported with task: str (registry id) replacing the enum; PolicyDenialReason grew exactly one code (task_unregistered — the registry makes the formerly-unrepresentable representable, degraded deterministically). flags.py = the shipped feature_flags re-homed onto the RC-10 Config seam (install_ai_config(cfg) at the composition root; UNINSTALLED fails closed; boot safety unchanged: AI_ENABLED default off + deterministic default provider). Two shipped dynamic env-name PATTERNS folded to typed CSV fields (RC-10 one-attribute-per-var): AI_TASK_<NAME>_ENABLED → AI_TASKS_DISABLED (kill-switch semantics preserved: global gate first, tasks default enabled); AI_ROUTING_<TASK> → AI_TASK_ROUTING (task=provider:model entries). Two shipped env vars harvested late (S1 sweep miss, source wins Q-0120): AI_TOOLS_ENABLED, AI_SERVER_MEMBER_LOOKUP_ENABLED. CONFIG_FIELDS 47→51. routing.py = shipped model tables verbatim keyed on legacy id strings (anthropic: realtime→claude-haiku-4-5, considered→claude-sonnet-4-6; openai: gpt-4o-mini) + cross-family model correction; FINAL model selection is owner-ratified (flagged). providers/ = the guarded-SDK boundary (base protocol + ToolLoopState budget enforcement shared by both adapters; anthropic_provider with prompt-cached system block + tool_use loop + json_schema unwrap; openai_provider with chat-completions loop; deterministic always-raises fallback = the CI/eval provider); API keys read via flags.api_key_for (Config-installed, never env). gateway.py = the single never-raises chokepoint ported pipeline-order-intact (task-registry admission → flags → safety precheck → REDACTION at the S6-hoisted sb.kernel.observability.redaction chokepoint → routing + per-guild overlay via install_guild_policy_reader PORT (replaces the shipped utils.db.ai coupling; settings/AI band installs the real reader) → provider attempt under wait_for → guarded ai_request_total/ai_request_seconds emission → JSON parse → degrade; AI_FALLBACK_PROVIDER single-retry on transport faults, invalid_json never retried; tool dispatch redaction-wrapped, unoffered tools uncallable, tool faults JSON-stringified). socket_guard.py = the A-17 socket-deny eval guard (deny_sockets(): INET socket construction + create_connection + getaddrinfo raise SocketDenied; restore exception-safe) — the deterministic eval tier's structural network ban (design-spec §8 Q9: required live-judge gates FORBIDDEN; llm_judge stays advisory). Deps: anthropic>=0.40,<1 + openai>=1.40,<2 adopted in requirements.txt with requirements.lock regenerated same-PR (17→33 dists, Q-0105/CL-6 discipline); the SDKs stay ABSENT in test containers (guarded imports under test). No new secrets (ANTHROPIC/OPENAI_API_KEY SecretSpecs + CREDENTIAL_REGISTRY rows existed since S1/S13); no manifest-grammar fields registered (snapshot hash unchanged, no A-2 entries needed — config grammar is not manifest grammar, S1/S15 precedent).
- why: B-1 verified the engine/data seam live: the gateway chokepoint + providers port near-verbatim; the enum/if-chain/btd6-hoist contaminations are cut at the seam instead of ported. The registry validates shape + legacy compat only (namespace-kind ai_task manifest validation arms when band-7 manifests declare tasks). Per-guild policy overlay is a PORT not a table: the shipped ai_guild_policy read becomes an installable reader so K10 carries no settings-band DB coupling.
- provenance: canonical plan §2.1 K10 + §2.4 B-1 + §11b A-17; disbot/core/runtime/ai/{contracts,feature_flags,routing,safety,gateway,diagnostics,providers/*}.py @7f7628e1; frozen design-spec §8 Q9; progress-log layer-V K10 pointers (redaction hoist, lockfile + credential obligations)

## [D-0023] K10 PR-2: NL front-end (policy/router/memory/instructions/audit) + tool orchestration — domain couplings become installable ports and registries
- status: decided
- date: 2026-07-08
- verdict: The NL front-end lands as domain-agnostic engines under sb/kernel/ai/, each shipped coupling cut at a named seam. policy.py = the should-reply resolver PRECEDENCE PORTED VERBATIM (guild hard gate → channel explicit mode → category → NL baseline → role gate w/ explicit-deny-wins + most-permissive level override + cooldown bypass → level gate + fresh-user mention allowance; snapshot hash + dry-run precedence_trace) over an installable PolicyBundle reader (install_policy_bundle_reader — the shipped 4-table ai_guild_policy read; NO READER = GUILD_NOT_CONFIGURED deny-by-default, an unconfigured deployment never silently replies); the shipped ai_permission_service cooldown + fresh-allowance bookkeeping folds in (in-process, forget_guild_throttles lifecycle hook; the K8 cooldown's fixed 5/60s AI axis stays the outer rail — this module owns real policy per the S9 note). router.py = B-1's route classification DE-CONTAMINATED: RouteProbe registry (deterministic, cheap, never-raise → fail-open pass), first-claim-in-order wins, fallback general.nl_answer @0.4 (shipped); RouteContext carries the conversation-cue domains + intake kinds so the shipped entity-less-follow-up and strategy-intake behaviours re-home INTO band 7's probes. conversation.py + memory.py = the shipped buffer verbatim (floor/window/LRU semantics, no-Redis stance) + gather_recent_turns over install_memory_settings_reader (window ∈ {0,15,30,60,120} clamp) and install_history_scanner (the ONE discord-touching leg — band 7 installs it in sb/adapters; scanned bodies never persist). instructions.py = the layered stack with the domain prose CUT: generic layers verbatim (system safety, bot identity/persona, the span-reading task-contract core: bracketed speaker labels + reserved-name/bad-char/length display-name sanitization checked on RAW input, bot_* spans, retrieved_fact entity-kind rule, untrusted containment); the shipped _TASK_CONTRACT's BTD6 playbook + capabilities overview become register_task_contract extensions (band 1 = manifest-generated capabilities; band 7 = knowledge-domain grounding contracts); profile bodies via install_profile_reader. ONE deliberate behaviour fix (shipped-docstring-wins): assistant-turn labeling checks role BEFORE the speaker map — the shipped map-first order mislabels the bot's own reply with the prompter's name when the prompter spoke earlier (replies are stored under the prompter's user_id). feature_facts.py = B-1 contamination #2 cut: register_fact_gatherer per task id (one owner, differing re-claim refused), dispatcher fail-safes to empty facts + error_reason. decision_audit.py + sb/kernel/db/ai_audit.py + migration 0008 = the one-row-per-invocation NL decision audit (shipped decision vocabulary {allowed,denied,skipped,replied,degraded,errored}; success rows forced to reason 'none'; no message bodies; DB faults contained loud-but-never-fatal); AI_DECISION_AUDIT_STORE registered with full S11/S14 lifecycle grammar (MEMBER_ID, erasure_ref kernel.ai.scrub_decision_audit w/ delete_subject_rows as the body-in-waiting, NEW_ONLY, retention=operational) + joined the checker rosters and the parity.yml kernel coverage home (9th kernel table). tools_catalogue.py = the catalogue as a REGISTRY (spec + metadata + handler factory; grounding allowlists DERIVED from grounding_domain; authority-never-widened selection precedence scope→explicit-disable→toolset verbatim). orchestration.py = presets-as-code (kernel ships compatible_default + balanced_helper; the shipped BTD6-narrowed presets move to band 7 via register_profile), most-specific-key resolver over install_profile_key_reader (unknown persisted key degrades to default, never raises), and the plan→execute→verify TEMPLATE as the answer-workflow registry (label → runner → AIAnswerWithEvidence|None; the shipped round-cash MVP re-homes at band 7 as the first runner, Q-0046/Q-0048).
- why: B-1's engine/data split verified: every precedence rule, invariant, and failure posture ports 1:1; only the data (tables, keyword lists, presets, prompt playbooks) moves to bands. Public names deduplicated per the kit namespace fence (resolve_policy / resolve_orchestration / reset_*_for_tests / forget_guild_throttles). Video-task empty-facts short-circuit and the deterministic-list floor are band-7 shell behaviour (they ride the domain gatherer/probe registrations), not kernel.
- provenance: canonical plan §2.4 B-1; disbot/services/{ai_natural_language_policy,ai_permission_service,ai_conversation_service,ai_memory_service,ai_instruction_service,ai_decision_audit_service,ai_task_router,ai_tool_catalogue,ai_orchestration_policy,ai_orchestration_presets}.py + core/runtime/ai/{feature_facts,natural_language_stage}.py @7f7628e1; frozen L0 specs 10/11/13 store grammar (S11/S13/S14 fences)

## [D-0024] K10 PR-3: grounded-answer engine hoisted out of the btd6 namespace; the NL engine core; the A-17 eval harness
- status: decided
- date: 2026-07-08
- verdict: sb/kernel/ai/grounding/ = B-1 contamination #3 CUT — the engine that lived under disbot/utils/btd6/ while projmoon imported across it is now kernel: format.py (sanitise/relative_time/render_grounding_line provenance-never-truncated + the 9,999,999 infinite-sentinel render rule — hoisted verbatim), name_guard.py (canonical-vs-alias matcher discipline verbatim; the ONE domain datum parameterised: alias stoplist is an argument with the shipped default), absence_guard.py (false-absence contradiction mechanics verbatim; the ATTRIBUTE TABLE becomes a registry — register_existence_attribute; the shipped paragon attribute is band-7 data; true negatives structurally unblockable), verify.py (GroundingResult shipped shape; per-task verifier REGISTRY with no-verifier=vacuously-grounded (the shipped general-path default) and raising-verifier=FAIL-CLOSED to the refusal floor; build_grounding_constraint domain-label-parameterised; verify_and_regenerate_once = THE loop the shipped stage implemented twice (btd6 + projmoon) hoisted once — verify → regenerate ONCE with the do-not-state constraint → re-verify → grounded|floor, with the degraded-retry-is-a-provider-outage-not-a-grounding-failure audit honesty carried). nl_engine.py = the domain-agnostic core of the shipped AINaturalLanguageStage terminating in K8's nl-adapter seam: the shipped 6-step flow + grounding + audit as pure engine (policy → route-first-for-audit → cooldown → bare-mention skip → installable preset port (zero-model vetted answers) → registered fact gatherers → memory gather BEFORE the triggering-mention record (carryover grounding sees only prior turns) → instruction stack → gateway → verify+regenerate-once → EXACTLY one ai_decision_audit row per invocation); outbound redaction at the S6 chokepoint before delivery AND before memory; the engine NEVER sends (returns NLOutcome; the band-7 shell delivers via the RC-21 ChannelEmitter then calls note_reply_delivered — cooldown + fresh-allowance charged per DELIVERED reply, the shipped budget semantics); refusal floors register per task (band 7 registers the versioned domain refusals). evals.py = the A-17 machinery: EvalSuiteSpec (the frozen KnowledgeDomainSpec.eval_suite payload shape; content_version binds data-bump⇒golden-bump; min_cases = the ≥10 corpus floor projmoon must mint against) + EvalCase/graders + run_suite executing through the REAL gateway under the deterministic provider with deny_sockets() armed (a provider that tries the network surfaces as a degraded SocketDenied response — the required gate stays deterministic by construction) + the advisory() marker making llm_judge grades STRUCTURALLY non-gating (SuiteResult.passed ignores advisory rows; design-spec §8 Q9) + the suite registry band-7 gates enumerate.
- why: hoist-don't-fork: one loop, one result shape, one constraint builder — the projmoon twin proved the duplication. The verifier registry keeps number-strictness a DOMAIN decision (shipped: numbers verified on btd6.answer only) without kernel knowledge of any domain. CUT-1 note: step 12's live milestone (test-guild boot on the test-bot token) needs a composition root main() that does not exist yet — the boot order is documented (S7-S9 notes + install_ai_config from D-0022); flagged in the K10 question-router block with the owner-side guild/token handoff, per never-block discipline.
- provenance: canonical plan §2.4 B-1 + §11b A-17 + §5 step 12; disbot/utils/btd6/{grounding_format,name_guard,absence_guard}.py + services/{btd6_grounding_service,projmoon_grounding_service}.py + core/runtime/ai/natural_language_stage.py + tests/evals/harness.py @7f7628e1; frozen design-spec §8 Q9

## [D-0025] Band 1 slice 1: settings subsystem — CommandSpec + §2.5 facet grammar minted; the scalar/binding lanes; K10 reader seams; parity flip DEFERRED pending the replay adapter
- status: decided
- date: 2026-07-08
- verdict: sb/spec/commands.py + sb/spec/settings.py mint the band-1 facet grammar: CommandSpec (§2.2 + Group-1 amendments; shipped two-lane capability_required/audience_tier fields verbatim with the frozen authority_ref as the derived property — the sb/spec/panels.py precedent; `kind` is a StrEnum ON PURPOSE for the compiler's duck-read; `capability` = the survival-checkers' grouping field the S15 note promised), CooldownSpec (G-4), SettingSpec/BindingSpec/ResourceRequirement/ProvisioningHint carried SHIPPED-FIELDS-VERBATIM (subsystem_schema.py:75/:109, resource_specs.py) + the §2.5 additive fields, SettingGroupSpec + DomainPanelSpec; the §4.4 fences (bool⇒activation declared, external_side_effects⇒off_until_opt_in, Q-0215 preset_kind, G-5 bounds, resource_link cross-validation) live in check_setting_spec/validate_settings_facets and run at the manifest bridge (sb.kernel.settings.register_manifest_settings — the one-declaration-path §4.1 seam; Activation RE-HOMED to the spec leaf, kernel re-exports). Representation decisions: value_type canonicalizes type→token at construction (a raw `type` is callable and the compiler refuses callables in specs — ported constructors parse unchanged); validator is typed PredicateRef|None (the "serializes by registered ref" rule enforced by type). Stores: migrations/0009 `settings` (guild_id=0=global, key=THE shipped persisted vocabulary verbatim — 124 keys/17 modules harvested to sb/domain/settings/keys.py; per-band manifests claim their slices) + `subsystem_bindings` (route-truth, slot for multiplicity); both NAME_STABLE/RENAME + bears_value=False ⇒ DECLARED_LOSS by posture (Q3-B — config is re-enterable; no reverse importer, and registering one would red the both-directions fence). Writes: four K7 CompoundOpSpecs (settings.set_scalar/clear_scalar/bind/unbind; NATURAL_KEY — upsert/keyed-delete intrinsically once; op-level authority_ref="" = ADMIN floor per shipped v1 policy, per-spec capability narrowing rides the governance band; settings.changed emitted BEST_EFFORT verbatim); an undeclared persisted key is REFUSED at the leg (§4.1 no-raw-KV). Reads: SettingResolution ported from services/settings_resolution.py onto the tri-state chain (provenance vocabulary re-based: explicit|global|activation|default). K10 seams: install_ai_readers wires policy-bundle/memory/profile-key/profile readers over the K7 resolve seam, FAIL-CLOSED until band 7 declares the ai.* specs (typed ai_guild_policy tables = a follow-up slice); settings.explain/settings.propose claimed byte-identical; register_task_contract("") = the manifest-GENERATED capabilities overview. S15 latch store installed (platform.* rows, sync write-through view + boot warm-up load_platform_state). A-15: export_guild_config (manifest-driven explicit rows + bindings) landed; the RESTORE draft (Producer IMPORT_REPAIR over spec-06) is successor work. Sim gate: CommandSpec.help_section_order pinned by an explicit Exempt legacy-seed overlay (manifest/layout/settings.lock.json, §9.3 confidence rule). V-2: tools/grammar_fit/measure.py minted with band-1's first rows (45% on the platform slice — the tier-3 rows are the platform's own thin seams, by design; the settings DATA units land with their owning bands). Kernel edits (all additive, fence-preserving): P1 ENSURE_REFS re-arm hook (clear_ref_table wipes import-time decorator registrations without evicting module caches — real manifests need idempotent re-arm), sim.space.a_tagged_fields imports ALL sb.spec modules (the D-0020 "fields join automatically" promise), governance._surface_of reads `kind` and counts `both` as slash-delivered.
- **PARITY FLIP DEFERRED (the load-bearing call):** the task brief says flip settings→ported at port; the MERGED gate driver (tools/run_golden_parity.py, PR #18) rules a flip without the new-bot replay adapter "impossible — the gate refuses it loudly", and ported→pending is a ONE-WAY DOOR (A-16 R4). Source + merged PRs win (Q-0120): rows stay `pending` until the fake-HTTP-over-sb-pipeline replay adapter exists and the subsystem's goldens replay green — exactly the "goldens green" clause the canonical plan step 13 puts AT the flip, and the "later per-subsystem activity" the kickoff mandate names. Flipping now would permanently redden the owner's designated required check. The replay adapter is named band-1 successor work.
- why: the platform proves itself on itself — the settings grammar + lanes are the substrate every later band declares against; deviations over-weighted fidelity to merged kernel contracts over brief phrasing per Q-0120.
- provenance: design-spec §2.2/§2.5/§4/§9.2-9.3; frozen-l0-grammar Group 1; canonical plan step 13 band 1 + A-9/A-15/A-16; disbot core/runtime/{subsystem_schema,resource_specs}.py + utils/settings_keys/* + services/{settings_resolution,settings_mutation}.py @7f7628e1; tools/run_golden_parity.py + parity/parity.yml @ PR #18

## [D-0026] Band 1 slice 2: HELP + DIAGNOSTIC subsystems — projection-first ports, hub surfaces, help.answer claimed
- status: decided
- date: 2026-07-08
- verdict: HELP = help-as-projection executed literally (K8 row): sb/domain/help/service.py derives the command inventory from EVERY sb.manifest declaration (name+summary per subsystem) and feeds the S9b help_panel_spec — help can never drift from the manifest; `help` CommandSpec (BOTH, domain lane audience_tier="user", slash_common) routes to PanelRef("help.home"); `help.answer` claimed byte-identical (LEGACY_TASK_IDS). The shipped per-guild help OVERLAY lanes (services/help_overlay*.py — operator copy overrides + mutation pipeline + store) are a NAMED successor slice: they need their own store + K7 ops. DIAGNOSTIC = the operator status surface over KERNEL truth sources only: platform_status() aggregates lifecycle.get_phase(), the S6 findings ring, the K7 declaration registry, and the K10 AI collector; ai_diagnostics() = the band-map obligation (diagnostics.get_default_collector().snapshot(), field names per AIDiagnosticsSnapshot) + recent_decisions() = decision_audit.query passthrough; `diagnostics` CommandSpec (shipped surface name, BOTH, slash_common) routes to PanelRef("diagnostic.hub"). The shipped deep-diagnostic subcommand fleet (cogs/diagnostic/platform_group.py: consistency/caches/backfill/anchors/findings/health/...; diagnostic_embeds.py 2.5k lines) re-homes surface-by-surface as successor slices — this slice is the hub + service seams. Checker evolution: check_symbol_shadowing grew MODULE_HOOKS={"ENSURE_REFS"} (a per-module PROTOCOL hook like the MANIFEST assignment — an interface, not a shadowing accident). Sim gate: the two new [A] command keys pinned by the same Exempt legacy-seed overlays (help.lock.json, diagnostic.lock.json). Parity rows help/diagnostic stay `pending` per the D-0025 replay-adapter ruling.
- why: projection-first keeps the two discovery/observer surfaces zero-drift by construction (the manifest is the single source), lands the band-map K10 obligations early, and leaves the overlay/deep-diagnostic data lanes to slices that can port them with their stores instead of stubbing them dark.
- provenance: design-spec §9.2 band 1 + K8 help-as-projection row; disbot cogs/help_cog.py + cogs/help/ + services/help_* + cogs/diagnostic/ + services/diagnostics_service.py @7f7628e1; D-0025

## [D-0027] Band 1 slice 3: SETUP skeleton — G-19 WizardSectionSpec frozen (A-9(2) flip), the 10 registrants verbatim, setup task claims
- status: decided
- date: 2026-07-08
- verdict: sb/spec/setup.py freezes G-19: the shipped SetupSection record (services/setup_sections.py) carried DECLARATIVE-FIELDS-VERBATIM (slug/label/emoji/order/step/op_kinds + the slug<=64 lowercase / label<=80 caps as check_wizard_section fences); the shipped `run` async callback becomes a registered ROUTE (§2.0 callable→ref; None = declared-not-yet-routed, hub renders coming-soon rather than stranding). SubsystemManifest GREW `wizard_sections` (additive, defaulted; A-2 ledgered; the G-19 "+ facet" note executed). rebuild-amendments.yml G-19 flipped pending-gate-0 → in-spec with consumers WIDENED to all 10 live registrants (A-9(2) executed verbatim; the G-10 flip-row precedent followed). sb/domain/setup/sections.py = the ported registry (validate-at-register, refuse differing dup, ORDER-field ordering never import order) + the 10 shipped registrants harvested verbatim from views/setup/sections/* (labels, orders incl. the 55/56 roles/role_templates adjacency, op_kinds, emoji). /setup CommandSpec (BOTH, config lane ADMIN floor, slash_common) → setup.hub projection panel. setup.suggest/setup.explain claimed byte-identical. Sim gate: WizardSectionSpec.order tagged [A] (hub arrangement IS the §9.3 hub-topology search space) — all 12 setup [A] keys pinned Exempt legacy-seed.
- NOT this slice (named successor work, A-9's remaining mass): the section FLOWS (channels/cleanup/cog_routing/... interactive panels + their K7 ops), the Essential Setup spine + quicksetup row, setup_session/progress/blockers/readiness/plan/draft services, the AI advisor (setup_ai_advisor + advisor_review + natural_language_advisor), recommended-ops + customize→preview→apply over the K9 draft lane (Producer.HUMAN_SETUP stays reserved-but-unconsumed per A-9(3)), setup_diagnostics.staged_repair_ops re-home (flagged ⚑ in A-9).
- why: the registry IS the seam every section flow plugs into (the shipped hub already derived its layout from it); landing it first-class with verbatim data gives successor slices a compile-checked target and makes the A-9 roster promotion real instead of aspirational.
- provenance: canonical plan §11b A-9; disbot services/setup_sections.py + views/setup/sections/* @7f7628e1; docs/planning/rebuild-amendments.yml G-19 row; D-0025/D-0026 conventions

## [D-0028] The NEW-bot replay adapter (sb/adapters/parity/) — goldens replayable; parity flips stay at zero honestly
- status: decided
- date: 2026-07-08
- verdict: the D-0025 named unblock built. `sb/adapters/parity/` = a fake-HTTP/gateway transport over sb/'s REAL pipeline satisfying the old harness's driving contract (`Harness.start()` / send_command / invoke_slash / click / take_calls / take_events / close): boot = the replay composition root (preflight → owner/secret/AI installs → db.init against the CI Postgres service → LIVE-manifest target index → manifest settings registration + read ports → panel runtime with a capture presenter → RC-21 capture emitter → ONE tapped EventBus → RUNNING); transport records in the imported corpus's EXACT wire vocabulary (send_message/interaction_response/followup_send…, response_id minted only on channel sends — the click-targeting contract); runner produces harness_version-1 documents REUSING the imported Normalizer/dbsnap/golden_path/_diff_docs so a diff line is behavior, never dialect. Case sourcing is OLD-BOT-FREE: curated cases import typed from parity.cases; sweep cases reconstruct LOSSLESSLY from their own golden documents' input records (465/465 reconstructable — pinned by test). tools/run_golden_parity.py rewired to bind sb.adapters.parity.boot: the gate leg replays ported subsystems' goldens for real; the report leg now replays the FULL corpus with per-subsystem green ratios (red-until-parity, exits nonzero while anything diffs).
- flips: ZERO rows flip in this PR. The adapter makes flips POSSIBLE, not TRUE: a `ported` flip needs that subsystem's goldens replaying GREEN in the gate (byte-parity after normalization), and local capture of settings.hub_open already shows honest diffs (the new panel wire output ≠ the old bot's embeds; plus cross-subsystem noise — every old prefix golden carries xp/ai_decision_audit rows from unported bands). A-16 one-way door: flip only on green evidence, per-subsystem, as ports converge.
- deviations (all deliberate): (1) the harness seeds the SAME placeholder-env convention as the old harness's boot.py — sb/adapters/parity/boot.py added to the config-accessor seam allowlist (substrate.config.json) + check_config_usage ALLOWED_PREFIXES, the ONE sanctioned env-seeding site under sb/ (it reads no config around the typed accessor); (2) the harness builds its dispatch index from LIVE manifests, not the snapshot — build_runtime's snapshot index projects specs structurally (routes serialize as {"$ref": ...}; also its `_build` iterates the subsystems DICT as a list — a latent leg-B-only gap, labeled follow-up for whoever writes main()); (3) reconstructed click steps with normalized (session-dynamic) custom_ids are impossible by construction — none exist outside curated cases (curated load typed).
- why: the merged gate driver ruled a flip without the adapter impossible (D-0025); every band's parity evidence flows through this seam, so it lands FIRST as its own PR, before band 2.
- provenance: design-spec §6 gate 5; parity/harness/* @7f7628e1 (the contract); D-0019 (V-1 import), D-0025 (flip deferral); tools/run_golden_parity.py PR #18

## [D-0029] Band 2 slice 1: MODERATION + LOGGING (operator spine core) — audited K7 lanes, guild-action port, fan-out engine, K10 claims
- status: decided
- date: 2026-07-08
- verdict: moderation = shipped commands VERBATIM (modmenu/moderation/warn/timeout/kick/ban/unban/clearwarnings/warnings/modlogs) over the audited seam: mod_logs + warnings stores (migration 0010, shipped shapes; NAME_STABLE forward map, bears_value=False ⇒ DECLARED_LOSS posture; MEMBER_ID ⇒ erasure bodies REGISTERED now — moderation.tombstone_subject keeps history rows and drops subject ids, moderation.clear_subject_warnings); six K7 CompoundOpSpecs with the shipped WarnOutcome escalation ladder INSIDE the DB leg (threshold → escalate → reset count, own history row, one txn) and the Discord state mutation as a post-commit EFFECT leg through the domain-minted GuildModerationActions port (RC-21 sibling: kernel-defined, adapter-implemented, not-installed default raises ⇒ PARTIAL + finding; method names *_member — the egress AST fence owns the bare verbs); ONE moderation.action_taken event (BEST_EFFORT, subscribers dispatch on payload.action, shipped contract); policy resolution over THE K7 settings seam with services/moderation_config.py defaults carried verbatim; moderation.assist claimed byte-identical (ADVISORY AISuggestion contract — never auto-executes). logging = manifest key `logging` in module server_logging (stdlib-shadow discipline): the shipped !logging group via the NEW CommandSpec.group field (K1 parent_group was projectable but undeclarable — A-2 ledgered, roles registered, snapshot regrown); 12 scalar keys + 6 channel BindingSpecs (legacy KV aliases per decision 3); handlers write ONLY through the band-1 settings ops (§4.1 one-write-path); the fan-out engine subscribes moderation.action_taken on THE bus → bound mod channel via the RC-21 emitter (TRUSTED) with the shipped counter vocabulary; logs.triage claimed byte-identical.
- deviations (deliberate, spec-wins-over-shipped): (1) !kick now carries a ConfirmationSpec (typed challenge) — the frozen §2.7 fence rules IRREVERSIBLE ⇒ confirmation and kick has no honest compensator; ban/unban declare each other as compensators (compensatable ⇒ no confirm), warn/timeout declare reversible (liftable). Parity goldens for kick will diff on the confirm step — that is the DESIGN winning, recorded for the flip review. (2) SettingSpec grew an `off_until_opt_in` PROPERTY bridging the compiler P6 external_cost fence's pre-band-1 duck-read dialect to the §4.4 activation truth (not a registered field). (3) K7 op keys get refs-table twin registrations (P2 resolves WorkflowRef command routes against the refs table; the engine still resolves via REGISTRY). (4) `logging create` (channel provisioning) declared + politely refused until the resource-provision port arms (server_management slice).
- also: harness (sb/adapters/parity/boot.py) tolerates re-registration of manifest settings (second start in one process) and duck-reads CommandSpec.qualified_name for group dispatch.
- why: the operator spine is band 2's core; moderation+logging land first because they own the curated goldens, the K10 claims, and the event->fan-out seam every other band-2 subsystem consumes; the frozen grammar fences (§2.7 confirmation, §4.1 one-write-path, RC-21 egress) win over shipped ergonomics wherever they disagree, with each divergence recorded for the A-16 flip review.
- provenance: disbot cogs/{moderation_cog,logging_cog}.py + services/{moderation_service,moderation_config,server_logging_config}.py + utils/db/{moderation,migrations}.py @7f7628e1; canonical plan step 13 band 2; D-0025 recipe; frozen design §2.7/§4.1/§4.4

## [D-0030] Band 2 slice 2: the operator-spine eight (admin/server_management/channel/cleanup/automod/security/welcome/counters/image_moderation) — declaration-first with pure decision cores
- status: decided
- date: 2026-07-08
- verdict: every remaining band-2 subsystem gets its manifest (shipped command names/aliases VERBATIM), its settings-keys slice claimed from sb/domain/settings/keys.py (SettingSpec/BindingSpec, shipped defaults verbatim from the *_config.py services; bool keys carry §4.4 activations; legacy KV pointer keys become BindingSpec aliases per decision 3), a REAL generated hub read-view (sb/domain/operator_spine.hub_spec: FieldsBlock over THE K7 declaration registry — the band-1 projection family, zero per-subsystem panel code), and its PURE decision core where the shipped subsystem had one: automod rule engine (spam/invites/caps/mentions/cross-channel/duplicate over supplied MessageFacts — deterministic-by-design, the go/no-go §3 injectable-clock lesson), security RaidWindow + age_gate_action (unknown action degrades to alert, never harsher), welcome template renderer ({user}/{server}/{count}, unknown braces pass through), counters template renderer. cleanup gets the prohibited_words store (migration 0011, NAME_STABLE, DataClass.NONE) + word add/remove K7 ops + list handler. admin re-homes onto kernel truth: coglist => the manifest registry, slashes => the declared slash surface, loglevel real, restart => K5 request_restart; serverstats = declared port-stub. image_moderation's master switch carries external_side_effects=True — the §4.4 privacy gate AS GRAMMAR (the fence's canonical example).
- the pending-terminal pattern: Discord state mutations whose port has not armed (17 channel ops, cleanuphistory, counterpreset, serverstats) route to `pending_handler` — DECLARED surface (K1 reservation + parity denominator real today) with an honest BLOCKED refusal, never silent, never fake. The channel-ops port + these handlers are the NAMED successor slice; the gateway feeds (on_message -> automod/image_moderation, member-join -> security/welcome) arm with the message/member bands, consuming the pure cores as-is.
- NOT ported (deliberate, the sweep's excluded-with-reason class): cog / loadall / unloadall (extension management — no analog in the compiled architecture: subsystems are manifests, not runtime-loadable cogs) and syncslash (deploy-ops; tree sync is sb/app/tree_sync.py + boot-gate leg C).
- A-14 anchors: welcome.entry_role BindingSpec (the deny-until-role gate's role source — ROLE_SET lane is already fail-closed at K6) + welcome.min_account_age_days + security age screen = the join-verification inputs; the join-gate WIRING is member-band work (anchored, not faked). Ban-appeal intake: no shipped source surface exists — anchored as the moderation hub's successor action set, not invented.
- why: Sequence C wants the operator spine DECLARED and its engines portable before the gateway feeds exist; declaration-first with honest pending terminals converts the whole band-2 namespace + settings vocabulary into compiled, checker-guarded truth in one pass, and every pure core is already unit-tested against the shipped thresholds.
- provenance: disbot cogs/{admin_cog,server_management_cog,channel_cog,cleanup_cog,automod_cog,security_cog,welcome_cog,counters_cog,image_moderation_cog}.py + services/{automod_config,security_config,welcome_config,counter_config,image_moderation_config}.py + cogs/cleanup/schemas.py @7f7628e1; D-0029 conventions; parity/_sweep_skips exclusion classes

## [D-0031] Band 3 slice 1: ECONOMY core — the audited money seam, five stores, reverse importers, the INV-F reconciliation invariant
- status: decided
- date: 2026-07-08
- verdict: economy = shipped commands VERBATIM (economymenu/economy/daily/work/shop/balance/pay/setlogchannel/joblist, aliases bal/wallet/transfer/jobs, the shipped @commands.cooldown rates as G-4 CooldownSpec data) over the audited seam. STORES (migration 0012): `economy_balances` = the money aggregate EXTRACTED from the shipped `xp.coins` column into its own table — forward_map_kind=RENAME (pure bijection), bears_value=True ⇒ REVERSE_IMPORTABLE with an aggregate absolute-value importer writing back over old `xp.coins`; `economy_audit_log` = THE hottest audit table, NAME_STABLE + LEDGER + bears_value=True ⇒ REVERSE_IMPORTABLE by mutation_id (the ADDITIVE mutation_id column is the S14 ledger-reinsert conflict key, minted per movement; the CUT-2 alias map adds the column+unique index old-side before any rollback window — rollback-playbook re-bind note); `economy` (daily/work tracking) + `job_progress` NAME_STABLE, bears_value=False ⇒ DECLARED_LOSS (Q3-B: streak/mastery state is re-enterable); `inventory` NAME_STABLE, bears_value=False DELIBERATELY — item ownership is game state whose MONEY trail lives in the reverse-importable ledger (every purchase is a shop:* ledger row), so rollback loss is declared-and-ledger-recoverable rather than double-counted as value. All five MEMBER_ID with erasure BODIES registered now (balances/track/jobs/inventory delete; audit TOMBSTONE — rows stay, subject ids → 0, the money trail must keep adding up). OPS: four K7 CompoundOpSpecs (economy.daily/work/pay/buy), all NATURAL_KEY (row-locked cooldown anchors + one-statement conditional grant/debit — the shipped race closures), authority_ref="user" (TIER lane, member-facing loops); every op's DB leg writes balance + ledger row in the SAME txn (CRIT-9 sole-path, INV-F's runtime half); `economy.balance_changed` emitted BEST_EFFORT post-commit with the shipped payload keys (pay emits TWO — one per side, the shipped transfer semantics). buy = the shipped Q-0071 shop workflow verbatim: conditional unique grant FIRST, audited debit second, an unaffordable debit rolls the grant back. INV-F data half: InvariantSpec `economy.balance_ledger_reconciliation` (RECONCILIATION over economy_balances×economy_audit_log, baseline_ref zeros-until-CUT-2, tolerance 0, QUARANTINE_ONLY per Q-D13 — the money repair direction is never guessed). THE XP BOUNDARY: shipped work pays coins AND XP; XP is band 4 — install_level_reader (default level 0 ⇒ tier-1 jobs only) + install_xp_awarder (default ⇒ xp_pending=True on the optional EFFECT leg) are the honest waiting ports, never fabricated levels. Log fan-out = the band-2 server_logging engine pattern (bus → bound economy.log_channel → RC-21 emitter, TRUSTED); the shipped on-ready #economy-log auto-provision rides the manifest ResourceRequirement (offer_on_enable) — wiring = the server_management provisioning slice. `!setlogchannel` routes through band-1 settings.bind (§4.1 one-write-path; the P0-3 binding-lane convergence carried: economy_log_channel is the BindingSpec legacy alias). The daily tier draw is the ONE randomness point, injectable (set_rng_for_tests) for tests/replay.
- deviations (deliberate): (1) bare `!work` lists eligible jobs and `!work <job>` runs the op — the shipped dropdown flow is the panel-action slice (successor work, every hub so far); no new command name minted (extended args on the shipped name). (2) Shop purchases have no prefix command shipped-side and none is invented — economy.buy is panel/test-facing until the panel-action slice. (3) sb/domain imports tools.importer.reverse for importer registration (the sb/app/boot_gate.py tools-import precedent; registration must ride manifest import so the both-directions coverage fence sees it).
- why: band 3's contract IS the money trail — the aggregate and the ledger commit-or-roll-back together, the hottest audit table imports name-stable, and the two value stores are the repo's first REVERSE_IMPORTABLE registrations, exercising the S14 helpers exactly as designed.
- provenance: disbot cogs/{economy_cog,inventory_cog}.py + services/{economy_service,economy_helpers,economy_flow_service,shop_purchase_workflow}.py + utils/db/{economy,inventory}.py + views/economy/* @7f7628e1; canonical plan step 13 band 3 (INV-F); frozen L0 spec 11 T-2 + spec 13 §2.4; D-0029/D-0030 recipe

## [D-0032] Band 3 slice 2: TREASURY + INVENTORY — the pool between economy and governance; the unified browser over the coupled namespace
- status: decided
- date: 2026-07-08
- verdict: treasury = the shipped group VERBATIM (`treasury` [bank/pool] → hub; `treasury contribute` [donate/deposit]; `treasury grant` [disburse/payout] — CommandSpec.group, the band-2 logging convention) over two K7 CompoundOpSpecs mirroring services/treasury_service.py exactly: contribute debits the member (conditional one-statement debit; InsufficientFunds rolls everything back) + credits the pool in ONE txn with the economy ledger's `treasury:contribute` row as the money trail; disburse conditionally debits the pool (NEVER overdraws — underfunded writes nothing) + credits the target with the MANAGER as ledger actor_id (attribution verbatim). AUTHORITY IS THE SHIPPED MAPPING MADE EXACT: disburse was perms_or_owner(manage_guild) and `staff` is THE tier whose TIER_DISCORD_PERMISSION bit is manage_guild — authority_ref="staff" (no ADMIN-floor over-tightening); contribute/user. `guild_treasury` store (migration 0013, shipped migration-092 shape, NAME_STABLE, AGGREGATE, bears_value=True ⇒ REVERSE_IMPORTABLE with the S14 aggregate absolute-value importer; DataClass.NONE — no member ids on the pool row, the attribution lives in the erasure-tombstoned ledger). Pool⊄ledger reconciliation invariant (pool == baseline + Σ(−delta) over treasury:* rows, QUARANTINE_ONLY per Q-D13) gives the value store its coverage. Both ops emit `economy.balance_changed` (owner: economy) — the shipped cross-subsystem emission carried; treasury declares no events of its own. inventory = projection-first: the shipped pure display algebra (rarity grouping, three total-order sort modes, hub preview lines) as pure functions; build_combined_inventory over the economy `inventory` store + install_extra_inventory_source (the band-6 mining/fishing merge seam — the shipped get_mining_inventory leg waits honestly); `!inventory` (inv) renders the grouped hub text; inventory.hub read-view over the catalogue. No inventory stores/settings — the table is slice 1's, sole-writer economy.store.
- deviations: (1) the shipped Contribute modal/dropdowns are the panel-action slice (hub convention); (2) inventory category detail views (paging/type filter/sort cycle) likewise — their pure cores landed and are unit-pinned so the panel slice is wiring, not logic.
- why: the treasury completes band 3's money graph (individual ⇄ collective) on the same one-txn ledger discipline, and the browser proves the coupled item namespace as a projection instead of a second data home.
- provenance: disbot cogs/{treasury_cog,inventory_cog}.py + services/treasury_service.py + utils/db/treasury.py + views/treasury/* @7f7628e1; sb/spec/authority.TIER_DISCORD_PERMISSION (visibility_rules.py verbatim); D-0031 conventions

## [D-0033] Owner answers applied: the six PR #30 router rulings recorded + the K10(b) routing-table override
- status: decided
- date: 2026-07-09
- verdict: the six question-router blocks the owner answered in PR #30 (merge c78a87b5, 2026-07-08) are RECORDED and, where they demanded code, APPLIED. (1) S11: rubric-v2 classes 11/12/13 + the 13>12>11 precedence adopted AS-SHAPED — docs/planning/rubric-v2-classes-11-12-13.md status flipped plan→adopted; zero code change (mechanics were live since D-0014). (2) S13: CL-1 full recovery arm ratified as built; CL-3 lockfile-with-adopt-freely ratified as built; CL-2 = a SCOPED POLICY AMENDMENT — the Q-0213 `*Delete` brake narrows for credential revocation ONLY (the closed RevocationRef set is agent-runnable in a compromise response; resource/store deletion stays ask-first) — encoded as the CUT-1 obligation on sb/kernel/credentials/rotation.py's RotationProvider (comment + runbook; no dispatch exists to change today) and docs/operations/credential-lifecycle.md updated; CL-5b (SB_PROD_ATTEST custody) stays OPEN, carried forward, re-append near CUT-1. (3) S14: Q1 = the 24h pg_dump floor as built (no infra spend); Q2(i) = HARD CUT-3 GATE — restore-verify.yml's conclusion must be green or CUT-3 does not proceed (nothing consumes the workflow's result as a gate yet: RE-FLAG AT CUT-3 PLANNING, canonical plan step 17); Q3 posture B stands, N carries the Q-D15 default 7d until superseded. (4) S15: PG-1 slash-first+parallel-verification, PG-3 binding cutover gate, PG-4 admin-notice+re-apply overlay, PG-5 SAME-application-id reuse — all four ratified as built, zero code change; PG-5 is a CONFIRMED NON-NEGOTIABLE cutover constraint (never stand up a new app id; call it out at CUT-2/CUT-3 planning). (5) V-5: (a) debt-list stands — was ALREADY ruled by superbot's Q-0244 (router-hygiene gap noted: cross-check superbot's router for already-ruled Q-numbers before minting a "pending" block); (b) DELEGATED SIGNER ALLOWED — a trusted alt-account operator may sign verified_live rows (no enforced allowlist yet; the signer+timestamp+build-SHA+evidence fields keep it auditable; recorded for the first allowlist build pre-CUT-1). (6) K10: (b) routing-table OVERRIDE APPLIED to sb/kernel/ai/routing.py — _ANTHROPIC_FALLBACK_MODEL sonnet-4-6→haiku-4-5 and the Sonnet set trimmed to the deeper-reasoning trio {settings.propose, moderation.assist, btd6.strategy_review}; setup.suggest/logs.triage/code_context.explain ruled down to haiku-4-5 (the exact trio kept on Sonnet is this port's judgment call under "reserve it for tasks that need deeper reasoning" — owner veto flips rows, not shape); gpt-4o-mini stays as the only OpenAI value with an UNRELIABLE flag comment (replacement model TBD — owner has not named one; openai is non-default so nothing routes there unratified); (c) tool-budget caps stand as built; (a)/(d) remain owner-action items (API keys + AI_ENABLED; test guild + token) — unchanged, still flagged.
- why: PR #30's routing results explicitly instruct decisions.md recording, and K10(b)/CL-2/S11 carry material posture changes later bands must read from the ledger, not a router diff.
- provenance: superbot-next PR #30 (merge c78a87b5) docs/question-router.md Answered section; sb/kernel/ai/routing.py @7f7628e1 shipped tables; D-0014 (S11 mechanics), D-0016 (S13), rotation runbook

## [D-0034] Band 3 slice 3: the PANEL-ACTION slice — the shipped economy/treasury/inventory interactions as declared grammar, and the G-10 arming it forced
- status: decided
- date: 2026-07-09
- verdict: the slices D-0031/D-0032 deferred land as PanelActionSpec/SelectorSpec/ModalSpec DATA over the already-audited K7 ops — zero new money paths, zero new stores. ECONOMY: economy.hub gains the shipped eight actions with the shipped persistent-view custom_ids pinned VERBATIM via custom_id_override (economy:daily/work/shop/balance/inventory/jobs/treasury/overview — the goldens' click vocabulary; layout = the shipped 3/4/1 row arrangement); Daily runs WorkflowRef economy.daily directly; Work/Shop open DECLARED sub-panels: economy.jobcenter (the shipped _WorkSubView Job Center — job_select SelectorSpec whose options are PROVIDER-FED per-user available_jobs and whose on_select IS WorkflowRef economy.work) and economy.shop_panel (the shipped _ShopSubView — static item_select over the coupled SHOP_ITEMS namespace, on_select = WorkflowRef economy.buy, the Q-0071 grant-first workflow re-deciding raced clicks in-txn); !shop re-routes PanelRef economy.shop_panel (the shipped interactive flow; economy.shop_view stays registered as the text fallback). TREASURY: the shipped TreasuryView made declarative — Contribute = defer_mode=MODAL + ModalSpec treasury.contribute_form (the shipped one-field modal, title/label verbatim; submit re-enters the frozen MODAL adapter and runs treasury.contribute with args["amount"]); Refresh = re-open PanelRef; disburse stays command-only (shipped posture: a member panel never moves pool coins out). INVENTORY: the hub provider becomes the invoker's grouped preview (shipped build_hub_embed semantics incl. the empty-state copy) + seven open-category actions over the STATIC catalogue category population -> seven declared detail panels (ListBlock provider rendering the rarity-grouped default view via the D-0032 unit-pinned pure cores; sort_options/filter_options/default_sort declared as ListSpec data). KERNEL ARMING (K8/G-10 — the first action-bearing panels forced the declared-but-unarmed paths live, all additive, no schema change): (1) resolve() gains the modal-issued TERMINAL — defer_mode=MODAL on a non-MODAL surface opens the declared form and STOPS (mirrors confirm-issued; trace note "modal-issued"); the MODAL-surface re-entry acks like AUTO and dispatches with the field args; previously open_modal fell through to dispatch (nothing exercised G-10 before this slice). (2) register_panel mints ModalSpec.modal_id into the ONE static table bound to the declaring action (the spec's "custom-id root" made routable). (3) the MODAL adapter falls through to the §3.4 static table exactly like the COMPONENT adapter (panel modals were unreachable otherwise). (4) render_panel materializes PROVIDER-FED SelectorSpec.options_source at render time (grammar said "static tuple | provider"; the render path only handled tuples) — empty/broken providers degrade to a disabled selector showing empty_state, never a crash. (5) SelectorSpec gains a `route` PROPERTY (-> on_select; the off_until_opt_in property precedent — resolve()'s duck-read, not a schema field). (6) manifest_compile P6 never_strand now reads placement from PanelSpec.layout (the ONE arrangement structure) — the pre-layout duck `components` fixture path kept; selectors recognized as their own declared population. DOMAIN GLUE: _job_from/_item_from accept the COMPONENT adapter's args["values"] spelling (param > values > argv precedence); treasury _amount_from parses the modal's free-text field and rejects non-numeric with the shipped copy ("… is not a whole number of coins."). SIM: 6 new above-floor [A] keys (economy.hub + inventory.hub layout triples) pinned Exempt legacy-seed; sub-panels/detail panels are below the PANEL_FLOOR (auto-exempt); baseline regenerated (317 pins).
- deviations (deliberate, flip-review material): (1) back/child-return ids are the engine's nav:* vocabulary, NOT the shipped economy:back/economy:shop:back/economy:work:back closure buttons — §2.4 serialized navigation replaces BackTarget/chain_back by design (band-1 convention; kick-confirm precedent: design wins over shipped). (2) The interactive re-sort / type-filter / page-cycle of the shipped inventory _CategoryView are DECLARED (ListSpec sort/filter options) but not yet interactive — the §2.3 "one shared BrowserView engine" is NAMED SUCCESSOR KERNEL WORK (band 6 leaderboards/browsers need it; the pure cores + declared algebra make it wiring, not logic). (3) Inventory hub category buttons are static (no per-render count badge / non-empty filtering; empty categories show their declared empty_state) — dynamic labels are not grammar; the shipped preview lines live in the hub provider instead. (4) Selector option labels are the option VALUES v1 (the shipped emoji/price-decorated labels await the adapter's option-label materialization at CUT-1 wiring). (5) balance/jobs render as RESULT_CARD replies, not in-place hub edits (the grammar's result-render model). (6) !inventory keeps its HandlerRef text route (the panel is invoker-locked; the shipped @user argument browses others' inventories — text keeps that; the hub panel is the invoker's).
- why: this closes band 3's declared surface — every shipped band-3 interaction now exists as compiled, checker-guarded grammar riding the ONE audited money seam, and the G-10/selector/never-strand arming is the K8 substrate every later action-bearing band (setup wizard flows, games, moderation hub actions) builds on.
- provenance: disbot views/economy/{main_panel,shop_panel,work_panel}.py + views/treasury/menu.py + cogs/inventory_cog.py (UnifiedInventoryView/_CategoryView) @7f7628e1; sb/spec/panels.py G-10/L-24 riders; D-0031/D-0032 deferrals; design-spec §2.3/§2.4/§2.6/§3.4

## [D-0035] The §6 named gates get their NAMES: named-gates.yml + check_compat_frozen (gate 6) — un-blocking the owner-armed ruleset
- status: decided
- date: 2026-07-09
- verdict: the owner armed required status checks on main (observed live at PR #35: merge refused with "N of 5 required status checks" where 4 stayed "expected" — i.e. required NAMES no workflow job carries; the one match was golden-parity's `gate`-shaped leg). Design-spec §6 names six gates (code-quality / manifest-validate / architecture / sim-gate / golden-parity / check_compat_frozen) and build-brief §8.1 splits the work: AGENTS AUTHOR THE WORKFLOW FILES, the owner flips settings. So: `.github/workflows/named-gates.yml` carries the six §6 NAMES as jobs, each mapped onto the tooling that already implements that gate's semantics (ci.yml keeps the full 19-checker fleet — these are the named entrances, not replacements): code-quality=pytest; manifest-validate=manifest_compile+namespace+escape-hatches(A-19-inside-the-gate)+schema-growth+amendments; architecture=symbol-shadowing+no-skip+config-seam+metric-cardinality+egress; sim-gate=check_sim_gate; golden-parity=the GATE leg vs the CI Postgres (check_parity_depth inside, per A-16; the red-by-design full-corpus `report` job stays in golden-parity.yml and must NEVER be required); check_compat_frozen=the NEW tools/check_compat_frozen.py — §6 gate 6 built for real: derives the compat artifacts from the committed manifest export (subsystem keys, command names/aliases/groups, event literals + payload field sets, the legacy custom_id list incl. G-10 modal roots, the frozen AITask value strings) and diffs against the committed pin compat/compat-frozen.json; ANY drift is red until the pin is regenerated (--write) in the same PR. The pin should be CODEOWNERS-routed (owner: add `compat/` to CODEOWNERS — the §5.3 amend-with-owner-sign-off mechanic) — flagged, not self-applied (CODEOWNERS is PR #31's owner file).
- why: merges were hard-blocked repo-wide (band-3 slice 3 could not land); the fix is exactly the §8.1 division of labor — the named jobs existed semantically but not nominally, and gate 6 genuinely did not exist yet.
- provenance: design-spec §6 "Required checks (one workflow, named gates)"; build-brief §8.1; PR #35 merge refusals 2026-07-09; A-16/A-19 in-gate placements

## [D-0036] Band 4 slice 1: XP — the audited progression seam, the coins-split complement store, the FILLED band-3 boundary, and the conditional-emission kernel widening
- status: decided
- date: 2026-07-09
- verdict: xp = shipped commands VERBATIM (xpmenu/rank/givexp/resetxp/xpconfig/xpimport; admin floor as tier data) over the audited K7 seam (INV-G: xp.award / xp.reset / xp.import_levels are the ONLY mutation paths; sb/domain/xp/store.py is the sole writer). STORE (migration 0014): `xp` = the shipped table MINUS the coins column — the COMPLEMENT of D-0031's economy_balances extraction: forward_map_kind=RENAME (bijective column subset), bears_value=True ⇒ REVERSE_IMPORTABLE with an S14 aggregate importer copying absolute (xp, level, messages) back over the old xp rows (last_xp epoch scopes the touched set; post-flip resets = declared loss in the M1 manifest); MEMBER_ID with the erasure body registered (delete rows). LEVEL MATH verbatim (xp_for_level/level_progress/total_xp_for_level — the ONE curve). EVENTS: the shipped vocabulary DECLARED (xp.awarded / xp.level_up / xp.reset, payload keys verbatim, BEST_EFFORT v1 per OD-1's built default). **KERNEL WIDENING (additive): conditional emission** — enqueue_all skips an emit whose payload builder returns None (emit_index positions stay declaration-stable, so dedup tokens never shift); xp.level_up is the arming case (a non-boundary award emits nothing — the shipped leveled_up branch as grammar). xp.reset carries ConfirmationSpec(irreversible, TYPED_PHRASE) + an irreversible leg (kick-confirm class); the panel Reset action mirrors it. xp.import_levels = the shipped raise-only batch seam (max-reduced per user, idempotent, NO events by design); the announcer parse formats (arcane/mee6/superbot/generic) ported verbatim as the Discord-free core; the channel SCAN waits on install_levelup_history_scanner (message band). THE BAND-3 WAITING PORTS ARE FILLED: importing sb/manifest/xp.py installs the REAL level reader (xp row → level ⇒ tier-gated jobs unlock) and the REAL xp awarder (economy.work's optional EFFECT leg now runs the audited xp.award op with source="work:<job>" as its own K7 invocation — the shipped two-separate-writes semantics; xp_pending=True is gone whenever the manifest is loaded). CHAT HOT PATH: handle_chat_message (cooldown via last_xp + xp config, PR-9 participation gate as an installable default-ALLOW port, injectable rng) → the K7 op; the MESSAGE FEED arms with the message band (automod precedent). Level-up fan-out: bus → bound xp.announce_channel → RC-21 emitter; threshold-role grants ride install_level_role_granter (band-5 roles seam, honest no-op default). INV-G data half: InvariantSpec xp.level_consistency (ROW_PREDICATE, REPAIRABLE — level is derived state, the xp column is in-row ground truth; repair = the registered pure-DB xp.repair_level_consistency op re-deriving level_progress(xp)).
- deviations (deliberate, flip-review material): (1) the rank card renders as TEXT (RESULT_CARD) — the shipped Pillow image card + Both/XP/Coins in-place attachment toggles are live-adapter presentation (embed/text stays the source of truth, the shipped fallback posture); stat views ride `!rank <stat>`. (2) announce-in-place (unbound channel ⇒ message.channel) is the MESSAGE SHELL's obligation — the compat-frozen level_up payload carries no channel id, so the headless fan-out only routes the BOUND channel. (3) xpconfig is a read view; edits ride the band-1 settings hub (§4.1 one-write-path — the shipped modal editors were a second write path by design retired). (4) xpimport scan-and-preview is honestly BLOCKED until the history-scanner port arms (formats help + the import op are live).
- why: band 4's contract is the progression seam — the economy boundary closes (tier-gated jobs + work XP become real), xp.level_up becomes a declared event other subsystems consume, and the conditional-emission widening is the minimal kernel truth for boundary-crossing events every later band (games level-ups, streak milestones) reuses.
- provenance: disbot cogs/xp_cog.py + cogs/xp/{listener,schemas,stage}.py + services/{xp_service,xp_helpers,xp_migration}.py + utils/db/xp.py + utils/xp_migration.py + views/xp/* @7f7628e1; D-0031 XP boundary; canonical plan step 13 band 4 (INV-G); spec 08 §3.3 (emit loop); D-0029/D-0034 recipe

## [D-0037] Band 4 slice 2: KARMA — peer reputation on the one-txn ledger discipline (INV-K)
- status: decided
- date: 2026-07-09
- verdict: karma = the shipped surface VERBATIM (!thanks [rep/thank] / !karma [card] / !karma add [group] / /karma [slash, EPHEMERAL]) over ONE K7 CompoundOpSpec (karma.give, NATURAL_KEY, authority user): the data-level rejection ladder (enabled → self → per-recipient cooldown → per-giver daily cap) enforces IN the leg with the shipped user copy, and the credit + given_count bump + audit row write in the SAME txn — nothing is written on a block (the shipped write-nothing guarantee, now transactional). STORES (migration 0015): `karma` (NAME_STABLE AGGREGATE, bears_value=True ⇒ REVERSE_IMPORTABLE aggregate importer — the S14 "XP/karma" instruction executed; MEMBER_ID, erasure = delete) + `karma_audit_log` (NAME_STABLE LEDGER, the anti-abuse source of truth — cooldown/cap are reads over it, NO separate cooldown table, shipped design carried; ADDITIVE mutation_id = the S14 reinsert conflict key, economy precedent; MEMBER_PII — the reason column is granter free text — erasure = TOMBSTONE with ids→0 + reason scrubbed). EVENT: karma.granted BEST_EFFORT, payload keys verbatim. SETTINGS: the shipped karma keys slice claimed (karma_enabled ON_BY_DEFAULT / karma_cooldown / karma_daily_cap / karma_reaction_emoji) with SettingSpec defaults PINNED equal to the policy constants (the shipped karma_config no-drift invariant, now a test). REACT-TO-THANK: handle_reaction = the shipped fast-gate core (one policy read; bail unless enabled + trigger-emoji match; self skips the write path) → the SAME audited op with source="reaction"; blocked grants swallow silently BY DESIGN; the reaction FEED + bot pre-filters arm with the message band. INV-K data half: karma.points_ledger_reconciliation (RECONCILIATION over karma × karma_audit_log to_user sums, QUARANTINE_ONLY, baseline zeros-until-CUT-2, tolerance 0).
- deviations: (1) the bot-recipient check needs a Discord member object — it stays adapter-side (the shipped cog kept it cog-side too; the data rules all hold in-leg). (2) The karma card renders as text (RESULT_CARD; embed-shape presentation is the live adapter's).
- why: karma completes the band-4 value stores on exactly the economy discipline (aggregate⊄ledger one-txn, reverse-importable both sides, reconciliation invariant) and proves the settings no-drift pattern for policy-backed subsystems.
- provenance: disbot cogs/karma_cog.py + cogs/karma/schemas.py + services/{karma_service,karma_config}.py + utils/db/karma.py @7f7628e1 (incl. the karma-reputation-plan-2026-06-22 lineage); D-0031/D-0036 conventions; canonical plan step 13 band 4 (INV-K)

## [D-0038] Band 4 slice 3: COMMUNITY family — the router hub, the provider-registry leaderboards, and the spotlight's DECLARED xp.level_up consumption
- status: decided
- date: 2026-07-09
- verdict: three manifests matching the parity roster keys. community = the shipped router-only hub (zero business logic): !community + /community (EPHEMERAL) open community.hub — pure navigation actions to xp.hub / karma card / leaderboard.board / community_spotlight.hub (Counting/Chain cross-links join at band 6: a PanelRef must resolve, never dangle). leaderboard = !leaderboard + the ELEVEN shipped per-game aliases VERBATIM (Q-A03 held default: legacy routes stay callable; aliases whose game providers don't exist yet resolve to the overview — honest, never a fake board) over the ported PR-G provider REGISTRY (sb/domain/community/rank_providers.py, headless: guild_id-keyed, mention labels): xp/coins/karma providers register at band 4 with the shipped alias rows (lb/rankings; rep/reputation/karmalb); band 6 adds game categories BY REGISTERING PROVIDERS (the shipped "never edit a consumer" invariant, test-pinned) — the board panel's category selector is PROVIDER-FED so new categories appear with zero edits. community_spotlight = !spotlight (!activity) over the shipped dashboard core: overview fields (XP/coins totals + top-3s + the level-up feed), XP Leaders / Richest / Games / Refresh actions, the games sub-panel's provider-fed selector filtered to the game subset (EMPTY until band 6 — the disabled-selector empty_state is the honest waiting surface). THE BAND CONTRACT LANDED: xp.level_up → community_spotlight is now a DECLARED event consumption — spotlight.subscribe(bus) caches the bounded per-guild feed (deque maxlen 5, shipped) off the DECLARED EventSpec; arming subscribe() is a composition-root/harness obligation (the band-2/3 fan-out precedent). ECONOMY READER WIDENING: economy_balances.reader_domains grew "community" (the coins provider + spotlight totals read the aggregate — declared, additive).
- deviations: (1) member display names render as mentions headlessly (the shipped resources.member_display cache read is live-adapter presentation); (2) the spotlight overview omits guild.member_count (gateway-cache datum — the live adapter may prepend it); (3) spotlight's Refresh action_id is `spotlight_refresh` (the compiler's custom_id namespace key is the bare action_id, repo-global; treasury owns `refresh`); (4) the shipped community:back closure id is replaced by engine nav:* (D-0034 class); (5) no image cards (leaderboard/rank Pillow rendering = presentation).
- why: the community family turns band 4's events and value stores into the user-visible standings loop, and the provider registry is the extension seam every band-6 game consumes — landing it now means games register data, not views.
- provenance: disbot cogs/{community_cog,community_spotlight_cog,leaderboard_cog}.py + services/rank_providers.py + views/community/hub.py @7f7628e1; D-0034 nav deviation class; canonical plan step 13 band 4

## [D-0039] Band 5 slice 1: GOVERNANCE engine — the scope-chain platform-control core, compiled onto the kernel, and the S7/S9 waiting-port fills
- status: decided
- date: 2026-07-09
- verdict: sb/domain/governance/ ports disbot/governance/ (16 modules @7f7628e1) in the shipped layer order (models → registry → cache → resolver → cleanup → execution → ops/service/templates) with migration 0016 (5 stores: subsystem_visibility, cleanup_policies, governance_audit_log MEMBER_ID w/ tombstone erasure body, capability_execution_overrides, governance_templates — all NAME_STABLE bears_value=False DECLARED_LOSS, the band-1 settings Q3-B posture). The shipped GovernanceMutationPipeline = four K7 CompoundOpSpecs (governance.set_visibility/set_cleanup/remove_cleanup at the shipped moderator floor; set_capability_override at the ADR-005 administrator floor) — validation + old-value read + upsert + governance_audit_log row in ONE leg txn, the central audit_log + audit.action_recorded twin from the engine, the 5 compat-frozen governance.* events as EventEmitSpecs (BEST_EFFORT), in-process cache invalidation post-run in the service wrappers. Registry = the shipped SUBSYSTEMS DATA harvested verbatim (43 rows: visibility_tier/dependencies/soft deps/102 capabilities, REGISTRY_VERSION 1 / SCHEMA 2) + validation (capability format, reserved prefixes, acyclic deps). THE PORT FILLS: install_capability_override_reader ← execution.get_capability_override (TTL-bounded cache, deterministic reload, DEBT-001), install_role_binding_reader ← the band-1 subsystem_bindings read (R-16), install_visibility_reader ← service.subsystem_enabled. Tier resolution: declared-tier path (Q-0045-b) verbatim; platform-owner elevation via sb.kernel.authority.owner; trusted/moderator configured-role grants read the governance settings slice through the K7 resolve seam, fail-toward-lower-tier.
- deviations: (1) GovernanceContext is headless (ids + role_ids + declared member_tier; no discord.Member) — adapters compute tier fresh per RC-12; (2) the K8 visibility reader is TIER-AGNOSTIC guild-scope (overrides + dependency rules only) because the (guild_id, subsystem) seam carries no member context — tier gating is CommandSpec.audience_tier's job in the compiled architecture; unknown subsystems fail OPEN (manifests own existence); (3) capability_execution_overrides re-keys to (guild_id, capability) — the shipped runtime read collapsed its scope columns; (4) set_capability_override authority = administrator (the shipped overlay had no in-repo write surface; the revoke lane lands at the floor its read side protects — ADR-005 _DEFAULT_REQUIRED_TIER); (5) the internal-bypass audit row opens its own txn (best-effort, shipped semantics) rather than riding a leg; (6) governance manifest declares no commands/panels — the shipped write surfaces are the channel/setup panels (band 2 declarations); (7) check_governance_version writes the version stamp through the band-1 settings.set_scalar lane (§4.1 — no raw KV write).
- why: governance is the platform-control floor every band-5 surface (control API, consistency, command access) reads through, and the three kernel waiting ports it fills have been running on fail-open/fail-closed defaults since S7/S9 — filling them is the band's highest-leverage obligation.
- provenance: disbot/governance/* + utils/subsystem_registry.py + utils/visibility_rules.py + utils/governance_exceptions.py @7f7628e1; S7/S9 notes (progress log) naming the ports; spec 04; canonical plan step 13 band 5

## [D-0040] Band 5 slice 2: ROLE family — thresholds/reaction-roles/menus/temp-grants as declared grammar + the band-4 level-role-granter fill
- status: decided
- date: 2026-07-09
- verdict: sb/domain/role/ ports the shipped role family (role_cog 786L + role_grants_cog + reaction_role_service + role_automation + role_exemption_service + role_grants_service + xp_role_sync + utils/role_feasibility @7f7628e1) with migration 0017 (8 stores: role_thresholds w/ the 003/056 columns folded, reaction_roles, reaction_role_message_modes, role_menus+options, role_grants MEMBER_ID w/ delete-rows erasure body, role_menu_pickup_stats, role_automation_exemptions — all NAME_STABLE bears_value=False DECLARED_LOSS). PURE DECISION CORES VERBATIM: evaluate_role (the ONE manageability oracle, (position,id) tiebreak), compute_assignments (never-demote, exempt/bot skips, keep_previous_tier, id-first tier resolve surviving renames), explain_assignment_for, check_preflight, the classified apply loop (systemic manage_roles guard, per-member isolation, reason-code failure grouping), plan_level_role_assignments (stack vs single-role). 12 K7 lanes (thresholds/exemption [shipped set_role_exemption audit]/reaction bind-unbind/message modes/menus CRUD/temp-grant record+EFFECT w/ compensator/expire under SYSTEM_ACTOR) + role.hub panel w/ the 7 shipped custom_ids pinned verbatim + 17 shipped commands (aliases ri/reaktionsrollen verbatim) + the role settings slice. role:grants_expiry ManagedTaskSpec (A-8; shipped 5-min loop; IN_MEMORY, HandlerRef fire = the sanctioned externally-effecting exemption, the rotation_fire precedent). THE BAND-4 FILL: install_xp_ports() installs the planner-backed level-role granter into sb.domain.xp.service (D-0031/D-0036 waiting port); it reads xp_auto_assign threshold rows + stack flag + exemptions and applies through the GuildRoleActions port — without a live guild view (install_guild_source, the CUT-1 adapter) it records nothing and never fakes a grant.
- deviations: (1) GuildRoleActions/install_guild_source are installable ports (band-2 pattern, fail-loud/fail-soft); Discord-effecting surfaces without ports are honest pending terminals (create/delete role, roleinfo, assignroles, debugroles, refreshmembers, the menu wizard); (2) temp-grant order flips to record-then-EFFECT (K7 post-commit EFFECT legs; shipped added the Discord role first) — an EFFECT failure leaves the row for the sweep, the compensator drops it; (3) apply()'s per-change audit rides the installed bus as audit.action_recorded facts (the shipped emit_audit_action was the same best-effort emit; K7-lane ops audit via the engine); (4) verify-mode reaction semantics kept EXACTLY (no grant on add, grant on remove); (5) rank-menu posting/pickup interactivity = live-adapter work; menus are DB-CRUD + declared surface; (6) role_exemption reads/stack toggles ride the K7 settings resolve seam with the shipped defaults (time stack off, xp stack on).
- why: the roles band is the band-4 contract's other half — xp.level_up grants were honestly no-oping since D-0036; the planner + port land the real policy, and the temp-grant sweep is the first A-8 ManagedTaskSpec consumer.
- provenance: disbot cogs/role_cog.py + cogs/role_grants_cog.py + services/{role_automation,reaction_role_service,role_exemption_service,role_grants_service,role_lifecycle_service,xp_role_sync}.py + utils/role_feasibility.py + migrations 003/052/056/078-081 @7f7628e1

## [D-0041] Band 5 slice 3: PLATFORM/CONTROL + PROOF_CHANNEL — command-access truth, teardown registry, consistency report, K10 claims, prize sessions; the control HTTP bridge deferred-named
- status: decided
- date: 2026-07-09
- verdict: sb/domain/platform/ compiles the essential control floor: command_access.py = the old-050 stores + TTL-cached read + K7 lanes (set_access_mode/set_access_channels, administrator floor) FILLING the S9 K8 waiting port install_access_policy_reader (the admission resolver ran on the mode=None safe default since S9; the DECISION logic was already in sb/kernel/authority/channel_access.py); guild_teardown.py = guild_lifecycle.py compiled to a hook REGISTRY (per-hook isolation verbatim; band-5 registers governance [shipped order: capability overrides before visibility cache, audit preserved] + command_access [cache+DB paired forget] + role family + proof locks; later bands append at manifest import); consistency.py = platform_consistency.py's frozen severity contract (CLEAN/WARNING/FATAL/SKIPPED, informational never promotes) + fail-isolated collector registry + 6 sb-native collectors; introspection.py = guild_introspection_service VERBATIM (caps 60/80/10/100; member surfaces stay behind the member-data opt-in); guild_snapshot.py = the closed-field metadata snapshot w/ EXCLUDED_FIELD_TOKENS pinned; ai_tasks.py = the band-5 K10 claims (platform.explain_status / platform.explain_consistency / code_context.explain byte-identical) + fact gatherers over diagnostic/consistency. sb/domain/proof_channel/ = the prize-session family: 0018 proof_channel_locks (bug #8), 3 K7 lanes (grant = record+EFFECT w/ row-drop compensator; end; the sweep's pure-DB unlock under SYSTEM_ACTOR), the ChannelPermActions port (fail-loud), proof:lock_reconcile ManagedTaskSpec (60s IN_MEMORY), the 5 shipped commands verbatim (+prize/-prize/prizestatus/prizemenu/timedprize), the hub panel w/ G-10 grant/timed modals, the Q-0064 proof_channel binding. CONFIG grew CONTROL_API_TOKEN (52nd field, SecretSpec DORMANT) + the S13-DEFERRED CREDENTIAL_REGISTRY row landed (control_api_token, ON_COMPROMISE, RAILWAY_VAR_ROTATE, CONTROL).
- deviations: (1) THE /control/* HTTP ROUTE SURFACE IS NAMED SUCCESSOR WORK — the token plumbing (SecretSpec + credential row + dormant-by-default posture) landed so the surface stays Q-0156-safe; the aiohttp adapter (sb/adapters/http/control.py: ping/authority/manifest/settings GET+POST fronting the audited seams, the _SlidingWindow write limiter, hmac compare, seam-error→HTTP map) ports from disbot/control_api.py when a worker has room; (2) shipped per-lock asyncio timers + on_ready reconcile collapse into the ONE minute-granularity sweep lane (durations were minute-granular anyway); (3) proof authority staff (shipped perms_or_owner(manage_channels) — no tier bit maps manage_channels; staff = registry visibility_tier); (4) prizestatus renders DB-truth lock state (live overwrite matrix = adapter presentation); (5) teardown steps for not-yet-ported subsystems (sessions/flags/AI-platform per-guild tables/BTD6/starboard/automation rules) register with THEIR bands; (6) guild_snapshot readiness findings ride an installable inspector port (resource_health = setup-band work); (7) /health + /lifecycle routes already existed in sb/adapters/http/health.py (no healthserver work needed).
- why: command access is the K8 admission floor every command crosses; teardown is the anti-leak contract every band owes; the consistency report is the operator's rollout gate — and all three kernel waiting ports band 5 owed (K6 overrides, K6 role bindings, K8 visibility, K8 access policy) are now filled.
- provenance: disbot/{control_api.py,guild_lifecycle.py,healthserver.py} + services/{platform_consistency,guild_introspection_service,guild_snapshot}.py + cogs/proof_channel_cog.py + cogs/proof_channel/schemas.py + core/runtime/command_access.py + migrations 050/104 @7f7628e1

## [D-0042] Band 6 slice 1: the GAMES SUBSTRATE + wager-workflow games — checkpoint store, escrow-once wager lanes, the shared game-XP track, and the g1: dynamic-session seam armed
- status: decided
- date: 2026-07-09
- verdict: three manifests (games / blackjack / rps_tournament — parity-roster keys verbatim) over ONE new substrate. STORES (migration 0019): `game_state` = the shipped restart-safe checkpoint table (015+018 shape; TEXT state, BIGINT epochs) — SESSION class (rollback derives COLLAPSE, restart-lossy BY DESIGN), bears_value=False DELIBERATELY: escrow/entry rows carry the staked amount under the shipped `bet` payload convention but every wallet movement is a first-class economy_audit_log row written by the owning K7 leg (reasons *:escrow / *:entry_fee / *:pvp_win / *:pvp_refund / *:solo_win / *:solo_loss / games:gc_refund), so the pot is ledger-reconstructible and the store itself never bears independent value; MEMBER_ID erasure = games.discard_subject_sessions (delete rows; a still-escrowed stake of an ERASED subject is forfeited-by-erasure — the ledger trail is tombstoned by the economy body; refund-then-delete would move money during privacy erasure). `game_xp` = the shipped 065 shared cross-game track (NAME_STABLE AGGREGATE, bears_value=True ⇒ S14 aggregate reverse importer by (user_id,guild_id,game); MEMBER_ID delete erasure; NO stored level — the shared level derives from SUM(xp) through the band-4 curve, one formula bot-wide). WAGER LANES: the shipped game_wager_workflow (P0-1) re-shaped from txn-OWNING service to CONN-THREADED primitives composed inside the calling game's K7 leg — the op owns the ONE txn, K7 owns audit/idempotency, and idempotency is DOUBLE-guarded exactly like shipped (once() fence + FOR-UPDATE row-consumption: a settle finding its escrow rows gone pays nothing). economy.balance_changed emits post-commit via SHARED conditional builders (games.balance_payload_0/1 read ctx.params["_balance_changes"]; a missing entry returns None — the D-0036 rider — so every game op declares max fan-out and the leg decides). debit_floor_in_txn = the shipped add_coins(GREATEST(0,…)) loss semantics with the ledger recording the ACTUAL delta (INV-F honesty). GAME XP: award policy/constants verbatim (soft cap 400/day/game, 25% capped rate floor 1; money moves award 0); award_in_txn in the earning op's txn; game_xp.awarded / game_xp.level_up declared (BEST_EFFORT; level_up builder None on non-boundary). THE g1 SEAM ARMED: the S9b install_dynamic_dispatcher port gets its first real installer — per-game session-action tables claimed via register_session_actions (game_key = manifest key; differing second claim REFUSED = the §3.4 prefix reservation by construction; K1 custom_id-prefix rows ride the Stage-2 harvest), dispatcher re-enters resolve() with the claiming game's PanelActionSpec (authority/cooldown/audit apply as for static components), unclaimed/unknown → polite expiry. KERNEL EDIT (additive): DynamicDispatcher signature fixed at first install to (route, interaction, responder) — the port had ZERO installed consumers; its S9b contract test updated in the same PR (S15 precedent). session_id = `<guild>.<user>.<channel>` (the game_state checkpoint key rendered; the id ROUTES to the recovered row, never authority — ownership re-checked in-leg). session_gc = the shipped game_state_cleanup GC re-homed onto the K9 declared-task rail (games:session_gc, IN_MEMORY Interval 3600; the S6 A-8 consumer list named it): stale rows past the 24h TTL refund through the AUDITED games.gc_sweep_row lane (credit+ledger+precise per-row delete, one txn) — SAFER than shipped deliberately: a failing refund KEEPS its row for the next sweep (shipped cleared it; a stranded stake must never vanish silently). recover_escrow = the same lane callable promptly (cog_load/on_guild_remove analog, composition-root/CUT-1). BLACKJACK: engine pure-verbatim (injectable rng); solo settle table verbatim (natural 1.5×/free FREE_WIN_COINS=50, dealer-bust/win 1×, push 0, loss −effective floored); solo keyed (guild,user,0,"blackjack_solo") = one solo game per user per guild (shipped _active semantics; shipped also pinned channel for view reattach — presentation, not key). PvP: pending-challenge row → accept escrows BOTH stakes + deals both hands ONE txn → hit/stand on own hand → both-done settles POT ONLY. **PvP deviation (flip-review, loud): shipped PvP hands ALSO ran the solo house credit/debit at each player's finish on top of the pot settle (solo_view._finish's tournament_chips-is-None branch runs for PvP games) — minting house coins in a player-vs-player match; the port pays the pot only (game_wager_workflow's own documented D1 design) and disables PvP double-down (mid-match re-escrow has no shipped shape).** Tournament: entry/payout money lanes REAL (summed-pot truth, row-consumption idempotent); the registration/round orchestration (private channels, reaction sign-up, auto-start timer) = HONEST PENDING terminals (live-adapter successor work; bjstatus reads the entry rows). RPS: rules tables verbatim (4 modes); quickplay solo one-leg settle (win bet|FREE_WIN=30, loss floored); PvP challenge→accept-escrow→hidden simultaneous throws→second throw settles in the SAME txn; move vocabulary = classic for quickplay (shipped); rpsregister/rpsstart/rpsbot/rpsmatchup pending terminals (reaction sign-up + match channels + no-prefix move parsing = live adapter / message band); rpssettings = READ view (§4.1 one-write-path, xpconfig precedent) with default_mode/default_best_of/default_entry_fee now DURABLE SettingSpecs (shipped kept mode/best_of in-memory, lost on restart — made declarations, defaults verbatim). GAMES HUB: router-only (zero game logic, shipped posture); !games kind=both (the shipped E1 slash front door); worldcard = text world card (shared level + per-game standing). Dropped shipped ids: the blackjack:solo:replay persistent button (dynamic-session views are g1-scheme now; replay = re-invoke), back-closures → nav:* (D-0034 class).
- why: the substrate (checkpoint store + escrow-once + game XP + g1 dispatch) is what every remaining band-6 game consumes — landing it with the two richest wager games proves the whole seam stack (K7 money legs, conditional emits, dynamic sessions, GC) before the checkpoint-game mass rides it.
- provenance: disbot services/{game_state_service,game_wager_workflow,game_xp_service,game_state_cleanup,blackjack_engine,blackjack_state}.py, cogs/blackjack_cog.py + views/blackjack/*, cogs/rps_tournament/* + views/rps/*, cogs/games_cog.py, migrations 015/018/065 @7f7628e1; design-spec §3.4 (g1 scheme); D-0031/D-0034/D-0036 conventions; spec 09 §3 (declared tasks)

## [D-0043] Band 6 slice 2: the CHECKPOINT games — farm complete, creature catch/collection, mining + fishing core loops over the FULL declared shipped surfaces, and the named deep-systems successor port
- status: decided
- date: 2026-07-09
- verdict: four manifests (farm / creature / mining / fishing — parity-roster keys verbatim) riding the slice-1 substrate. STORES (migration 0020, shipped shapes, BIGINT epochs): chicken_farm, creature_collection_log, creature_battle_record, mining_inventory (TEXT user ids KEPT + a guild_id column added — the shipped table was global-keyed; scoping is additive, ids stay byte-compatible), mining_player_state, fishing_catch_log — all MEMBER_ID with delete erasure bodies registered. FARM (complete): core.py pure-verbatim idle math (5-min lay interval, coop caps, 1.55×/1.8× price growth); three K7 money lanes (collect / buy_chicken / upgrade_coop) with shipped copy — buying a chicken settles at the OLD flock size first (the shipped no-retroactive-rate subtlety); uninitialized-timestamp normalization lives in ops._stored, NOT core (core.settle is pure — the slice test asserting otherwise was the bug and was fixed to pass updated_at=now). CREATURE (catch + collection complete): catalog verbatim, data/creatures.json byte-identical; catch op with injectable rng — a FLED attempt writes NOTHING (shipped semantics); the battle RECORD lane is live (cbrecord/cbattletop read it) while the interactive PvP battle engine (utils/creatures/battle.py 523 + creature_battle_service 193) is a named successor port — cbattle is an honest pending terminal. MINING (core loop live, FULL 37-command surface declared): rewards.py verbatim MINUS the equipped-tool multiplier path — until the equipment/wear system ports, rewards ride the LEGACY inventory-pickaxe path only (shipped kept the two paths matched, so pre-equipment players are byte-identical); market.py = GEAR_SHOP + RESOURCE values + fish size-values; six K7 lanes (mine/harvest/explore/sell/sell_all/buy) with ledger rows + game-XP emits per leg; 27 deep-system commands (equipment/wear, energy, grid dig, vault, structures, skills, forge/workshop, titles, loadouts, descend/ascend, character — oracle disbot/utils/mining/ 17 modules 4,895 lines + mining_workflow 1,430) are HONEST PENDING terminals: the D-0043 named successor port. FISHING (core cast live, FULL 20-command surface declared): catalog + weight verbatim, fish.json copied — NOTE the venue split grew the catalog to 32 species (21 shore + 11 deepwater; the shipped docstring's 21 is the shore set); fishing.cast runs at the STARTER profile (starter rod rarity_pull 1.0, no bait, shore venue, neutral weather, base double-catch) — byte-identical to a fresh shipped player — and commits dex upsert + pearl/coral + the fish as a tangible mining_inventory item + game-XP in ONE leg txn; 15 gear/venue/craft/structure commands pending (oracle disbot/utils/fishing/ 12 modules 1,788 lines + fishing_workflow 1,151). SHARED SEAMS FILLED: install_extra_inventory_source gets its D-0032 body (mining_inventory — which also holds caught fish + pearls — merges into !inventory with zero inventory-band edits); six RankProviders registered from the games manifest (mining/creatures/fishing/farm/gamexp/crafting, shipped alias rows) so the band-4 leaderboard categories + spotlight games selector light up with zero consumer edits (Deathmatch/RPS/Counting providers ride slice 3 with their stat stores); games.world grew Mine/Fish/Farm/Creatures navs. SUCCESSOR SCOPE NAMED (the "D-0043 port"): deep mining systems, fishing gear/venue systems, creature battle engine, blackjack/RPS tournament orchestration — the per-domain pending-terminal lists in sb/domain/{mining,fishing,creature}/service.py are the exact scope.
- why: the checkpoint-game mass proves the slice-1 substrate at scale (four domains, one migration, zero kernel edits) while declaring the FULL shipped command surfaces up front — K1 owns every name from birth, parity flips stay honest (A-16), and the deep systems become a bounded, precisely-pointed port instead of an amorphous tail.
- provenance: disbot cogs/{farm_cog,creature_cog,mining_cog,fishing_cog}.py, utils/farm/farm.py, utils/creatures/{catalog,data}, utils/mining/{rewards,market}.py, utils/fishing/{catalog,weight,data}, services/{mining,fishing,creature}*.py, migrations (farm/creature/mining/fishing tables) @7f7628e1; D-0032 (inventory merge port), D-0036 (conditional emits), D-0042 (substrate + g1 seam)

## [D-0044] Band 6 slice 3: the MESSAGE games — counting (full 10-command surface + the V/M/A hot path as an audited lane) and word-chain (the RS07 canonical-writer lanes), with the feeds waiting on the message band
- status: decided
- date: 2026-07-09
- verdict: two manifests (counting / chain — parity-roster keys verbatim) + migration 0021 (counting_state JSONB-blob-per-guild, chain_channels row-per-channel — shipped shapes). COUNTING: the parser pipeline (constants/parsing/game_logic — word numbers, phrases, roman numerals, emoji, hardened-AST math expressions, the 11 modes incl. the guess-the-secret-number random mode) ported VERBATIM as pure modules; the shipped handler.py compute_decision made HEADLESS (state-in/decision-out, discord.Message narrowed to content+author_mention; reply copy byte-identical — parity surface); the on_message hot path = the `counting.record_count` K7 lane (read blob → decide → persist-if-mutated in ONE leg txn — the leg txn REPLACES the shipped per-channel scope_lock; the decision returns to the MESSAGE FEED in `after`, and the feed owns delete-via-moderation-auto-delete / reply / reaction exactly as shipped apply_decision did). Config lanes: enable (the shipped start_match config shape verbatim incl. multiples/custom/skip args + the mode-specific 0-vs-1000 starting count), disable, reset (config-preserving), the two toggles, set_skip_step — all audited K7. Erasure: counting_state is MEMBER_ID (per-user tallies keyed by uid inside the blob) ⇒ `counting.scrub_subject_counts` REWRITES rows (strips the subject's leaderboard entries + last_user across guilds) rather than deleting them. CountingProvider (aliases countlb/counting_leaderboard) = the shipped cross-channel totals fold. CHAIN: chain_service's canonical-writer semantics as K7 lanes — create (strip().lower() normalize, refuse active word, PRESERVE a limit-only row's word_limit: the shipped regression pin), delete, set_limit (0 removes; no-change skips the write, matching the shipped no-audit-on-no-change), record_progress (per-message increment); the rule core (`check_message`) headless with the shipped warning copy; chain.hub = the shipped _ChainMenuView as four G-10 modals (the 'Channel (mention/ID, blank = current)' field kept) + List. DEVIATIONS (flip-review): (1) the shipped start_match CREATES a `<mode>-counting-<timestamp>` channel and end_match DELETES it — channel provisioning/teardown rides the resource-provision port (the logging.create_channels honest-wait posture); `!start_match` arms counting in the CURRENT channel (the shipped panel's Enable-Here lane semantics) and says so; `!end_match` removes the match row and keeps the channel. (2) The shipped hot paths were DELIBERATELY unaudited (scope-lock + fire-and-forget saves; chain record_chain_progress documented "not audited, hot path") — the rebuild's one-write-discipline routes both through K7, so every accepted count / chain step now writes an audit row (volume is bounded by human typing in dedicated channels; blackjack-hit precedent, D-0042). (3) The MESSAGE FEEDS (on_message → handle_message → apply decision) arm with the message band / live adapter — the xp.handle_chat_message precedent; until then the games are fully driveable through the lanes (the parity harness's path).
- why: the two message-driven games complete band 6's checkpoint+message families with ZERO new kernel seams — they prove the record_count-style "decision lane" pattern the live message shell will feed, and the chain/counting stores land NAME_STABLE so the importer maps rows 1:1.
- provenance: disbot cogs/counting/{_constants,parsing,game_logic,handler,_channel_manager,_stage}.py, cogs/counting_cog.py, views/counting/hub_panel.py, services/chain_service.py, cogs/chain_cog.py, utils/db/games/{counting,chain}.py, inlined-baseline DDL @7f7628e1; D-0042 (band substrate), D-0036 (conditional emits), band-2 moderation auto-delete seam

## [D-0045] Band 6 slice 4: DEATHMATCH on the g1 duel recipe + the CASINO pure layers + the PvP stat stores (Deathmatch/RPS providers live)
- status: decided
- date: 2026-07-09
- verdict: two new manifests (deathmatch / casino — parity key `casino` verbatim; deathmatch has no parity row) + migration 0022 (deathmatch_stats + rps_players, both the shipped (user_id, guild_id)-PK shapes). DEATHMATCH: the shipped _Duel + pick_bot_action ported HEADLESS (core.py — 100 HP / 15 attack / 30 crit @10% / defend-halves-once / armor-floor-1, injectable rng; the EffectiveStats equipment tilt rides the deferred equipment/wear system (D-0043) so every fighter duels at the shipped bare baseline); the duel flow = the blackjack-PvP g1 recipe over game_state SESSION rows (pending challenge → Accept/Decline session components [only the target may act] → alternating Attack/Defend components [both END the turn, the shipped _resolve swap] → the finishing move records the W/L pair atomically via deathmatch_stats.record_result AND consumes the duel row in the SAME txn — row-consumption IS the shipped settle-once claim); bot duels (Fight Bot on the hub) = player move + pick_bot_action reply in ONE leg, keyed (guild, user, 0, "deathmatch_bot"), and NEVER write stats (the shipped PR-6 anti-farming rule, copy says so); commands dm_challenge (+ fluency aliases deathmatch/challenge/dm) and dm_help (+deathmatch_help) — shipped surface exactly, NO invented menu command (the hub rides the games.world nav, mirroring the shipped Games-hub hook); turn_timeout SettingSpec verbatim (deathmatch_turn_timeout, default 60, presets 30/60/120/300) — DECLARED; enforcement (the shipped on_timeout win-by-default) is the live-adapter duel view's obligation, stale rows fall to games:session_gc meanwhile. STAT STORES + PROVIDERS: rps_players lands in the rps_tournament manifest (MEMBER_PII — it snapshots a display name at game time per the shipped leaderboard query; erasure = delete); the rps quick-play lane now writes the stats row in-txn (the shipped _bot_matches.update_player_stats site — quick play IS the shipped bot match; display name via ctx.params["_display_name"] when the feed provides it, mention fallback headless — the live adapter upgrades it); DeathmatchProvider (aliases dm_leaderboard/dm_lb/board, card_theme ember) + RpsProvider (alias rpslb) register from the deathmatch manifest — the band-4 leaderboard/spotlight surfaces light up with zero consumer edits. CASINO: the pure layers port VERBATIM (sb/domain/casino/cards.py = utils/cards 52-card model; evaluate.py = the brute-force C(n,5) hand evaluator — HandRank.key is the comparison contract); !casino opens the hub; !poker (alias holdem) is an HONEST PENDING terminal — the shipped v1 table (views/casino/poker_table.py) is per-player auto-updating EPHEMERAL messages, which has no headless shape by construction (live-adapter successor work, the D-0045 named port: poker_table orchestration + utils/poker/engine.py's betting state machine dock onto the ported evaluator). games.world grew Deathmatch + Casino navs.
- why: deathmatch closes the last playable band-6 game on rails that already exist (zero kernel edits, the g1 recipe third-proved); the stat stores complete the leaderboard-provider family the earlier slices left honestly empty; casino lands its money-free declared surface + the hardest-to-get-wrong pure math now so the live-adapter band ports ONLY plumbing.
- provenance: disbot cogs/deathmatch_cog.py + cogs/deathmatch/{actions,schemas}.py, views/games/deathmatch_panel.py, utils/db/games/{deathmatch,rps}.py, cogs/rps_tournament/_helpers.py + _bot_matches.py, cogs/casino_cog.py, utils/cards/__init__.py, utils/poker/{evaluate,engine}.py, views/casino/* @7f7628e1; D-0042 (g1 recipe), D-0043 (equipment deferral), migration-005 PK widening

## [D-0046] Band 7 slice 1: BTD6 knowledge domain onto K10 — the mature L4 exemplar (corpus, grounding, eval gate, strategy memory)
- status: decided
- date: 2026-07-09
- verdict: one manifest (btd6 — parity key verbatim) + migration 0023 (btd6_strategies, the shipped migration-041 shape) + the committed CORPUS (sb/domain/btd6/data/ = disbot/data/btd6/ copied file-for-file, 74 JSON blobs, 7.1 MB — L4 data is band-7 scope). K10 REGISTRATIONS (the whole point of the band): legacy ids `btd6.answer` + `btd6.strategy_review` claimed BYTE-IDENTICAL (strategy_review = Sonnet-reserved per the PR #30 K10(b) owner ruling — the D-0033 routing row already pins claude-sonnet-4-6; this slice registers the task that row was waiting for); route probe = the shipped ai_task_router BTD6 leg ported whole (curated keyword fast-path via the verbatim utils/btd6/keywords list, dataset entity-alias matcher with the possessive/plural "geraldos" fold, r-round shorthand + money-cue rescue, farm-money short-alias rescue, paragon-degree cue (BUG-0015), MK-tower cue, conversation-cue follow-up via RouteContext.conversation_context_domains, strategy-intake via intake_kinds="btd6_strategy"); fact gatherer = context.build for BOTH tasks; grounding verifiers = validate_btd6_reply (name index over heroes+bosses single-token / towers+bloons multi-word / all 13 paragon names / multi-word upgrade names; numbers gated btd6.answer-only exactly as shipped; absence claims via the kernel guard) registered for both task ids on the K10 verify registry (the owner-ruled plug-in point); the PARAGON existence attribute (the shipped utils/btd6/absence_guard _PARAGON regexes verbatim) = the first register_existence_attribute payload — the "band-7 data" the K10 worker left waiting; versioned refusal floor (the shipped version-stamped no-data refusal, game_version-stamped); BTD6 task contract prose (the shipped playbook's normative directives, condensed); EVAL SUITE (A-17): the 16-probe QA-accuracy corpus (tests/evals/btd6_corpus.py) imported VERBATIM as EvalSuiteSpec "btd6_qa_accuracy" (content_version btd6@55.1) — deterministic tier greps the REAL retrieval's grounded facts for every expect/forbid needle (the oracle's offline layer, now run through run_suite + DeterministicProvider + deny_sockets in tests/unit/band7); the rubric tier rides advisory llm_judge graders (recorded, never gating — §8 Q9). RETRIEVAL: context.build is a FOCUSED port of the 5,622-line btd6_context_service — passes carried: resolver fixture facts (tower identity/cost/upgrades/description + paragon identity line + ALL-DIFFICULTY pricing over the verbatim ×0.85/1.08/1.20-round-to-$5 util; hero identity+abilities; bloon immunity/properties/description), paragon-name + paragon-degree facts (the d67 disambiguation over verbatim wiki scaling formulas), catalog facts (bosses with the BUG-0002 Standard-vs-ELITE labeling, powers, MK behind the knowledge/mk cue, rounds incl. the ABR-cue swap), damage-type/status INTERACTION facts (btd6_interaction_service VERBATIM — the "Lead resists glue" class), and the dataset-only coverage note. All 16 probes ground green on first run. STRATEGY MEMORY: btd6_strategies store (MEMBER_PII: submitter id + display snapshot; erasure = btd6.scrub_strategy_submitter ANONYMIZE — identity detached, row retained, the shipped identity-state transition); K7 lanes submit_strategy (user) / review_strategy (staff-authority; the AI reviewer's verdict lands through this SAME lane so Sonnet output is audited like any staff write); the shipped btd6_strategy_audit side table FOLDS into the K7 central audit row (one-write discipline, D-0044 precedent). SURFACE: btd6menu (hub: lookup modals + strategies + grounding-check + events pending), btd6ref group (tower/hero/round/income/rbe/relic live over the dataset; ct = pending), btd6strat group (browse/mine/strategy/strategy-audit/submit/pending/strategies/why-no-response — why-no-response reads the K10 decision audit), btd6events group (grounding = LIVE retrieval diagnosis; the 7 live-event subcommands = honest pending terminals), btd6ops group (5 pending terminals). SETTING: btd6.strategy_submission_channel (the intake cue). NAMED SUCCESSOR PORTS (the D-0046 port): the NK live-ingestion subsystem (source registry/snapshots/audit, btd6_facts, ingestion runs+supervisor, patch notes, data blobs — oracle migrations 040/048/054; feeds btd6events/btd6ops + the DB grounding pass + freshness signals), the deep reference/stats surfaces (full tower/hero per-tier normal-view stats, upgrade-detail + path/parent-tower grounding, minion indexes, crosspath cumulative pricing, CT tile geometry/map render, maps/modes/relic-mask resolution, round-cash workflow tools), the paragon power calculator (sacrifice math/reverse solver), conversation-carryover facts (needs the history scanner armed), and the strategy AI-review pipeline wiring (Sonnet call → review lane) which lands with the band-7 AI surface slice.
- why: BTD6 is the mature exemplar the whole knowledge-domain pattern is judged by — landing the corpus, the grounded-answer verifiers, the probe corpus as a REQUIRED deterministic gate, and the byte-identical task ids first means projmoon/video (slices 2–3) are pattern-stamps, not inventions.
- provenance: disbot data/btd6/** (74 blobs), services/{btd6_interaction_service,btd6_resolver_service,btd6_grounding_service,btd6_context_service,btd6_stats_service,btd6_strategy_service},.py, utils/btd6/{keywords,difficulty_costs,paragon_degrees,paragon_math,absence_guard,name_guard}.py, services/ai_task_router.py, tests/evals/btd6_corpus.py + test_btd6_qa_corpus.py, cogs/btd6*.py, views/btd6/panel.py, migrations 041 @7f7628e1; PR #30 owner rulings (D-0033 K10(b)); K10 seams D-0022/D-0023/D-0024

## [D-0047] Band 7 slice 2: ProjMoon (Limbus) knowledge domain + the shared VIDEO tasks onto K10 — the pattern-stamp slice (minted A-17 corpus)
- status: decided
- date: 2026-07-09
- verdict: one manifest (projmoon — parity key verbatim; !pm group + aliases limbus/projectmoon, the 9 shipped category subcommands with alias rows verbatim, projmoon.hub browse panel) + the committed Limbus corpus (sb/domain/projmoon/data/ = disbot/data/projmoon/limbus/ file-for-file: 12 Sinners / 7 Sins / 3 damage types / 5 E.G.O grades / 13 mechanics / 8 statuses). VERBATIM ports: projmoon_data_service (typed loader, alias-collision validation, longest-token resolve), projmoon_context_service (entity + roster grounding, ambiguous-bare-token curation he/don/sang, 16-fact cap), projmoon_grounding_service (names-only verifier — Sinners + the four non-ambiguous grade letters ONLY; fail-OPEN on internal error, the shipped additive-hardening posture, ledgered as the deliberate contrast to BTD6's fail-closed numeric path), utils/projmoon/keywords (has_limbus_context). K10: `projmoon.answer` claimed BYTE-IDENTICAL; route probe order 110 (AFTER btd6 — the shipped classify order, pinned by test); fact gatherer; verifier on the K10 registry; deterministic refusal floor (shipped copy); Limbus task contract (structural-facts-only, no-numbers directive). **MINTED EVAL CORPUS (the A-17(d) mandate — the oracle had NO projmoon corpus): 12 probes** over sinner identity/literary origins (incl. roster completeness), E.G.O grade ordering, clash/coin mechanics, damage-type roster, burn/tremor statuses — same GroundingProbe shape + gate harness as the BTD6 corpus (deterministic tier = real-retrieval expect/forbid grep through run_suite + DeterministicProvider + deny_sockets; rubric tier advisory). VIDEO (shared media ingestion): `video.describe`/`video.compare`/`video.qa` claimed BYTE-IDENTICAL from sb/domain/media (registered via the projmoon manifest — media has no command surface; the AI-surface slice wires the NL shell); route probe = the shipped classify tail verbatim (≥2 URLs → compare @0.90, 1 URL + question word → qa @0.80, 1 URL → describe @0.85, order 120); gatherer = the shipped youtube_context_service DETERMINISTIC half (URL regex verbatim, mention/control-char sanitisation, fact rendering, transcript-unavailable line, error_reason short-circuits video_feature_disabled/video_grounding_failed) over a NEW `install_video_metadata_reader` port — the network fetch (YouTube Data API + transcript + video_reference_cache DB cache) is the NAMED SUCCESSOR PORT (D-0047 port), and its YOUTUBE_API_KEY row joins the S13 CREDENTIAL_REGISTRY when that port arms (no credential row minted for an unbuilt fetcher — CredentialSpec rows assert live lifecycle obligations); video refusal floor + untrusted-metadata task contract registered. NAMED SUCCESSOR PORTS (the D-0047 port): the Limbus StaticData exact-number ingest (shipped plan Slice A item 1 — numbers join the verifier only when ingested), the youtube fetch/cache/diagnostics lane (youtube_fetch_service + video_reference_cache + youtube_diagnostics + YOUTUBE_API_KEY CredentialSpec + the youtube.context.enabled setting as a band-1 declaration), and the shared KnowledgeDomain renderer fold (shipped plan Slice B — three context builders now share shape by convention; folding them onto one renderer is refactor-class work, not port work).
- why: projmoon proves the knowledge-domain pattern stamps (every K10 seam BTD6 filled, filled again in ~600 lines instead of ~3,000), and the minted corpus discharges the band's one hard A-17 obligation; the video tasks complete the LEGACY_TASK_IDS claim set — all 17 shipped ids are now registered by their owning bands.
- provenance: disbot data/projmoon/limbus/** (6 fixtures), services/{projmoon_data_service,projmoon_context_service,projmoon_grounding_service,youtube_context_service}.py, utils/projmoon/keywords.py, services/ai_task_router.py (limbus+video legs), cogs/project_moon_cog.py, views/projmoon/* @7f7628e1; D-0046 (the BTD6 exemplar + gate harness); K10 D-0022/D-0024
