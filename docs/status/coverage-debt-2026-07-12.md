# Coverage-debt publication — 2026-07-12

> **Status:** `living-ledger` — the A-18(3)/Q-0244 CUT-2/CUT-3 coverage-debt
> publication (`docs/operations/cutover-runbook.md` C3.4). Generated by
> `python3 tools/check_verified_live.py --debt-list`; **published, NEVER a
> CUT-3 blocker** (unsigned human-tier rows never block the flip). Regenerate
> at cutover to publish the live remainder.

## Verbatim `--debt-list` output (2026-07-12, at branch `docs/cutover-runbook`)

```
$ python3 tools/check_verified_live.py --debt-list
verified_live coverage-debt list (0 row(s)) — published per A-18(3)/Q-0244, NEVER a CUT-3 blocker:
```

Exit code: `0`.

## Reading

**0 rows** at this build: `verification/verified_live.yml` carries every
`parity.yml` subsystem (+ `kernel`) as BORN `unverified` with records empty
(`docs/decisions.md` D-0021) — port bands mint per-surface sign-off rows and
CUT-1 arms the live lanes, so the debt list is the unsigned human-tier +
explicit DEBT remainder, which is currently empty. This is the honest
starting state, not a gap: an empty debt list means nothing in the human lane
is being carried as unpublished debt, NOT that every surface is verified
(V3 dashboard-honesty enforces the automated-tier side separately).

Regenerate this file at CUT-3 (C3.4) so the published remainder reflects the
sign-off rows minted by then; the row count is expected to be non-zero once
live lanes are armed and human-tier walks accrue.
