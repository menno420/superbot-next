# 2026-07-11 — substrate-kit upgrade v1.10.1 → v1.11.0 (distribution wave)

> **Status:** `complete`

- **📊 Model:** fable-5 · high · maintenance kit upgrade (Q-0261.3)

## Scope

Kit-seat distribution session (owner directive Q-0261.3): upgrade the
vendored substrate-kit from v1.10.1 to v1.11.0 using the pinned,
sha256-verified release asset (tag v1.11.0, annotated `81992be` →
commit `640f8a1`, release run 29152928040, asset sha256
`c339bd6a2eb3a139dd0106d5fd3873eb2d067f79723fccb5781d4e72a74a8d29`,
673,937 bytes — coordinator-stated == local sha256; release.json 449
bytes sha256 `a8c7ddc1…d650c27703` verified after download). Kit-owned
files only; no domain work.

## What shipped

1. **bootstrap.py v1.10.1 → v1.11.0** — canonical path (staged
   `bootstrap.py.new` + `release.json` in repo root, `python3
   bootstrap.py.new upgrade`); engine self-verified sha256+version
   against the adjacent release.json and self-cleaned both inputs.
   Vendored dist now sha256 `c339bd6a…` (byte-exact release asset);
   `kit_version: "1.11.0"` in substrate.config.json; `KIT_VERSION =
   "1.11.0"` stamped in the dist header.
2. **Exactly ONE new backup banked:**
   `.substrate/backup/bootstrap-1.10.1.py` sha256
   `fbe83ce35d1fb3b544ac58fc60ee2609eaa6c69c13d77883e9fdc5da6bbad158`
   == the pre-upgrade vendored bootstrap.py (byte-identical); all nine
   pre-existing `bootstrap-*.py` banks byte-identical before/after
   (hash-diffed).
3. **HANDOFF payload verified:** the vendored dist ships the HANDOFF.md
   pointer composer (`HANDOFF_POINTER_FILENAME`/`_MARKER`/`_NEEDLE`,
   bootstrap.py ~L4885); the staged CLAUDE.md template
   (`.substrate/claude/CLAUDE.md` L16) carries the new
   HANDOFF-read-first line. HANDOFF.md generates at boot, stays
   untracked and NOT gitignored by design — none generated by check
   runs, none committed. This repo has no live root CLAUDE.md
   (staged-only), so nothing host-owned was touched.
4. **Staged gate regen carries the v1.11.0 action bump:**
   `.substrate/ci/substrate-gate.yml` now `actions/checkout@v5` (L21) +
   `actions/setup-python@v6` (L80). Still STAGED only — no live
   substrate-gate on this repo by design (ci.yml's folded `checkers`
   job runs bare `check --strict`).
5. **guard-fires 10-min dedupe observed working:** first post-upgrade
   `check --strict` appended 2 fires (96→98 lines in
   `.substrate/guard-fires.jsonl`); an immediate second identical run
   appended 0 (98→98) — duplicate designed-hold/advisory fires within
   the window deduped.
6. **Carve-out section intact** in `.substrate/upgrade-report.md`:
   "carve-out scan: ran — no kit-owned live workflow installed, nothing
   to scan" (correct N/A form for this staged-only repo).
7. **Sibling-card needle scan (mtime-lottery defense, #159/#166
   precedent):** all `.sessions/` cards scanned for the four needles
   (Status / 💡 / review / 📊 Model) — every sibling compliant; no
   backfill needed this wave.
8. **Heartbeat NOT bumped:** the `control/status.md` `kit:` line bump
   is LANE-OWED this wave (coordinator directive) — deliberately not in
   this PR, unlike #166 which bumped it in-PR.

## Verification

- `python3 bootstrap.py check --strict` → exit 0 at close (mid-session
  it held with the designed born-red HOLD on this card; the
  pre-existing owner-ask-wall-unrecorded advisory on control/status.md
  remains lane-owed, not touched here).
- `python3 -m pytest tests/ -q` → 1374 passed, 2 skipped.
- `python3 tools/manifest_compile.py` → green (46 manifests).
- Upgrade notice (informational, pre-existing): automerge
  `required_context 'substrate-gate'` matches no job in this repo's
  workflows — expected, no live gate by design.

## 💡 Session idea

The guard-fires dedupe (v1.11.0) makes `.substrate/guard-fires.jsonl`
churn much less per session, so the file is now a clean per-session
delta; a tiny `tools/`-side summarizer that groups the JSONL by guard
kind and last-fired date would turn it into a zero-cost "which
advisories are chronic?" readout for reconciliation passes — the
owner-ask-wall-unrecorded advisory has now fired across at least three
sessions (11:34/12:04/12:58 entries) and a chronic-counter would have
surfaced that lane debt automatically.

## ⟲ Previous-session review

The #166 (v1.10.1) kit-upgrade card was again the playbook: its
canonical-path recipe, backup-hash discipline, and sibling-scan step
transferred 1:1 and its close-out note ("superbot-next is on kit
v1.10.1") was the correct starting fact. One improvement it surfaces:
#166 bumped the control/status.md `kit:` heartbeat in-PR while this
wave declares it lane-owed — the distribution recipe should state ONE
canonical owner for the heartbeat bump so successive waves stop
alternating conventions (this card records the delta explicitly to
keep the trail honest).

- Next session should know: superbot-next is on kit v1.11.0; the
  `control/status.md` `kit:` heartbeat bump is still OWED by the
  control lane (NOT done in upgrade PR #182); HANDOFF.md will appear
  untracked at repo root after boots — that is kit design, leave it
  uncommitted.
