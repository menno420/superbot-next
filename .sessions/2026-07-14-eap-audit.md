# 2026-07-14 — docs: EAP close-out project audit

> **Status:** `complete`

- **📊 Model:** fable-5 · EAP close-out project audit

## Scope

EAP close-out project audit. Deliverable:
`docs/audits/eap-project-audit-2026-07-14.md` — a consolidated
close-out audit with measured totals, tooling verdicts, walls,
friction, fixes, pains, wishlist, and honest gaps. Landed on this
branch (PR #468); badge `historical` (the same docs-gate path the
07-13 fleet-cleanup audit used), no bare decision-stamp tokens,
every load-bearing claim cited at `path@dd33fb3` / PR # / verbatim.

Order provenance: coordinator dispatch relaying owner directive
2026-07-14 ~08:26Z; NOT present in control/inbox.md@dd33fb3 —
proceeding as contained+reversible docs work per PL-001
decide-and-flag; flagged in PR body.

Lane claimed via `control/claims/eap-audit.md` (claim PR #467,
fast lane). This card was the branch's born-red first commit; it
flips to `complete` as the branch's final commit, per the enabler's
card-flip arm precondition.

## Verification

- `python3 -m pytest tests/ -q`: **3113 passed, 15 skipped** (76.58s;
  pytest/pytest-asyncio pip-installed first, per the CAPABILITIES
  fresh-container entry).
- `python3 bootstrap.py check --strict` (pre-flip): all doc gates
  clean for the new audit doc — the only red was the designed
  born-red HOLD on this card; 4 pre-existing claims advisories,
  never exit-affecting. `tools/check_doc_cites.py`: checked=843
  missing=0, OK.
- Audit-doc hygiene verified directly: badge in first 12 lines,
  zero bare `D-NNNN` tokens, zero relative markdown links, all
  `file.py:NN` cites resolve.
- `control/status.md`, `control/inbox.md`, `control/outbox.md`
  untouched. Branch synced by merging origin/main in (claim #467
  landed post-branch); never rebased.

## 💡 Session idea

The three §3.3 "relayed but not on record" denials all share one
failure shape: a seat hits a wall, the coordinator relays the story,
and the verbatim string dies with the originating transcript. A
tiny wall-capture reflex — on ANY classifier/platform denial, append
the exact string to docs/CAPABILITIES.md in the same session, before
reporting it upward — would make audits like this one grep-cheap
instead of forensic. The audit itself took as long to prove absences
as to compile facts; verbatim-at-source would delete that cost class.

## ⟲ Previous-session review

(Covers the 07-14 fishing lane pair: `.sessions/2026-07-14-hotfix-weather-goldens.md`
and `.sessions/2026-07-14-fishing-cast-weather-seed.md`.) Strong pair:
the seed card fixed the date-live time-bomb at the source with zero
golden bytes edited, and the hotfix card honestly recorded its own
mid-session first-merged-wins collision (#448 vs #449) — caused by
deliberately skipping claims-on-main-first, which this audit's §7 now
cites as the rule's best evidence. Gap carried forward by both cards
and still open: the mint-time weather-seed guard is proposed twice
(both 💡 sections) but remains unbuilt — it is wishlist item 7 in the
audit; a third proposal without a build would be ceremony.
