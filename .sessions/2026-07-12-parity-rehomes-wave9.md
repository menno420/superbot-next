# 2026-07-12 — parity re-homes wave 9 (the `_unmapped` pool DRAINED — 65 re-homed, 3 retired ⚑, deathmatch row 51 born; only the 41 owner-parked files remain)

> **Status:** `complete`

- **📊 Model:** Fable · high · parity re-home (Q-0194)

## Scope

The `_unmapped` re-home wave the wave-8 card's 💡 idea named: attribution
work over ALREADY-PORTED rows (plus one ruled row BIRTH), never porting.
TWELVE lane PRs squash-merged on green on the 6-check ruleset (`report`
red-by-design, non-required) took the pool from 106 files to 41 — and the
41 that remain are ALL owner-parked (the 40-file D-0043 deep-systems set +
btd6 sweep_paragon on the band-7 shelf). **The workable `_unmapped` pool
is DRAINED: no agent-shaped parity item remains anywhere in the program.**
Along the way the corpus took its FIRST THREE RETIREMENTS (⚑ flagged loud
below — coordinator-ruled, owner-vetoable, reversible), the deathmatch
subsystem row was BORN ported (50→51 rows, D-0042's defect fixed in the
same PR), and the channel-ops D-0030 successor batch armed 13 sweeps live.
Every count below is re-verified at HEAD `9ae4902` (#285's merge, main
HEAD at wrap-up) per Q-0120, not replayed from mid-wave memory. This
wrap-up card + its telemetry row + the status fold ride the wrap-up PR
(control fast lane, docs-only — trap 25: no Postgres ladder in the
wrap-up seat; counts are CI-LOG-VERIFIED at HEAD instead).

## The twelve PRs (each merge sha verified against `git log origin/main` at `9ae4902`)

1. **#248** `c148d2e10800d59c4fab184cb0458d2781eec065` — 5 alias-green
   re-homes → proof_channel / rps_tournament / counting; gate 365→370.
   The proof_channel_locks citation re-pathed with the moves (KEPT
   covered-elsewhere, not upgraded).
2. **#249** `c169e92da2f2901126fa97642e49ea31474e0970` — 5 byte-fix
   re-homes → counters / admin (shipped CogManagerView, presets card,
   serverstats census, loglevel/slashes copy) PLUS ⚑ THE FIRST CORPUS
   RETIREMENT: sweep_cog retired per the coordinator ruling 2026-07-12
   (admin.py deploy-ops declaration + the diagnostic_bot_status/
   system_info precedent), corpus 471→470. Gate 375/375.
3. **#250** `aece46b0666ec84d404a4fb1d3a5b7857dd84145` — 8 re-homes →
   mining-core (6) + counting (2); the #217 locking legs untouched,
   check_money_race green in the same CI. Gate 383/383; `_unmapped` 87.
4. **#252** `526d9f44e12e0289cec7814386feaa537eab7a79` — 6 re-homes →
   fishing (hub rebuilt to the shipped 7-button session view,
   fishtop/trophies) + economy (joblist card, shop session panel, and
   the setlogchannel ORACLE-SHAPE bindings reshape: migration 0038,
   binding_audit_log, `bindings.changed` event, write-then-audit
   in-txn; the subsystem_bindings exemption UPGRADED to first-class).
   Gate 389/389; `_unmapped` 81.
5. **#253** `6bc9b31c35ef0ee0f07c82f7dbb3d83ce97adbfd` — the mid-wave
   control heartbeat: #248/#249/#250/#252 folded into status.md, the
   wave-9 lane record opened. Control-only, no golden movement.
6. **#254** `c79207958b0da5a856d1f9bfa89858e6ae8051cb` — 7 general-fun
   entry points re-homed onto the ported general row (!fact !joke
   !quote !trivia !motivate !eightball !greet; pools complete and IN
   ORDER — length + order are load-bearing under seed-42; the
   trivia 'Reveal Answer' session button). THE XP-BYTE FINDING: 15/16
   vs the menu's 25 is RNG DRAW ORDER on the shared module-global
   stream, not a formula fork (dispatch-before-award in both roots).
   Gate 396/396; general 8/8; `_unmapped` 74.
7. **#255** `edfeca8ed746aea20f581aacb8de247479c2c2c4` — 8 utility
   entry points re-homed (!botinfo !info !userinfo !membercount !poll
   !remind !invite !clear): three zero-component param cards + the
   BotIdentity capture-environment read port; **!invite extends the
   D-0077 channel-ops port with `create_invite`** — the twin records
   fake_http's eight-key body verbatim THEN raises the named
   CaptureInviteParseError (the edit_member record-then-raise
   precedent); the handler degrades through bot1.py's generic copy.
   Gate 404/404; utility 15/15; `_unmapped` 66.
8. **#258** `2e51b4b936f306408b4da935426d9a53079c4f22` — 6 diagnostic
   tool commands declared + re-homed (lifecycle/checkdb/findcmd/
   listcmds/testnotify/validatejson; hub buttons rewired off the
   pending terminal) PLUS ⚑ TWO MORE CORPUS RETIREMENTS:
   sweep_query_logs + sweep_recent_errors retired
   (coordinator-approved 2026-07-12 — both pin run-order-dependent
   live log-ring lines from other sweep cases, the process-state
   class), corpus 470→468; `retired_goldens` 1→3, import pin stays
   465; count pins re-raised. Gate 410/410; diagnostic 43/43;
   `_unmapped` 58.
9. **#261** `5050b8f52c7dc447e9eef135d564ccfbec956725` — deathmatch
   row BIRTH (coordinator ruling; starboard #211 / four_twenty #227
   precedents): parity.yml 50→51 subsystem rows, born ported, 2
   re-homes byte-green. **D-0042's game_state-on-challenge defect
   reviewed and FIXED port-side**: pending-challenge state rides the
   session binding, the duel row mints at Accept, D-0045 settle-once
   intact. Time-driven exemption for table:deathmatch_stats; ratchet
   {events: 1, tables: 2, settings: 0}; verified_live roster row.
   Gate 412/412 across 51; `_unmapped` 56.
10. **#265** `6e8c66669c64427d10dfe2c9a03e6002ef110825` — the
    channel-ops batch, 13 sweeps armed live onto the channel row (the
    D-0030 successor slice over #242's adapter): ChannelStateActions
    grows rename/set_topic/move/clone; a new ChannelDirectory READ
    port; two zero-component info cards. **The no-DB-leg class**: the
    oracle's channel ops were pure Discord state + best-effort events
    (#207 posture) — D-0065/D-0077 never enter, the compensator
    allowlist stays EMPTY. Channel row 17/17; gate 425/425;
    `_unmapped` 43. Zero sweep-skip entries for any of the 13.
11. **#274** `1aec607d701c156bca2e7058c565c755bd13e499` — codex
    follow-ups to #265, Q-0120 verdicts on 5 line-anchored findings:
    2 oracle-verified FIXED (category children matched by NAME →
    parent_id relation; silent root-create on unresolvable [category]
    → not-found refusal), 2 hardening FIXED (bulkcreate partial-
    success recording with the oracle-verbatim created+failed blocks;
    param-card override now under the grammar renderer's budget
    clamps), 1 DECLINED with citation (live root not installing the
    port is the SANCTIONED #207/#245 posture until the D-0049-family
    wiring slice). Gate 425/425 unchanged.
12. **#285** `9ae490237a90fb4173d139da8516a66d234f5072` (main HEAD) —
    the LAST 2 workable strays re-homed onto the ported setup row:
    !setupadvanced / !setupdescribe prefix twins of the #245 slash
    family (same handlers; the leaked-workspace find branch replays
    over CAPTURE_WORLD_CHANNELS; describe gains the shipped
    _EMPTY_HINT guard verbatim). Fence note: check_no_skip greps
    `commands.command` tokens even in DOCSTRINGS — the oracle-quoting
    comment had to drop the decorator literal. Gate 427/427; setup
    row 11/11; `_unmapped` 43→41. THE WORKABLE POOL IS DRAINED.

Interleaved sibling merges in the wave window (other lanes, zero golden
movement, listed for fold accounting): #251/#260 kit v1.13.0/v1.14.0,
#256 program review, #257 ORDER-014, #259 ORDER-015, #262 cross-project
requests, #268 sim-lab review, #271 admin-surface audit, #273 ORDER 016
runtime-smoke merge gate.

## ⚑⚑ CORPUS RETIREMENTS — three, flagged loud (owner-vetoable, REVERSIBLE)

- ⚑ **sweep_cog** retired at #249 (`c169e92`), 471→470 — the FIRST
  retirement ever. Grounds: the admin.py deploy-ops declaration + the
  diagnostic_bot_status/system_info precedent. Coordinator ruling
  2026-07-12.
- ⚑ **sweep_query_logs + sweep_recent_errors** retired at #258
  (`2e51b4b`), 470→468 — both pin run-order-dependent live log-ring
  lines from OTHER sweep cases (process-state class);
  `_sweep_skips.json` entries close the leak.
- Accounting at HEAD: parity.yml `minted_goldens: 6` +
  `retired_goldens: 3`, import pin 465 — **corpus 468 = 465 imported +
  6 minted − 3 retired** (hand-verified on disk: 468 golden files).
- Veto path (all three): restore the golden from git history, drop the
  skip entry, re-raise the count pins — nothing else moved with them.

## End counts (wave-9 END state, CI-LOG-VERIFIED at main `9ae4902`, #285's merge)

- parity **51 ported rows** — check_parity_depth in CI: "OK — 51
  subsystems (50 ported), kernel ported, 468 goldens" (51 subsystem
  entries with 50 ported — only `_unmapped: pending` — plus the kernel
  coverage home; deathmatch is row 51, born at #261). The program that
  completed at 50/50 in wave 8 now stands at 51/51 real rows ported.
- gate **GREEN 427/427 across 51 ported** — main-push golden-parity
  run 29199668523 gate job 86668709369: "gate: GREEN — all 427
  golden(s) across 51 ported subsystem(s) replay clean" +
  "golden-parity gate: 51 ported / 1 pending" with the pending table
  down to "_unmapped [41 goldens]" + "check_parity_depth: OK — 51
  subsystems (50 ported), kernel ported, 468 goldens" same job;
  integration **11 passed** same job (the F-001/F-002 real-Postgres
  concurrency regressions INCLUDED).
- report **427/468 green, 468/468 replayable** — report job
  86668709384 same run: "corpus: 468 goldens across 52 subsystem
  dirs" + "green: 427/468 replayed cases match their golden" +
  "replayable: 468/468" + "ported: 51/52 subsystems" with EVERY ported
  row green; "_unmapped 0/41 green [pending]" is the ONLY non-green
  line; "report: RED — 41 golden(s) not yet at parity (EXPECTED until
  the last subsystem flips ported)" — red-by-design, non-required.
  The red set IS exactly the owner-parked set; gate green = report
  green everywhere an owner decision isn't pending.
- ci + named-gates green at HEAD: ci run 29199668475 and named-gates
  run 29199668480 both completed success at `9ae4902` (units 1736
  passed / 2 skipped serial at the #285 pre-merge head per its PR
  body; the deps-free CI shape differs by the standing guarded-import
  skip delta).
- corpus **468 = 465 imported + 6 minted − 3 retired** (⚑ section
  above); `_unmapped` **106→41** across the wave (65 re-homed +
  3 retired: −5 #248, −6 #249, −8 #250, −6 #252, −7 #254, −8 #255,
  −8 #258, −2 #261, −13 #265, −2 #285).
- REMAINING `_unmapped` at HEAD is **41 files, ALL owner-parked — the
  workable pool is DRAINED**: 40 D-0043 deep-systems files (25
  mining-deep: ascend/build/buildable/buildlist/character/cook/
  descend/equip/forge/gear/home/loadout/mineworld/quickcraft/repair/
  skill/skills/stash/titles/unequip/unstash/use/vault/vaultupgrade/
  workshop + 15 fishing-gear: bait/boathouse/craftbait/craftcharm/
  craftcurio/craftpearl/craftrod/curios/dock/fishery/forecast/rod/
  rodrecipes/sail/tidepool — the D-0043 named successor port's exact
  surface, hand-counted at HEAD) + 1 btd6 sweep_paragon (band-7
  shelf). A fully-green report job now needs exactly TWO owner
  decisions: the D-0043 deep-systems port go/no-go and the
  sweep_paragon disposition. No agent-shaped parity work remains.

## Traps confirmed / new intel

- **XP-byte mismatches on fun commands are RNG DRAW ORDER, not formula
  forks** (#254): the shipped choice draw ran before the chat-award
  randint on the same module-global stream — simulate the seed-42
  trajectory (pool length + order both load-bearing) BEFORE porting;
  never "fix" the frozen award formula.
- **The corpus-retirement recipe is proven, twice** (#249, #258):
  `_sweep_skips.json` entry + golden delete + `retired_goldens` count
  + count-pin re-raise + pin greps — exercised cleanly both times.
  Retirements need a coordinator ruling and a loud ⚑ in the PR, the
  lane record, and the health line; they are reversible by design.
- **Channel-ops ops are the no-DB-leg class** (#265): pure Discord
  state + best-effort events (#207 posture) — the D-0065/D-0077
  compensator question never arises; the allowlist stays EMPTY.
  Effect twins extend by growing ChannelStateActions verbs; twins
  that can't parse fake_http's canned payload record the wire call
  verbatim THEN raise a named CaptureArtifact error (create_invite,
  #255 — the edit_member precedent generalized).
- **Declared-vs-live install split is SANCTIONED** (#274): the live
  composition root not installing a port (honest BLOCKED refusal on
  use) is the standing #207/#245 posture until the D-0049-family
  wiring lane lands — decline codex findings that re-litigate it,
  with the citation.
- **check_no_skip greps `commands.command` tokens even in docstrings**
  (#285): oracle-quoting comments must paraphrase the decorator line.
- **Recon labels go stale against golden bytes** (wave-wide): "public"
  / "needs twin" labels from pool recon were wrong more than once —
  replay the corpus locally against a scratch DB and trust the
  golden's own bytes (trap 35e generalized to re-home planning).

## Verification

Merge shas: all 12 verified against `git log origin/main` at `9ae4902`
(full shas in the PR list above; #285 = main HEAD). End counts
CI-LOG-VERIFIED at `9ae4902` (run/job ids per claim above). parity.yml
re-read at HEAD: 51 subsystems entries (50 ported + `_unmapped:
pending`), kernel ported, `minted_goldens: 6`, `retired_goldens: 3`.
`_unmapped` 41 files hand-counted on disk at HEAD (25 mining-deep + 15
fishing-gear + sweep_paragon); goldens 468 hand-counted.
`python3 tools/check_parity_depth.py` at the wrap-up head: "OK — 51
subsystems (50 ported), kernel ported, 468 goldens" (matches CI
verbatim). `python3 bootstrap.py check --strict` green at the wrap-up
head.

## 💡 Session idea

The parity program has NO agent-shaped work left — the next
parity-adjacent value is in the two owner decisions this card names
(D-0043 deep-systems go/no-go; sweep_paragon disposition) and in
FLIPPING `report` TO A REQUIRED CHECK once they resolve: with the red
set exactly equal to the owner-parked set, a small PR could split the
report job into "report-workable" (green-required today — everything
except the parked 41) and the standing red dashboard, retiring the
last always-red CI surface without waiting on the owner. Separately,
the D-0049-family live wiring lane (#274's declined-with-citation
successor: install ChannelDirectory/ChannelStateActions +
GuildModerationActions/ChannelPermActions in the live composition
root) is now the highest-leverage builder lane — parity byte-pinned
the domain semantics; the live adapters just need arming.

## ⟲ Previous-session review

(Covers `.sessions/2026-07-12-parity-flips-wave8.md`, this lane's
direct predecessor.) Its 💡 idea called this wave almost exactly: "a
dedicated re-home wave — or two, split btd6-family vs games-family —
is the shortest path to a fully-green report job." The split happened
(the btd6-family bulk went in the sibling #218-lineage re-homes; this
wave took everything else), and the "fully-green report" framing was
right with one honest correction: fully-green now requires OWNER
decisions, not agent work — the wave discovered that ~40% of the
pool's residue (the D-0043 set) was never re-home-shaped at all. Its
sequencing note ("land btd6 re-homes only when the band-7 lane's btd6
slices are idle") was honored and cost nothing. Its trap doctrine
held: trap 37's "re-derive park claims at pick-up time" was applied
in reverse this wave — old recon labels on pool files were re-derived
against golden bytes and found stale more than once (the new intel
row above). What it could not have predicted: three corpus
retirements and a row BIRTH inside an "attribution-only" wave — the
pool held both a process-state leak class (retire) and a ruled
missing row (deathmatch), confirming that even pure-attribution lanes
need the coordinator loop open.
