# 2026-07-12 — AIP-07 enum/preset settings refresh + AIP-08 hub title alignment

> **Status:** `in-progress`

- **📊 Model:** Claude 5 family · AIP-07/08 slice from the admin-surface audit

## Task

One PR (branch `claude/aip-07-08`) consuming the two parked VERDICT 009
items from `docs/review/admin-surface-audit-2026-07-12.md` §8 @ 8cdb7c3:

- **AIP-07 (medium)** — `sb/domain/ai/settings_widgets.py`
  `settings_enum_pick` / `settings_preset_pick` ack with
  `Reply(SUCCESS, …)` but never `await _refresh_settings_page(req)`,
  leaving a stale highlight after the pick. Siblings (toggle, reset,
  number submit, text submit) all refresh after a successful write,
  BEFORE the Reply. Fix: mirror the sibling pattern exactly.
- **AIP-08 (low, latent-only)** — `sb/domain/ai/panels.py`
  `ai_hub_spec()` declares `title="💤 AI Platform"` but
  `renderer_override=HandlerRef("ai.render_hub")` always swaps the whole
  embed (`operator_cards.build_panel_embed()`), so the declared title
  never renders live. The live title is DYNAMIC —
  `f"{status_emoji} AI Platform"`, emoji ∈ ✅/⚠️/💤 — so per
  decide-and-flag the declared title becomes the neutral base form
  `"AI Platform"`. Zero golden re-cuts (the golden pins the live bytes,
  which do not change).

## Plan

1. Claim + this born-red card (this commit).
2. Add the refresh call to both pick handlers, sibling-pattern verbatim
   (refresh after outcome check, before the success Reply; error arms
   return early without refreshing).
3. Align the declared hub title to `"AI Platform"`.
4. Tests: no existing test covers the refresh for ANY sibling (verified
   by rg over tests/) — add small unit tests asserting enum + preset
   picks trigger the refresh on success and skip it on write failure;
   one-line assertion for the declared hub title.
5. `python3 -m pytest tests/ -q` + `python3 bootstrap.py check --strict`,
   push, open PR READY, poll CI ≤3x, flip card + drop claim.
