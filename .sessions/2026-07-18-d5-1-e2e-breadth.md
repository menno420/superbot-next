# Session — D5.1 in-process e2e adapter test tier (breadth slice)

> **Status:** `in-progress`

## Order

Build + land a BREADTH slice extending the D5.1 in-process e2e test tier landed
in PR #573 (`tests/e2e/`, `boot_e2e_harness()` + `RealAdapterRecorder`). Add
~3-4 MORE representative e2e flows that exercise DIFFERENT real
command→adapter→discord-type paths than the existing 4 (`!help`, `!ping`, the
two S11 egress-trust flows), materially hardening the integration net. Reuse the
existing tier machinery — no new claim (within the merged
`control/claims/d5-1-e2e-tier.md` scope). Decide-and-flag build.

## Plan

New flows, each grounded in a REAL command and driving the REAL discord adapter:

1. **Component-click → panel refresh** — `!economymenu` opens the real
   `economy.hub` panel, then a real button click (`economy:jobs`) drives the
   component feed → the real `panel_view` re-renders `economy.joblist_card`.
2. **Write/mutation command** — `!daily` runs the audited `economy.daily` K7
   write op, then the real adapter materializes the `economy.daily_card` embed
   (Coins earned / Balance reflect the write).
3. **Slash command** — `invoke_slash("economy")` drives the real slash /
   app_commands path → the real adapter materializes the `economy.hub` embed +
   button view (distinct SURFACE from the `!`-prefix flows).

Economy read/open + a basic daily write are outside the claimed test-depth
scopes (fishing / role / casino / poker / server_management / xp).

## Verification (to fill on completion)

- `python3 -m pytest -q tests/e2e` — new tests PASS by name (not skipped).
- Full `python3 -m pytest -q --ignore=examples` green.
- Guards clean; `python3 bootstrap.py check` exit 0.
