# Session — D5.1 in-process e2e adapter test tier (breadth slice)

> **Status:** `complete`

- **📊 Model:** opus-4.8 · high · test writing

## Order

Build + land a BREADTH slice extending the D5.1 in-process e2e test tier landed
in PR #573 (`tests/e2e/`, `boot_e2e_harness()` + `RealAdapterRecorder`). Add
~3-4 MORE representative e2e flows that exercise DIFFERENT real
command→adapter→discord-type paths than the existing 4 (`!help`, `!ping`, the
two S11 egress-trust flows), materially hardening the integration net. Reuse the
existing tier machinery — no new claim (within the merged
`control/claims/d5-1-e2e-tier.md` scope). Decide-and-flag build.

## What this slice is

Three new `tests/e2e/` flows, each grounded in a REAL command and driving the
REAL `sb/adapters/discord/*` adapter — reusing `boot_e2e_harness()` +
`RealAdapterRecorder` verbatim (no rebuild, no `sb/` source edit):

1. **Component-click → panel refresh** (`test_economy_hub_click_e2e.py`) —
   `!economymenu` opens the real `economy.hub` panel (real `build_embed` +
   `PanelRuntimeView` of `Button`s), then a real `economy:jobs` button click is
   driven through the live component feed (`dispatch_component` → §3.4 static
   route → `resolve`) → the action handler opens `economy.joblist_card`,
   materialized AGAIN by the REAL `panel_view.py` into a second `discord.Embed`
   ("📋 All Jobs"). The click→re-render round-trip the initial slice couldn't do.
2. **Write / mutation command** (`test_economy_daily_e2e.py`) — `!daily` runs
   the audited `economy.daily` K7 WRITE op (a real Postgres balance grant on a
   fresh row), then the REAL adapter materializes the `economy.daily_card`
   embed; asserts the `Coins earned` / `Balance` fields reflect the persisted
   write (command → real domain op → real egress, not just a render).
3. **Slash command** (`test_economy_slash_e2e.py`) — `invoke_slash("economy")`
   drives the real slash / app_commands path (`dispatch_interaction`) → the real
   adapter materializes the `economy.hub` embed + a `PanelRuntimeView` of the
   hub's `Button` grid. A distinct SURFACE from the `!`-prefix flows.

Economy read/open + a basic daily write sit outside the claimed test-depth
scopes (fishing / role / casino / poker / server_management / xp).

## Flags

- Decide-and-flag: agent-decidable breadth within the merged D5.1 claim; the
  owner may reshape flow choices. The D5.2 LIVE tier stays owner-gated (untouched).
- **Modal-submit flow dropped (honest):** the harness DOES expose
  `modal_submit`, but the one stable modal in the corpus (the `btd6` strategy
  form) rides a test-depth-adjacent domain and needs more fixture setup than a
  breadth slice warrants. Deferred rather than forced — kept the slice at 3
  solid, hermetic flows. `click` and `invoke_slash` both worked as-is.

## Verification

- `python3 -m pytest -q tests/e2e` (Postgres + discord present) → **7 passed**
  (the 4 from #573 + the 3 new); every new test PASSES by name, none skip.
- Full `python3 -m pytest -q --ignore=examples` → **3530 passed, 2 skipped**
  (the 2 skips are the pre-existing inverse-path guards `test_db_pool` /
  `test_misfire` — unrelated; 3527 → 3530 = the 3 new flows).
- Guards clean (0 fires): `check_namespace`, `check_symbol_shadowing`,
  `check_config_usage`, `check_no_skip`. `python3 bootstrap.py check` → exit 0.
- No dependency change — `requirements.lock` untouched, so the pip-audit gate is
  a no-op here.
- Postgres provisioned the CI-shaped way: `pg_ctlcluster 16 main start` +
  `python3 tools/setup_local_env.py`. discord-py 2.7.1 already in the runtime lock.

## ⟲ Previous-session review

Predecessor in this thread: the D5.1 initial tier (#573), which stood up
`tests/e2e/` and explicitly flagged breadth as follow-up — naming
component-click / modal-submit round-trips and the slash surface as un-driven.
That flag held up exactly: the harness already carried `click`, `invoke_slash`,
and `modal_submit` (parity/boot.Harness), and the recorder's returned message
id doubles as the panel-session key, so all three new flows landed with **zero
`sb/` edits** and no recorder changes. One honest confirmation of its P1 note:
the static component-route path (`custom_id_override` → `ComponentBinding`) does
NOT session-look-up the clicked message, so a button click re-renders through
the real adapter with no need to thread the exact minted message id — the
cleaner test shape the initial slice couldn't yet see.

## ⟲ This-session review

The three flows were chosen to each hit a surface the initial four missed — the
interaction/component feed, a real K7 write, and slash dispatch — rather than
piling more prefix-read exemplars. The write flow is the highest-signal add: it
is the only e2e flow that asserts a real Postgres MUTATION landed (the embed's
`Balance == earned` on a fresh row), closing the read-only bias of the tier.
Held the line at 3 and dropped modal honestly rather than force a
claimed-domain form — legibility over a fourth flow of lower marginal signal.

## 💡 Idea

Fold an e2e **click depth ladder** into the recorder: expose the presenter's
returned message-key on `PanelCapture` so a test can chain click → click (hub →
joblist → back) and assert the REFRESH_PANEL in-place edit path
(`edit_message_ref` set), which this slice's static-route clicks skip — the
next rung of adapter-surface coverage toward the D5.3 `--record` bridge.
