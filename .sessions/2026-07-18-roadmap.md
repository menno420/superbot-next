# 2026-07-18 — production-readiness roadmap: forward proposal queue for post-rebuild superbot-next

> **Status:** `in-progress`
>
> Born-red first commit (per `.sessions/README.md`). This HOLD stays red on
> purpose — the roadmap is durable *planning* output for the owner to react to,
> not committed work. The card is left `in-progress`; the owner (or a follow-up
> session) flips it to `complete` after the morning review, releasing the
> substrate-gate HOLD so the server-side lander can merge on green.

- **📊 Model:** Opus family · medium · planning-doc / roadmap
- **Born:** 2026-07-18 (born-red first commit)

## Scope

Author `docs/status/roadmap-2026-07-18.md` — a forward roadmap of 6–10 proposals
for taking the (now complete, all-green) superbot-next rebuild from "boots to
RUNNING on real Postgres" to fully production-ready. Generated during the EAP
wind-down (the Project goes read-only 2026-07-21; `main` is the durable artifact
that carries across to the recreated Project). The roadmap is a **planning
floor** — proposals, not commitments — that the owner reprioritizes and the
coordinator can dispatch from.

Branch `claude/roadmap-2026-07-18` off origin/main `1893d32`. This card is the
first commit (born red); the roadmap doc follows in a second commit.

## Files touched

- `.sessions/2026-07-18-roadmap.md` — this card (born-red HOLD).
- `docs/status/roadmap-2026-07-18.md` — the roadmap doc (second commit).

## Grounding (read this session)

- `docs/current-state.md` — stability baseline (7 bands, 49 subsystems, 3,160
  tests, 523/523 goldens, six named gates green, boots to RUNNING).
- `docs/NEXT-TASKS.md` — the owner-directed build backlog (port-to-parity,
  game-surface backlog, live production, data safety net, autonomy retire).
- `.claude/CLAUDE.md` — layer/import rules the proposals must respect.
- `CONSTITUTION.md` § "Autonomy rails" — merge doctrine (owner/server-side only).
- Real anchors cited in the roadmap: `sb/kernel/observability/metrics.py`,
  `sb/adapters/http/health.py`, `sb/kernel/db/pool.py`,
  `sb/kernel/outbox/relay.py`, `sb/kernel/scheduler/misfire.py`,
  `sb/kernel/credentials/`, and the `docs/ideas/` + `docs/operations/` sets.

## Verification

- Doc-only change: this card carries a Status badge in the first 12 lines and is
  reachable; the roadmap doc carries a `> **Status:**` badge mirroring the
  `docs/status/` convention so the docs-gate passes. No code paths touched.

## 💡 Session idea

💡 Idea — fold this roadmap's proposal IDs into `docs/NEXT-TASKS.md` as a stable
`R-<n>` numbering the coordinator can dispatch against, so a proposal accepted in
the morning review becomes a one-line backlog reference instead of a re-paste.
The roadmap is the "why/what"; NEXT-TASKS stays the "what's next" queue — keeping
them cross-linked by ID avoids the two drifting.

## ⟲ Previous-session review

🔎 Previous-session review (`.sessions/2026-07-17-provisioning-unblock-record.md`):
it landed the env-specific correction that autonomous DB provisioning + the
golden-parity gate run green in the project-default env after a one-line boot
recovery (`pg_ctlcluster 16 main start && python3 tools/setup_local_env.py`).
That result directly informs this roadmap's Ops/resilience proposals: the port
loop is autonomously runnable here, so the remaining prod-readiness gap is
resilience/observability/ops hardening around a rebuild that is already
functionally complete — not more core porting.
