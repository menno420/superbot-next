# 2026-07-18 — D5 end-to-end / live-guild test harness design doc

> **Status:** `in-progress`

- **📊 Model:** opus-4.8 · medium · docs · D5 end-to-end / live-guild test harness design doc (born-red, holds substrate-gate)

## Scope

Second doc in the **planning-mode design-doc series** the 07-18 completeness snapshot
opened (`docs/status/completeness-table-2026-07-18.md`, #525; series home + index in
`docs/design/README.md`, the D4 PR #528). This slice is the **D5 end-to-end /
live-guild test harness** design doc: how to exercise the bot end-to-end — the real
Discord adapter + gateway + a live private guild — beyond the byte-level golden-parity
replay.

It is a docs-only planning artifact — no `sb/` or test code changes. The doc is
grounded evidence-first in the ACTUAL test layers and boot surfaces read this session
(`tools/run_golden_parity.py`, `sb/adapters/parity/boot.py`, `tools/check_runtime_smoke.py`,
`tools/check_verified_live.py`, `sb/adapters/discord/gateway.py`, the composition root
`sb/app/main.py`, `tests/integration/conftest.py`, and `docs/CAPABILITIES.md`), with
`file:line` citations at HEAD `b39a37f`.

## Deliver

- `docs/design/D5-e2e-test-harness.md` — the fuller design doc mirroring D4's shape:
  Problem (the gap between "525 goldens green" and "the bot works in a guild"), Proposed
  design (a tiered e2e harness — an in-process adapter-level tier driving the composition
  root against a fake/record gateway, plus an optional LIVE tier against the private test
  guild / Galaxy Bot running a scripted command sweep), Affected surfaces, Rough size
  (S/M/L + slicing), Open questions for the owner. `> **Status:** \`plan\`` badge.
- `docs/design/README.md` — flip the existing **D5** planning-series row from
  `planned` to the `[D5](D5-e2e-test-harness.md)` link. NO other row touched.

## Verification

- `python3 bootstrap.py check --strict` → 0 exit-affecting findings (badge valid + the
  D5 doc reachable via the index link); the only red in CI is this card's own designed
  born-red hold on the substrate-gate until the card flips complete.
- No `sb/` or test code touched — docs-only; the functional CI gates ride green,
  substrate-gate is the expected sole hold.

## 💡 Session idea

The load-bearing finding of the grounding pass is that golden-parity and the e2e
harness prove **disjoint** things and neither substitutes for the other. Golden-parity
replays recorded inputs through `sb/adapters/parity/` — a fake-HTTP/gateway transport
over the REAL interaction pipeline (`tools/run_golden_parity.py:20-27`) that
deliberately skips "gateway anything" (`sb/adapters/parity/boot.py` docstring). So a
regression that lives ONLY in `sb/adapters/discord/*` (the 19 real adapter modules) or
in the gateway→RUNNING composition path (`sb/app/main.py` step 10/11) is byte-invisible
to all 525 goldens. `check_runtime_smoke` proves the wiring graph is intact but
dispatches nothing (`tools/check_runtime_smoke.py:66-68`). The design's spine is that
the same in-process boot pattern the parity harness already ships (`Harness.start()` →
drive → `close()`) is the natural seam for a NON-replay e2e tier that drives real
adapter code with synthetic gateway events — and that the LIVE tier is the ONLY thing
that closes the last mile (real Discord tokens, real interactions, the test guild the
composition root already fences behind `SB_DATA_PLANE=test` + `SB_APPCMD_SYNC_GUILD_ID`,
`sb/app/main.py:112-131,427`). The recurring series theme holds: arm what exists (the
harness boot seam, the test-guild fence) rather than invent.

## ⟲ Previous-session review

Reviewed `.sessions/2026-07-18-design-d4-observability.md` (#528), which opened the
planning-mode design-doc series with D4 and established the shared index home in
`docs/design/README.md`. This card is the direct successor: the second doc in that
series (D5), following the same method — read the ACTUAL surfaces in source, cite
`file:line`, verdict only on verified ground; preserve every existing README row and
add only the D5 link. D4's own "declared far ahead of where it is wired" theme recurs
here as "proven far short of where it runs": the replay corpus is deep but stops at the
adapter boundary, which is exactly the gap D5 fills.
