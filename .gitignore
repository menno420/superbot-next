__pycache__/
*.pyc
*.pyo
.pytest_cache/
# packaging build artifacts (pyproject.toml — the plugin-contract dist)
build/
dist/
*.egg-info/
# local deploy env (docker-compose) — secrets never enter git; the committed
# template is .env.example
.env
.env.*
!.env.example
