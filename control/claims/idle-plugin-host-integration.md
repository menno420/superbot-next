- `idle-plugin-host-integration` · **host-side idle-plugin coexistence proof** — vendor the FIXED `superbot-idle` plugin as the second in-tree exemplar (`examples/superbot-idle-plugin`, capability `idle.game.play`) and prove hello + idle boot TOGETHER against the host with zero violations: add the second `plugins.lock.json` row (pinned via `tools/plugin_pin.py`), a `test_hello_and_idle_coexist` in `tests/unit/app/test_plugin_boot_real_exemplar.py`, generalize `tools/check_runtime_smoke.py::plugin_boot_problems` to iterate an exemplar list, and extend the live-drive runbook with idle's owner-gated live-only seams (render forwarding / settings persistence / event emission). Stacks conceptually on the `superbot-idle` capability-3-part fix (its PR #85). · `examples/superbot-idle-plugin/`, `plugins.lock.json`, `tests/unit/app/test_plugin_boot_real_exemplar.py`, `tools/check_runtime_smoke.py`, `docs/operations/plugin-proof-live-drive.md` · 2026-07-13
