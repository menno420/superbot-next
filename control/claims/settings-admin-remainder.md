# Settings admin remainder — claim — `claude/settings-admin-1` ff.

> **CLAIM** — the settings-hub admin-action lane: the five hub clicks
> that still route (or routed) to pending terminals, taken in three
> slices on named-successor branches.

- `claude/settings-admin-1` · **scope** — sb/domain/settings hub admin
  actions needs_setup/invalid/missing_bindings/audit/command_access,
  slices 1-3 (slice 1 = the three read-only diagnostics; slice 2 =
  audit; slice 3 = command_access) · area: `sb/domain/settings/`,
  `sb/manifest/settings.py`, `manifest.snapshot.json`,
  `tests/unit/settings_band/` · 2026-07-13
