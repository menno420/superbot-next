# Claim — CI-enforce the born-red session-card hold

- `claude/ci-card-gate-fix` · **install the kit's substrate-gate workflow + deterministic card selection in ci.yml + regression test** — copy the staged `.substrate/ci/substrate-gate.yml` to `.github/workflows/substrate-gate.yml` byte-for-byte (grades exactly the card(s) in the PR diff; born-red HOLD on in-progress/drafted added cards); pin ci.yml's `checkers` job to an explicit `--session-log .sessions/__no-card-in-diff__.md` sentinel so it stops mtime-guessing which card to grade (the #466/#477 green-with-in-progress-card bug); add `tests/test_session_card_gate.py` pinning the check_added_card hold seam · area: `.github/workflows/substrate-gate.yml` (new), `.github/workflows/ci.yml` (one line), `tests/test_session_card_gate.py` (new), own session card · 2026-07-14
