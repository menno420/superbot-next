# Claim — operator-hub edits slice B (ORDER 017 item 1, Top-gaps 6)

> **CLAIM (2026-07-13)** — operator-hub EDIT-controls lane, slice B
> (sibling of slice A, PR #355). The read-only nav slice is peer-claimed
> (`operator-hubs-interactive`) and explicitly defers the EDIT controls —
> this lane builds the channel hub's. EXCLUDED (peer-owned): the
> server_management/mining/utility nav wiring (#332) and every other
> subsystem's panels.

- `claude/operator-hub-edits-b` · **operator-hub edits B — the channel hub's five sub-panel flows (create/delete/restrict/move/visibility) over the live ChannelActions/Directory ports + the governance visibility op** — retires the 5 `channel.*_pending` hub terminals (D-0030 named successor) · sb/domain/channel/, manifest/layout/channel.lock.json, sim/sim-gate-baseline.json, compat/compat-frozen.json, tests/ · 2026-07-13
