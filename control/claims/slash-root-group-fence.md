- `slash-root-group-fence` · **headless compile fence for the slash-root vs subcommand-group name collision (Finding #3, PR #370)** — a manifest declaring a slash-capable ROOT command and a same-named subcommand GROUP compiles green in CI but crashes discord.py at live startup (`tree.add_command` raises `CommandAlreadyRegistered` when `register_app_commands` adds both a root `Command` and a top-level `Group` under one name — `sb/adapters/discord/command_tree.py:107-120`); add a new headless compile pass `_p3b_app_tree` in `tools/manifest_compile.py` that models `register_app_commands` EXACTLY (slash surface only, root = `group==""`, top-level group = first segment of a slash leaf's `parent_group`) so it rejects the collision statically over the joint manifest set (host + plugins) without false-positiving the PREFIX-only-root + group shape (#86 idle fix / the in-tree G-6 `!karma`/`/karma` coexistence) · `tools/manifest_compile.py`, `tests/unit/compiler/test_app_tree_collision.py`, `tests/unit/app/test_plugin_host.py`, `.sessions/2026-07-13-slash-root-group-fence.md`, `telemetry/model-usage.jsonl` · 2026-07-13
