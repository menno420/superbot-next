# Claim — operator-hub edits slice A (ORDER 017 item 1, Top-gaps 6)

> **CLAIM (2026-07-13)** — operator-hub EDIT-controls lane, slice A. The
> read-only nav slice is peer-claimed (`operator-hubs-interactive`) and its
> claim explicitly defers the per-group EDIT/mutation controls — this lane
> builds those. EXCLUDED (peer-owned, in flight): xp.config (#345), cleanup
> words + hub logging nav (#333), server_management nav trio + mining
> ws_back + utility 🔗 Invite (#332).

- `claude/operator-hub-edits-a` · **operator-hub edits A — utility Poll/Remind modal ingress + 420 forward, role.hub Create modal over the live createrole lane, argful `!counterpreset` apply over audited settings writes** — retires 5 pending terminals whose live targets/twins already ship · sb/domain/utility/, sb/domain/role/, sb/domain/counters/, sb/manifest/counters.py, compat/compat-frozen.json, tests/ · 2026-07-13
