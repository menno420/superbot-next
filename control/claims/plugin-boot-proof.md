- `plugin-boot-proof` · **headless proof that a REAL external plugin boots against the committed pin, + refresh the stale hello lock** — nothing in CI boots a real out-of-tree plugin (test_plugin_host.py is fakes/stubs only) and the committed `plugins.lock.json` pin for the in-tree `superbot-plugin-hello` exemplar is STALE (born correct at #75, drifted by the #232 CommandSpec `modal` facet without a re-pin), so `load_plugins` reds the exemplar with a `manifest hash drift → FAILED_STARTUP`; add a deterministic headless boot test (real entry point + real manifest + committed pin → zero violations + `hello.home` registers) and re-pin the lock · `tests/unit/app/test_plugin_boot_real_exemplar.py`, `plugins.lock.json`, `tools/check_runtime_smoke.py`, `docs/operations/plugin-proof-live-drive.md`, `.sessions/2026-07-12-plugin-boot-proof.md`, `telemetry/model-usage.jsonl` · 2026-07-12
