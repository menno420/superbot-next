# claim — live-channel-adapters

- `live-channel-adapters` · **live channel EFFECT adapters (SLICE 3)** — arm the LIVE, test-plane + test-guild-gated channel adapters behind the `ChannelStateActions` (channel domain) + `ChannelPermActions` (proof_channel domain) EFFECT ports, the channel/proof twin of SLICE 1's `DiscordModerationActions` and SLICE 2's role adapters (D-0049) · `sb/adapters/discord/channel_actions.py`, `sb/app/main.py` step 10a, `tests/unit/band6/`, `tests/unit/band5/`, `tests/unit/app/` · 2026-07-12
