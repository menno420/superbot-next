# Fishing depth ladder claim — lane `fishing-slice2-rod-bait`

This lane claims fishing depth slice 2 (the gear shops: `!rod` + `!bait`)
— the second rung of the D-0043 fishing successor port, branched from the
PR #313 (slice 1) post-merge head. The remaining 11 fishing `PENDING`
keys (craft* family, curios, structures) stay unclaimed for sibling
lanes.

- `fishing-slice2-rod-bait` · **fishing depth slice 2 — rod/bait (the gear shops) [IN FLIGHT — this branch]** — `!rod` rod-ladder shop + `!bait` bait shop live off new `sb/domain/fishing/rods.py`/`bait.py` + `fishing_rod`/`fishing_bait` stores + migrations 0049/0050 + audited buy ops; re-home 2 `_unmapped` sweeps into the gated `fishing` row · area: sb/domain/fishing/, sb/manifest/fishing.py, migrations/, parity/ · 2026-07-13 (claimed 00:54:49Z)
