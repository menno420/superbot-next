# Claim — stale-stack reconcile #392 + #476 (additive merges)

- `claude/energy-slice-3` · **stale-stack reconcile #392+#476, additive merges** — merge base `mining-write-parity-wp3` into #392's head, resolve the 6-file conflict class, re-sum count pins from the on-disk golden corpus; merge-only, no rebase/force-push, PR stays owner-click · area: `parity/cases/curated.py`, `parity/parity.yml`, `sb/domain/mining/{ops,service}.py`, `tests/unit/parity_adapter/test_replay_adapter.py`, `tests/unit/parity_gate/test_check_parity_depth.py` · 2026-07-15
- `claude/curation-row72` · **stale-stack reconcile #392+#476, additive merges** — sibling worker's leg: merge #476's base into its head, same additive-merge-only rules · area: PR #476 head branch · 2026-07-15

**Takeover note.** This claim supersedes-in-spirit the prior lanes'
claims `control/claims/energy-lane-slices-1-3.md` and
`control/claims/wp-stack-reconcile.md` — those belong to ENDED sessions
and are deliberately left untouched here (foreign claims; the prior
sessions' close-out owns their deletion). This session adds merge
commits only to the two branches above.
