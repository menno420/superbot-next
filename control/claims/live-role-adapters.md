# claim — live-role-adapters

- `live-role-adapters` · **live role EFFECT adapters (SLICE 2)** — arm three LIVE, test-plane + test-guild-gated role adapters (`GuildRoleActions` / `RoleProvisioning` / `MessageOps`) behind the role EFFECT ports, the role twin of SLICE 1's `DiscordModerationActions` (D-0049) · `sb/adapters/discord/role_actions.py`, `sb/app/main.py` step 10a, `tests/unit/band5/`, `tests/unit/app/` · 2026-07-12
