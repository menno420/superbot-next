- `claude/rework-server-management-name-pair` · **curation REWORK row 73 — `server-management` slash-twin name-pair regularization: fold the prefix/slash pair (`servermanagement` + `server-management`, both → panel:server_management.hub) into one CommandSpec if the grammar grows a slash-name field, else ledger the pair as deliberate; unify the split golden directories `parity/goldens/server_management/` + `parity/goldens/servermanagement/`** — (docs/review/curation-report-2026-07-13.md:1210 + §Rework backlog "server-management"; pair at sb/manifest/server_management.py:27-39; goldens sweep_servermanagement.json / sweep_slash_server-management.json) · `sb/spec/commands.py` (grammar, if fold), `sb/manifest/server_management.py`, `parity/goldens/server_management/`, `parity/goldens/servermanagement/`, `docs/`, `tests/` · 2026-07-13
