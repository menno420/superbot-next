# Claim — port tooling: mint_golden + orphan-pendings checker

- `claude/port-tooling` · **port tooling slice — golden mint helper + orphan-pendings checker** — `tools/mint_golden.py` (wraps the sanctioned D-0073 mint procedure: `sb/adapters/parity/runner.capture_case` + disposition strip + count-pin update guidance) and `tools/check_orphan_pendings.py` (cross-references `*_pending` HandlerRefs declared on panels against registered handlers, flags orphans) · `tools/mint_golden.py`, `tools/check_orphan_pendings.py` · 2026-07-13T21:35:36Z
