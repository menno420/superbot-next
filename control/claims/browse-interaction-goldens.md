- `browse-interaction-goldens` · **mint the multi-step interaction goldens for the armed browse surfaces (D-0034 capstone — stacks on #288→#279→#270)** — capture click/select→re-render goldens for the shared BrowserView engine (inventory detail sort/filter/page + the dex element filter), fixing the corpus's interaction blind spot (before: one button click corpus-wide, the selects untested); grows the replay click-`values` vocabulary + adds the curated cases + their minted goldens · `parity/cases/curated.py`, `parity/harness/runner.py`, `sb/adapters/parity/cases.py`, `parity/parity.yml`, `parity/goldens/inventory/inventory_browse_*.json`, `parity/goldens/creature/creature_dex_filter_element.json`, `tests/unit/parity_adapter/test_replay_adapter.py`, `tests/unit/parity_gate/test_check_parity_depth.py` · 2026-07-12
