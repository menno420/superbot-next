# claim

- `ban-compensator-refuse-path` · **moderation ban compensator refuse-path guard (ORDER-004 live-drive ⚑ follow-up)** — a Discord-REFUSED ban (apply 404) must withdraw its committed `mod_logs` row instead of leaving the false history claim: guard `_compensate_ban` with kick's `withdraw_mod_log_rows` fallback when the compensating unban reports NotFound (ban never landed); non-NotFound failures keep propagating · `sb/domain/moderation/ops.py`, `tests/unit/band2/test_band2_slice1.py` · 2026-07-12
