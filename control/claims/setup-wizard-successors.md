# Setup wizard successors — claim — `claude/setup-wizard-1` ff.

> **CLAIM** — the named-successor lane the wizard-lifecycle slice
> (PR #340, `.sessions/2026-07-13-setup-wizard-interior.md`) declared:
> the honest BLOCKED terminals in `sb/domain/setup/wizard.py`'s module
> docstring (final-review apply lane · essential steps 2–8 · the ten
> per-section flows · the suggestion Edit lane).

- `claude/setup-wizard-2` · scope: setup-wizard successors, slice 2 =
  essential steps 2–8 (the guided spine after the Step-1 starter set:
  greet · moderators · spam · log channels · rewards · help desk ·
  command access, each direct-apply through the audited kernel seams;
  the closing summary + extras + check-my-setup; the restart-resume
  lane over the persisted `essential_step`) · area: `sb/domain/setup/`,
  `sb/manifest/setup.py`, `manifest.snapshot.json`,
  `manifest/layout/setup.lock.json`, `sim/sim-gate-baseline.json`,
  `compat/`, `tests/unit/setup_band/` · 2026-07-13
- `claude/setup-wizard-1` · scope: setup-wizard successors, slice 1 =
  final-review apply lane (render the final-review card from staged K9
  draft ops; gated Apply through the audited kernel seams; apply
  summary; partial-apply recovery; setup-complete view; flip
  `setup.open_section_final_review` only — every other honest terminal
  stays untouched) · area: `sb/domain/setup/`, `sb/manifest/setup.py`,
  `manifest.snapshot.json`, `manifest/layout/setup.lock.json`,
  `sim/sim-gate-baseline.json`, `tests/unit/setup_band/` · 2026-07-13
