- `fishing-bait-race-fence` · **fishing cast/bait-slot race fence + secondary ledger notes** — `begin_cast` (`sb/domain/fishing/service.py`, `fishing.cast_open`) reads the active bait slot unlocked and writes back an ABSOLUTE `charges - 1` (or clears the row) while the buy/craft legs stack/replace behind `store.lock_bait_slot`; a bait purchase committing inside the cast's read→write window loses its coin-bought charges — convert the consume to a single conditional relative decrement (`store.consume_bait_charge`), byte-identical replies, plus ledgered-posture docstring notes on the three triaged non-money races (`!cook` mint-from-one-stack, cross-craft fish spend under per-kind locks; `!use` double-settle already noted) · branch `claude/fishing-bait-race-fence` · `sb/domain/fishing/{service,store}.py`, `sb/domain/{mining,fishing}/ops.py` (docstrings only), `tests/unit/band6/test_band6_fishing_cast_wiring.py`, `tests/integration/test_fishing_bait_consume_race.py`, `.sessions/2026-07-13-fishing-bait-race-fence.md` · 2026-07-13
