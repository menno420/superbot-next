# Claim

- `fishing-cast-depth` · **cast-leg depth wiring — venue/rod/bait/structure/charge modifiers driving `!fish` cast outcomes** — wire the live persisted state (fishing_venue / fishing_rod / fishing_bait / mining_structures) into the cast LEG per the recorded next-depth-step notes (sb/domain/fishing/service.py:715-719, ops.py header): deepwater species pool + coral drop, rod rarity_pull on the roll, loaded-bait knobs + the per-cast charge spend, the pull/bite/regen/double-catch structure mults; mint the new cast-write goldens via the canonical capture harness (sb/adapters/parity/runner.capture_case + parity/cases/curated.py) and update the parity.yml fishing rows; EXCLUDES the named follow-ups (wood-vs-mining floor-at-zero race, #342 fish-pool sibling, check_money_race mis-classification) unless one blocks the wiring; branches ride prefix `claude/fishing-cast-` · sb/domain/fishing/ (catalog/ops/service cast path), parity/cases/curated.py, parity/goldens/fishing/, parity/parity.yml · 2026-07-13
