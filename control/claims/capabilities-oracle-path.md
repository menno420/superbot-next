# Claim

- `claude/capabilities-oracle-path` · **CAPABILITIES ledger — verified worker-session port-oracle path** — append the verified oracle route (claude-code-remote `list_repos` → `add_repo menno420/superbot` → local shallow clone, read-only oracle) plus the pytest-install/tests-target env facts to the ledger's append log, per its own discovery rule · `docs/CAPABILITIES.md` · 2026-07-12 (claimed 2026-07-12T21:55Z)
