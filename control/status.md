# superbot-next · status
updated: 2026-07-09T15:20Z
phase: game-plugin contract (ORDER 002) host side SHIPPED — `sb.plugins` entry-point discovery + committed hash pins (plugins.lock.json) + joint compile at main() step 9b, hello-world plugin proven end-to-end in-process; live-testing roadmap otherwise unchanged (band-2 slice 2 / operator-spine eight is the next unclaimed step)
health: red-by-design (golden-parity `report` dashboard red while golden rows are `pending` — unchanged class). Everything else green: pytest 1094/2-skipped (17 new plugin-host tests), manifest_compile verify, 19-checker fleet, `check --strict`, PR #75/#76 CI green end-to-end
last-shipped: #76 — testing-report O2 row + evidence + flag 18; before it #75 (game-plugin contract v1: sb/app/plugin_host.py, plugins.lock.json pin registry, tools/plugin_pin.py, pyproject.toml installable `sb`, docs/game-plugin-contract.md, examples/superbot-plugin-hello seed — ledger D-0056 homed in the contract doc)
blockers: ORDER 002's done-when tail is owner-blocked twice over: (a) the separate plugin repo cannot be agent-created (GitHub App token 403s on repo creation; git proxy pushes only to session repos), (b) the live-guild `!hello` render needs a bot restart and the RUNNING bot is deliberately held for the owner's presentation hand-pass
orders: acked=001,002,003 done=003
⚑ needs-owner: (1) create `menno420/superbot-plugin-hello` and move `examples/superbot-plugin-hello/` into it verbatim (testing-report flag 18a); (2) after the presentation hand-pass: restart the bot from current main — superbot-plugin-hello is already pip-installed in the container and pinned, so `/hello` + `!hello` then register+render live (flag 18b closes ORDER 002's done-when); carried: hand-verify the presentation surfaces (bot RUNNING @ 59755e3, click list with the coordinator), settings-EDIT/setup-FLOW boundary ruling (flag 14/17a), hub-topology ratification (flag 21), sacrificial member (flag 9), old SuperBot shares `!` on the test guild (flag 15), kernel-surface-drift parity ruling (flag 13)
notes: ORDER 002 deliverables 1–4 are all built and merged (packaging strategy, discovery+registration+gates, hello-world plugin — one command + one panel — and the contract doc); done=002 is withheld ONLY on the done-when's "from a separate repo registers and renders in the test guild", which is one owner-created repo + one bot restart away. In-process proof rode the real pipeline (parity composition root + the exact step-9b sequence): panel rendered, drift + unpinned gates refuse. ORDER 003 executed: stale heartbeat PR #60 closed with a one-line supersede comment. ORDER 001 state unchanged this session. Handoff: session scratchpad order002-handoff.md.
