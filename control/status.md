# superbot-next · status
updated: 2026-07-09T16:05Z
phase: band-2 slice-2 (operator-spine eight, ladder step 4) COMPLETE — goldens 0/14 all classified (no new red class), full live ladder driven (admin reads, pending-terminal refusals in a throwaway channel, cleanup word K7 lane, automod/security decision cores, welcome/counters templates), three genuine bugs fixed (D-0057 responder chunking + word-op acks; D-0058 warn-escalation compensator = ORDER 004 item 1); ORDER 002's live-render half CLOSED (`!hello` plugin panel rendered in the test guild from a fresh post-#75 boot; 13 guild commands incl. /hello). Next ladder step: band 3 economy — now bound by ORDER 004 item 3 (walking-skeleton live-drive + classify-or-fix)
health: red-by-design (golden-parity `report` dashboard red while golden rows are `pending` — unchanged class; operator-spine 0/14 is fully decomposed into the ledgered classes). Everything else green: pytest 1104/2-skipped, manifest_compile verify, 19-checker fleet, `check --strict`, PRs #79/#80/#81 CI green end-to-end
last-shipped: #81 — testing-report step-4 row + evidence + flag-18b resolution; before it #80 (warn-escalation compensator, oracle-aligned, D-0058 — ORDER 004 item 1) and #79 (live responders chunk >2000-char success copy; cleanup word ops speak acks; D-0057)
blockers: ORDER 002 done-when now hangs ONLY on the owner-created separate repo (menno420/superbot-plugin-hello still does not exist — re-verified via GitHub search 15:41Z; the live-guild render half is DONE); ORDER 004 items 2 (help byte-parity + first A-16 flip) and 4 (K7 `_state_write` bypass, Reply boilerplate collapse, target-DM successor naming) are unstarted — item 2 collides with the standing kernel-surface-drift/cross-band-noise question (help's prefix golden carries the xp.awarded noise class; a flip needs that ruling or a normalizer, see notes)
orders: acked=001,002,004 done=003
⚑ needs-owner: (1) create `menno420/superbot-plugin-hello` and move `examples/superbot-plugin-hello/` verbatim (flag 18a — the ONLY remaining ORDER 002 tail; no re-pin needed); (2) kernel-surface-drift parity ruling (flag 13) — now ALSO the gate on ORDER 004 item 2's help flip (the help goldens embed old-bot passive xp/ai rows + the new dispatch trace shapes; byte-parity needs an owner-ruled exemption class or normalizer scope, we will not fudge goldens); carried: sacrificial member for kick/ban full-effect proof (flag 9), guild-action live adapter scheduling (flag 10), settings-EDIT/setup-FLOW boundary ruling (flag 14/17a), old SuperBot shares `!` on the test guild (flag 15), hub-topology ratification (flag 21), owner hand-pass of the presentation surfaces (click list with the coordinator — bot is RUNNING from latest main ec307c2 with all fixes + the hello plugin armed)
notes: ORDER 004 (P0) landed mid-run; executed per priority: item 1 DONE (PR #80 — WARN op EFFECT leg carries `moderation.compensate_warn_escalation`: a Discord-refused escalation keeps the count and withdraws the phantom history rows, matching the oracle's escalation_blocked; enshrining test re-pinned; proven end-to-end on the real engine, replay 0/15 unchanged); item 3's binding was ALREADY the shape of this slice's run (fresh-boot walking skeleton + every replay red classified-or-fixed in-PR) and is written into the band-3 handoff as binding; item 5's demo rule applied to the step-4 report row ("Live: PASS" names its known-red presentation classes); items 2+4 handed to the next session via the handoff. Band-2 slice-2 detail: replay `parity_band2s2` fresh-DB 0/14 both runs; live evidence + verbatim lines in the testing report's step-4 block; state restored after every drive (words removed, automod settings reverted, throwaway channel deleted); verified_live: nothing minted (Q-0244). The live bot process was restarted TWICE deliberately (flag-16 pattern): once to close flag 18b, once at run end — now RUNNING from main ec307c2 (post-#79/#80/#81), HEALTH 8080, plugin admitted, 13 guild commands. Handoff: session scratchpad band2s2-handoff.md (band-3 economy: silent daily/pay heads-up confirmed still real — economy legs carry no user_message).
