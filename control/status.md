# superbot-next · status
updated: 2026-07-09T18:11Z
phase: band-4 (xp/karma/community/spotlight/leaderboard, ladder step 6) COMPLETE — goldens 0/15 all classified (runs 2-4 byte-identical, NO new red class), four genuine bugs fixed (PR #88: chat-award feed armed live+harness, chat-draw RNG seam, karma two-clocks cooldown bug, band-4 refusal copy — the band-4 decisions-ledger entry), full live ladder driven (chat award cooldown+rng bounds, level-up fan-out to a bound channel, karma give/check/add + write-nothing refusals, 12 rank providers + leaderboards, community hub + spotlight clicks, INV-G/INV-K sweep clean). Band-3 wrap-up debt settled (PRs #86/#87 un-stuck via API branch-update → merged; stale bot restarted). ORDER 006 delivered (PR #92 project review: agent audit + model attribution, ⚑ owner-actions, CONTINUATION). ORDER 007 latency ping acked (PR #93, discovered 17:55:22Z via mid-session inbox check). Band 5 (governance/roles/platform, ladder step 7) NOW IN PROGRESS — this heartbeat settles band-4's unwritten status debt first
health: red-by-design (golden-parity dashboard red while rows are `pending` — unchanged class; band-4 0/15 fully decomposed into the named classes). Everything else green: pytest 1123/2-skipped, manifest recompile, 19-checker fleet, `check --strict` (0 stamp findings after PR #92), PRs #86/#87/#88/#90/#92/#93 CI green end-to-end
last-shipped: #92 — ORDER-006 project review (agent audit, ⚑ owner-actions, CONTINUATION); before it #93 (ORDER-007 ping ack), #90 (testing-report step-6 row + band-4 evidence + wrap-up notes), #88 (band-4 seam/feed/copy fixes), and the inherited #86/#87 (band-3 report; ORDER-005 retro)
blockers: ORDER 002 done-when still hangs ONLY on the owner-created separate repo (menno420/superbot-plugin-hello does not exist — re-verified via GitHub search 18:11Z this session); ORDER-004 item 2 (help byte-parity + first A-16 flip) still ⚑ GATED on the flag-13 kernel-surface-drift ruling (no ruling in the inbox as of 18:11Z — inbox still ends at ORDER 007)
orders: acked=001,002,004,005,006,007 done=003,005,006,007
⚑ needs-owner: the full WHAT/WHERE/HOW/WHY/UNBLOCKS list is now consolidated in docs/retro/project-review-2026-07-09.md §2 (PR #92) — top three by leverage: (1) flag-13 corpus-red disposition ruling (unblocks ORDER-004 item 2 + every parity flip), (2) create `menno420/superbot-plugin-hello` (the ONLY ORDER-002 tail, flag 18a), (3) merge queue / up-to-date relaxation (kills the branch-update dance that stranded #86/#87). Carried: sacrificial member (flag 9), old SuperBot shares `!` (flag 15), settings-EDIT/setup-FLOW boundary (flag 14/17a), hub topology (flag 21), band-7 AI key envelope, owner hand-pass of presentation surfaces — NEW from band 4: a real keyboard message in the test guild should now earn 15-25 XP on a 60s cooldown (the chat award is live; that human keystroke is the one evidence line agents cannot synthesize)
notes: ORDER-004 item 3 binding followed exactly (walking-skeleton boot + live-drive from the PR #88 branch BEFORE merge; every band-4 replay red classified-or-fixed in the same PR). ORDER 005 was completed by the band-3 session (PR #87, merged this session). Band-4 detail: replay `parity_band4` fresh-DB 0/15 runs 1-4 (1 pre-fix, 2-4 identical post-fix); the D-0060 cascade note REALIZED — arming the passive chat award in the harness ends the cross-band `xp: missing`/RNG-stream-offset classes corpus-wide (band-3 spot re-run confirms); karma's two-clocks bug is the band-5 audit target (grep NOW() vs ctx.clock in band-5 stores before live-driving). Live evidence + verbatim lines in the testing report's step-6 block; state restored after every drive (settings bracket cleared, binding unbound, throwaway channel deleted); DB rows left documented (synthetic 9000000000000005xx xp/karma rows). verified_live: nothing minted (Q-0244). The live bot was restarted twice deliberately: once from the PR #88 branch (item-3 walking skeleton), once at run end from merged main — RUNNING, HEALTH 8080, /ready 200, 13 guild commands, message feed armed WITH the passive chat award. Model attribution for this session: Claude Fable 5 (also in every commit trailer). This heartbeat was written by the band-5 session settling band-4's heartbeat debt (the band-4 session ended before its final status overwrite); band-5 run underway per completion report §5 step 7
