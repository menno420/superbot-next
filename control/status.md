# superbot-next · status
updated: 2026-07-12T09:35Z
phase: band-5 COMPLETE — live-bug fix lane DONE via #111 (merge `569beea`; CI https://github.com/menno420/superbot-next/actions/runs/29116022101 — all 6 checks in that run green, `report` red-by-design/non-required): the 3 live-only bugs from #109's ledger fixed test-first — (1) role pending terminals now register at MODULE IMPORT (`sb/domain/role/handlers.py` calls `_register_pending()` top-level; the live root never ran ENSURE_REFS with zero plugins), (2) setrole/unsetrole/removereactrole acks read the engine's REAL `after` keys (rollup keys by step target_name, never `after["record"]`) with copy pinned to the oracle (disbot/cogs/role_cog.py:534/:565/:705), (3) `!temprole` failure copy no longer leaks the raw WorkflowResult repr — plus the #108 symmetric twin folded in: `_compensate_lock` deletes IF-MATCH (winner_id + unlock_at via `store.delete_lock_if_match`; a permanent grant compensates nothing) + the extended compensator-invariant test (every declared compensator ref resolves to a registered leg). AND the first parity flip landed: #112 (merge `c0f0783`) — `help: ported` in parity/parity.yml through the A-16 door: 3/3 help goldens byte-green (help.panel_open, sweep.help, sweep.slash_help), 100% declared-surface coverage (R2 vacuously — help declares none), zero exemptions, ratchet row `help: {events: 1, tables: 3, settings: 0}`. Dispositions that bit: kernel-surface-drift, xp-coins-alias, invoking-message-deletion, plus symmetric `<msg:N>` ref renumbering added to `apply_dispositions` (docs/parity/flag-13-disposition-2026-07-10.md § Ref renumbering). ▶ LANES (2026-07-10/11, parallel): (a) parity-flips lane — SESSION END: two waves shipped (wave 1 #118/#119/#121/#123/#125, wave 2 #128/#129/#131/#132) PLUS post-wrap wave-3 sibling flips #137 (settings pending→ported, 4/4 goldens) + #140 (cleanup pending→ported, 3/3 goldens) + wave-4 sibling flips #143 (xp pending→ported, 3/3 goldens) + #145 (proof_channel pending→ported, 3/3 goldens) + wave-5 flips #152 (economy pending→ported, 6/6 goldens) + #154 (ticket pending→ported, 6/6 goldens) + #157 (karma pending→ported, 8/8 goldens — wave-5 records below) + wave-6 #163 (moderation pending→ported, 8/8 goldens — wave-6 record below; the #161 P2-triage closures are folded at lane (e) and the wave-5 UPDATE row), quicksetup attempted and honestly parked at a verbatim wall (record below) and setup PARKED at the same create-channel wall in a new flavor (trap 17; wave-6 record below); the parity session is now FULLY ENDED — 8 flips + the #161 fix PR + 3 heartbeats (session-end note in the lane record); (b) band-6 games lane — SESSION END: SEVEN slices merged in continuous mode per Q-0265 (#114/#117/#120/#124/#130/#133/#138, lane record below) — the composition-parity class-killer, both game ports, PvP, both tournaments + the reaction-ingress seam, the settle-once + render-once guards, and the last red game-family golden flipped by #138; both games playable solo + PvP + tournament, ALL game-family goldens green (rps_tournament 6/6, blackjack 5/5); remaining surface is unpinned polish only (ledgered in the two docs/ideas remaining-surface files) — NO golden-pinned work left in this lane; next wake either starts band-7 per the canonical order or takes owner direction; (c) kit lane — vendored substrate-kit v1.6.0→v1.7.0 (#116, merge `a700975`) →v1.7.1 (#122, merge `1ba8607`) →v1.8.0 (#135, merge `3dfc194`; new plants control/claims/README.md + scripts/env-setup.sh, auto-merge enabler STAGED only, v1.7.1 dist banked); (d) band-7 builder lane — OPENED per the Q-0265 honesty-guard handoff (record below): #141 test-order-flake fix (merge `78509df`) + #144 btd6 pending→ported (merge `596fb2d`, 39/39 goldens) + #148 project_moon+projectmoon pending→ported (merge `f2dcf0d`, 11/11 goldens) + #151 ai pending→ported (merge `1bea65d`, 20/20 goldens fixed-in-PR, deterministic operator surface; the live-NL leg stays owner-key-gated, OWNER-ACTION 5 below) + #155 aireview sweep family re-homed `_unmapped`→ai (merge `4c8c5b0`, 11/11 goldens — the #151 parked item 4 CLOSED) + #156 k10 test-order-flake fix (merge `179dfb2` — AI-registry isolation in both directions) + #160 (merge `0a29d37`, ai policy/settings-mutation surface: the shipped chooser PAGES + the S6/S7 settings edit/reset widgets over the audited `settings.set_scalar` lane, band-7 slice 5 — the #151 parked item 3 CLOSED) + #165 (merge `62d850f`, band-7 slice 6 — the live MODAL-SUBMIT lane arms + the #160-parked ai free-text/Override editors ship on it, D-0066) + #177 (merge `f986177`, band-7 slice 7 — the ai policy SCOPE PICKERS: typed override tables + audited `ai.set_*_policy` ops + the K10 overlay reader, D-0070) — the lane's session was COMPLETE at the wrap-up heartbeat (12 merged PRs; lane record below), then REOPENED in Q-0265 continuous mode: #185 behavior-preset slice (merge `168ef80`, D-0071, NEW `seeded-catalog` depth class, migration 0030) + #187 orchestration-mutation slice (merge `d9c6b35` — main HEAD, D-0072, migration 0031 — the tools profile pickers + K10 resolver consumption; 3 codex line findings fixed in-PR, 1 declined with oracle citation); the D-0066 slice LANDED as #194 (merge `4024624d`, D-0073 — replay `modal` step kind + the first 2 MINTED goldens, btd6_strategy_form_submit{,_minimal}; corpus 465→467) followed by #199 (merge `029b4001`, hygiene — `--write-ratchet` now splices ONLY the depth.ratchet block, comment-preserving) + #204 (merge `f82ac804`, D-0074 — the routing-matrix picker live; EVERY views/ai/* chooser surface now live, the last chooser pending terminal retired) + #208 (merge `0e7cacd0` — btd6 resolver maps/modes matching, the first #144 parked DOMAIN item retired); the continuation is now SESSION COMPLETE at the 19:50Z wrap-up fold (OWNER WRAP-UP DIRECTIVE, archive-prep — enders only, executed; 8 continuation PRs all verified merged, final counts in the health line; the lane record below carries the successor map); (e) codex-P2-triage lane — #161 (merge `8ad243f`, card `.sessions/2026-07-11-codex-p2-triage.md`): the three post-merge Codex P2s from #154/#157 all verified REAL and fixed (group-scoped command aliases via one shared `sb/spec/commands.py command_dispatch_keys()` truth, the ticket post-panel staff gate with the shipped refusal byte, the live `/karma` acked-interaction followup send — live-adapter only). Still queued: role/proof_channel live EFFECT action ports (GuildRoleActions, ChannelPermActions still unarmed)
health: green with the standing red-by-design (`report` dashboard red until the `_unmapped` re-home pool is green — the golden-parity GATE is non-vacuous and GREEN). ★ PROGRAM COMPLETE / WAVE-8 END waypoint (parity-flips wave-8 wrap-up fold, 2026-07-12T09:35Z, HEAD `7334083` — #246's merge, main HEAD): **THE PARITY PROGRAM IS COMPLETE AT 50/50** — all 49 real subsystem rows + the kernel coverage home ported; parity.yml at HEAD carries 49 `ported` subsystem rows with ONLY `_unmapped: pending` and kernel `status: ported` (machine-counted). Gate GREEN — **365/365** goldens across the 50 ported at main `7334083` (main-push golden-parity run 29186950621 **gate job 86634702696** CI-LOG-VERIFIED: "gate: GREEN — all 365 golden(s) across 50 ported subsystem(s) replay clean" + "golden-parity gate: 50 ported / 1 pending" with the pending table down to its LAST line, _unmapped [106 goldens] + "check_parity_depth: OK — 50 subsystems (49 ported), kernel ported, 471 goldens" same job; integration 11 passed same job — the F-001/F-002 real-Postgres concurrency regressions INCLUDED); report **370/471** green, 471/471 replayable (report job 86634702704 same run: "green: 370/471 replayed cases match their golden" + "replayable: 471/471" + "ported: 50/51 subsystems" with setup 9/9 · quicksetup 1/1 green [ported] and EVERY ported row green in the per-subsystem table — `_unmapped 5/106 green [pending]` is the only non-green line; "report: RED — 101 golden(s) not yet at parity (EXPECTED until the last subsystem flips ported)" — red-by-design, non-required); units **1722 passed / 8 skipped in CI** at `7334083` (ci run 29186950619 tests job 86634702742 — deps-free CI shape; local ladders with deps differ by the standing guarded-import skip delta); corpus **471 = 465 imported + 6 minted** (unchanged — the wave minted zero); `_unmapped` **107→106** across the wave (sweep_setup → setup at #245). The movement from the WAVE-7 END waypoint (355→365 gate, 48→50 ported rows) is the wave-8 lane's two flips joining: #245 (`32544d9`, setup pending→ported, 9/9 goldens incl. the re-homed sweep_setup — gate 364/364 across 49 at its merge, run 29186558036 gate job 86633632162; trap-17 wall RESOLVED) and #246 (`7334083`, quicksetup pending→ported, 1/1 — the program's 50th and FINAL row; lane record "Parity-flips wave-8 lane record 2026-07-12" below). REMAINING parity work at HEAD is ONE POOL: the `_unmapped` re-homes — **106 files, 101 not yet green** (5 alias green through ported rows), mostly btd6-family sweeps over the PORTED btd6 row + deep-system mining strays + fishing gear surfaces + mixed singletons — attribution work toward a fully-green report job, NOT port work; no subsystem row is pending, no wall remains. WAVE-7 END waypoint (parity-flips wave-7 wrap-up fold, 2026-07-12T07:45Z, HEAD `553ff2b` — #240's merge, main HEAD): parity **47/50** subsystem rows ported PLUS the kernel coverage home (= **48** rows in the gate — EVERY REAL SUBSYSTEM ROW IS NOW PORTED); gate GREEN — **355/355** goldens across the 48 ported at main `553ff2b` (main-push golden-parity run 29182868238 gate job 86623673864 CI-LOG-VERIFIED: "gate: GREEN — all 355 golden(s) across 48 ported subsystem(s) replay clean" + "golden-parity gate: 48 ported / 3 pending" with the pending table _unmapped [107] / quicksetup [1] / setup [8] + "check_parity_depth: OK — 50 subsystems (47 ported), kernel ported, 471 goldens" same job; integration 11 passed same job — the F-001/F-002 real-Postgres concurrency regressions INCLUDED, so each flip's locking-untouched claim is CI-proven); report **361/471** green, 471/471 replayable (report job 86623673865 same run: "green: 361/471 replayed cases match their golden" + "replayable: 471/471" + "ported: 48/51 subsystems" with farm 1/1 · fishing 2/2 · mining 6/6 green [ported] in the per-subsystem table; "report: RED — 110 golden(s) not yet at parity (EXPECTED until the last subsystem flips ported)" — red-by-design, non-required); units **1713 passed / 8 skipped in CI** at `553ff2b` (ci run 29182868237 tests job 86623673877 — deps-free CI shape; local ladders with deps differ by the standing guarded-import skip delta); corpus **471 = 465 imported + 6 minted** (unchanged — the wave minted zero); `_unmapped` **111→107** across the wave (sweep_fastmine + sweep_chop + sweep_explore + sweep_reset_inventory → mining at #240). The movement from the WAVE-6 END waypoint (346→355 gate, 45→48 ported rows) is the wave-7 lane's three flips joining: #233 (`c3e8ad0`, farm pending→ported, gate 347/347 at its merge), #236 (`c1f56cd`, fishing pending→ported, gate 349/349 at its merge), #240 (`553ff2b`, mining pending→ported — lane record "Parity-flips wave-7 lane record 2026-07-12" below); the band-7 successor lane's #232/#234/#235/#237/#238/#239 landed interleaved and minted zero goldens. REMAINING-PENDING MAP at HEAD (verified in parity.yml + the gate job's pending table): ONLY setup (8, PARKED trap 17) and quicksetup (1, BLOCKED D-0030) — both walls owner-shaped — plus the `_unmapped` re-home pool (107); the parity program stands at **48/50**. WAVE-6 END waypoint (parity-flips wave-6 wrap-up fold, 2026-07-12T03:45Z, HEAD `8730200` — #230's merge, main HEAD): parity **44/50** subsystem rows ported PLUS the kernel coverage home (= **45** rows in the gate); gate GREEN — **346/346** goldens across the 45 ported at main `8730200` (main-push golden-parity run 29178063196 gate job 86610739481 CI-LOG-VERIFIED: "gate: GREEN — all 346 golden(s) across 45 ported subsystem(s) replay clean" + "golden-parity gate: 45 ported / 6 pending" with the pending table _unmapped [111] / farm [1] / fishing [2] / mining [2] / quicksetup [1] / setup [8] + "check_parity_depth: OK — 50 subsystems (44 ported), kernel ported, 471 goldens" same job; integration 10 passed same job); report **352/471** green, 471/471 replayable (report job 86610739470 same run: "report: RED — 119 golden(s) not yet at parity (EXPECTED until the last subsystem flips ported)"); units **1568 passed / 7 skipped in CI** at `8730200` (ci run 29178063197 tests job 86610739516 — deps-free CI shape; local ladders with deps differ by the standing guarded-import skip delta); corpus **471 = 465 imported + 6 minted** (unchanged — the wave minted zero); `_unmapped` **114→111** across the wave (sweep_dextop→creature at #226, sweep_world+sweep_worldcard→games at #230). The movement from the POST-#228 waypoint (342→346 gate, 43→44 ported subsystems) is the wave's last two merges joining: #229 (`59e2e22`, the post-#228 heartbeat) then #230 (`8730200`, games pending→ported — lane record "Parity-flips wave-6 lane record" below; the wave's earlier flips #219/#224/#226/#227 were folded at POST-#228). REMAINING-PENDING MAP at HEAD (verified in parity.yml + the gate job's pending table): farm (1) / fishing (2) / mining (2) — real rows, NOT yet flipped (the #217 farm/mining merge was the money-race FIX, not a flip); setup (8, PARKED trap 17); quicksetup (1, BLOCKED D-0030); `_unmapped` (111, the re-home pool). POST-#228 waypoint (kernel-band heartbeat fold, 2026-07-12T03:20Z, HEAD `01d49688`): parity **43/50** subsystems ported PLUS the KERNEL coverage home ported (parity.yml `kernel.status: ported` — #228, D-0075; the kernel home is a pseudo-subsystem, never a subsystems row); gate GREEN — **342/342** goldens across **44** ported (43 subsystems + kernel) at the #228 PR merge ref (PR-head golden-parity gate job 86609318389 in run 29177571964 CI-LOG-VERIFIED: "gate: GREEN — all 342 golden(s) across 44 ported subsystem(s) replay clean" + "golden-parity gate: 44 ported / 7 pending" + "check_parity_depth: OK — 50 subsystems (43 ported), kernel ported, 471 goldens" same job; integration 10 passed same job); corpus **471 = 465 imported + 6 minted** (#194's 2 + #228's 4 kernel-band mints, `minted_goldens: 6`); report 471/471 replayable, **348/471** green local serial at the #228 merged head (kernel 4/4 [ported] in the table); units 1575 passed / 2 skipped local at the same head. The movement from the POST-#217 waypoint (328→342 gate, 467→471 corpus, 39→43 ported + kernel) folds the previously-unwaypointed merges: #219 (`d3dba9b`, inventory pending→ported), #222 (`9b80444`, the 11-red canonical-order flake root-caused and fixed — one-way registry wipes vs the #213 session-start composition import; gate 329/329 at its head), #224 (`cc59a6e`, casino pending→ported), #225 (`af985c1`, btd6 freeplay MOAB scaling — the bloon_rbe_at_round spawn-tree walk ported, gate 337/337 at its head), #226 (`f032e8a`, creature pending→ported), #227 (`39bf226`, four_twenty pending→ported), and #228 itself (merge `01d49688`, slice record "Kernel-band goldens slice 2026-07-12" below). POST-#217 waypoint (builder heartbeat fold, 2026-07-12T00:15Z, HEAD `ed8eed34`): parity UNCHANGED at **39/50** ported; gate GREEN — **328/328** goldens across the 39 ported at the #217 PR head `60bce59` (PR-head golden-parity gate job 86596896937 in run 29172910085 CI-LOG-VERIFIED: "gate: GREEN — all 328 golden(s) across 39 ported subsystem(s) replay clean" + "golden-parity gate: 39 ported / 11 pending" + "check_parity_depth: OK — 50 subsystems (39 ported), 467 goldens" same job — the 266→328 movement inside the #217 window is the parallel lane's #218 btd6-family re-home (62 goldens) joining the gate; #217 itself minted/moved ZERO goldens, farm 1 + mining 2 + fishing 2 stay pending expected-red in that job's table); integration suite **9 passed** in the same CI job (the 4 NEW farm/mining race tests + #213's 5); units 1473 passed / 2 skipped local at the pre-merge head (canonical ladder, real Postgres 16). POST-#215 waypoint (coordinator heartbeat fold, 2026-07-11T23:10Z, HEAD `977bb27b`): Parity is now **39/50** subsystems ported — #214 (merge `827b134`) flipped treasury pending→ported (39 ported / 11 pending / 50 subsystem rows in parity/parity.yml at HEAD); gate GREEN — **266/266** goldens across the 39 ported (per the #214 merge body); the F-001/F-002 wallet-race and F-003 parity-gate false-green fixes are IN at HEAD via #213 (merge `f71d60b` — round record below). WAVE-5 END waypoint (verbatim, still valid at its shas — the parity lane's wrap-up fold, 2026-07-11T20:20Z): Parity is now **38/50** subsystems ported — the wave-5 lane's #211 starboard BIRTH added row 50 to parity.yml AND flipped it ported (hand-counted at HEAD: 38 ported / 12 pending incl. `_unmapped`); gate GREEN — **264/264** goldens across the 38 ported at main `a283f3f` (#211's merge — main-push golden-parity run 29166086847 gate job 86579320344 CI-LOG-VERIFIED: "gate: GREEN — all 264 golden(s) across 38 ported subsystem(s) replay clean" + "golden-parity gate: 38 ported / 12 pending" + "check_parity_depth: OK — 50 subsystems (38 ported), 467 goldens" same job); report **301/467** green, 467/467 replayable, 38/50 ported (report job 86579320337 same run: "green: 301/467 replayed cases match their golden" + "replayable: 467/467" + "ported: 38/50 subsystems"; starboard 6/6 · ticket 12/12 · moderation 10/10 · channel 4/4 · cleanup 7/7 · xp 7/7 · btd6 41/41 [ported] and `_unmapped` 35/176 [pending] in the per-subsystem table); units **1456 passed / 4 skipped** in CI at `a283f3f` (ci run 29166086818 tests job 86579320209 — "1456 passed, 4 skipped"; local canonical ladders differ by the standing guarded-import skip delta); corpus 467 = 465 imported + 2 minted (unchanged); `_unmapped` **201→176** across wave 5 (25 goldens re-homed: #200 6 + #202 4 + #203 4 + #207 5 + #211 6); compensator allowlist EMPTY at HEAD (tests/unit/workflow/test_compensator_invariant.py `_ALLOWLIST = {}`). PRE-STARBOARD waypoint (verbatim, still valid at its shas): Parity: 37/49 subsystems ported — admin, ai, automod, blackjack, btd6, chain, channel, cleanup, community, community_spotlight, counters, counting, diagnostic, economy, general, help, hermes, image_moderation, karma, leaderboard, logging, moderation, project_moon, projectmoon, proof_channel, role, rps_tournament, security, server_management, servermanagement, settings, ticket, utility, ux_lab, uxlab, welcome, xp (admin/community/community_spotlight/role are the wave-10 flips #186/#188/#189/#190 — wave-10 record below; counted by hand in parity/parity.yml at `fd62f53`, #190's merge); gate GREEN — 258/258 goldens across the 37 ported at main `0e7cacd0` (#208's merge, main HEAD — measured by the #208 worker at the merged tree, local serial ladder on real Postgres: units 1458 passed / 2 skipped; report 295/467 green, 467/467 replayable, 37/49 ported; corpus 467 = 465 imported + 2 minted; the gate/report movement 253→258 / 291→295 inside the #208 window is the parallel parity lane's #207 moderation/channel re-home joining the gate — the band-7 slices #199/#204/#208 minted ZERO goldens). POST-#204 waypoint (verbatim, still valid at its shas): gate GREEN — 253/253 goldens across the 37 ported at `f82ac804` (#204's merge — measured by the #204 worker at merge, local serial ladder on real Postgres: units 1435 passed / 2 skipped; report 291/467 green, 467/467 replayable, 37/49 ported; the gate movement 245→253 is the parallel parity lane's #202 word family (4) + #203 xp-admin family (4) joining the gate — the band-7 slices #199/#204 minted ZERO goldens, #194's two mints were already in the 245). WAVE-5+#200 waypoint (verbatim, still valid at its shas): gate GREEN — 245/245 goldens across the 37 ported at #200's head `f8b828f` (PR-head golden-parity gate job 86568168396 CI-LOG-VERIFIED; check_parity_depth 49 subsystems (37 ported) / 467 goldens same job — the corpus grew 465→467 via #194's two minted btd6 strategy-form goldens, D-0073; `_unmapped` 201→195 — #200 re-homed the 6 ticket-admin sweeps onto the ported ticket row, ticket now 12/12; units 1416 passed / 4 skipped at the #200 head). RE-HOME-LANE waypoint (verbatim, still valid at its shas): gate GREEN — 237/237 goldens across the 37 ported at #193's head `84f744c` (PR-head golden-parity gate job 86562928776 in run 29159846075 CI-LOG-VERIFIED: "gate: GREEN — all 237 golden(s) across 37 ported subsystem(s) replay clean" + "golden-parity gate: 37 ported / 12 pending" + check_parity_depth "OK — 49 subsystems (37 ported), 465 goldens" same job; report leg 276/465 green, replayable 465/465 CI-LOG-VERIFIED same run [report job 86562928761: "green: 276/465 replayed cases match their golden" + "replayable: 465/465" + "ported: 37/49 subsystems", role 17/17 + counting 6/6 [ported] and `_unmapped` 37/201 [pending] in the per-subsystem table — the 19 re-homed sweeps joined the gate, `_unmapped` 220→201]; units 1405 passed / 2 skipped local at the merged head, serial ladder — trap 25). WAVE-10 gate waypoint (verbatim, still valid at its shas): gate GREEN — 218/218 goldens across the 37 ported at #190's head `47d5cb8` (PR-head golden-parity gate job 86556592810 in run 29157369580 CI-LOG-VERIFIED: "gate: GREEN — all 218 golden(s) across 37 ported subsystem(s) replay clean" + "golden-parity gate: 37 ported / 12 pending" + check_parity_depth "OK — 49 subsystems (37 ported), 465 goldens" same job; the wave's per-flip PR-head gate jobs each CI-log-verified: 86554235291 [#186, run 29156448544, "all 214 golden(s) across 34"], 86555023205 [#188, run 29156756289, "all 216 golden(s) across 35"], 86555799262 [#189, run 29157062088, "all 217 golden(s) across 36"]); report leg 256/465 green, replayable 465/465 (measured SERIALLY on local real Postgres at the #190 head `47d5cb8` — trap 25; admin 2/2, community 2/2, community_spotlight 1/1, role 1/1 [ported]; local waypoints along the wave: 251/465 at the #186 pre-merge head, 253/465 at #188's head `20049e0`, 254/465 at #189's head `a7fa6ba` — the extra green over the +6 flip sum is `_unmapped` role-family sweeps aliasing the reshaped role hub; the main-push run at `fd62f53` re-verifies on its own logs); units 1388 passed / 2 skipped local at the #190 head (canonical order; the +14 over wave 9 is the sibling #185's test file). POST-#187 waypoint (band-7 lane continuation, folded at the 15:45Z heartbeat — local serial ladder on real Postgres at main `d9c6b35`, verified by the #187 worker): units 1405 passed / 2 skipped (canonical order — the +17 over the #190 head is the #185/#187 slices' tests); gate GREEN 218/218 across the 37 ported; report 256/465 green, 465/465 replayable, 37/49 ported — gate/report/ported counts UNCHANGED by #185/#187 (both slices mint zero goldens; the golden movement this window came from the parallel parity-lane session's #183/#186/#188–#191, not these two slices). WAVE-9 waypoint (verbatim, still valid at its shas): Parity was 33/49 — ai, automod, blackjack, btd6, chain, channel, cleanup, counters, counting, diagnostic, economy, general, help, hermes, image_moderation, karma, leaderboard, logging, moderation, project_moon, projectmoon, proof_channel, rps_tournament, security, server_management, servermanagement, settings, ticket, utility, ux_lab, uxlab, welcome, xp (diagnostic is the wave-9 flip #183, merge `10bf073`; counted by hand in parity/parity.yml at `10bf073`); gate GREEN — 212/212 goldens across the 33 ported at #183's head `78bc2c5` (PR-head golden-parity gate job 86548347925 in run 29154127654 CI-LOG-VERIFIED: "gate: GREEN — all 212 golden(s) across 33 ported subsystem(s) replay clean" + "golden-parity gate: 33 ported / 16 pending" + check_parity_depth "OK — 49 subsystems (33 ported), 465 goldens" same job); report leg 249/465 green, replayable 465/465 (measured SERIALLY on local real Postgres at the #183 head — trap 25; diagnostic 37/37 [ported] in the per-subsystem table; the main-push run at `10bf073` re-verifies on its own logs); units 1374 passed / 2 skipped local at the #183 head (canonical order). WAVE-8-AND-EARLIER waypoints (verbatim, still valid at their shas): Parity was 32/49 — ai, automod, blackjack, btd6, chain, channel, cleanup, counters, counting, economy, general, help, hermes, image_moderation, karma, leaderboard, logging, moderation, project_moon, projectmoon, proof_channel, rps_tournament, security, server_management, servermanagement, settings, ticket, utility, ux_lab, uxlab, welcome, xp (image_moderation/counters/server_management are the wave-8 flips #176/#178/#179 — wave-8 record below; counted by hand in parity/parity.yml at `f986177`, #177's merge and main HEAD — #177 flips no row, unchanged since `12b4135`); gate GREEN — 175/175 goldens across the 32 ported subsystems at `f986177` (main-push golden-parity run 29152481842, gate job 86544105424 CI-log-verified: "gate: GREEN — all 175 golden(s) across 32 ported subsystem(s) replay clean" + "golden-parity gate: 32 ported / 17 pending" + check_parity_depth "OK — 49 subsystems (32 ported), 465 goldens" same job; prior waypoint at `12b4135`: main-push golden-parity run 29151976612, gate job 86542787762 CI-log-verified: "gate: GREEN — all 175 golden(s) across 32 ported subsystem(s) replay clean" + "golden-parity gate: 32 ported / 17 pending" + check_parity_depth "OK — 49 subsystems (32 ported), 465 goldens" same job; report job 86542787756 same run: "green: 212/465" + "replayable: 465/465" + "ported: 32/49 subsystems" with counters 2/2, image_moderation 1/1, server_management 1/1 [ported] in the per-subsystem table; per-flip PR-head gate jobs also green: 86539735173 [#176], 86541097455 [#178], 86542675635 [#179], each flip's local ladder proving 172/172 across 30, 174/174 across 31 and 175/175 across 32 on real Postgres; units 1357/2 local at the #179 head [1356/2 at #176/#178 heads]; wave-7 waypoint: 171/171 across 29 at `f87ffb0` [main-push gate job 86538208849 in run 29150138337] — that gate job log verified: "gate: GREEN — all 171 golden(s) across 29 ported subsystem(s) replay clean" + "golden-parity gate: 29 ported / 20 pending" + check_parity_depth "OK — 49 subsystems (29 ported), 465 goldens" same job; per-flip PR-head CI also log-verified: 169/169 across 27 at #172's merge ref [gate job 86536598335 run 29149501523], gate jobs 86537419185 [#173] and 86538133157 [#174] green with each flip's local ladder proving 170/170 across 28 and 171/171 across 29 on real Postgres; earlier waypoints: 168/168 across 26 at `8b55d95` [gate job 86534763788 run 29148787003, log-verified: "gate: GREEN — all 168 golden(s) across 26 ported subsystem(s) replay clean" + "golden-parity gate: 26 ported / 23 pending" + check_parity_depth "OK — 49 subsystems (26 ported), 465 goldens" same job], 151/151 across 23 at `bd211e9` [gate job 86525137517 run 29145082777], 143/143 across 22 at `0a29d37` [gate job 86523808594 run 29144580286, log-verified with check_parity_depth "OK — 49 subsystems (22 ported), 465 goldens"], 143/143 across 22 at `9053311` [gate job 86518931866 run 29142780549], 112/112 across 19 at `1bea65d` [gate job 86512108907 run 29140344074], 92/92 across 18 at `531f14ee` [gate job 86510003976 run 29139525710], 78/78 across 15 at `596fb2d` [gate job 86504916507 run 29137655334], 36/36 at `3bbe93e` [gate job 86500434884 run 29136042436], 33/33 at `9ea90e9`); report leg 212/465 goldens green, replayable 465/465 at `f986177` (main-push run 29152481842 report job 86544105417: "green: 212/465 replayed cases match their golden" + "replayable: 465/465" + "ported: 32/49 subsystems", ai 31/31 + btd6 39/39 + project_moon 10/10 + projectmoon 1/1 [ported] in the per-subsystem table — unchanged from `12b4135`, #165/#177 minted zero new goldens; prior waypoint at `12b4135`: main-push run 29151976612 report job above; local waypoints along wave 8: 209/465 at #176's head `52f7efa`, 211/465 at #178's head `c30ce9e`, 212/465 at #179's head `aaa34bf` — each measured SERIALLY, see the wave-8 record's DB-concurrency note); wave-7 waypoint 208/465 (read from main's own CI report job 86538208853 in run 29150138337 at `f87ffb0`: "green: 208/465 replayed cases match their golden" + "replayable: 465/465" + "ported: 29/49 subsystems", welcome 1/1 + automod 1/1 + security 1/1 [ported] in the per-subsystem table; local waypoints along the wave: 206/465 at #172's head, 207/465 at #173's head, 208/465 at #174's head `a11b90c`; earlier CI-log waypoints: 205/465 at `8b55d95` [report job 86534763801 run 29148787003: "green: 205/465 replayed cases match their golden" + "replayable: 465/465" + "ported: 26/49 subsystems", chain 7/7 + counting 3/3 + logging 7/7 [ported] in the per-subsystem table], 191/465 at `bd211e9` [report job 86525137534 run 29145082777 — moderation 8/8 + ai 31/31 ported, setup 1/8 pending in that table], 182/465 at `0a29d37` [report job 86523808591 run 29144580286 — the #155 re-home moved the 11 aireview goldens ai-side, `_unmapped` 34/220 there], 182/465 at `9053311` [report job 86518931887 run 29142780549], economy 6/6 + ticket 6/6 + karma 8/8 [ported] in that table; the job stays EXPECTED RED, not required; earlier waypoints: 152/465 at `1bea65d` [job 86512108915 run 29140344074], 101/465 at `531f14ee` [job 86510003975 run 29139525710], 87/465 at `596fb2d` [job 86504916495 run 29137655334], 48/465 at `3bbe93e`, 45/465 local at the #138 head, 40/465 at `9923151`, 38/465 at `12886b9`, 30/465 at `7c819b1` run 29128810545, help 3/3 first verified in #112's run 29116678733). Unit suite 1372 passed / 4 skipped in CI at `f986177` (main-push ci run 29152481834 tests job 86544105246 — "1372 passed, 4 skipped"; local canonical ladder at the same sha measured 1374/2 with deps, the standing guarded-import skip delta); earlier: green through all three singleton flips (tests job green on every PR head: 86536598346 [#172], 86537419179 [#173], 86538133125 [#174]; local canonical ladders measured 1356 passed / 2 skipped at each flip head); prior CI waypoint 1354 passed / 4 skipped at `8b55d95` (run 29148787004 job 86534763765 — "1354 passed, 4 skipped"; was 1335/4 at `bd211e9` [run 29145082755 job 86525137618]; was 1334/4 at `0a29d37` [run 29144580270 job 86523808535], 1313/4 at `9053311` [run 29142780551 job 86518931877], 1303/4 at `1bea65d` [run 29140344073 job 86512108932], 1293/4 at `531f14ee` [run 29139525706 job 86510003965], 1288/4 at `596fb2d`, 1283/4 at `3bbe93e`, 1272/4 at `9ea90e9`; local canonical-order ladders run with deps and differ by the guarded-import skip delta — the standing note: #160's local ladder measured 1336/2 at its head `e8bbb66`). Main-push CI at `f986177` green on the required set: ci run 29152481834, named-gates run 29152481839, golden-parity gate job green in run 29152481842 (that run's overall red is only its red-by-design `report` job, not required; earlier `8b55d95`: runs 29148787004/29148786997/29148787003, same shape; prior waypoints `bd211e9`: runs 29145082755/29145082739/29145082777, `0a29d37`: runs 29144580270/29144580269/29144580286 and `9053311`: runs 29142780551/29142780559/29142780549, same shape). Every merge this session — all lanes plus the heartbeats — went in green on the 6-check ruleset (`report` red-by-design, not required), as were #111 and #112 before them
kit: v1.12.1 (#215 2026-07-11, merge `977bb27` — substrate-kit v1.12.0→v1.12.1, `kit_version` 1.12.1 pinned in substrate.config.json:47) · prior v1.10.1 check: green (#166 2026-07-11, card `.sessions/2026-07-11-kit-upgrade-v1.10.1.md`: asset sha256 `fbe83ce3…` three-way verified against tag v1.10.1 @ kit commit `7e361bb` release run 29146372884; v1.10.0 banked byte-identical as `.substrate/backup/bootstrap-1.10.0.py` — exactly one new bank; staged gate regen now carries the v1.10.1 every-card-graded `tail -1` shadowing fix — still STAGED only, no live substrate-gate by design; `.sessions/README.md` model doctrine correctly no-oped again (emphasis-blind presence check now in the engine), byte-identical since the v1.9.0 regen; prior: v1.10.0 #159 merge `72db87b`, v1.9.0 #150 merge `d653ba1`, v1.8.0 #135 merge `3dfc194`) · engaged: yes
last-shipped: parity-flips WAVE-8 lane COMPLETE — **THE PARITY PROGRAM IS COMPLETE AT 50/50**: the last two walls fell in their own squash-merged-on-green PRs (#245 setup `32544d9` · #246 quicksetup `7334083`, main HEAD; lane record "Parity-flips wave-8 lane record 2026-07-12" below; end counts CI-LOG-VERIFIED in the ★ PROGRAM COMPLETE / WAVE-8 END waypoint of the health line — gate GREEN 365/365 across 50 ported, gate job 86634702696 in run 29186950621; card `.sessions/2026-07-12-parity-flips-wave8.md` + telemetry row ride the wrap-up PR per Q-0194). #245 resolved the trap-17 create-channel wall WITHIN EXISTING VOCABULARY (find-branch replay over runner-seeded CAPTURE_WORLD_CHANNELS + the `_unmapped/sweep_setup` create-carrying golden re-homed onto the row — trap 17's "no ruled twin" claim was stale, retired by #242's twin; `setup.open_workspace` takes the D-0065 create-BEFORE-DB shape, NO compensator, allowlist EMPTY; zero depth exemptions; migration 0037_setup_session; check_egress gained `actions` as sanctioned port receiver); #246 flipped the program's 50th and final row via ONE CAPTURE_WORLD_CHANNELS seed reusing #245's machinery (zero exemptions, all-zero ratchet row; codex reviewed for real — comment 4950625506, "Didn't find any major issues"). Remaining parity work is ONLY the `_unmapped` re-home pool (106 files, 101 not yet green; mostly btd6-family + deep-system mining strays + fishing gear surfaces + mixed singletons) toward a fully-green report job — attribution work over ported rows, not porting. PRIOR FOLD (verbatim): band-7 successor lane — channel-ops ENABLER #242 (merge `66249cb`, main HEAD, "feat: channel-ops adapter surface + capture twin + D-0077 compensator ruling" — all 6 named checks green on the FIRST push, `report` red-by-design; card `.sessions/2026-07-12-channel-ops-adapter.md` + telemetry row rode the PR per Q-0194): the D-0030 named successor's ENABLER half — `ChannelStateActions` (sb/domain/channel/service.py) grows `create_text_channel` (overwrite set passed AT creation via the new `ChannelOverwrite` entries, returns the minted channel id) + `delete_channel` (live-adapter contract: Discord NotFound treated as success; name/id guards stay in the calling domain) on the existing fail-loud default; the capture twin (`ParityChannelStateActions`) records the corpus's OWN `create_channel`/`delete_channel` wire verbs (goldens/_unmapped/sweep_setup + sweep_del, fake_http mirrored, created id minted off the SHARED allocator — the golden's `<msg:1>`; zero boot change, the twin was already armed per-case); and `setup.compensate_create_channel` (sb/domain/setup/ops.py, NEW) is the **D-0077**-ruled compensator class for a setup-lane create-channel EFFECT leg that sits AFTER a DB leg — delete-the-created-channel, ID-GUARDED (only the stashed `_created_channel_id`, never a name/pointer guess), BEST-EFFORT (refused delete → operator finding, never a raise), NotFound-as-success via the port contract; oracle contrast ledgered in the D-record for the flip lane: the oracle's setup lane sequences create BEFORE any DB write and never rollback-deletes, so a faithful D-0065 call-first port needs NO compensator. NOTHING FLIPPED, by design: parity stays **48/50** rows ported, gate GREEN **355/355** across 48 (PR golden-parity run 29184272216 job 86627449564 + local ladder before AND after), pending unchanged — setup (8, PARKED trap 17) / quicksetup (1, D-0030's adapter-layer blockers now CLEARED: the flip lane's remaining work is the setup compound op + the trap-17 leaked-workspace disposition + the pending-terminal retirements, D-0077 §4 + the depth finding in the card: the setup/quicksetup SLASH goldens record ZERO channel calls, so the create vocabulary lives only in the `_unmapped` sweeps). PRIOR FOLD (verbatim): parity-flips WAVE-7 lane COMPLETE — the last three REAL pending rows flipped, each its own squash-merged-on-green PR (#233 farm `c3e8ad0` · #236 fishing `c1f56cd` · #240 mining `553ff2b`, main HEAD; lane record "Parity-flips wave-7 lane record 2026-07-12" below; end counts CI-LOG-VERIFIED in the WAVE-7 END waypoint of the health line; card `.sessions/2026-07-12-parity-flips-wave7.md` + telemetry row ride the wrap-up PR per Q-0194). The parity program stands at **48/50** rows ported (47 subsystems + the kernel coverage home); remaining pending is ONLY setup (8, PARKED trap 17) / quicksetup (1, BLOCKED D-0030) / `_unmapped` (107, the re-home pool — the last agent-shaped parity lane). PRIOR FOLD (verbatim): band-7 successor lane — #238 (merge `dc4e9cca`, main HEAD, "band7: btd6 boss-fight estimator — deterministic HP/DPS/cost arithmetic, deviation ledger emptied"; card `.sessions/2026-07-12-btd6-boss-estimator.md` + telemetry row ride the PR per Q-0194): the LAST non-gated #144 parked domain item lands — `sb/domain/btd6/estimator.py` ports oracle `services/btd6_estimator_service.py` @7f7628e1 VERBATIM (re-verified byte-identical at oracle head b0713fcd via ls-remote + raw full-file diff — zero churn this window, trap 24; the `_builders.py` consumer also drift-free): cost/DPS primitives with the `_INSTAKILL_DAMAGE_CAP` 100_000 sentinel exclusion, find_boss, the 60-row RBS track index, estimate with track/escape context, parse_request (tier default 5, map extraction), cheapest_counters `max(1, limit)` clamp, both text formatters; `estimate_card` ports the shipped `build_estimate_embed` query branch and `cmd_estimate`'s BLOCKED stub (oracle_surface.py:137-140) + the oracle_cards.py:28 deviation bullet retire in the same diff — the oracle_cards DEVIATION LEDGER IS NOW EMPTY. Anchor evidence (the #225/#237 method): the oracle's VERBATIM module ran over the committed data tree BEFORE the port; the port re-ran the identical harness — ANCHORS IDENTICAL (harness GLOBAL sha256 `5193d02961e3a2d1…`; cost/DPS census 1600 rows `39970a7c…`; cheapest-counters census all 7 bosses × tiers 1–5 = 35 rankings `81159c1c…` + rank-text `aa0719f6…`), all pinned in 70 new unit tests incl. both census digests. Ingress DECLARED after trap-17/28 checked FIRST: no `_sweep_skips` estimate row; prefix `!btd6 estimate` already declared, bare-usage golden untouched, query path golden-FREE; `sweep_slash_btd6_estimate` pins slash SILENCE — satisfied unchanged (prefix-only tree, the D-0076 posture unbroken). Zero manifest/golden/parity.yml/ratchet/sim-gate/compat movement. Ladder at the pre-merge head `282fc18` (serial, real Postgres — trap 25): units 1715/2, gate GREEN 349/349 across 47 ported, report 355/471 green + 471/471 replayable (red-by-design), depth OK (50 subsystems, 46 ported + kernel), full named-gates fleet + `bootstrap.py check --strict` green; CI all 11 required-class checks green (runs 29182227898/29182227912/29182227930; `report` red-by-design/non-required). @codex question posted on the final head (comment 4950185005: any computation-bearing byte beyond the four import seams the census digests could mask) — NO reply at merge time, merged on green per Q-0258; Q-0120 stands for any later reply. #144 REMAINING MAP after this: EVERY remaining item is GATED — CT-team flow (NK-live), review-channel poster (NL arming), live-NL leg (OWNER-ACTION 5: ANTHROPIC_API_KEY + AI_ENABLED absent), testing-report row 9 (the band's live-testing pass); the non-gated domain ledger is empty. SIBLING FOLDS this window: #237 (merge `691de2ba`, this lane's prerequisite slice, "band7: btd6 per-crosspath combat-stats derivation"; card `.sessions/2026-07-12-btd6-stats-normal-view.md`) — `sb/domain/btd6/tier_codes.py` (oracle verbatim) + the full tower/hero normal-view derivation in stats.py (NormalStats/TowerStats/HeroStats, attack_breakdown, normal_stats), 48 unit tests + the 1600-tier corpus census sha `c8096554…41`, anchors minted from the oracle's verbatim modules over committed data (drift check 7f7628e1↔b0713fcd EMPTY; 9 stats blobs byte-identical), zero golden/manifest movement, units 1654/2 + gate 347/347 at its own head — the raw.githubusercontent.com full-file-at-pinned-sha lane ledgered as the reconstruction method of record; #236 (merge `c1f56cd7`, parity lane, "parity: fishing flip pending→ported") — sweep_fish + sweep_fishlog green byte-for-byte, gate 349/349 across 47 ported; !fish reshaped to the shipped cast-open lane (fishing_energy store/migration 0035, daily sha256 weather verbatim, waiting-for-a-bite session panel via renderer_override), !fishlog the 32-species fishdex embed; capture-day weather seeded via CAPTURE_WORLD_WEATHER (the #163/#167 reseed lane); `table:fishing_catch_log` exempt under the EXISTING time-driven class; ratchet fishing {events: 1, tables: 3, settings: 0}; #217 locking paths untouched — the parity pending map shrinks to mining 2 / setup 8 PARKED (trap 17) / quicksetup 1 BLOCKED (D-0030). PRIOR FOLD (verbatim): #234 (merge `4bf5850`, main HEAD, "band7: !btd6 ops seed-data terminal goes real — audited btd6.seed_data op over the migration-054 btd6_data_blobs store"; card `.sessions/2026-07-12-btd6-seed-data-terminal.md` + telemetry row ride the PR per Q-0194): the LAST #144 parked domain item's bounded core — of the two remaining candidates the lane took seed-data over the boss estimator (estimator is deterministic on committed data, NO NK-live gate, but needs the stats normal-view derivation `sb/domain/btd6/stats.py` parks as a named successor — a multi-service reconstruction, oracle PRs #1574/#1578; parked with the prerequisite NAMED). Shipped: migration 0034 `btd6_data_blobs` (oracle 054 DDL verbatim, NAME_STABLE), `BTD6_DATA_BLOBS_STORE` + upsert/count/get accessors, the `btd6.seed_data` op (ONE DB leg — the shipped seed_postgres_from_files loop verbatim incl. the manifest.json skip + canonical-dump sha256; count on the ctx.params side-channel; authority "administrator", the shipped ADMIN_DENIED gate on the K6 floor; NO effect leg — compensator allowlist stays EMPTY), `cmd_ops_seed` real (drift-before-seed ordering, orange zero-files arm, post-commit cache-drop reload, green receipt at oracle bytes incl. the #1263 changed-report and "Safe to re-run any time (it upserts)"), the service.py PENDING-TERMINALS ledger sentence retired same-diff (retirement loop, 6th application), ONE depth exemption `table:btd6_data_blobs` under the EXISTING `dataset-scale` class (its FIRST exemptions use — the `_sweep_skips.json` reason IS the class definition verbatim, zero vocabulary growth, no new decision record). Oracle reconstructed at heads 1ecc2113→b0713fcd (trap 24 ledgered; corpus pin stays 7f7628e1 — seed-data predates it, shipped since oracle #676). Both #218-routed alias forms (`!btd6 ops seed-data` + `!btd6ops seed-data`) now serve the real terminal — the #218 codex exchange's open half CLOSED. Evidence: 14 unit pins + a real-Postgres integration leg through the REAL K7 engine (K6 deny writes NOTHING; all 74 committed blobs land, sha256/body round-trip verified; idempotent re-run; 2 `btd6_data_seeded` audit rows). Ladder at the merged head `e489687` (post forward-merge of #233): units 1606/2, gate GREEN 347/347 across 46 ported, report 352/471 unchanged (zero goldens minted by design), `--write-ratchet` byte-stable (btd6 stays {events: 1, tables: 4, settings: 1}), full checker fleet + strict check green; CI on the PR head all-required-green (runs 29180208491/29180208493/29180208494; `report` red-by-design, non-required); codex review 4949999713 on the ACTUAL final head `e489687` — zero findings, and its companion reply 4949995491 for once claimed NO phantom commit/PR ("No commit or PR was created"), Q-0120-consistent. The codex question (comment 4949993588, the one deliberate deviation: all-or-nothing txn seed vs the oracle's per-blob autocommit) drew no substantive answer — carried as the lane's open sequencing note for the D-0046 postgres-serving successor. SIBLING FOLDS this window: #233 (merge `c3e8ad0`, parity lane, "parity: farm flip pending→ported") — farm joined the gate inside the #234 window (45 subsystem rows + kernel ported; the pending map shrinks to fishing 2 / mining 2 / setup 8 PARKED / quicksetup 1 BLOCKED + `_unmapped` 111); #232 (merge `9b9a322`, this lane's prior slice, "band7: CommandSpec modal facet — slash-opens-modal ingress as grammar; btd6 declaration ruled golden-blocked (D-0076)") — the slash-opens-modal ingress landed as CommandSpec grammar with the btd6 declaration itself ruled GOLDEN-BLOCKED (trap 17: the 30 empty sweep_slash_btd6_* goldens pin the drop silence; D-0076 is the decision record), gate 346/346 across 45 at its merge. PRIOR FOLD (verbatim): parity-flips WAVE-6 lane COMPLETE — five games-family PRs merged green (#219 inventory `d3dba9b` · #224 casino `cc59a6e` · #226 creature `f032e8a` · #227 four_twenty BIRTH `39bf226` · #230 games `8730200`; lane record "Parity-flips wave-6 lane record 2026-07-12" below; end counts CI-LOG-VERIFIED in the WAVE-6 END waypoint of the health line; card `.sessions/2026-07-12-parity-flips-wave6.md` + telemetry row ride the wrap-up PR per Q-0194). PRIOR FOLD (verbatim): #228 (merge `01d49688`, "band7: kernel-band golden set — kernel coverage home flips pending→ported (D-0075)") — the D-0073 named 💡 follow-up executed (slice record "Kernel-band goldens slice 2026-07-12" below): four kernel-band goldens minted by the D-0073 capture_case procedure with the strip INVERTED (parity/goldens/kernel/ — audited modal op, audited prefix dispatch, pre-ack tier deny with NO dispatch trace, slash trace over the un-audited ensure lane) now pin the NEW kernel's spine contract (audit_log / event_outbox / command.dispatched bytes) as required-green regression tripwires; apply_dispositions skips the kernel-surface-drift drop for `subsystem: kernel` docs (band-scoped, symmetric — the ORDER-009 ruled classes NOT re-encoded); check_parity_depth grows kernel R1–R4 (R2 over the kernel section's own lists — the flag-13 "coverage obligation lives in the kernel section" sentence now enforceable) and run_golden_parity gates goldens/kernel/ when ported; 7 never-writable kernel tables exempted under EXISTING reason classes (zero vocabulary growth); corpus 471 = 465 + 6 minted; kernel ratchet {events: 4, tables: 5, settings: 0}; @codex question posted on the final head `dfdb547` (dir-vs-field kernel-band keying), merged on green without waiting per Q-0258. PRIOR FOLD (verbatim): #223 (this PR, "fix: tournament-entry double-debit race — advisory slot lock + existence check before the fee debit") — the #221 KNOWN_RISKS row FIXED and CLEARED (slice record "Tournament-entry race fix slice 2026-07-12" below): enter_tournament_in_txn now fences with lock_new_checkpoint_slot + existence check BEFORE the debit (the #213 solo_start precedent), duplicate refused with the oracle copy "You're already registered!" having debited nothing; real-Postgres red-then-green repro in tests/integration/test_tournament_entry_race.py; KNOWN_RISKS now EMPTY, checker green with zero new ledger rows. PRIOR FOLD (verbatim): #221 ("lint: money-race guard — static F-001/F-002 class checker, CI-wired, red-then-green pinned") — the twice-evidenced `check_natural_key_fencing` session idea BUILT (slice record "Money-race lint slice 2026-07-12" below): tools/check_money_race.py + red/green unit pins + ci.yml checker-fleet slot + the architecture named-gate step; baseline 4 findings all dispositioned — 2 allowlisted verified-safe (wager.debit_floor_in_txn, wager.escrow_pvp_in_txn) + 1 KNOWN-RISK suspected REAL live bug (sb/domain/games/wager.py::enter_tournament_in_txn same-user concurrent-entry double-debit, ledgered NOT fixed and NOT called safe — see the slice record + #221 PR body). PRIOR FOLD (verbatim): #217 (merge `ed8eed34`, "fix: farm/mining money races — locking reads on every credit-bearing leg (F-001/F-002 class)") — the #213 defect-class sweep completed for farm/mining (slice record below: "Farm/mining money-race slice 2026-07-12"); the parked codex risk reviews #196/#206 are now SUPERSEDED-AND-CLOSED (comments 4949239568 / 4949239857 citing #213 `f71d60b` + #217 `ed8eed34`) and the `codex-risk-review-prs-196-206` claim file is DELETED per the claims convention (rule 4 — this PR). PRIOR FOLD (verbatim): #215 (merge `977bb27`, "kit: upgrade substrate-kit v1.12.0 → v1.12.1") — preceded this round by #214 (merge `827b134`, treasury pending→ported, parity 39/50, gate 266/266) and #213 (merge `f71d60b`, F-001/F-002 wallet-race + F-003 parity-gate false-green fixes); full round record below ("Kernel-band goldens slice 2026-07-12 (band-7 successor lane, #228, merge `01d49688`, decision record D-0075, card `.sessions/2026-07-12-kernel-band-goldens.md`):
- The gap: the D-0073 mint strip + the flag-13 kernel-surface-drift disposition together made kernel-only regressions (changed audit verb, lost event_outbox row, lost command.dispatched event) invisible to EVERY gate; the #194 codex P2 had flagged it and was declined-with-citation naming this slice as the successor.
- The counter: 4 goldens in parity/goldens/kernel/ (`subsystem: kernel`) minted by capture_case with the strip KEPT — kernel.audited_modal_submit (btd6.submit_strategy audit row + outbox row + modal trace), kernel.audited_prefix_command (!createrole admin — xp audit pair + role BUS audit + prefix trace), kernel.denied_prefix_command (member deny copy, NO trace — resolve.py _deny pre-ack contract pinned), kernel.slash_dispatch_trace (/economy — slash trace, spine silent on the un-audited ensure). Double-captured across two harness boots, byte-identical; tripwire semantics proven in-session (each drift class = exactly one red line on the kernel band, disposition-inert on domain bands).
- Machinery: apply_dispositions kernel-band skip (sb/adapters/parity/dispositions.py); check_parity_depth kernel pseudo-subsystem R1–R4 + --write-ratchet kernel row; run_golden_parity _statuses_with_kernel (goldens/kernel/ required-green when kernel.status ported, denominator-checked). FRAMING ledgered in D-0075: kernel-band goldens pin OUR kernel's contract (no oracle exists for the spine) — deliberate contract changes re-mint with the diff explained (parity/README.md integrity rule), accidental ones go red.
- Depth: depth.exemptions.kernel — idempotency_keys (ops-not-behavior: NO domain op rides DURABLE_ONCE this epoch, verified), sb_due_queue/sb_quarantine/sb_invariant_sweep_log (time-driven), sb_drafts/sb_credential_rotation (ops-not-behavior), ai_decision_audit (env-keyed-integration, OWNER-ACTION 5 key gate). Zero new reason classes, zero manifest/sim-gate/compat churn.
- Codex: question posted on final head `dfdb547` (comment 4949719408 — can a doc whose subsystem field and directory disagree weaken either band?); merged on green without waiting (Q-0258); any returned findings get Q-0120 verification next wake.
- Next named steps for this lane (updated at the 2026-07-12T05:05Z heartbeat — seed-data DONE via #234, CommandSpec modal facet DONE via #232/D-0076): boss estimator (the last non-gated #144 domain item; prerequisite NAMED — the stats normal-view derivation slice per sb/domain/btd6/stats.py's docstring, then the estimator service rides it), CT-team guided-set flow (NK-live-gated — skip until live NK data arms), the D-0046 postgres-serving provider for btd6_data_blobs (BTD6_DATA_BACKEND=postgres + PostgresRawProvider + non-None content_drift + auto-seed-on-boot Q-0077(b); carries the #234 open sequencing note — txn-atomic seed vs the oracle's per-blob autocommit), review-channel poster (parks with NL arming), testing-report row 9 (pending the band's live-testing pass), live-NL leg (OWNER-ACTION 5, unchanged — ANTHROPIC_API_KEY + AI_ENABLED absent).

Coordinator round 2026-07-11T23:00Z"). PRIOR FOLD (verbatim): parity-flips WAVE-5 lane WRAP-UP (OWNER WRAP-UP DIRECTIVE, archive-prep) — the wave-5 lane closed COMPLETE at this fold: 7 PRs verified merged against `git log origin/main` at `323e304` (#197 `2d65739` · #200 `870a16c` · #201 `99962a6` heartbeat · #202 `2f4b2c3` · #203 `ab1e916` · #207 `2c62a09` · #211 `a283f3f`), no open PRs and no unmerged `wave5/*` branches (the seven `origin/wave5/*` refs are the merged PRs' squashed source branches; repo branch auto-delete is off), no control/claims/ files from this lane; the enders ride THIS wrap-up PR — the session card `.sessions/2026-07-11-parity-flips-wave5.md` flipped to the full-wave record (its telemetry row shipped with #197 per Q-0194, one row per card, NOT re-added), retro lessons `docs/retro/lessons-2026-07-11-parity-flips-wave5.md` (trap-30 closure mechanics, the #193 flip-sized re-home law held 25-for-25, the RoleSelect type-6 seam, the verified_live.yml birth-kit step, worker-stall/resume cadence) linked from docs/retro/README.md — after #211 (merge `a283f3f`, main HEAD before the #210 closeout): starboard subsystem BORN ported (6 goldens re-homed onto the NEW row, migration 0033, the full birth kit + the `verification/verified_live.yml` roster row the V4 gate demands; ONE new depth exemption row `table:starboard_settings` under the EXISTING guard-only-capture class — zero new classes all wave); wave-5 END counts in the health line (gate 264/264, report 301/467, `_unmapped` 176, all CI-LOG-VERIFIED at `a283f3f`); the lane record below carries the successor map + the `_unmapped` family histogram. Earlier: band-7 lane WRAP-UP (OWNER WRAP-UP DIRECTIVE, archive-prep) — the continuation session closed COMPLETE at this fold: 8 PRs verified merged via the API (#185 `168ef808` · #187 `d9c6b35a` · #192 `14e5037` · #194 `4024624d` · #199 `029b4001` · #204 `f82ac804` · #205 `81b04bc` · #208 `0e7cacd0`), no open PRs / stray branches / claim files from this lane; the enders ride THIS wrap-up PR — session card `.sessions/2026-07-11-band7-continuation-session.md` + its telemetry row (Q-0194), retro lessons `docs/retro/lessons-2026-07-11-band7-continuation.md`, the OWNER-ACTION 3 ruleset wall appended to docs/CAPABILITIES.md (the #199-surfaced `owner-ask-wall-unrecorded` advisory CLOSED; `bootstrap.py check --strict` green, zero advisories), the stale ux-lab housekeeping flag groomed — after #208 (merge `0e7cacd0`, main HEAD): btd6 resolver maps/modes matching — the first #144 parked DOMAIN item retires its oracle_cards ledger line (typed MapEntry/ModeEntry dataset accessors, the shipped `_match_terms` discipline incl. the common-word quirk carried never "improved", deterministic_answer's shipped towers→heroes→maps→modes→rounds→bloons precedence, test-intent Maps/Modes rows live with the golden-pinned empty state byte-identical); ZERO manifest/golden/ratchet/compat movement; codex zero-findings review at the final head — final counts in the health line. Earlier: band-7 lane continuation (Q-0265 continuous mode) — #204 (merge `f82ac804`; decision record D-0074): the behavior ROUTING-MATRIX picker goes live — oracle views/ai/routing/matrix.py ported whole (build_routing_matrix_embed over a verbatim resolve_policy dry-run, the behavior_matrix_picker panel, the chooser button live), the LAST chooser pending terminal retired (chooser_scope_pending/_SCOPE_COPY deleted — every views/ai/* chooser surface is now live); ZERO schema growth; codex ZERO findings — after #199 (merge `029b4001`, hygiene slice): tools/check_parity_depth.py `--write-ratchet` now splices ONLY the depth.ratchet block (parity.yml header/comments preserved byte-for-byte, missing-key = loud SystemExit, 9 unit pins; 3 codex P2s — interstitial comment, blank+sibling comment, CRLF — all verified real and fixed in-PR; tests/unit/ai consolidated to one dir-wide after-only conftest reset, orchestration keeping a slim documented pre-leg — pure after-only empirically regresses #156, 3 failures reproduced) and #194 (merge `4024624d`; decision record D-0073): the D-0066-successor modal vocabulary — the replay corpus grew the `modal` step kind + the first 2 MINTED goldens (btd6_strategy_form_submit{,_minimal}, corpus 465→467), the btd6.strategy_form modal + submit handler on the audited btd6.submit_strategy op, PanelActionSpec.reply_visibility grammar growth, the D-0063 exemption row DELETED (btd6 ratchet tables 3→4), #187's seeded-catalog 0029→0030 typo fixed; codex P2 (kernel-band golden gap) verified real but DECLINED with doctrine citation (flag-13/A-16-clause-3 — the kernel-band mint is the named follow-up); ledgered pre-existing latent bug: sb/domain/btd6/service.py::_run_op reads result.ok / flat result.after (unreachable via golden-driven lanes; guard recipe in the session card) — post-#204 counts in the health line. Earlier: parity-flips lane wave 5 — #200 (merge `870a16c`): ticket-admin `_unmapped` re-home, 6/6 goldens green onto the PORTED ticket row (ticket row now 12/12) — new ticket_blacklist + ticket_config stores (migration 0032), K7 ops `ticket.blacklist_add`/`ticket.set_limit`, new ticket.launcher + ticket.setup panels, and a NEW kernel seam: options-source-less SelectorKind.ROLE renders as the native RoleSelect (wire type 6) in BOTH presenters (provider-fed ROLE selectors keep type-3); zero new depth exemptions, zero compensators (allowlist stays EMPTY), no `_unmapped`/sweep_ticket* citations existed to retire — after #197 (merge `2d65739`): trap-30 checker hardening (check_sim_gate now reds overlay-masks-manifest VALUE drift; `--write-baseline` refuses on drift; 42 pre-existing masked drifts amended to manifest truth), the wave-5 session card + telemetry row rode it (Q-0194); wave-5 record below, gate 245/245 CI-log-verified at the #200 head (health line above). Earlier: parity re-home lane — #193 (merge `501e73f`): the role-family (16 goldens) + counting-family (3 goldens) `_unmapped` re-home, the wave-10 ledgered follow-up TAKEN (re-home lane record below) — gate 218/218→237/237 CI-log-verified, report 256/465→276/465, `_unmapped` 220→201, TWO role covered-elsewhere depth exemptions RETIRED, two band-5 miss-branch inventions retired ORACLE-WINS, zero new reason classes, zero decision records. Earlier: band-7 lane continuation (Q-0265 continuous mode) — #187 orchestration-mutation slice (merge `d9c6b35`; decision record D-0072, migration 0031 — the #177-parked tools PROFILE PICKERS live + K10 resolver consumption of the orchestration overlay; 3 codex line findings fixed in-PR, 1 declined with oracle citation) after #185 behavior-preset slice (merge `168ef80`; decision record D-0071, NEW `seeded-catalog` depth reason class, migration 0030) — both squash-merged on green, zero new goldens, counts at the POST-#187 waypoint in the health line; the D-0066 slice has since LANDED as #194 (lead entry above; continuation record below). Earlier: parity lane wave 10 — the lane's LAST FOUR NON-GAME ROWS flipped, each its own merged-on-green PR: #186 (admin pending→ported, merge `2f946f8`, 2/2 goldens — the shipped ⚙️ Server & Admin `_AdminPanelView` as a session-lifecycle hub with the invoker-lock footer override), #188 (community pending→ported, merge `94dafd5`, 2/2 goldens — the shipped 🌱 Community Hub, roster pinned, persistent `community:open:*` ids through the session mint, grammar nav:help), #189 (community_spotlight pending→ported, merge `ff2cf09`, 1/1 goldens — the shipped SpotlightView embed: guild-named title, inline field flags, the `%H:%M UTC` clock footer over the shared Normalizer), #190 (role pending→ported, merge `fd62f53`, main HEAD, 1/1 goldens — the RoleHubPanelView reshape at anchored semantics + 8 depth exemptions ALL under existing classes); wave-10 record below. THE LANE'S NON-GAME MAP IS COMPLETE except setup (PARKED, trap 17) / quicksetup (BLOCKED, D-0030) / `_unmapped` re-homes; the games-family rows (casino 2, creature 5, farm 1, fishing 2, four_twenty 1, games 2, inventory 1, mining 2, treasury 2) belong to the band-6 games lane. Earlier: parity lane wave 9 — #183 (diagnostic pending→ported, merge `10bf073`, 37/37 goldens — the LARGEST single flip: the shipped !diagnostics hub, !latency, the 🛰 !platform//platform hub with verbatim platform_hub.*/flag_manager:* persistent-id pins, 28 pinned capture-snapshot operator cards, the flag/automation panels, and the platform_migration_checkpoints backfill dry-run WRITE lane [migration 0029]; ZERO depth exemptions, ZERO new reason classes, ZERO decision records; wave-9 record below); next parity targets: the games-family tail (treasury 2, four_twenty 1, admin 2, casino 2, community 2, community_spotlight 1, creature 5, farm 1, fishing 2, games 2, inventory 1, mining 2, role 1). Earlier: band-7 builder lane SESSION WRAP — #177 (ai policy scope pickers, merge `f986177`, main HEAD, band-7 slice 7, D-0070, codex triage 2-taken/1-declined) after #165 (the live modal-submit lane arms + the ai free-text/Override editors, merge `62d850f`, band-7 slice 6, D-0066); session card `.sessions/2026-07-11-band7-builder-session.md` + its telemetry row ride THIS wrap-up PR (Q-0194). Earlier: parity lane wave 8 — #179 (server_management pending→ported, merge `12b4135`, 1/1 goldens — the #132 slash flip's prefix sibling at anchored-panel semantics; slash sibling regression-proved green) after #178 (counters pending→ported, merge `519d909`, 2/2 goldens incl. the `/counters` ephemeral slash twin + the type-4 empty-components capture-twin fix) and #176 (image_moderation pending→ported, merge `d034e30`, 1/1 goldens, first-replay green), ZERO depth exemptions and ZERO new reason classes across all three (wave-8 record below); next parity target: diagnostic (37 goldens). Earlier: parity lane wave 7 R2-singleton run — #174 (security pending→ported, merge `f87ffb0`, 1/1 goldens) after #173 (automod pending→ported, merge `b61e954`, 1/1 goldens) and #172 (welcome pending→ported, merge `dc32367`, 1/1 goldens), each first-replay green with ZERO depth exemptions (wave-7 record below); the ORDER-013 self-review + heartbeat landed as #171 (merge `569b967`). Earlier same wave: #170 (chain pending→ported, merge `8b55d95`, 7/7 goldens) after #168 (counting pending→ported, merge `1730026`, 3/3 goldens) and #167 (logging pending→ported, merge `6a0921c`, 7/7 goldens; wave-7 record below), with sibling merges #165 (band-7 modal-submit lane, merge `62d850f`, D-0066), #166 (kit v1.10.1, merge `5c064dd`) and #169 (fm ORDER-013 relay, merge `48ad4a6`) in the same window. Earlier: parity lane wave 6 — #163 (moderation pending→ported, merge `bd211e9`, 8/8 goldens; wave-6 record below) after band-7 heartbeat #162 (merge `dd626b2`), band-7 lane #160 (ai policy/settings-mutation surface, merge `0a29d37`; lane record below), #161 (codex P2 triage fix, merge `8ad243f`), kit lane #159 (substrate-kit v1.9.0→v1.10.0, merge `72db87b`) and parity-lane heartbeat #158 (merge `b1cda9c`). Earlier same session: parity lane wave 5 — #157 (karma pending→ported, merge `9053311`, 8/8 goldens) after #152 (economy flip, merge `b2ec7d9`, 6/6 goldens) + #154 (ticket flip, merge `5b750c8`, 6/6 goldens; wave-5 records below); band-7 lane #155 (aireview family re-homed `_unmapped`→ai, merge `4c8c5b0`, 11/11 goldens) + #156 (k10 test-order-flake fix, merge `179dfb2`) landed between them, plus band-7 heartbeat #153 (merge `119ecce`). Earlier same session: band-7 lane #141 (test-order-flake fix, merge `78509df`) + #144 (btd6 flip, merge `596fb2d`, 39/39 goldens) + #148 (project_moon+projectmoon flip, merge `f2dcf0d`, 11/11 goldens) + #151 (ai flip, merge `1bea65d`, 20/20 goldens); kit lane #150 (substrate-kit v1.8.0→v1.9.0, merge `d653ba1`); parity lane wave 4 — #143 (xp flip, merge `f97a4c7`, 3/3 goldens) + #145 (proof_channel flip, merge `531f14ee`, 3/3 goldens); heartbeats #142 (merge `aef3135`) + #147 (merge `9f83ea1` — carried D-0063) + #149 (merge `dccac62` — parity wave 4) and fm relay #146 (merge `928e212`). Earlier same session: band-6 lane SESSION COMPLETE #114/#117/#120/#124/#130/#133/#138 (class-killer, rps_tournament port, blackjack port, PvP, rps tournament + reaction seam, blackjack tournament + settle-once, rpssettings + render-once; latest merge `9ea90e9`; session card `.sessions/2026-07-11-band6-games-session.md` + telemetry row ride THIS heartbeat PR — Q-0194); parity lane #118/#119/#121/#123/#125 + wave 2 #128/#129/#131/#132 + post-wrap wave 3 #137 (settings flip, merge `77ae8b4`, card `.sessions/2026-07-11-settings-parity-flip.md`) + #140 (cleanup flip, merge `3bbe93e`, card `.sessions/2026-07-11-cleanup-parity-flip.md`); kit lane #116/#122/#135 (v1.8.0; latest merge `3dfc194`). Earlier: #111 (merge `569beea`) + #112 (merge `c0f0783`; session card `.sessions/2026-07-10-help-parity-flip.md`)
blockers: ORDER 002 done-when still hangs ONLY on the owner-created separate repo (OWNER-ACTION 2 below). Codex capacity is FLAPPING, not hard-walled: after the late-night usage-limit replies on #148 (comment 4942100526) and #151 (comment 4942514698) and one more on #152 (comment 4942577962, 05:08Z), the connector returned FULL P2 reviews on #154 (review 4676723779, 05:54Z), #157 (review 4676836079, 06:31Z) and #160 (review 4676953047, 07:10Z — 1 real P2 fixed in-PR, 1 P3 taken; but its "committed changes" reply claimed a PHANTOM commit/PR, documented in the #160 lane record) — OWNER-ACTION 4 stays retired, flap evidence in the item 4 record below; questions still posted on every substantive PR per the standing Q-0259/Q-0258 rule, and every returned finding gets Q-0120 source verification before action (the #160 phantom is the newest evidence for why). Wave-8 codex addendum (12:20Z heartbeat): the pool is UP this window — real source-cited replies on #176 (comment 4945218656, verdict adopted-as-ledger) and #178, but the #178 reply claimed a PHANTOM commit `10e8153` + follow-up PR (THIRD phantom instance after #144/#160; verified nonexistent, documented at comment 4945355808 — Q-0120 guard held, its non-phantom suggestion evaluated and declined on source). Band-7 continuation codex addendum (15:45Z heartbeat): the pool was UP for #187 — 3 real line findings returned and fixed in-PR, 1 declined with oracle citation — then codex hit its usage limit tonight; future reviews may not arrive until the pool refills (the flap record stands; standing Q-0259/Q-0258 rule unchanged — questions keep landing, merge on green without waiting). Post-#204 codex addendum (18:51Z heartbeat): the mid-evening usage-limit hit RECOVERED within the window — line-anchored findings stayed REAL throughout (3/4 fixed in-PR at #187, 1 verified-then-declined with doctrine citation at #194, 3/3 verified-and-fixed at #199, ZERO findings at #204); top-level claims (commits/PRs codex says it made) remain phantom-prone — Q-0120 verification stays mandatory before acting. Band-7 live-NL leg owner-key-gated (OWNER-ACTION 5 below); no band blockers otherwise
orders: acked=001,002,003,004,005,006,007,008,009,010,011,012,013 done=003,005,006,007,008,009,010,011,012,013 — ORDER 013 (P1 owner-directed fleet self-review, filed by fm via #169 `48ad4a6`) ACKED and DONE at this wake, within the two-wake window: the dated "Self-review 2026-07-11" section below carries (1) went-wrong items each with a PR/run/commit citation, (2) the owner-attention mirror (⚑ items 2/3/5 + the codex flap/phantom FYI), (3) the one-line health — ORDER 012 (P3 fleet standing rule, model-attribution ground truth) ACKED at the 06:35Z heartbeat and its done-when now MET, re-verified item-by-item at THIS heartbeat (`0a29d37`): (1) the session-card template half was already DONE by the kit lane (already-done-by-other-lane, nothing added here) — the template lives in the kit's own draft/enforce path, verified at HEAD: bootstrap.py v1.10.0 `_default_session_markers` (line ~244) requires the `📊 Model:` needle on every card, `_marker_line`/`draft_card` auto-draft it (`- **📊 Model:** [[fill:model]] · …`), the strict session-log guard blocks a card without it (guard-fire evidence in `.substrate/guard-fires.jsonl`), and `.sessions/README.md` carries the family-level-names-only doctrine (planted at the v1.9.0 regeneration #150, byte-identical through v1.10.0); (2) fired sessions' committed cards carry real family-level lines (`📊 Model: Claude Fable 5` in the wave-5 flip cards, `.sessions/2026-07-11-kit-upgrade-v1.10.0.md` and `.sessions/2026-07-11-codex-p2-triage.md`, verified at HEAD; this heartbeat's own card too); (3) n/a — the standing rule binds every future fired session's card — ORDER 004: items 1 (#80), 2 (#112) and 4 (#83) are DONE, and band-6 now demonstrably carries BOTH bindings (item 3: walking-skeleton CI tests in tests/unit/band6/ ride every band-6 port PR #117/#120/#124/#130/#133/#138 + classify-or-fix in each testing-report); still NOT marking done=004 — its done-when reads "bands 3-9 orders carry the two binding steps" and item 5's demo rule binds every future demo; bands 7-9 haven't run yet
⚑ needs-owner: FOUR live OWNER-ACTION items below (six-field format per control/README.md) — items 2 and 3 unchanged; item 5 (band-7 AI key envelope — the live-NL leg is blocked on it); item 6 NEW at close-out: fleet failsafes may still be dead from the 2026-07-11T16:31Z platform env-teardown (this lane self-healed; sibling lanes could not be — record in docs/retro/q0265-routine-loop-2026-07-11.md); item 4 stays RETIRED, its capacity half now FLAPPING rather than walled (usage-limit replies on #148/#151/#152 but full P2 reviews on #154/#157/#160 the same morning; flap update in the item 4 record). The ORDER-013 "Self-review 2026-07-11" section below MIRRORS all three live items click-level plus one FYI (codex phantom-artifact claims, Q-0120 guard holds) for the manager sweep

Coordinator round 2026-07-11T23:00Z (post-#213/#214/#215 heartbeat) — status.md brought current at HEAD `977bb27b` (it was last written by #212 `c3037d5` at 20:20Z and predated all three merges below); every claim cites its squash commit:
- #213 (merge `f71d60b`, "fix: F-001/F-002 wallet-race + F-003 parity-gate false-green"): F-001/F-002 wallet double-settle FIXED — FOR UPDATE checkpoint loads + `lock_new_checkpoint_slot` in sb/domain/games/store.py, blackjack/rps caller fixes, and the session_gc credit-after-delete fix; F-003 parity-gate false-green FIXED — run_gate now asserts replayed-count == golden-count per ported row, with integration tests added to the gate job.
- #214 (merge `827b134`, "parity: treasury flip pending→ported"): treasury pending→ported — parity now 39 ported / 11 pending / 50 subsystem rows in parity/parity.yml; gate 266/266 goldens across the 39 ported subsystems (per the #214 merge body).
- #215 (merge `977bb27`, "kit: upgrade substrate-kit v1.12.0 → v1.12.1"): `kit_version` 1.12.1 pinned in substrate.config.json:47; that commit body itself flagged the heartbeat bump as lane-owed — settled by this round.
- Owner-directed MONEY-RACE priority this round: the remaining same-class sites in sb/domain/farm and sb/domain/mining (named in control/claims/codex-risk-review-prs-196-206.md) VERIFIED UNFIXED at `977bb27b` (grep for FOR UPDATE / with_for_update in both dirs returns no matches) — a "Farm/mining money-race fix slice" child session was dispatched for them; the band-7 successor lane was nudged to resume.


Parity-flips wave-8 lane record 2026-07-12 (setup + quicksetup — THE LAST TWO WALLS, session COMPLETE at this wrap-up fold — **THE PARITY PROGRAM IS COMPLETE AT 50/50** — card `.sessions/2026-07-12-parity-flips-wave8.md` + telemetry row ride the wrap-up PR per Q-0194; every count re-verified at HEAD `7334083` per Q-0120, not replayed from mid-wave memory; both flips ride the #242/D-0077 channel-ops enabler):
- #245 (merge `32544d97eda69f29637170f7e81ff143fa7bc840`) — setup pending→ported, 9/9 goldens (the 8 setup-dir goldens + `_unmapped/sweep_setup` re-homed onto the row as its create-carrying golden). TRAP-17 WALL RESOLVED within existing vocabulary: the slash goldens' zero-channel-call shape replays via find-branch over runner-seeded CAPTURE_WORLD_CHANNELS (sb/adapters/parity/runner.py:137, per-case reseed at :267 — the sweep_xpimport/#203/#207 leaked-state precedent generalized to gateway-cache channel state); the create branch is exercised by the re-homed sweep_setup — trap 17's "no ruled twin" claim was STALE, retired by #242's ParityChannelStateActions twin + this seam. `setup.open_workspace` takes the D-0065 create-BEFORE-DB shape (oracle-faithful: create precedes any DB write, never rollback-deletes) — NO compensator declared, allowlist stays EMPTY (tests/unit/workflow/test_compensator_invariant.py `_ALLOWLIST = {}` at HEAD); the D-0077 `setup.compensate_create_channel` handler stays reserved for the DB-first shape. ZERO depth exemptions; migration 0037_setup_session.sql; ratchet setup {events: 2, tables: 3, settings: 0}; tools/check_egress.py grew `actions` as a sanctioned port receiver (the `actions = active_actions()` binding convention; the S11 fence still bans create_text_channel by name outside adapters). Depth notes: /setup-depth green-by-absence; /setup-delegate + /setup-undelegate capture-skipped ("unsupported required option type user"). Gate at its merge: run 29186558036 gate job 86633632162 — "gate: GREEN — all 364 golden(s) across 49 ported subsystem(s) replay clean" + pending _unmapped [106] / quicksetup [1] + "check_parity_depth: OK — 50 subsystems (48 ported), kernel ported, 471 goldens"; integration 11 passed same job.
- #246 (merge `73340834a77b0098352a20e5e9fa489070e3f14f`, main HEAD) — quicksetup pending→ported, 1/1 goldens (sweep_slash_setup), THE PROGRAM'S 50TH AND FINAL ROW: one CAPTURE_WORLD_CHANNELS seed (`"sweep.slash_setup"`, reusing #245's leaked-workspace machinery) is the flip's only code change; zero exemptions; ratchet quicksetup {events: 0, tables: 0, settings: 0} — the all-zero row. Codex reviewed FOR REAL (comment 4950625506 on the actual final head `015569d712`: "Didn't find any major issues"; the cross-case-ordering question posted as comment 4950621154; no phantom artifacts claimed, Q-0120-consistent).
- END COUNTS (CI-LOG-VERIFIED at `7334083`, the ★ PROGRAM COMPLETE / WAVE-8 END waypoint in the health line carries the full quote set): parity 50/50 program rows ported (49 subsystem rows + kernel home; parity.yml machine-counted at HEAD — only `_unmapped: pending` remains); gate GREEN 365/365 across 50 (run 29186950621 gate job 86634702696); report 370/471 green + 471/471 replayable (report job 86634702704 — every ported row green, `_unmapped 5/106` the only non-green line; red-by-design); units 1722/8 in CI (run 29186950619 tests job 86634702742); corpus 471 = 465 + 6 minted (zero mints this wave); `_unmapped` 107→106 (sweep_setup → setup at #245).
- REMAINING map (truth at HEAD): ONE POOL ONLY — the `_unmapped` re-homes, 106 files / 101 not yet green (5 alias green through ported rows), mostly btd6-family sweeps over the PORTED btd6 row + deep-system mining strays (skill/stash/vault/workshop `_pending` families) + fishing gear surfaces (rodrecipes/sail/tidepool) + mixed singletons — attribution work toward a fully-green report job, NOT porting. No subsystem row is pending; no wall remains; both trap-17 and D-0030 are closed in the tree.
- NEW DOCTRINE (playbook trap 37): re-evaluate old park claims against the CURRENT tree before honoring them — trap 17's wall justification was recorded true in wave 6 and falsified by #242 before wave 8 picked it up.
- LANE COMPLETE: no open PRs, no unmerged wave8/* work beyond this wrap-up, no control/claims/ files from this lane; the parity program's port phase is CLOSED — successor lane = the `_unmapped` re-home wave (sequencing: btd6-family re-homes only when the band-7 successor lane's btd6 slices are idle).

Parity-flips wave-7 lane record 2026-07-12 (the money-race-family flips farm/fishing/mining — the LAST THREE REAL PENDING ROWS, session COMPLETE at this wrap-up fold — card `.sessions/2026-07-12-parity-flips-wave7.md` + telemetry row ride the wrap-up PR per Q-0194; every count re-verified at HEAD `553ff2b` per Q-0120, not replayed from mid-wave memory — the band-7 successor lane's #232/#234/#235/#237/#238/#239 landed interleaved):
- #233 (merge `c3e8ad0a5d71f3d86cd75810edd1f67bab78091f`) — farm pending→ported, 1/1 goldens (sweep_farm): farm.hub reshaped to the shipped FarmMenuView (Collect/Shop/Refresh, purple embed, live fields + balance footer via renderer_override, session_lifecycle) + the FarmShopView sub-panel (sb/domain/farm/panels.py). ONE depth exemption table:chicken_farm under the EXISTING time-driven class. #217 locking untouched BY CONSTRUCTION — render-only diff; farm store.py/ops.py not in the diff at all. Ledgered in-PR: `pytest tests/unit tests/integration` in ONE invocation fails 4 money-race cases on PRISTINE main (pre-existing cross-suite pollution — run the suites separately; follow-up candidate, not fixed this wave).
- #236 (merge `c1f56cd769ef3348b52b581d32c2e689734ffdfe`) — fishing pending→ported, 2/2 goldens (sweep_fish + sweep_fishlog): !fish the shipped begin_cast lane (fishing_energy store, migration 0035 — renumbered from 0034 on the forward-merge collision with #234's btd6_data_blobs, checksums.json keeping BOTH rows; energy math + daily sha256 weather verbatim; the waiting-for-a-bite cast_panel session panel via renderer_override), !fishlog the shipped 32-species fishdex embed. NEW replay-vocabulary seam: the capture harness never patched datetime.now, so the golden's weather byte is the CAPTURE DAY's draw — replay seeds it per case via CAPTURE_WORLD_WEATHER (sb/adapters/parity/runner.py:133, "sweep.fish": "rain" — the #163/#167 reseed lane extended to the shipped unpatched wall-clock read). ONE depth exemption table:fishing_catch_log under the EXISTING time-driven class (rows land only at bite resolution inside the interactive cast session — the rps_players precedent). Locking untouched (no FOR UPDATE / advisory / txn-boundary edits; games wager/store not in the diff). Ratchet fishing {events: 1, tables: 3, settings: 0}.
- #240 (merge `553ff2b9ff50ba13a7a48c3ea0724dc350eca049`, main HEAD) — mining pending→ported, 6/6 goldens (the 2 mining sweeps + 4 strays re-homed `_unmapped`→mining: sweep_fastmine / sweep_chop / sweep_explore / sweep_reset_inventory): shipped core-loop bytes verbatim (!fastmine swing / !chop / !explore / the !mine capture-artifact copy / !reset_inventory admin wipe) + the ANCHORED MiningHubView (sb/domain/mining/panels.py — 7 persistent `mining:*` custom_id_override ids, dark_grey token, live-overview fields via renderer_override; the golden pins the panel_anchors row [goldens/mining/sweep_minemenu.json] so anchored semantics, NOT the session-lifecycle default). The wave-6 promissory note EXECUTED: the #230 game_xp covered-elsewhere citations (table:game_xp / event:game_xp.awarded / event:game_xp.level_up) re-pathed `_unmapped`→mining AND upgraded to PORTED-row coverage. game_xp substrate to shipped shapes (migration 0036_game_xp_shipped_types + the corpus event payload); mining ops moved to the module-global RNG stream the runner reseeds per case (seed-42 trajectories verified BEFORE the build). ONE depth exemption table:mining_player_state under the EXISTING guard-only-capture class (D-0069 — the captured !descend/!ascend sweeps pin the shipped GUARD bytes with no row). Locking untouched (no FOR UPDATE / advisory / txn-boundary edits — the games store/ops edits are ADDITIVE game_xp accessors only; check_money_race green in the same CI). Ratchet mining {events: 2, tables: 5, settings: 0}. Side repair ledgered in-PR: the shared dev Postgres ledger healed (0034's DDL applied, its checksum row corrected).
- END COUNTS (CI-LOG-VERIFIED at `553ff2b`, the WAVE-7 END waypoint in the health line carries the full quote set): gate GREEN 355/355 across 48 ported (47 subsystem rows + kernel home; run 29182868238 gate job 86623673864); report 361/471 green + 471/471 replayable (report job 86623673865 same run); units 1713 passed / 8 skipped in CI (ci run 29182868237 tests job 86623673877); corpus 471 = 465 imported + 6 minted (zero mints this wave); `_unmapped` 111→107. Zero new exemption reason classes for the second wave running; zero decision records; zero compensators (allowlist stays EMPTY).
- REMAINING map (truth at HEAD, parity.yml + the gate job's pending table): ONLY setup (8) PARKED at the create-channel wall (trap 17) and quicksetup (1) BLOCKED (D-0030) — both owner-shaped — plus the `_unmapped` re-home pool (107, mostly btd6-family sweeps over the PORTED btd6 row; the wave-7 card's 💡 names the residual re-home sweep as the last agent-shaped parity lane, sequenced AFTER the band-7 btd6 slices settle). EVERY real subsystem row is ported — the parity program stands at 48/50.
- LANE COMPLETE: no open PRs, no unmerged wave7/* work beyond this wrap-up, no control/claims/ files from this lane; the wrap-up PR (control fast lane, docs-only — trap 25, no Postgres ladder in the wrap-up seat; counts CI-LOG-VERIFIED instead).

Parity-flips wave-6 lane record 2026-07-12 (games-family flips, session COMPLETE at this wrap-up fold — card `.sessions/2026-07-12-parity-flips-wave6.md` + telemetry row ride the wrap-up PR per Q-0194; every count re-verified at HEAD `8730200` per Q-0120, not replayed from mid-wave memory — main moved constantly around this lane, #217/#218/#220–#223/#225/#228/#229 landed interleaved):
- #219 (merge `d3dba9b53bf87ededee6ed4942a1e7c87e185add`) — inventory pending→ported, 1/1 goldens (sweep_inventory); codex returned findings: 2 verified REAL and fixed in-PR, 1 verified-and-declined with source citation (Q-0120 held both ways).
- #224 (merge `cc59a6eabd603df173fad74ede53240c61f63e77`) — casino pending→ported, 2/2 goldens (sweep_casino + sweep_poker); kernel seam: HUB_NAV_LABELS gained "games" (sb/kernel/panels/render.py) for the back-nav byte.
- #226 (merge `f032e8aec85383c1f2a0e209ce6ca3405b1fd2c6`) — creature pending→ported, 6/6 goldens (5 creature sweeps + sweep_dextop re-homed `_unmapped`→creature). DOCTRINE ledgered in-PR: `!catch` KEPT DECLARED while skip-listed — skip-listed-but-declared is NOT a contradiction when golden-pinned bytes depend on the lane (the treasury grant / admin restart precedent; _sweep_skips.json pins WHY, the manifest pins the shipped surface). Two depth exemptions under EXISTING classes: table:creature_collection_log env-keyed-integration (the catch writer's unseeded private RNG kept it out of the capture corpus by design), table:creature_battle_record time-driven (rows land only at PvP resolution inside the interactive challenge view — the rps_players precedent).
- #227 (merge `39bf226b98b48083f9b9d1b1cc8a644cd3dae9e3`) — four_twenty subsystem BORN ported, 1/1 goldens (sweep_420). The passive content-trigger armed LIVE: handle_four_twenty rides the shared message feed (sb/adapters/discord/message_feed.py) ordered AFTER handle_chat_award, and the parity harness's boot.send_command drives the same stage — the golden pins the trigger firing on the command message itself, module-global RNG simulated under the runner seed before the panel build. STYLE_TOKEN_COLORS gained leaf_green (render.py:97).
- #230 (merge `87302001f993ea36480fedfc6cc7e85eab8d4255`, main HEAD) — games pending→ported, 4/4 goldens (2 games sweeps + sweep_world/sweep_worldcard re-homed `_unmapped`→games). Hub reshaped 4→10 rows on STATIC persistent ids (custom_id_override="games:open:<key>", no _mint_ephemeral — the community precedent); the shipped BOTH command split PREFIX+SLASH with the slash twin EPHEMERAL (recon said "public"; the golden's flags byte 64 won). Five depth exemptions, all covered-elsewhere/trap-14f under existing classes: game_state (ported blackjack siblings), guild_settings (ported rps_tournament sibling), game_xp + event:game_xp.awarded + event:game_xp.level_up citing NAMED `_unmapped` mining-lane siblings (sweep_fastmine/sweep_chop/sweep_explore each carry the game_xp row — the settings table:subsystem_bindings precedent); a reviewer suggestion to UNDECLARE game_xp was REJECTED on source (creature ops award game_xp in-txn).
- END COUNTS (CI-LOG-VERIFIED at `8730200`, the WAVE-6 END waypoint in the health line carries the full quote set): gate GREEN 346/346 across 45 ported (44 subsystem rows + kernel home; run 29178063196 gate job 86610739481), report 352/471 green / 471/471 replayable (report job 86610739470), units 1568/7 in CI (run 29178063197 tests job 86610739516), corpus 471 = 465 + 6 minted (wave minted ZERO), `_unmapped` 114→111.
- REMAINING map (truth at HEAD, parity.yml + the gate job's pending table): farm (1) / fishing (2) / mining (2) still pending — the #217 merge was their money-race FIX, not a flip; setup (8) PARKED trap 17; quicksetup (1) BLOCKED D-0030; `_unmapped` 111 re-home pool. Named next slice (session-idea): fishing+mining flips re-home their `_unmapped` game families in the same motion and convert the #230 game_xp covered-elsewhere rows to direct coverage (the exemption rows name the files).
- LANE COMPLETE: no open PRs, no unmerged wave6/* work beyond this wrap-up, no control/claims/ files from this lane; the wrap-up PR (control fast lane, docs-only — trap 25, no Postgres ladder in the wrap-up seat) carries the card flip + this fold.

Tournament-entry race fix slice 2026-07-12 (builder lane, one bounded slice — PR #223, branch fix/tournament-entry-race; card `.sessions/2026-07-12-tournament-entry-race-fix.md` + telemetry row ride the PR per Q-0194):
- Band position: post-band-6/7 hardening lane — the #221 KNOWN_RISKS row (the lint's single ledgered real bug) fixed and cleared; the stale-row guard forced exactly this coupling (fix + row deletion in one PR).
- ORACLE SEMANTICS (search_code reconstruction; direct oracle file reads + list_commits DENIED for this seat — head not pinnable, all fragments at indexed ref `1ecc21138fe…`): duplicate entry is REJECTED BEFORE any fee is taken — disbot/cogs/rps_tournament_cog.py try_register_player guards paid_players/players ahead of the fee block; the Join button (disbot/views/rps/registration.py) replies "You're already registered!" ephemeral; disbot/utils/tournaments.py returns the same copy before its balance check. Pinned: reject-with-message, no debit, no refund needed. (Noted drift, out of scope: sb/domain/rps/tournament.py:153's in-memory guard copy uses "." where the oracle uses "!".)
- Fix (sb/domain/games/wager.py::enter_tournament_in_txn ~L260): store.lock_new_checkpoint_slot (pg_advisory_xact_lock on the (guild,user,subsystem) triple — the #213 solo_start precedent, identical acquisition order advisory→game_state read→economy write) + fetch_user_checkpoint existence check under the lock, BEFORE the debit; a committed entry row raises the new AlreadyEnteredError (D-0060 domain refusal, oracle copy verbatim) — the duplicate path never touches the wallet. Blackjack's launch loop treats the refusal as the shipped broke-player skip (duplicates unreachable there anyway, single-driver).
- Red-then-green on real Postgres 16 (tests/integration/test_tournament_entry_race.py, the #213 test pattern — two genuinely concurrent register_player calls, monkeypatched hold): RED at HEAD `71af879` verbatim "expected exactly one entry fee (-25); got a net change of -50 — one fee vanished (outcomes: (True, '') / (True, ''))"; GREEN with the fix — integration 10 passed (the new test + #213's 5 + #217's 4). Asserts: one fee debited, one rps:entry_fee ledger row, one entry row, one roster entry, loser refused with the oracle copy.
- KNOWN_RISKS row DELETED (tools/check_money_race.py — ledger now EMPTY): `check_money_race: OK — 0 violations under sb/domain (2 allowlisted site(s), 0 ledgered known-risk site(s))`, ZERO new ALLOWLIST/KNOWN_RISKS entries (the fix is structural — advisory fence before the money mutation); test_main_exit_zero_on_head re-pinned to the empty-ledger truth.
- Ladder at the branch head: units 1487/2 local (real Postgres, canonical order), integration 10 passed, manifest_compile/check_sim_gate/check_compat_frozen green, check_parity_depth OK (50 subsystems, 40 ported, 467 goldens), golden-parity gate local GREEN 329/329 across 40 ported (zero golden movement from this fix).

Money-race lint slice 2026-07-12 (builder lane, one bounded slice — PR #221, branch lint/money-race-guard; card `.sessions/2026-07-12-money-race-lint.md` + telemetry row ride the PR per Q-0194):
- Band position: post-band-6/7 hardening lane — the structural guard for the F-001/F-002 money-race class, the "check_natural_key_fencing" idea named in BOTH the #213 and #217 session cards (twice-evidenced), now BUILT and CI-wired.
- tools/check_money_race.py: DB-free AST/regex lint over sb/domain — rule A (unlocked SELECT with no FOR UPDATE / pg_advisory_xact_lock fence earlier in the txn scope, followed by wager.credit_in_txn/debit_in_txn/economy balance writes) + rule B (natural-key INSERT…ON CONFLICT…DO UPDATE writing caller-computed state with no preceding locking read/advisory fence). Import-aware store-seam resolution, name-fixpoint money/fence propagation (transitive credit via local helpers, `_load_pending`-style inherited fences), branch-aware ordering (error-copy reads on raise/return-terminated branches never leak — economy.pay / treasury.disburse error branches verified non-findings by construction). Two stale-row-guarded ledgers in the tool (committed-checker allowlist idiom): ALLOWLIST (verified-safe, per-row justification) and KNOWN_RISKS (real class members, loud on every run, never called safe; fix-without-delete reds the checker).
- Red-then-green proof: tests/unit/invariants/test_check_money_race.py pins the checker RED on the verbatim pre-#217 shapes (farm.collect A, farm.buy_chicken A+B, mining.sell A via the transitive _sell_rows credit) and GREEN on the shipped post-#217 fixes; neutering the money seed makes all pre-fix fixtures produce zero findings (the RED half has teeth). Real-tree baseline pin: every HEAD finding dispositioned, no stale rows.
- CI wiring: ci.yml checkers fleet loop + named-gates.yml `architecture` job step (sibling shape, stdlib-only, runs under the no-runtime-deps installs). NO new required status check.
- ⚠ REAL-BUG FINDING (ledgered, not fixed — this slice is the lint): sb/domain/games/wager.py::enter_tournament_in_txn (L224–242) debits the entry fee and upserts the natural-key entry row with NO advisory slot lock and NO existence check — the #217 buy_chicken first-insert shape. Reachable surface: rps sb/domain/rps/tournament.py::register_player (button/reaction sign-up; its "already registered" guard is in-memory and yields at awaits), where two concurrent same-user entries both debit and collapse into ONE entry row — one fee vanishes (not in the settlement pot, not refunded by abort). Blackjack's launch-loop caller is single-driver, effectively safe. Fix shape: lock_new_checkpoint_slot + existence check under the lock (#213 solo_start precedent) + a duplicate-entry semantics decision (shipped intent: refuse) + a real-Postgres race test; the KNOWN_RISKS stale-row guard forces the fixing PR to delete the ledger row.

Farm/mining money-race slice 2026-07-12 (builder lane, one bounded slice merged-on-green — #217, merge `ed8eed34`; card `.sessions/2026-07-11-farm-mining-money-race-fix.md` + telemetry row rode the PR per Q-0194):
- FOUR races of the F-001/F-002 class (unlocked read → settle → credit under NATURAL_KEY) reproduced RED live on real Postgres at `977bb27` BEFORE fixing, then green: farm.collect double-credit (sb/domain/farm/ops.py::_record_collect over the plain-SELECT get_farm), farm.buy_chicken no-row first-insert race (FOR UPDATE cannot fence a row that doesn't exist — user double-charged, one purchase swallowed), mining.sell + mining.sell_all double-payout (plain-SELECT get_mining_inventory + the GREATEST(0,…) decrement floor absorbing the loser's decrement).
- Fix mirrors #213: `get_farm`/`get_mining_inventory` grew `for_update=True` (FOR UPDATE row locks; farm additionally `pg_advisory_xact_lock` on the upsert's own (guild,user) key — the lock_new_checkpoint_slot precedent; mining locks ORDER BY item_name for deterministic multi-row acquisition); all five money legs pass it; display/roll reads stay unlocked. DB-free SQL pins guard the fix where asyncpg is absent (tests/unit/band6/test_band6_games_substrate.py).
- Sweep named in the PR body: farm 3 legs FIXED, mining 2 legs FIXED; mine/harvest/explore/buy/depth/totals CLEAN; bounded check of fishing/treasury/creature/casino/inventory/economy — ALL CLEAN (fishing moves no money; treasury/economy already conditional-write or FOR UPDATE; games/blackjack/rps = #213; deathmatch moves no money).
- Classify-or-fix: farm/mining/fishing goldens replayed at HEAD and at the branch head with IDENTICAL report totals (302/467 both sides, stash-diffed on the same local DB) — their reds are the standing pending-row expected-red class (flag-13 disposition), ZERO movement from this fix.
- Codex question posted on the final head per Q-0259 r.3 (#217 comments 4949158345/4949230955/4949238362 — the farm-advisory-lock vs economy-row deadlock-ordering invariant); merged on green without waiting; any reply gets Q-0120 source verification.
- Session idea (in the card): tools/check_natural_key_fencing.py is now TWICE-evidenced (games at #213, farm/mining here) — a structural NATURAL_KEY fencing checker would catch the next instance at review time.

Self-review 2026-07-11 (ORDER 013 — owner-directed fleet self-review, window 2026-07-10 ~20:00Z → 2026-07-11 ~10:25Z; every claim cited; committed by this lane's seat at its first wake after the ORDER landed via #169 `48ad4a6`):
(1) WENT WRONG in the window, each with its citation:
- Real defects shipped in merged flips, caught post-merge by codex review and fixed same-morning by #161 (merge `8ad243f`): group-scoped subcommand aliases indexed unqualified (from #154), the `ticket_post_panel` staff gate missing (from #154), the live `/karma` acked-interaction reply landing on the wrong branch (from #157) — all three verified REAL against oracle source before fixing (Q-0120). A fourth real P2 was caught and fixed IN-PR at #160 (uncoerced widget-written scalar raws rendered `**invalid**`; fix + regression test at `e8bbb66`).
- Codex connector misbehavior, both directions: capacity FLAPPING — usage-limit non-reviews on #148 (comment 4942100526), #151 (4942514698), #152 (4942577962), then full P2 reviews on #154 (review 4676723779), #157 (4676836079), #160 (4676953047) the same morning — and PHANTOM claims: the #160 reply (comment 4943407864) claimed a commit `64d607a` + follow-up PR that exist nowhere (documented on the PR, comment 4943535922); the #144 reply (comment 4942002321) likewise claimed an invisible regression-test PR. The Q-0120 verify-before-acting guard caught both phantoms; no action was taken on invented artifacts.
- Two test-order flakes found at clean main and killed test-first: #141 (merge `78509df`, `register_ops()` re-arms workflow leg refs) and #156 (merge `179dfb2`, k10 suite isolates the AI registries both directions).
- Flip-development red classes hit and resolved pre-merge (no red reached main): logging's K1 bare-action_id collisions + mode-dependent process counters (#167, D-0067), counting's SelectorSpec visible_when gap (#168, D-0068), chain's 4-of-6 "let the K7 op raise" reds (#170, D-0069).
- Walls hit honestly, no PR: setup PARKED at the leaked-workspace create-channel wall (playbook trap 17, wave-6 record); quicksetup stays BLOCKED (D-0030). Partial flips don't exist (A-16 full-corpus door), so both rows park whole.
- Record errors made and corrected in-ledger: the #144 and #148 PR bodies carried a +23 offset in the ensure-only burn-down counts (the file counts are the measured truth — band-7 lane record); D-0029's earlier claims (kick confirm, `!warnings` lane, config-not-reseedable) were corrected ORACLE-WINS at the moderation flip (D-0065).
- Infrastructure anomaly: GitHub Actions silently dropped pull_request synchronize events for one branch ~50 min in the #145 window (playbook trap 13a); diagnosed, not recurred since.
(2) REQUIRING OWNER ATTENTION (click-level; mirrors the ⚑ items below): (a) create the empty repo `superbot-plugin-hello` at github.com/new — one click, unblocks ORDER 002 (OWNER-ACTION 2); (b) repo Settings → Rulesets: enable merge queue or drop require-up-to-date for docs/control paths — kills the update-branch dance (OWNER-ACTION 3); (c) put a real `ANTHROPIC_API_KEY` + `AI_ENABLED=true` in the agents' session env — gates band-7 live-NL EVIDENCE only, code shipped deterministic (OWNER-ACTION 5); (d) FYI, no action: the Codex pool flaps and its replies have twice claimed phantom commits/PRs — the Q-0120 guard holds, but treat "Codex says it committed X" as unverified until seen in the repo; (e) decide-and-flag decisions taken under the Q-0262.3 delegation this window: D-0064 through D-0069, all reversible-on-paper (Q-0240 class), each in docs/decisions.md with its revert path.
(3) HEALTH (one line): 26/49 parity rows ported with gate 168/168 GREEN and report 205/465 at `8b55d95` (CI-log-verified, health line above), ~30 PRs merged-on-green in the window with zero required-check reds on main; next: the R2 singleton flips welcome → automod → security.

ORDER 008 record — AMENDED by owner directive Q-0265 (CONTINUOUS-MODE AMENDMENT, 2026-07-10). Q-0265 supersedes the one-slice-per-wake throttle: the Builder loop now runs continuously — slice after slice, each slice still its own merged-on-green PR; backpressure comes from queue saturation, not time; honesty guard: out of useful work → say so and idle until the failsafe. Cutover record (exact calls + outcomes, replacing the old builder-wake arming record):
- DELETED: delete_trigger {"trigger_id":"trig_01VYZQ7GHxYq3ecSw8UNZek8"} → "deleted trigger trig_01VYZQ7GHxYq3ecSw8UNZek8" (old builder-wake cron, verified absent from list_triggers).
- CREATED failsafe: create_trigger {"name":"Builder failsafe wake","cron_expression":"0 */2 * * *","persistent_session_id":"session_01HRfuSKiQSnGHXKne3yzadg","prompt":"FAILSAFE WAKE (Builder, Q-0265): if your send_later continuation chain is alive, verify that in one line and end. If it stalled, resume the work loop (sync HEAD → inbox → slice after slice, each merged-on-green) and re-arm the chain (~15 min) before ending."} → SUCCESS, trigger trig_01L5JBefGSCM1fUdwm4SRQnY, enabled, next_run_at 2026-07-10T20:05:55Z, verified via list_triggers.
- CHAIN MECHANISM: send_later cannot target another session (schema: message/at/delay_minutes only, fires into the calling session), so the continuation chain runs on ONE-SHOT create_trigger calls into the coordinator session instead — first link trig_01KedTiCKYMbB3oaNZL5rHmf ("Builder continuation chain (one-shot)", run_once_at 2026-07-10T20:03:00Z); the coordinator re-arms a fresh one-shot each turn (a one-shot self-disables after firing, ended_reason=run_once_fired). Failsafe cron catches any stalled chain.
- Q-0265 honesty guard INVOKED by the games lane at this wrap-up: the band-6 lane is out of golden-pinned work (record below) — next wake starts band-7 per the canonical order or takes owner direction.
- LOOP RUN RECORD + ENV-TEARDOWN INCIDENT (folded at the 2026-07-11 close-out; full record docs/retro/q0265-routine-loop-2026-07-11.md): the chain ran as ~40 one-shot create_trigger links over 2026-07-10T19:48Z → 2026-07-11T19:40Z. A platform ENV TEARDOWN at 2026-07-11T16:31Z auto-disabled scheduled triggers FLEET-WIDE (ended_reason "auto_disabled_env_deleted"); this lane self-healed (failsafe recreated as trig_01GLBYyf4aDS6AwpLVybZvVy) but sibling lanes' failsafes (substrate-kit, trading-strategy, sim-lab, idea-engine, product-forge; venture-lab/superbot-games/superbot-idle also touched) were killed and could not be healed from this seat; owner alerted ~2026-07-11T17:31Z (OWNER-ACTION 6 below).
- CLOSE-OUT DISARM (2026-07-11 ~19:30Z, verbatim coordinator facts): delete_trigger trig_01Ae6nZZakaZxJnhQ9bzWJcn → deleted (pending chain link, was to fire 19:40Z); delete_trigger trig_01GLBYyf4aDS6AwpLVybZvVy → deleted (failsafe cron, "0 */2 * * *"). Residual check via list_triggers: ZERO enabled triggers now target the coordinator session; 24 spent chain links remain listed, all disabled (23 run_once_fired, 1 auto_disabled_env_deleted from the 16:31Z teardown). The Q-0265 loop is FULLY DISARMED. A fresh coordinator RE-ARMS per the recorded recipe: one-shot chain links ~16 min apart + the 2-hourly failsafe cron, both created via create_trigger with persistent_session_id — recipe durably in docs/collaboration-model.md § Continuous mode and docs/retro/q0265-routine-loop-2026-07-11.md.

parity-flips lane record (session 2026-07-10/11) — SESSION COMPLETE: two waves, nine lane PRs (8 flips + the #118 groundwork) plus post-wrap wave-3 sibling flips (#137 + #140), wave-4 sibling flips (#143 + #145), wave-5 flips (#152 + #154 + #157), wave-6 (#163 moderation flip; #161 P2-triage rode as its own lane) and heartbeats #126 + #134 + #142 + #149 + #158, ALL squash-merged on green with the 6-check ruleset. Wave 1:
- #118 leaderboard groundwork — board panel oracle fidelity + games-lane-granted 3-string providers fix; merge `9757755c61034edad4b5dee5ab715783da18f1a6`
- #119 leaderboard flip pending→ported (1/1 goldens); merge `7d1cf9a2133032f9f930189762eed8b80a49f35b`
- #121 hermes flip (2/2 goldens; webhook-shaped followups in the parity transport; sim-gate overlay machinery per seed `a627153`); merge `9e4180d48dac515b200a06d35344a8640adb541f`
- #123 general flip (1/1 goldens; shipped GeneralMenuView as a session-lifecycle panel); merge `4a32f618838e430ca71fe2c241a23236cb614fe3`
- #125 utility flip (7/7 goldens; profile-card placeholder, capture-world guild directory, DeferMode.NONE trap documented); merge `7c819b197b969d8dc1bcdaffa6c7bf7a6f68d74c`
Wave 2 (all squash-merged on green 6-check CI):
- #128 ux_lab flip pending→ported (1/1 goldens); merge `71eee908fa2795012efca6461ec45b4e30038530`
- #129 uxlab flip (1/1 goldens; the corpus's only get_original_response call ported); merge `038992cd4078583a0e76507aa538b9d2af7808fc`
- #131 channel flip (1/1 goldens; shipped ChannelManagerView hub — sub-panels are pending terminals per D-0030); merge `287566584ba7e3b15ede9106cb00e47742ae07f7`
- #132 servermanagement flip (1/1 goldens; slash row only — the sibling server_management prefix row stays pending, 2 diffs from anchored-persistent-panel semantics); merge `12886b9be94aae76dd81a4d68db888fa03bf51e4`
Post-wrap wave 3 (folded into this record at the 2026-07-11 heartbeats — both landed after the lane's own LANE END entry):
- #137 settings flip pending→ported (4/4 goldens — settings_hub_open, sweep_settings, sweep_slash_settings, sweep_settings_access: the shipped ⚙️ Settings Manager hub verbatim on both surfaces — a DeferMode.NONE hub — + the 🔍 Access Policy Explorer behind the NEW `settings access` grouped prefix front door, the corpus's first; the FIRST R2-non-vacuous flip — three depth-exemption rows under the existing `covered-elsewhere` reason class; ratchet row `settings: {events: 1, tables: 2, settings: 0}`); merge `77ae8b4`; card `.sessions/2026-07-11-settings-parity-flip.md`
- #140 cleanup flip pending→ported (3/3 goldens — the shipped 🧹 Cleanup Hub + Prohibited Words Manager + `!cleanuphistory` scan; the FIRST Discord-READ capture port — the goldens' `logs_from` wire verb gets a twin in the parity transport and the live history reader is armed in boot; the sweep_wordmenu golden pins the oracle's process-memory `_word_cache` leak as a literal (`` `test` `` — the capture's alphabetical sweep order left `!word add test` in the cog's in-memory cache; in-code under-port note), ONE `covered-elsewhere` depth exemption (`prohibited_words`, written only by `_unmapped/sweep_word_add`); codex P2 fixed on the PR head `c510830` — honest refusal for unported scan-mode matchers; ratchet row `cleanup: {events: 1, tables: 2, settings: 0}`); merge `3bbe93e`; card `.sessions/2026-07-11-cleanup-parity-flip.md`
Post-wrap wave 4 (folded into this record at the 2026-07-11T04:45Z heartbeat — both squash-merged on green 6-check CI):
- #143 xp flip pending→ported (3/3 goldens — the rank-card session panel + the avatar READ port/capture twin; 2 depth exemptions under the existing `covered-elsewhere` reason class: xp.reset, xp.level_up; codex question on the final head confirmed the arg-walk abort behaves as intended — no change); merge `f97a4c7`; card `.sessions/2026-07-11-xp-parity-flip.md`
- #145 proof_channel flip pending→ported (3/3 goldens — the `_PrizeManagerView` port; ALL THREE codex P2s were real and fixed on the PR: the locks depth exemption reclassified `covered-elsewhere`→`env-keyed-integration`, DeferMode.MODAL on the modal-backed actions, the `+prize` wire literal; the end_access compensator needed NO new work — already fixed pre-flip by the #105/#108/#111 lineage; the compensator allowlist stays EMPTY); merge `531f14ee`; card `.sessions/2026-07-11-proof-channel-parity-flip.md`
Post-wrap wave 5 (folded into this record at the 2026-07-11T06:35Z heartbeat — all three squash-merged on green 6-check CI; every fact below re-verified against parity.yml + the PR threads at HEAD `9053311`):
- #152 economy flip pending→ported (6/6 goldens — the wallet card, the 💰 Economy Menu hub and the Job Center work panels; the `/economy` slash twin rides the AUTO+EPHEMERAL defer inverse; THREE depth exemptions in parity.yml: `table:economy_balances` under the existing `env-keyed-integration` class — the ledgered-coins boundary's NEW home, no old-bot capture can carry the row — and `table:job_progress` + `table:inventory` under the NEW `select-driven` reason class, decision record D-0064 in docs/decisions.md: a write behind a shipped run-minted select-menu pick is structurally outside the imported corpus's input vocabulary — the click schema carries no `values` field and the run-minted select ids normalize to `<cid:N>`; ratchet row `economy: {events: 2, tables: 5, settings: 0}`; @codex question on the head `522df24` [comment 4942577422] got only the usage-limit reply [comment 4942577962] — non-review); merge `b2ec7d9`; card `.sessions/2026-07-11-economy-parity-flip.md`
- #154 ticket flip pending→ported (6/6 goldens on the FIRST local replay — the all-guard-byte row: the `!ticket` hub panel with the three shipped buttons carrying label + emoji as SEPARATE wire fields, plus five guard bytes [empty-subject usage, REASON_NOT_CONFIGURED, the "This isn't an open ticket channel." in-channel guard, the TicketOpenModal not-configured refusal]; the first fully-from-zero NEW-SUBSYSTEM birth kit — no prior sb/domain/ticket existed; ZERO declared surfaces and ZERO depth exemptions BY DESIGN: the shipped ticket config/row tables are structurally absent from the v1 schema epoch, so the slice declares only what it fully carries instead of minting dead tables — no D-0065 needed, the under-port boundary centralized in sb/domain/ticket/service.py; ratchet row `ticket: {events: 1, tables: 2, settings: 0}`; @codex question on the A-16 zero-declaration reading [comment 4942796411] answered POST-merge with a real review [4676723779, final head `43b9b44`]: TWO open P2s — bare `open`/`create` subcommand aliases indexed unqualified in the live prefix index, and `ticket_post_panel` declared `user`-tier without the shipped staff gate — both are follow-up rows for the next ticket slice, neither golden-pinned); merge `5b750c8`; card `.sessions/2026-07-11-ticket-parity-flip.md`
- #157 karma flip pending→ported (8/8 goldens — the shipped thanks/karma surfaces incl. `karma.card` AND the corpus's first RED `error_card` envelope port [`karma.error_card`, the shipped utils/embeds.error refusal — sb/domain/karma/panels.py]; `karma_audit_log.mutation_id` added to the EXISTING kernel-surface-drift `columns` disposition [parity/parity.yml:149, the economy_audit_log precedent under the Q-0262.3 delegation — the pinned-list test grew with it]; the K7 engine-swallows-typed-errors problem solved via a `ctx.params["_karma_refusal"]` side-channel [sb/domain/karma/ops.py `_mark_refusal` → handlers.py — leg exceptions classify into frozen-five results, so the class identity never reaches the cog otherwise]; ZERO depth exemptions; ratchet row `karma: {events: 2, tables: 4, settings: 0}`; @codex question on the final head `f8532ef` — is there a cleaner sanctioned lane than the shared-params marker — [comment 4943018975] answered POST-merge with a real review [4676836079]: ONE open P2 — live slash `/karma` panel-present lands on the acked-interaction `edit_original_response` branch instead of the shipped followup send, a live-wire drift the parity presenter can't see — follow-up row, not golden-pinned); merge `9053311` — main HEAD; card `.sessions/2026-07-11-karma-parity-flip.md`
- Codex-capacity flap note for this wave: usage-limited at #152 (05:08Z) yet FULL P2 reviews on #154 (05:54Z) and #157 (06:31Z) — the pool refilled the same morning it walled #148/#151/#152; all three wave-5 questions plus the three open P2s above stay ledgered here since post-merge reviews can't block the merged heads (Q-0258 merge-on-green).
- UPDATE folded at the 07:38Z heartbeat: all THREE open wave-5 P2s (the #154 pair + the #157 followup drift) are CLOSED by #161 (merge `8ad243f`, card `.sessions/2026-07-11-codex-p2-triage.md`) — each verified REAL against oracle source before fixing (Q-0120): (1) group-scoped subcommand aliases (one shared `command_dispatch_keys()` truth in sb/spec/commands.py, both indexers; also corrects treasury's group-scoped aliases and un-shadows the shipped `!create` channel op), (2) the `ticket_post_panel` staff gate with the shipped `"Only staff can post the ticket panel."` refusal byte (gate = ActorRef.is_guild_operator at the v1 config-absent epoch), (3) the live `/karma` acked-interaction path now sends a webhook FOLLOWUP (live-adapter only, parity replay untouched). Zero golden-byte movement; that PR's local ladder: 1319/5 units, gate 143/143 GREEN. NEW follow-up from the #161 work, ledgered at the wave-6 heartbeat: the K1 reservation lane still treats subcommand aliases as bare top-level names — `command_dispatch_keys()` fixed dispatch, not reservation.
Wave 6 (folded into this record at the 2026-07-11T08:10Z END-OF-SESSION heartbeat — squash-merged on green 6-check CI; every count re-verified against parity.yml + the main-push CI job logs at `bd211e9`):
- #163 moderation flip pending→ported (merge `bd211e9`; 8/8 goldens — the shipped modmenu/warn/timeout/kick/ban/unban surfaces at byte parity, band 2): call-Discord-first record-leg sequencing for the timeout half-applied golden (TIMEOUT now has no effect leg; the kick effect leg carries compensate_kick; the compensator allowlist stays EMPTY); decision record D-0065 (kick-confirm off per capture — the D-0029 flip-review); the capture-world config reseed seam (CAPTURE_WORLD_SETTINGS); the typo-suggest synonym surface live corpus-wide; ZERO depth exemptions; codex eyes-only (no P2s on the PR).
- setup: PARKED, no PR — 2/8 goldens (advanced, status) hit the quicksetup create-channel wall in a new flavor: a workspace channel leaked from `_unmapped` sweep_setup's capture into the goldens; 5/8 are portable by the D-0030 successor slice, 1/8 is green-by-absence (depth). Playbook trap 17 carries the per-golden record.
- SESSION-END note: this parity session shipped 8 flips (#137 settings, #140 cleanup, #143 xp, #145 proof_channel, #152 economy, #154 ticket, #157 karma, #163 moderation) + the #161 fix PR + 3 heartbeats (#142/#149/#158, plus this end-of-session one); ORDER 012 acked and applied — the lane's committed session cards carry `📊 Model: Claude Fable 5`.
quicksetup: attempted and SKIPPED at a verbatim wall — its golden requires `/setup` to provision a private setup channel (a create-channel EFFECT with no capture twin and no compensator ruling; D-0030 names channel-ops as the successor slice; cross-lane sb/domain/setup files). The lane parked it — no PR, no exemption rows. This is the lane's honest stop, not a regression.
LANE END (SESSION FULLY ENDED at this heartbeat): the session's slices are complete. Remaining pending non-game rows are either settings-heavy R2 singletons (welcome, automod, security, image_moderation, counters), multi-golden mid-size (diagnostic 37, logging 7, chain 7, counting 3 — settings left this list via #137, cleanup via #140, xp via #143, proof_channel via #145, ai via the BAND-7 lane's #151, wave 5 removed economy via #152, ticket via #154, karma via #157, and wave 6 removed moderation via #163; golden counts re-verified against the gate job's PENDING table at `bd211e9` [job 86525137517]), the server_management prefix sibling, parked (setup — the create-channel wall, trap 17; wave-6 record above), or blocked (quicksetup, unchanged); the next parity session should start from those with the playbook in the .sessions/ cards. Logging pre-read from the moderation session: D-0029 pre-names logging's reds, and moderation.action_taken fan-out is logging's subscriber — the mutation_id payload addition may be visible in logging's goldens. Band-6 games lane wrapped separately (record below).
Wave 7 (parity lane, 2026-07-11 — folded at the 10:25Z ORDER-013 heartbeat; all three squash-merged on green 6-check CI; counts CI-log-verified at `8b55d95`, health line above):
- #167 logging flip pending→ported (merge `6a0921c`; 7/7 goldens — the shipped panel-first `!logging` surface, band 2): D-0029's ledgered logging shapes resolved ORACLE-WINS per decision record D-0067 (the projection hub replaced by the shipped panel, enable/disable retired, the 11-slot route table landed); TWO new playbook traps minted here — K1 claims panel action_ids BARE and cross-subsystem (internal ids renamed + `custom_id_override` pins the shipped wire byte) and golden-rendered process-local counters are MODE-dependent (gate filters / report replays all / isolation replays one), so they are RUNNER-SEEDED now (`CAPTURE_WORLD_COUNTERS` in sb/adapters/parity/runner.py).
- #168 counting flip pending→ported (merge `1730026`; 3/3 goldens — the shipped `_CountingHubView` Counting Manager): decision record D-0068 — the band-6 declarative hub reshaped to the shipped view via renderer_override state-keyed component assembly (SelectorSpec has NO `visible_when` — only PanelActionSpec does — so state-keyed SELECT visibility lives in the override's component-drop lane, not grammar gating), provider-fed rich mode options, no nav row; `counting_state` exempted under the EXISTING `select-driven` class (D-0064).
- #170 chain flip pending→ported (merge `8b55d95`, main HEAD; 7/7 goldens — the shipped chain_cog guard bytes + `_ChainMenuView` at byte parity): 4 of the 6 initial reds were one class — the band-6 "let the K7 op raise" shape renders `Missing/invalid argument: …` where the shipped cog answered its own ❌/ℹ️ plain-content guard BEFORE the op, so the guards re-homed handler-side; decision record D-0069 mints the NEW depth reason class `guard-only-capture` for `table:chain_channels` (the capture sweeps drove every chain subcommand argument-less — no imported golden can carry the row; a future argful capture sweep deletes the class).
- Sibling merges in this window (other lanes, recorded for the fold): #165 (merge `62d850f`, band-7 slice 6 — the live MODAL-SUBMIT lane arms, decision record D-0066), #166 (kit v1.10.1, merge `5c064dd`, kit line above), #169 (merge `48ad4a6`, fm relay — ORDER 013 appended to control/inbox.md).
- #172 welcome flip pending→ported (merge `dc32367`; 1/1 goldens, FIRST-replay green): the shipped cogs/welcome_cog.py `_policy_embed` as a component-less session-lifecycle status card (`welcome.status`, the karma.card/#167 recipe — zero panel_anchors rows, zero lock rows); NEW sb/domain/welcome/service.py (policy through THE kernel settings seam, channel/entry_role bindings via `get_binding`, the shipped `---` variant splitter verbatim); guild name/member count via the guild-directory port. ZERO depth exemptions (trap 7 did NOT bite: the row's "settings-heaviness" is all SettingSpecs [dead dimension, trap 10c] + BindingSpecs [not a depth dimension] with stores=()/events=() declared). Ratchet `welcome: {events: 1, tables: 2, settings: 0}`. @codex question on the head `7582ee5` (comment 4945003958 — the DM-preview empty-template seam).
- #173 automod flip pending→ported (merge `b61e954`; 1/1 goldens, first-replay green): same recipe (`automod.status`, MOD_COLOR = the existing `orange` token). The flip's one real decision — CAPTURE-SHA DRIFT: the oracle's CURRENT head renders two MORE rule lines (🌐 Cross-channel spam, 🔁 Duplicate content) than the imported golden; pinned to the corpus @7f7628e1 (parity.yml source.sha), the drift ledgered in the module docstring, and the drift class written up in the flip card (search_code reconstructs the oracle's DEFAULT BRANCH — diff fragments against the golden FIRST). Ratchet `automod: {events: 1, tables: 2, settings: 0}`, ZERO exemptions. @codex question on the head `24fa28b` (comment 4945044867 — is the current-head extra-line pair genuinely unconditional, i.e. is the four-line card capture-faithful under enabled toggles too).
- #174 security flip pending→ported (merge `f87ffb0`, main HEAD; 1/1 goldens, first-replay green): same recipe (`security.status`, GENERAL_COLOR green) + two binding reads (alert_channel, raid_slowmode_channel) and the shipped `applies_raid_slowmode` conjunction verbatim (channel bound AND slowmode seconds > 0 — security_config.py); the corpus's first STATE-CARRYING FIELD-NAME surface (per-tier flags live in the field names — renderer_override territory, the #145 12c precedent extended). NO capture-sha drift on this row (checked as a mandatory pre-step). Ratchet `security: {events: 1, tables: 2, settings: 0}`, ZERO exemptions. @codex question on the head `a11b90c` (comment 4945076812 — should the status card resolve the slowmode channel's liveness before rendering the slowmode sentence).
- Wave-7 singleton-run summary: THREE flips, ZERO depth exemptions, ZERO new reason classes, ZERO decision records, ZERO lock/compat churn (route changes are pin-invisible — compat pins {name, group, aliases} only), compensator allowlist EMPTY throughout (all read-only slices). The `_policy_embed` status-card recipe is now five-for-five (karma.card → logging status → welcome → automod → security).
- NEXT wave-7 targets (RESOLVED by wave 8, record below): image_moderation ✅ #176, counters ✅ #178, the server_management prefix sibling ✅ #179; remaining: diagnostic [37], setup stays PARKED (trap 17), quicksetup BLOCKED (D-0030).
Wave 8 (parity lane, 2026-07-11 — folded at the 12:20Z heartbeat; all three squash-merged on green 6-check CI, counts CI-log-verified at `12b4135` via main-push golden-parity run 29151976612, health line above):
- #176 image_moderation flip pending→ported (merge `d034e30`; 1/1 goldens, FIRST-replay green): the shipped cogs/image_moderation_cog.py policy embed (Q-0108) as a component-less session-lifecycle status card (`image_moderation.status`, MOD_COLOR orange — the recipe's sixth use), NEW sb/domain/image_moderation/ package (the band-2 manifest had no domain dir). Trap-24 drift check PASSED (current-head fragments match the golden's seven lines exactly); the shipped STATE-CONDITIONAL exempt-roles/channels tail is not ported (no golden reaches the state; module-docstring ledger). Ratchet `image_moderation: {events: 1, tables: 2, settings: 0}`, ZERO exemptions. @codex question on the head `52f7efa` (comment 4945214897) got a REAL, source-cited review back (comment 4945218656, Q-0120-verified against the cited files): NO operator-facing mutation path can write the exempt keys today (the settings hub's group selector routes to `settings.group_pending`; the only settings-mutation widget is scoped `_SUBSYSTEM="ai"`), so the omission is harmless UNTIL an image_moderation settings page lands — the reply's own verdict: the real risk boundary is the settings-mutation surface, and the status card must grow the conditional exempt tail IN THE SAME SLICE that arms those writes (plus one narrower caveat: a pre-seeded/imported/manual DB value would already make the hub read what `!imagemod` omits — import-state concern, not a flow). Follow-up LEDGERED here, no code change (codex made none either — honestly stated this time).
- #178 counters flip pending→ported (merge `519d909`; 2/2 goldens — the `!counters` prefix sweep + the `/counters` ephemeral type-4 slash twin, the singleton run's first slash golden): the shipped status embed (blurple, the -# small-text rendered-name previews over live member counts via shipped `render_counter_name` = comma-thousands + 100-cap); slash twin cost ZERO posture edits (slash+PanelRef resolver default = DeferMode.NONE; Audience.INVOKER ⇒ flags 64 on interactions). The flip's one real change is REPLAY-WIDE: `ParityResponder.present_panel`'s type-4 panel path dropped its unconditional `components: []` when EMPTY (discord.py's send_message omits the key with no view; corpus scan: zero goldens pin the empty key on type-4, exactly this golden pins its absence; full gate = the regression proof). `GuildInfo` grew a defaulted `bots` field (shipped compute_counts read the same gateway cache; boot twin bots=1, goldens pin 4/3/1). Ratchet `counters: {events: 1, tables: 2, settings: 0}`, ZERO exemptions. @codex question on the head `c30ce9e` (comment 4945339836 — should the drop live in one shared type-4 serializer with `_reply`) drew a reply claiming a PHANTOM commit `10e8153` + follow-up PR — verified nonexistent (git cat-file across refs + PR list; documented on the PR, comment 4945355808; the THIRD phantom instance after #144/#160, Q-0120 guard held); the underlying shared-serializer suggestion evaluated against source and DECLINED (`_reply`'s components:[] shape is what fake_http captured for content-only replies; no golden forces the change) — ledgered as a follow-up only if a future golden pins a content-only type-4 without the key.
- #179 server_management flip pending→ported (merge `12b4135`, main HEAD; 1/1 goldens AND the ported servermanagement slash sibling regression-proved green): the #132 record's predicted 2 diffs closed with one spec flag + one declared action + one additive kernel field — `session_lifecycle=False` (anchored panel-manager semantics; the engine's `_record_anchor` already carries the surface split, so the prefix open records the golden-pinned `panel_anchors` row and the slash twin's empty delta held), and the shipped back-to-help hook's button (bot1.py→help_cog `_attach_back_to_help_button`: persistent id `help:back`, `↩ Back to Help`, grey, MESSAGE path only) landed as a real routable action (override `help:back` → the ported help.home) with a surface-keyed component drop in the override (D-0068 lane). NEW ADDITIVE KERNEL SEAM: `PanelContext.surface` (engine-set from req.surface) — renderer overrides gain the surface dimension (diagnostic's 37 prefix/slash twins are the likely next consumer). compat pin amended in-PR (`help:back` joined legacy_custom_ids — the id the rebuild design spec froze verbatim). Never-strand satisfied by `navigation.parent=help.home`; the grammar's injected nav:back button dropped in the override (both goldens pin its absence). Ledgered unpinned corner (in-code): the slash-ephemeral's 🔄 Refresh re-render would regain the 4th row (surface alone can't split the two refresh homes; no golden drives either refresh path) — the @codex question on the head `aaa34bf` (comment 4945528701) asks whether the stored PanelSession's `message_ref is None` is the right engine-side signal. Ratchet `server_management: {events: 1, tables: 3, settings: 0}`, ZERO exemptions; two band-6 hub tests updated to the flipped shape (their docstring deferred prefix semantics to this flip) + one NEW slash-drop test (units 1357/2).
- Wave-8 summary: THREE flips, ZERO depth exemptions, ZERO new reason classes, ZERO decision records (D-0070 stays free for the sibling band-7 lane's open #177, which carries its own D-0070 — watch the collision if both claim it), compensator allowlist EMPTY throughout (read-only slices; #179's one new write path is the engine's EXISTING anchor insert). NEW LOCAL-LADDER HAZARD (bit twice this wave): report/pytest/isolation-replay must NEVER run concurrently on the shared Postgres — a raced report mis-read 142/465 and a raced isolation replay invented xp double-award diffs; serial reruns were clean both times. Playbook trap 24 (capture-sha drift pre-check) executed on all three rows: NO drift anywhere in this wave (the class stays automod-only).
Wave 9 (parity lane, 2026-07-11 — folded at the 13:40Z heartbeat; squash-merged on green 6-check CI, gate counts CI-log-verified at the PR head, health line above):
- #183 diagnostic flip pending→ported (merge `10bf073`, main HEAD; 37/37 goldens GREEN ON THE FIRST ISOLATION REPLAY — the largest single flip: sweep_diagnostics + sweep_latency + sweep_platform + sweep_slash_platform + 33 sweep_platform_* subcommand sweeps): (1) the PINNED-CARD LANE AT SCALE — 28 `!platform <view>` operator cards are capture-world snapshot literals GENERATED from the goldens (sb/domain/diagnostic/platform_views.py; the shipped surfaces are process-introspection over the OLD bot's own runtime, unreproducible by v1 self-introspection — the trap-10a/20 lane, one golden per surface; only four world seams parameterize: channel mention / guild id / ISO now / member tier); (2) the shipped 🛰 Platform hub on BOTH surfaces (4 persistent-id selects `platform_hub.*` + Overview/Flag-manager buttons, all verbatim custom_id_override pins on a session-lifecycle panel — no panel_anchors row in either golden; the slash twin needed EXPLICIT DeferMode.NONE because the front door is a HandlerRef and slash+HandlerRef AUTO-defers — the trap-26/14a inverse's inverse); (3) diagnostic.hub reshaped ORACLE-WINS to the shipped 8-button 🔧 Diagnostics Hub (D-0067 lane; run-minted <cid:N>); (4) the 🚩 Flag Manager (flag_manager:* verbatim pins, capture-world 8-flag registry pinned, mutations on honest pending terminals) + the 🤖 Automation panel (true session view); (5) the row's ONE WRITE: migration 0029 platform_migration_checkpoints (oracle 026 DDL NAME_STABLE by fragment) + store + the audited one-DB-leg `diagnostic.backfill_dry_run` op (classification computed from real binding reads + the structurally-None v1 legacy read → the golden's both_absent bytes; `backfill apply` NOT ported — honest refusal; ledgered reconstruction limit: the shipped enum member for the binding-present cell never surfaced via search_code — the port's `binding_present` token is live-only, unreachable from a truncated parity DB); (6) guard bytes handler-side (trap 22): finding/setting/latency (`nan ms` — the capture gateway's NaN, live read arms via install_ws_latency_reader); (7) ONE additive kernel seam: RenderedEmbed.timestamp (the corpus's first embed-timestamp pin, sweep_platform_findings; both presenters carry it — trap 14b); (8) `platform health/runtime/slow/startup/status` stay deliberately UNDECLARED — the capture SKIPPED those five as nondeterministic (parity/goldens/_sweep_skips.json), so declaring them would mean inventing bytes; the root handler answers the honest refusal (check _sweep_skips.json FIRST is the new pre-step for every future row). Ratchet `diagnostic: {events: 1, tables: 3, settings: 0}`; ZERO depth exemptions, ZERO new reason classes, ZERO decision records; compensator allowlist EMPTY. Locks amended additively (47 legacy-seed rows), compat +33 command rows +10 verbatim custom_id pins. @codex question on the final head `78bc2c5` (comment 4946159920 — is the golden-template escape/format round-trip identity-safe for every pinned byte sequence, i.e. can a future table regeneration silently drift a card); merged on green without waiting (Q-0258). Playbook traps 25 (serial ladder — it also bit FILE-mutation-wise this session: a background pytest raced the parity.yml scratch-learn, phantom red, serial rerun clean) and 26 (type-4 empty components + resolver defaults + asyncio.run) were appended to the team playbook at session start as ordered.
- NEXT wave targets (RESOLVED by wave 10, record below): admin ✅ #186, community ✅ #188, community_spotlight ✅ #189, role ✅ #190; the remaining pending map is games-family only (casino 2, creature 5, farm 1, fishing 2, four_twenty 1, games 2, inventory 1, mining 2, treasury 2 — the band-6 games lane's successor rows), setup [8, PARKED trap 17], quicksetup [1, BLOCKED D-0030], and `_unmapped` [220 — re-home lane].
Wave 10 (parity lane, 2026-07-11 — folded at the 15:20Z heartbeat; all four squash-merged on green 6-check CI, each flip's gate CI-LOG-VERIFIED at its own PR head, health line above):
- #186 admin flip pending→ported (merge `2f946f8`; 2/2 goldens, FIRST-replay green): the operator-spine placeholder hub reshaped ORACLE-WINS to the shipped `_AdminPanelView` (⚙️ Server & Admin, oracle help-menu-regrouping PR #1290) — red three-section legend embed with the "Loaded cogs: **58**" capture literal (trap 10a), the invoker-lock footer via a footer-ONLY renderer_override, four button rows (4/4/5/2) session-minted `<cid:N>` on both surfaces (no panel_anchors — trap 11d; slash+PanelRef = DeferMode.NONE, zero posture edits), nine nav buttons routed to REAL ported hubs + ↩ Overview as REFRESH_PANEL. TRAP-28's first formal use: `_sweep_skips.json`'s admin-family skips (force/loadall/unloadall/syncslash/restart/system_info) — NONE newly declared; the band-2 `restart` (K5 lifecycle) predates the flip, ledgered. Ratchet `admin: {events: 1, tables: 2, settings: 0}`; ZERO exemptions/classes/records; lock +3 arrangement rows (legacy-seed Exempt) + baseline regen; compat untouched. @codex question comment 4946328914 (nav-slot injection risk on the footer-only override) on head `33a784c`, head-update note 4946867867 → final `e8bbb49`; merged on green without waiting (Q-0258). INFRA NOTE: the PR spawned ZERO check runs for ~45 min — NOT trap 13a: `mergeable_state: dirty` (main had moved to #185) and GitHub spawns no pull_request run when the merge ref cannot build; the fix is the forward-merge, not the empty-commit retrigger (one was burned before the diagnosis — trap 13a's diagnosis list should start with mergeable_state).
- #188 community flip pending→ported (merge `94dafd5`; 2/2 goldens, FIRST-replay green): the invented band-4 4-button hub reshaped to the shipped 🌱 Community Hub (views/community/hub.py) — the registry-discovered child roster as a capture-world PIN (COMMUNITY_PRIMARY 7 blurple rows 5+2 / COMMUNITY_CROSS_LINKS 3 grey; the utility UTILITY_CHILDREN precedent), the two-section bullet legend verbatim, GENERAL_COLOR green, footer-only override, and ALL TEN buttons riding persistent `community:open:<key>` custom_id_override pins through the session mint (`_mint_ephemeral` skips overrides — the utility mixed-id precedent generalizes to all-pinned session views); the row-4 📚 Help is the grammar's OWN nav:help slot (show_help=True, byte-native). Every child routes to its REAL ported surface. Ratchet `community: {events: 1, tables: 2, settings: 0}`; ZERO exemptions; compat +10 verbatim `community:open:*` pins; lock +3 rows + baseline regen. @codex question comment 4946908160 on final head `20049e0` (does static-route dispatch enforce the invoker lock the minted components get).
- #189 community_spotlight flip pending→ported (merge `ff2cf09`; 1/1 goldens, FIRST-replay green): the spotlight hub becomes a session view whose delegation override composes build_spotlight_embed verbatim — guild-named title (guild-directory read, welcome degradation posture), the shipped 👥 members line, per-field empty-state literals (XP Leaders "*No activity yet*" / Richest "*No coins earned yet*"), explicit inline flags (grammar FieldsBlock 2-tuples serialize inline=false), and the shipped `%H:%M UTC` wall-clock footer — NO clock seam needed: the replay runner reuses the CAPTURE Normalizer (parity/harness/capture.py `_SHORT_TIME`), so any live clock read normalizes to the golden's `<hh:mm>` (cheaper than the #183 ISO-now world seam). The four buttons needed ZERO edits (labels/separate-wire-emoji/styles all matched band 4). Ratchet `community_spotlight: {events: 1, tables: 2, settings: 0}`; ZERO exemptions; ZERO lock/compat churn. The overview provider re-registered at MODULE IMPORT (composition-parity doctrine — it went ensure-only when the FieldsBlock call site left the spec; `test_no_new_ensure_only_refs` caught it pre-push). @codex question comment 4946961130 on final head `a7fa6ba` (could a non-zero capture world collide a real byte with a different normalizer pattern).
- #190 role flip pending→ported (merge `fd62f53`, main HEAD; 1/1 goldens, FIRST-replay green — the lane's LAST non-game row): the band-5 hub reshaped to the golden's bytes — teal, NO description, the SEVEN static inline-true blurb fields via delegation override (role_cog.py's literal 3-tuple list), emoji IN the labels (trap 15a's INVERSE — both shipped wire forms exist, read the golden first), rows 3/3/1, grammar nav:help + nav:hub:community ("↩ Community", explicit home_hub); ANCHORED semantics kept (the golden pins the panel_anchors row; session_lifecycle stays False, the #179 split); the seven `role:*` override pins were already band-5-declared (zero compat churn). EIGHT depth exemptions ALL under EXISTING classes (trap 7's biggest bite): role_thresholds + reaction_roles covered-elsewhere (named siblings `_unmapped/sweep_setrole.json` / `sweep_reactroles.json` carry the rows verbatim), role_grants guard-only-capture (D-0069 — sweep_temprole drove an unparseable duration, guard bytes only), role_menus/role_menu_options/role_menu_pickup_stats/reaction_role_message_modes/role_automation_exemptions select-driven (D-0064 re-argued per row — the PR-2 menu builder pickers / published RoleMenuView dropdown / manager mode picker / Exemptions role picker). ZERO new classes, ZERO decision records (D-0071 was minted by the sibling #185). NEW CHECKER GAP LEDGERED (@codex question comment 4947003427, final head `47d5cb8`): check_sim_gate does NOT flag VALUE drift on an existing [A] pin — the 3/2/2→3/3/1 reshape passed silently while lock+baseline pinned the old split (admin/community redded only because their keys were NEW); values hand-amended + baseline regen; a value-comparing checker is the hygiene follow-up. `role.hub_overview` re-registered at module import (same doctrine bite as #189).
- Wave-10 summary: FOUR flips, 6 goldens, 8 depth exemptions (all existing classes, all on role), ZERO new reason classes, ZERO decision records, compensator allowlist EMPTY throughout (read-only slices). Traps 27/28 appended to the team playbook at session start as ordered; trap 28 executed on all four rows (only admin-family hits), trap 24 executed on all four rows (NO drift anywhere — the class stays automod-only). Codex ledger: 4 questions posted (one per flip, final heads), zero replies at merge time (Q-0258 merge-on-green); ledgered follow-ups — the role-family + counting-family `_unmapped` re-homes (TAKEN by #193, re-home lane record below) and the sim-gate value-drift checker (still open).
Re-home lane record (2026-07-11, one session, one PR — the #155 lane at family scale; folded at the 16:45Z heartbeat):
- #193 role-family (16) + counting-family (3) sweeps re-homed `_unmapped`→role/counting (merge `501e73f`, main HEAD; squash-merged on green 6-check CI, `report` red-by-design non-required): 19 goldens moved with the 2-line subsystem flip (bytes otherwise verbatim), gate 237/237 + report 276/465 + `_unmapped` 201 all CI-LOG-VERIFIED at the PR head `84f744c` (health line above). THE LAW THIS SLICE PROVED: re-homes over a ported row are FLIP-SIZED, not free — 15/16 role sweeps + 3/3 counting sweeps REDDED on first replay, every red a real port gap fixed PORT-side. What the reds forced: (1) TWO BAND-5 INVENTIONS RETIRED ORACLE-WINS — shipped unsetrole/removereactrole ack success UNCONDITIONALLY (role_cog.py `next((...), role_name)` fallback + bare DELETE / :705), the invented miss-branch copies came off and the two band-5 tests re-pinned (the D-0065 tests-enshrine-the-wrong-behavior lesson at handler scale); (2) rolecreator/rolesettings route to the SHIPPED hub (`ctx.invoke(self.roles_hub)`) and count_rules to the shipped static rules EMBED (new `counting.rules_card`, welcome status-card recipe); (3) six pending terminals became real handlers (roleinfo card verbatim from views/roles/role_info.py; createrole through the NEW RoleProvisioning port — twin records fake_http's `create_role` wire verb + mints the role id off the message-id allocator, the golden's `<msg:2>`; deleterole on the shipped feasibility `_REASONS` bytes; assignroles' emitter progress line + `_format_role_check_result` verbatim; debugroles' cached-role dump; refreshmembers' trap-11b capture-artifact literal); (4) reactroles rides the NEW MessageOps port (`get_message` + `add_reaction` wire pair); (5) the parity boot arms the capture-world GUILD DUCK (`_build_world_guild` → role.service.install_guild_source) and `role.service.subscribe(bus)` — the FIRST golden-pinned domain events outside a flip (`audit.action_recorded` + the NEW `role.lifecycle_changed` EventSpec, shared mutation_id, compat event_payloads +1 row) were silent until the harness subscribed what the live root's SUBSCRIBE_ROSTER already carried; (6) counting's `_require_channel` guard flipped to the copy-only ValidatorError form (the shipped plain byte, trap-22 class); (7) an A-5 fence collision — the port method could not be named the banned bare verb `create_role` (named `create_guild_role`; the wire string stays `create_role`). parity.yml: role's `table:role_thresholds` + `table:reaction_roles` covered-elsewhere rows RETIRED (in-directory now; checker agrees), role_grants citation re-pathed, ratchet `role: {events: 3, tables: 5, settings: 0}` (scratch-learned, trap 1). Sim gate ZERO new lock rows (both new cards component-less session panels). @codex question on the final head `84f744c` (comment 4947647358 — a TOCTOU/normalization seam in unsetrole's pre-read-then-op match); merged on green without waiting (Q-0258), no reply at merge time. Card `.sessions/2026-07-11-role-counting-rehome.md` + telemetry row rode the PR (Q-0194).
- REMAINING `_unmapped` MAP (201 goldens; the card's Session-idea section carries the probe list): re-home-shaped families over PORTED rows — ticket admin family (ticketblacklist*/ticketlimit/ticketpanel/ticketsetup — DONE by #200, wave-5 record below), word family (sweep_word* → cleanup), xp admin family (xpconfig/givexp/resetxp/xpimport), moderation/channel strays (modlogs/clearwarnings/slowmode/lock/unlock), starboard (6 — sweep_starboard/_ignore/_off/_panel/_selfstar/_unignore; needs a subsystem row first; this record's earlier count of 7 was wrong, corrected by directory listing at the wave-5 heartbeat); plus the games-family `_unmapped` strays that belong to the band-6 lane. Expect flip-sized port work per family (this slice's law), not free greens.
Wave 5 (parity-flips lane, 2026-07-11 — the re-home lane's continuation; folded at the 17:40Z heartbeat; both squash-merged on green 6-check CI, `report` red-by-design non-required; gate counts CI-log-verified at #200's head `f8b828f`, health line above):
- #197 trap-30 checker hardening (merge `2d65739`): check_sim_gate now REDS overlay-masks-manifest VALUE drift and `--write-baseline` REFUSES on drift — the #190 ledgered sim-gate value-drift follow-up CLOSED (playbook trap 30); 42 pre-existing masked drifts amended to manifest truth — 6 real reshape residues (games.world ×3 missing world_deathmatch/world_casino, server_management.hub ×3 missing help_back) + 36 seed-time value:0 placeholders; the wave-5 session card + telemetry row shipped here (Q-0194).
- #200 ticket-admin `_unmapped` re-home (merge `870a16c`, main HEAD): 6/6 goldens green onto the PORTED ticket row (ticket row now 12/12) — new ticket_blacklist + ticket_config stores (migration 0032), K7 ops `ticket.blacklist_add` / `ticket.set_limit`, new ticket.launcher + ticket.setup panels, and a NEW kernel seam: options-source-less SelectorKind.ROLE → native RoleSelect (wire type 6) in BOTH presenters (provider-fed ROLE selectors keep type-3). Zero new depth exemptions; zero compensators (allowlist stays EMPTY); no `_unmapped`/sweep_ticket* citations existed to retire.
- Wave-5 remaining `_unmapped` lane map (195 goldens after #200): word→cleanup (4, IN FLIGHT), xp-admin (4), moderation/channel strays (5), starboard (6 — needs a NEW subsystem row first). DO NOT touch: setup (PARKED, trap 17), quicksetup (BLOCKED, D-0030), games-family rows (band-6 lane). [UPDATE at the wrap-up fold: ALL FOUR families landed — records below.]
Wave 5 CONTINUATION + LANE COMPLETE (folded at the 2026-07-11T20:20Z wrap-up heartbeat — the OWNER WRAP-UP DIRECTIVE, archive-prep, received and EXECUTED: enders only, no new slices; all four squash-merged on green 6-check CI, `report` red-by-design non-required; every merge sha verified against `git log origin/main` at `323e304`, end counts CI-LOG-VERIFIED at main `a283f3f` in the health line above):
- #202 word family (4) re-homed `_unmapped`→cleanup (merge `2f4b2c3`; cleanup row 3/3→7/7): oracle copy + the shipped process-memory `_word_cache` ported for real (the #140 flip had pinned its leak as a literal); the `table:prohibited_words` covered-elsewhere depth exemption RETIRED — the family's own goldens (sweep_word_add et al.) carry the row in-directory now, exactly the #193 role-exemption retirement shape.
- #203 xp-admin family (4) re-homed `_unmapped`→xp (merge `ab1e916`; xp row 3/3→7/7): xpconfig panel + xpimport scan flow + resetxp one-shot ported; the `event:xp.reset` covered-elsewhere exemption RETIRED (sweep_resetxp carries the event verbatim) and `event:xp.level_up` re-pathed onto surviving citations (parity.yml at HEAD carries the re-pathed row only).
- #207 moderation/channel strays (5) re-homed `_unmapped`→moderation+channel (merge `2c62a09`; moderation 8/8→10/10, channel 1/1→4/4): the modlogs card + the NEW ChannelLifecycleService slice with the shipped `channel.lifecycle_changed` advisory event (sb/domain/channel/service.py, shared mutation_id). NOT the D-0030 create-channel wall — these goldens pin fake_http EDIT verbs (slowmode/lock/unlock), not channel creation; the wall stands only where a golden demands `create_channel`.
- #211 starboard subsystem BORN ported (merge `a283f3f`; 6 goldens re-homed onto the NEW row — 38/50): the shipped `!starboard` config group + config panel at byte parity, migration 0033, the full trap-15 birth kit PLUS the undocumented birth-kit step — a `verification/verified_live.yml` roster row (`starboard: unverified`) required by the V4 verified-live gate (now ledgered in the wave-5 retro lessons doc). ONE new depth exemption row `table:starboard_settings` under the EXISTING `guard-only-capture` class (D-0069 cited, not re-argued); ZERO new reason classes, ZERO new disposition classes, compensator allowlist EMPTY across the whole wave.
- WAVE-5 LANE COMPLETE (SESSION FULLY ENDED at this fold): 7 PRs (#197 checker hardening · #200 ticket-admin · #201 heartbeat · #202 word · #203 xp-admin · #207 mod/channel strays · #211 starboard birth), 25 goldens re-homed (`_unmapped` 201→176), 2 depth exemption rows RETIRED vs 1 added under an existing class (net −1), 2 new migrations (0032, 0033), 1 new kernel seam (RoleSelect type-6), the #193 flip-sized law confirmed on every family (zero free greens). Enders: session card flipped complete + retro lessons doc + this fold (the card's telemetry row shipped at #197 — one row per card, the #209 precedent).
- REMAINING `_unmapped` FAMILY HISTOGRAM (176 goldens, counted by directory listing at HEAD `323e304` — the successor's map): **btd6-family 62** (30 `sweep_slash_btd6_*` + btd6events 9 + btd6strat 9 + btd6ref 8 + btd6ops 6 — re-home candidates over the PORTED btd6 row; band-7-successor territory, the highest-leverage next slice), **fishing-game 20** + **mining-game 16** (sweeps over PENDING game rows — blocked until the band-6 games lane ports those rows; the singles below add more), **setup-family strays 3** (sweep_setup/setupadvanced/setupdescribe — PARKED with the setup row, trap 17), **slashes-index 1** (sweep_slashes), **mixed singletons ~74** (fun/general cards: eightball/fact/joke/motivate/quote/topic/trivia/poll/remind/botinfo/userinfo/serverstats/membercount…; channel-ops-adjacent admin strays: create/clone/move/rename/del/bulkcreate/bulkdelete/setlogchannel… — several sit near the D-0030 wall, verify the pinned wire VERB per golden before assuming; game-economy singles: shop/buy/sell/market/vault/craft*/build*/world*… — band-6 adjacent; ops/diagnostic strays: cog/coglist/loglevel/query_logs/recent_errors…). SUCCESSOR MAP: setup stays PARKED (trap 17, 2/8 goldens walled), quicksetup stays BLOCKED (D-0030 channel-ops), games-family pending rows (casino 2, creature 5, farm 1, fishing 2, four_twenty 1, games 2, inventory 1, mining 2, treasury 2) + the games-family `_unmapped` strays belong to the band-6 games lane, the btd6-family 62 belong to a band-7 successor, and the mixed singletons need per-family probes with the #193 flip-sized law priced in.
A-16 latent-defect note (surfaced at the #140 flip): the R2 SETTINGS dimension is dead code — `check_parity_depth.declared_surfaces` reads a `key` field per declared setting but the compiler emits `settings_key`/`name`, so the settings leg of every ratchet row is vacuously satisfied today; arming it is its own future sweep PR (several already-ported rows would red at once), not a per-flip fix (#140).
Disposition note: the session-lifecycle custom_id class (`<cid:N>`) was resolved by fidelity fix #117 `_mint_ephemeral` — no new disposition class needed (D-0061 analysis: ORDER 009 acceptance is scoped to the three ruled classes).
Housekeeping flag — RESOLVED (verified at the 19:50Z band-7 wrap-up fold): `.sessions/2026-07-10-ux-lab-parity-flip.md` now carries all required markers (grep at HEAD `0e7cacd0`: `💡` present, previous-session review present, `📊 Model:` present; the card was completed in the #159 window — `bootstrap.py check --strict` passes at HEAD). Original flag (games lane, 2026-07-11): the card failed the strict session-card check, missing the Session-idea and Previous-session-review sections; two games-lane workers tripped over it. No action remains.

band-6 games lane record (session 2026-07-10/11, continuous mode Q-0265) — SESSION COMPLETE: SEVEN PRs shipped, ALL merged on green with the 6-check ruleset; lane heartbeats #127 + #136 + this wrap-up:
- #114 the composition-parity invariant test (`tests/unit/invariants/test_composition_parity.py`) — BUG A CLASS-killer: both roots' registered ref sets are diffed in a clean subprocess, so unregistered pending terminals now fail CI for EVERY plugin; plus the blackjack/rps register-at-import fix (closing the prior session's parked note). The sweep exposed a 99-ref ensure-only burn-down across 8 more subsystems (ai 20, btd6 23, projmoon 10, mining 28, fishing 15, creature 1, proof_channel 1, role 1), ledgered in docs/ideas/ensure-only-registration-gaps-2026-07-10.md and gated by the test's `_KNOWN_ENSURE_ONLY` list (may only shrink); merge `385df11`
- #117 rps_tournament pending→ported — `!rps` quick-play playable end-to-end on a NEW session-view kernel seam (minted 32-hex component ids + invoker-lock in `sb/kernel/panels/engine.py` / `component.py`); merge `ec2bcf2`
- #120 blackjack pending→ported — `!blackjack` solo playable (hit/stand/double on an edit-in-place refresh seam); carried the ⚑ flag-13 ENCODING COMPLETION for `economy_audit_log.mutation_id` + `economy_balances` new-home (Q-0262.3 delegated class, decision record docs/parity/flag-13-disposition-2026-07-10.md amended, revert path recorded — reversible on paper, Q-0240); merge `4c1882b`
- #124 PvP on the wire for BOTH games — challenge → accept (escrow-in-one-txn) → play → resolve on one staged message; merge `2958224`
- #130 rps tournament orchestration + THE REACTION SEAM (slice 5) — the reaction/live-adapter seam decision landed as three plugin-agnostic kernel seams: reaction ingress (`sb/kernel/interaction/reactions.py` named-consumer registry + never-raise dispatch, live twin `sb/adapters/discord/reaction_feed.py` armed in main(), intent non-privileged; starboard/reaction-roles are now pure-domain successors), `RenderedPanel.self_reactions` (the shipped ✅ primer as a render-model field), and click-driven CHANNEL_ANCHOR opens as fresh sends. Full rps tournament on the wire: `!rpsregister` (golden-pinned embed + Join button + ✅ primer + the shipped `active_tournament=rps` flag row, migration 0027) → sign-up via button AND ✅ reaction on one audited body (`rps.tournament_enter`) → `!rpsstart` bracket (byes advance) → best-of match views → champion through `rps.tournament_payout` (flag cleared same txn) + "🏆" copy. 4 sweeps re-homed `_unmapped→rps_tournament` (rpsregister/rpsstart/rpsbot/rpsmatchup; sweep.rpsstart is corpus-order green by construction — the golden captured the oracle's own in-memory cross-case leak, ledgered). Merge `c3f7a02`
- #133 blackjack tournament orchestration (slice 6) — the #130 shape pure-domain (no new kernel seams): `!bjtournament` registration embed + Join/✅ sign-up on the reaction seam → `!bjstart` (per-player fee debits at launch, broke player silently skipped) → chips-space rounds (1000 start / flat 200 per round) on in-place-edit views → ranked 🏆 results + audited champion payout, flag row cleared same txn. PLUS SETTLE-ONCE for BOTH games: the free branch had a double-payout race (no escrow rows to consume — two racing champion resolutions could both pay the consolation, the #130 review question's race); the champion-payout leg's flag-row delete now runs FIRST as a check-and-set keyed on the atomic row-deletion count, retrofit onto `rps.tournament_payout` too, with direct-leg double-call tests pinning settle-once in both games. sweep.bjtournament + sweep.bjstart flipped honestly and re-homed `_unmapped→blackjack` with sweep.bjstatus. Merge `9923151`
- #138 copy-parity polish sweep (slice 7) — flipped `sweep.rpssettings`, the LAST red game-family golden (class settings-view-copy-deviation): the shipped `rps_settings` command verbatim (invalid-setting / invalid-mode / odd-positive-int guards + the "Setting `{setting}` updated to `{value}`." success copy, oracle-pinned via search_code fragments), the write riding the band-1 `settings.set_scalar` op onto declared persisted keys `rps_default_mode`/`rps_default_best_of` (§4.1 one-write-path, ZERO new kernel seams), golden re-homed `_unmapped→rps_tournament` (gating dir 6/6), verified green in ISOLATION and in corpus order. PLUS the RENDER-ONCE settle guard in BOTH games' tournament finish paths — an in-memory `state.settled` check-and-set (no await between) gates blackjack `_finish_tournament` and the rps champion branch, closing the #133 codex question's cosmetic half: the racing loser renders no second results/champion frame and never re-runs the payout op or the teardown (money was already settle-once by the flag-row delete count since #133). 4 new walking-skeleton tests; classify tables updated in both ideas ledgers. Merge `9ea90e9`
LANE END (SESSION COMPLETE): both games playable solo + PvP + tournament; ALL game-family goldens GREEN — rps_tournament 6/6, blackjack 5/5. Counts at main `9ea90e9`, verified from the main-push CI job logs: check_parity_depth OK — 49 subsystems (12 ported), 465 goldens; gate GREEN 33/33 across the 12 ported (gate job 86496962174, run 29134820004); report 45/465 green / 465/465 replayable (local serial run at the #138 head; CI report job 86496962191 red-by-design, non-required); units 1272 passed / 4 skipped in CI (run 29134819997 job 86496962169). Remaining game surface is UNPINNED polish only, ledgered with classes in docs/ideas/rps-tournament-remaining-surface-2026-07-10.md + docs/ideas/blackjack-remaining-surface-2026-07-10.md: rps `!rpsbot` deep flow, rps time-driven depth (reminder loop / timed close), rps solo edit-in-place, private round/match channels, blackjack natural-at-deal exact wire shape, blackjack PvP double-down, hub-button solo flow, autostart timer. NO golden-pinned work left in this lane — per the Q-0265 honesty guard the lane says so and stops: next wake either starts band-7 per the canonical order or takes owner direction. Session card `.sessions/2026-07-11-band6-games-session.md` + its telemetry/model-usage.jsonl row ship WITH this heartbeat (Q-0194: no card without its telemetry row).

band-7 builder lane record (session 2026-07-11, continuous mode Q-0265) — OPENED: Band 7 = Knowledge + AI, deterministic-first per docs/status/rebuild-completion-report-2026-07-09.md §5 step 9 (goldens ai 20, btd6 39, project_moon 10+1; live NL needs `ANTHROPIC_API_KEY` + `AI_ENABLED` — sb/spec/config.py:148/:166; deterministic provider in CI). The lane executed the band-6 wrap-up's next-wake ruling (Q-0265 honesty guard above: no golden-pinned game work left → canonical order says band 7). Four PRs merged on green so far — all three of the band's golden-pinned deterministic rows (btd6, project_moon+projectmoon, ai) are now PORTED — plus two heartbeats:
- #141 (merge `78509df`) — the pre-existing test-order flake from note (3) below KILLED: root cause was `register_ops()` in rps + blackjack re-arming only the CompoundOpSpecs + op markers after the sanctioned `clear_ref_table` seam — a manifest re-import re-fired decorators only for freshly-imported domain modules, so the CACHED ops modules' `@workflow` leg refs stayed unregistered and the first engine leg resolution died in `RefUnresolved: workflow:rps.record_tournament_open…` under non-canonical pytest selection (canonical order was green only because band5's teardown happened to re-arm every manifest). Fix per the ensure-only doctrine: `register_ops()` now ends with the idempotent `ensure_ops_refs()` (two files, one line of logic each, no allowlist touched); the PR body's ladder shows all permutation families green (app+authority+band6 in every order) and canonical `pytest tests/` unchanged.
- #144 (merge `596fb2d`, main HEAD) — btd6 pending→ported at BYTE parity: 39/39 goldens green (the shipped unified `!btd6` tree — 🐵 hub with persistent `btd6:*` ids, flat lookups, strat/ops/events groups incl. the bare-group silence and the dataset-derived round economy numbers); parity.yml `btd6: ported` through the A-16 door, ratchet row `btd6: {events: 1, tables: 3, settings: 1}`; gate 78/78 across 15 ported at HEAD `596fb2d` (health line above, gate job 86504916507 — the 15 includes the parity lane's sibling #143 xp flip). ENSURE-ONLY BURN-DOWN: all 23 btd6 handler refs register at import now — `_KNOWN_ENSURE_ONLY` measured 99 at `f97a4c7` (pre-#144) → 76 at `596fb2d` (remaining: ai 20, mining 28, fishing 15, projmoon 10, creature 1, proof_channel 1, role 1; NOTE the #144 PR body says "122 → 99" — off by the same 23; the file counts are the measured truth, matching the #114 ledger's original 99). ORDER 004 walking-skeleton LIVE-DRIVE done for real: real composition root booted (27 migrations, health 127.0.0.1:18099), gateway READY as Galaxy Bot#6724, four real `!btd6` posts in Superbot Admin #general through the live prefix entrypoint (message links in the PR body — round-3 card 1525336889748361300, status, hub with the 8 `btd6:*` component ids + nav:help, ops refusal) + the CI-runnable skeleton tests/unit/band7/test_band7_btd6_walking_skeleton.py. NEW DEPTH REASON CLASS `modal-driven` minted for `table:btd6_strategies` (the write sits behind the shipped `/btd6 strat submit` Discord modal — an input kind the corpus schema structurally lacks; cases reconstruct command/slash/click only): shipped flagged-for-review but record-less — decision record D-0063 rides THIS heartbeat and is the required ledger entry. @codex question posted on the final head (comments 4941993818 + 4941996704, head `94f59c1` — the §3.4 scheme-token fence relaxation ordering hole); codex answered with a verdict ("Didn't find any major issues", comment 4942006230, reviewed `b0377b6` — the pre-compat-pin head) and comment 4942002321 claiming a regression-test PR that is NOT visible in this repo; the ordering-hole question itself stays the lane's open question.
- PARKED from #144 (ledgered unpinned deviations, no golden drives them): freeplay MOAB scaling (effective RBE rounds 81+ — RBE renders the wiki base), the resolver's maps/modes matching, the boss-fight estimator (`!btd6 estimate`), the CT-team guided-set flow, `!btd6 ops seed-data` — all honest pending/successor terminals. HYGIENE-FIX CANDIDATE: `tools/check_parity_depth.py --write-ratchet` rewrites parity.yml via `yaml.safe_dump` and destroys every comment incl. the depth-block rules (the tool itself prints "comments are lost"; flip PRs hand-restore the header — a comment-preserving writer is the fix).
- Testing-report row 9 (band 7) deliberately NOT touched by this heartbeat: the convention check shows rows update at the band's own TESTING pass (replay + live legs per the 9-step order), not at port slices — band 6 shipped its entire seven-slice port lane with row 8 still "— pending".
- #148 (merge `f2dcf0d`) — project_moon + projectmoon pending→ported at BYTE parity, BOTH rows flipped in one slice: 11/11 goldens green (10 prefix sweeps + the 1 `sweep_slash_pm` slash golden — the shipped `!pm` LimbusBrowseView with the 8 session-minted buttons in rows 5+3, the GAME_COLOR 10181046 overview/kind/entry/origins cards with the verify-at-ingest provenance footer, the two greyple 10070709 FOOTER-LESS miss cards incl. the shipped `rstrip("s")` "No statuse matches" byte, and `/pm` as a real ephemeral type-4/flags-64 slash twin); parity.yml both rows through the A-16 door, ratchet rows `project_moon: {events: 1, tables: 2, settings: 0}` + `projectmoon: {events: 0, tables: 0, settings: 0}`, no new exemption row or reason class; the invented 7-button hub + lookup modal deleted (compat pin sheds `projmoon.lookup_form`, + slash `pm`, same-PR). Ensure-only burn-down: all 10 projmoon refs register at import — `_KNOWN_ENSURE_ONLY` measured 76 at `596fb2d` → 66 at `f2dcf0d` (the PR body says "99 → 89" — the same +23 offset the #144 body carried; the file counts are the measured truth). ORDER 004 live drive: real root booted, gateway READY as Galaxy Bot#6724, four real `!pm` posts in Superbot Admin #general (message links in the PR body, incl. the footer-less greyple miss live) + tests/unit/band7/test_band7_projmoon_walking_skeleton.py. Ledgered unpinned deviations: shipped buttons edited in place vs result-lane cards; shipped view was public/180s vs the port's INVOKER audience — the #148 codex question (comment 4942099573, split-audience-by-surface) got only a usage-limits reply (comment 4942100526).
- #151 (merge `1bea65d`, main HEAD) — ai pending→ported at BYTE parity, the band's largest row: 20/20 goldens green, ALL fixed-in-PR (the shipped AIPanelView hub on verbatim persistent `ai:*` custom_ids + standard nav row, the status/diagnostics/providers/routing/readiness/policy/support-report embeds — 17-task routing table in shipped enum order, 7-finding readiness chain, dual dry-run policy trace with the restored `no ai_guild_policy row → deny GUILD_NOT_CONFIGURED` byte —, the shipped SubsystemSettingsView ai page, the ✅ forget byte over the K10 conversation buffer, the ephemeral `/aimenu` slash twin, and the 7 EMPTY grouped-slash goldens green by the harness drop rule below); ratchet row `ai: {events: 1, tables: 2, settings: 0}`, TWO depth exemptions under EXISTING classes (`table:ai_review_log` env-keyed-integration, `table:ai_answer_presets` covered-elsewhere). TWO REPLAY-HARNESS SEMANTICS CHANGES rode this PR — they affect EVERY subsystem's replay and are the flagged-to-codex risk (question comment 4942514348; only a usage-limits reply, comment 4942514698): (1) `sb/adapters/parity/boot.py` now DROPS unknown-name slash interactions exactly like the old harness's discord.py ConnectionState did (boot.py:479-485 — an unregistered app command never reached dispatch; the empty grouped `sweep_slash_ai_*` goldens pin zero calls), (2) `sb/adapters/parity/cases.py` `load_replay_cases` restored to the capture's CURATED-FIRST order (the trailing id-sort replayed curated `xp.chat_award` AFTER the ai sweeps, starving `sweep_ai_forget`'s conversation buffer). Ensure-only burn-down: all 20 ai refs register at import — `_KNOWN_ENSURE_ONLY` measured 66 at `f2dcf0d` → 65 at `531f14ee` (the parity lane's #145 pruned proof_channel) → 45 at `1bea65d` (verified by hand at each sha; remaining: mining 28, fishing 15, creature 1, role 1). Gate 112/112 across 19 ported in CI on the PR head `16f9151` (run 29140265573 gate job 86511895348) and again at main `1bea65d` (run 29140344074 gate job 86512108907). ORDER 004 live drive: real root + gateway READY as Galaxy Bot#6724, six real posts in Superbot Admin #general (`!aimenu` hub with the live `ai:*` ids, `!ai status/policy/settings/forget/support-report` — message links in the PR body) + tests/unit/band7/test_band7_ai_walking_skeleton.py. KEY-GAP HONESTY: `ANTHROPIC_API_KEY` absent + `AI_ENABLED` off in the agents' environment (verified during the live drive) — no real NL model call attempted; every shipped surface is the deterministic operator surface (OWNER-ACTION 5 below).
- PARKED from #151 (ledgered, no golden drives them): (1) CLI-order artifact — a band7 walking-skeleton Harness boot pollutes `tools_catalogue` for a later-listed `tests/unit/ai/test_k10_orchestration.py` under non-canonical pytest selection (reproduces with the merged projmoon skeleton alone; canonical `pytest tests/` green — successor hygiene slice, the #141 family); (2) the diagnostics card's `setup_advisor_provider` serves the shipped FALLBACK (`ai_default_provider`) instead of the legacy `SETUP_ADVISOR_PROVIDER` env override — NOTE the module docstring says the override "is not yet an RC-10 Config field" but `sb/spec/config.py:170` HAS declared `SETUP_ADVISOR_PROVIDER` since ~#90; the real parked work is wiring the card read to it (byte-identical wherever the var was never set, i.e. the goldens' state); (3) the shipped ai policy/behavior/tools chooser PAGES + the settings page's edit/reset select widgets land on declared pending terminals — pending the policy/settings-mutation slice; (4) the `!aireview` sweep family (11 goldens counted in parity/goldens/_unmapped/sweep_aireview*.json) stays in `_unmapped` until a re-home slice.
- NEXT per the lane plan: the ai review-loop/presets/aireview-re-home deterministic slice (dispatched in parallel — no PR/branch visible at that heartbeat; UPDATE folded at the 06:35Z heartbeat: LANDED as #155, merge `4c8c5b0`, 11/11 sweep_aireview* goldens re-homed `_unmapped`→ai), then the policy/settings-mutation slice, then the live-NL leg (verify_and_regenerate_once / NL shell / routing — owner-key-gated, OWNER-ACTION 5). Sibling merges this window: parity lane's #145 (proof_channel, `531f14ee`) + kit lane's #150 (v1.9.0, `d653ba1` — NOT a parity flip; the kit-line record above). Health-line counts are measured at `1bea65d`; future counts must re-verify against parity.yml at their own HEAD.
- #155 (merge `4c8c5b0`) — the `!aireview` review-loop + presets surface at BYTE parity, the sweep_aireview family re-homed `_unmapped`→ai: 11/11 goldens green, ai now 31 goldens total (verified at `0a29d37`: `parity/goldens/ai/` counts 31 with 11 aireview files, zero sweep_aireview* left in `_unmapped`); the `table:ai_answer_presets` covered-elsewhere depth exemption REMOVED (parity.yml at HEAD carries ONLY `table:ai_review_log` under `env-keyed-integration`, its reason rewritten to name the now-pinned empty-state read goldens); ratchet row grew `ai: {events: 2, tables: 4, settings: 1}` (was {1,2,0} at #151 — read from parity.yml at HEAD). The #151 parked item 4 CLOSED.
- #156 (merge `179dfb2`) — k10 test-order-flake fix, TEST-ONLY (the #141 doctrine family): the `tests/unit/ai/test_k10_orchestration.py` suite now isolates the AI registries in BOTH directions (tools catalogue + profiles + answer workflows) — the #151 parked item 1 (band7-skeleton-boot pollutes `tools_catalogue` under non-canonical pytest selection) CLOSED.
- #160 (merge `0a29d37`, main HEAD) — ai policy/settings-mutation surface (band 7 slice 5): the three shipped chooser PAGES land (`ai:policy` / `ai:behavior` / `ai:tools` → the AI Policy / AI Behavior / AI Tools & Workflows embeds at oracle bytes, incl. the Behavior→Advanced punt to the raw policy page and the tools page's fresh-guild `Current` decoration) and the `!ai settings` page's S6/S7 edit/reset selects RUN the shipped dispatch — bool single-click toggle, numeric-presets page (the shipped rosters RESTORED onto the four int scalars, an omission vs @7f7628e1), enum select page, reset-to-default — EVERY write one audited `settings.set_scalar` op (the §4.1 sole-writer lane, upsert + central audit in one txn, `settings.changed` after commit; compensator allowlist stays EMPTY; K6 floor DB-verified refusing a plain user). No golden drives any click on this surface (the #151 ledger) — gate GREEN 143/143 across 22 at the final head `e8bbb66` (CI runs 29144436250/29144436275/29144436272, all 11 required-class checks green) AND at main `0a29d37` (health line above); ratchet + compat pin untouched; unit 1336/2 local at `e8bbb66`, 1334/4 in CI at `0a29d37`. Ensure-only allowlist 45 measured at HEAD (counted by hand: mining 28, fishing 15, creature 1, role 1 — zero ai rows; the #151 note's `role` ref persists). ORDER 004 live drive: real root + gateway READY as Galaxy Bot#6724, chooser clicks editing IN PLACE + real `settings` rows written/reset/cleaned in Superbot Admin #general (message links + DB reads in the PR body) + the 14-test CI skeleton. CODEX RETURN (capacity UP again this window — review 4676953047 on head `0c95e97` at 07:10Z): ONE REAL P2 — widget-written scalar raws (e.g. `'15'` for ai_memory_window_minutes) rendered `**invalid**` on the settings page because explicit rows were compared against `allowed_values` uncoerced — FIXED IN-PR (coerce-before-validity, the shipped `resolve_setting` posture, + regression test at `e8bbb66`; the all-defaults golden never reaches the branch, `sweep_ai_settings` byte-identical); ONE P3 TAKEN — the presets reply's `(was …)` prior now derives from the write leg's IN-TRANSACTION `LegOutcome.before` (concurrent-click accuracy); the toggle-fold suggestion DECLINED + ledgered (would change the band-1 generic op contract; the shipped pipeline had the same read-then-transact shape). Q-0120 PHANTOM documented (PR comment 4943535922): the codex reply (comment 4943407864) claimed it "committed changes … with commit `64d607a` and created a follow-up PR" — the commit exists NOWHERE in the repo (git cat-file across all refs — re-verified at this heartbeat) and no such PR exists; no action taken on the claimed artifacts. Parked honestly (declared pending terminals, named successors): the free-form text modal editors (`ai_default_model`, `ai_guild_instruction_profile`) + the presets Override… number modal — the live root's modal-submit lane is dormant by design (sb/app/main.py step 10c), so they port with the MODAL-ARMING slice; the chooser scope pickers (policy channel/category/role/preview/list, behavior scope+preset, tools profiles) — the policy/orchestration-mutation slices.
- #165 (merge `62d850f`) — the live MODAL-SUBMIT lane arms (band 7 slice 6, D-0054's wire-type-5 successor; decision record D-0066): step 10c's dormant half goes live — `arm_component_feed`'s on_interaction listener consumes EVERY wire-type-5 submit (`sb.confirm:` keeps the S9b typed-phrase check; every other id dispatches through the frozen modal adapter), G-10 declared ModalSpecs materialize as real `discord.ui.Modal` objects (sb/adapters/discord/modal_view.py — before this, `open_modal` sent the raw dataclass and a failed open FELL THROUGH to a field-less dispatch; `modal_issued` now sets before the await), and a kernel modal-args stash carries parameterized session context across the static form wire bytes. On the armed lane the #160-parked remainder ships: the ai free-text editors (`ai_default_model` / `ai_guild_instruction_profile` via the shipped TextSettingModal twin behind an intermediating Edit… page — a selector pick is AUTO-deferred, D-0054 posture) + the presets page's Override… (NumberSettingModal twin), every write through the ONE audited `settings.set_scalar` op with the shipped acks byte-for-byte. CODEX P2 taken in-PR: the stash first keyed `(modal_id, user)` last-open-wins — one user's concurrent opens of the same form from different pages could cross-write; re-keyed per originating message (the submit interaction's `message` IS the opening component's message — zero wire change; D-0066 carries the full analysis). Goldens untouched (gate GREEN pre/post), depth ratchet untouched, compat pin +2 G-10 modal roots, ensure-only stays 45. D-0063/D-0064 deletion path NOT yet satisfied — the exemptions' burden was corpus EXPRESSIBILITY, so deleting them still needs the replay-case `modal` step vocabulary + type-5-carrying goldens + the btd6 strategy form itself (named successors in D-0066).
- #177 (merge `f986177`, main HEAD) — the ai policy SCOPE PICKERS go live (band 7 slice 7; decision record D-0070 — NOTE the PR's first commit body says "D-0067" by typo, the committed record in docs/decisions.md is D-0070): the #160-parked policy-mutation slice. The policy chooser's five scope pickers (Channel / Category / Role / Effective-policy preview / List overrides) ship on the armed component+modal lanes; migration 0028 lands the shipped migration-039 typed override tables NAME_STABLE (`instruction_profile_id` a plain nullable column — its FK target ports with the behavior-preset slice); every scoped write is ONE audited op (`ai.set_channel_policy` / `ai.set_category_policy` / `ai.set_role_policy` — scoped upsert + bump_generation in one transaction, K7 central audit, advisory `ai.policy.*_changed` after commit; the per-guild generation counter rides a `guild_settings` KV row per D-0025, first write mints 1 and reproduces the shipped minted-row deny flip GUILD_NOT_CONFIGURED→AI_GLOBALLY_DISABLED); the widened K10 bundle reader (`_policy_bundle_with_overlays`, installed over the band-1 reader) consumes the rows FAIL-SAFE (store miss degrades to guild-level, DB-free replay keeps band-1 behavior). Channel pickers ride the Discord-NATIVE type-8 channel select (the #167 lane); category/role render as engine string selects over an installable guild-scope-roster port CAPPED at 25 (named successor: native category/role select lanes). SIX depth exemptions (3 tables + 3 events) under the EXISTING `modal-driven` class, D-0063's structural test re-argued per row; ratchet untouched; compat pin +2 modal roots +3 event payloads. CODEX TRIAGE on the PR — 2 taken / 1 declined, each Q-0120 source-verified: P2 REAL and fixed in-PR (with typed category overlays live, the `!ai policy` command path skipped category rows — build_policy_embed received category_id=None; the invoking channel's category now rides through, golden bytes unchanged + regression-pinned), P3 taken (the live string-select materializer dropped RenderedComponent.disabled — an empty roster rendered a clickable '—' filler), P3 DECLINED with citation (op-leg negative-int validation: the shipped seam validated ONLY mode on the channel/category setters — the >=0 check existed on guild/role alone, and the legs mirror the seam exactly). PARKED with honest pending terminals: the behavior preset pickers (need the preset catalog + ai_instruction_profile + apply_preset seam) and the tools profile pickers (need ai_orchestration_mutation + the orchestration_profile columns) — the behavior-preset / orchestration-mutation slices.
- BAND-7 LANE WRAP-UP (2026-07-11 ~12:45Z heartbeat) — SESSION COMPLETE: 12 merged PRs this session, all squash-merged on green with the 6-check ruleset (#141 flake fix `78509df` · #144 btd6 39/39 `596fb2d` · #147 heartbeat1+D-0063 `9f83ea1` · #148 project_moon 11/11 `f2dcf0d` · #151 ai 20/20 `1bea65d` · #153 heartbeat2+OWNER-ACTION 5 `119ecce` · #155 aireview re-home 11/11 `4c8c5b0` · #156 tools_catalogue isolation `179dfb2` · #160 ai choosers+settings widgets `0a29d37` · #162 heartbeat3+ORDER 012 `dd626b2` · #165 modal lane `62d850f` · #177 policy scope pickers `f986177`). COUNTS RE-MEASURED at `f986177` (main HEAD, this wrap-up's own verification): gate GREEN 175/175 across 32 ported, 32/17 ported/pending, check_parity_depth OK 49 subsystems / 465 goldens (main-push golden-parity run 29152481842 gate job 86544105424, CI-log-verified); report 212/465 green, 465/465 replayable, all four band-7 rows green-ported in the table (ai 31/31, btd6 39/39, project_moon 10/10, projectmoon 1/1 — report job 86544105417 same run); units 1372/4 in CI (run 29152481834 job 86544105246), 1374/2 local canonical; parity.yml hand-count 32 ported / 17 pending; `_KNOWN_ENSURE_ONLY` hand-count 45 = mining 28 / fishing 15 / creature 1 / role 1 (unchanged since #160 — the remaining refs belong to unported bands, zero band-7 rows). BAND-7 REMAINING MAP: behavior-preset slice + orchestration-mutation slice (parked per D-0070; UPDATE folded at the 15:45Z heartbeat: BOTH LANDED — #185 D-0071 + #187 D-0072, continuation record below); replay modal vocabulary + btd6 strategy form goldens (D-0066 successors); review-channel poster (parks with NL arming); live-NL leg owner-key-gated (OWNER-ACTION 5 stands — ANTHROPIC_API_KEY + AI_ENABLED absent); testing-report row 9 stays pending until the band's own live-testing pass; #144/#148 domain parked items unchanged. Session card `.sessions/2026-07-11-band7-builder-session.md` + its telemetry/model-usage.jsonl row ride THIS wrap-up PR (Q-0194: no card without its telemetry row). Next wake continues the band-7 remaining slices per the map above or takes owner direction.
- CONTINUATION (Q-0265 continuous mode — the lane REOPENED post-wrap; folded at the 15:45Z heartbeat): TWO more slices merged on green. #185 (merge `168ef8080347905766893fd92ae3be1ec2ebbc4c`) — the behavior-preset slice (the #177-parked behavior preset pickers land): decision record D-0071, NEW `seeded-catalog` depth reason class, migration 0030. #187 (merge `d9c6b35a1c08e8ab0e4523880a807ea07bcad23a`, main HEAD) — the orchestration-mutation slice (the #177-parked tools profile pickers land): decision record D-0072, migration 0031, the tools profile pickers + K10 resolver consumption of the orchestration overlay; codex returned 3 line findings on the PR — all verified and fixed in-PR — plus 1 declined with oracle citation, then the pool hit its usage limit tonight (future reviews may not arrive; blockers line above). POST-#187 COUNTS (local serial ladder on real Postgres at `d9c6b35`, verified by the #187 worker): units 1405 passed / 2 skipped; gate GREEN 218/218 across the 37 ported; report 256/465 green, 465/465 replayable, 37/49 ported — both slices mint zero goldens (the window's golden movement is the parallel parity-lane session's #183/#186/#188–#191). BAND-7 REMAINING MAP (post-#185/#187): replay modal vocabulary + btd6 strategy-form goldens (the D-0066 successors) — IN PROGRESS in this lane now; the routing-matrix picker follow-up slice (the D-0071 re-scope); parked domain items unchanged (freeplay MOAB scaling, resolver maps/modes matching, boss estimator, CT-team flow, `!btd6 ops seed-data` terminals); hygiene (the `--write-ratchet` comment-destruction fix, conftest after-only reset in tests/unit/ai); review-channel poster (parks with NL arming); live-NL leg BLOCKED on OWNER-ACTION 5 (ANTHROPIC_API_KEY + AI_ENABLED still absent); testing-report row 9 stays pending the band's own live-testing pass.
- CONTINUATION UPDATE (folded at the 18:51Z heartbeat): THREE more slices merged on green. #194 (merge `4024624d`) — the D-0066-successor modal-vocabulary slice, decision record D-0073: the replay corpus grew the `modal` step kind + the first 2 MINTED goldens (btd6_strategy_form_submit + btd6_strategy_form_submit_minimal, corpus 465→467), the btd6.strategy_form modal + submit handler on the audited `btd6.submit_strategy` op, PanelActionSpec.reply_visibility grammar growth, the D-0063 `modal-driven` exemption row DELETED (btd6 ratchet tables 3→4), and #187's seeded-catalog 0029→0030 typo fixed; codex P2 (kernel-band golden gap) verified REAL but DECLINED with doctrine citation (flag-13/A-16-clause-3 — the kernel-band mint is the named follow-up); LEDGERED pre-existing latent bug: sb/domain/btd6/service.py::_run_op reads `result.ok` / flat `result.after` (unreachable via golden-driven lanes; guard recipe in the session card). #199 (merge `029b4001`) — the hygiene slice (the #144-ledgered `--write-ratchet` comment-destruction fix + the tests/unit/ai conftest consolidation): `--write-ratchet` now splices ONLY the depth.ratchet block — parity.yml header/comments preserved byte-for-byte, missing-key = loud SystemExit, 9 unit pins; 3 codex P2s (interstitial comment, blank+sibling comment, CRLF) all verified real and fixed in-PR; tests/unit/ai consolidated to one dir-wide after-only conftest reset (orchestration keeps a slim documented pre-leg — pure after-only empirically regresses #156, 3 failures reproduced); advisory surfaced for the lane: pre-existing `owner-ask-wall-unrecorded` (the OWNER-ACTION 3 wall is not in docs/CAPABILITIES.md — out of scope, ledgered here). #204 (merge `f82ac804`, main HEAD) — the routing-matrix picker slice (the D-0071 re-scope follow-up TAKEN), decision record D-0074: oracle views/ai/routing/matrix.py ported WHOLE (build_routing_matrix_embed over a verbatim resolve_policy dry-run, the behavior_matrix_picker panel, the chooser button live), the LAST chooser pending terminal retired (chooser_scope_pending/_SCOPE_COPY deleted — every views/ai/* chooser surface now live); ZERO schema growth; codex ZERO findings. POST-#204 COUNTS (measured by the #204 worker at merge `f82ac804`, health line above): units 1435/2 skipped; gate GREEN 253/253 across the 37 ported; report 291/467 green, 467/467 replayable, 37/49 ported (gate movement 245→253 is the parallel lane's #202 word family + #203 xp-admin family). BAND-7 REMAINING MAP (post-#194/#199/#204): parked domain items (freeplay MOAB scaling, resolver maps/modes matching, boss estimator, CT-team flow, `!btd6 ops seed-data` terminals — a worker is on the next one now); the kernel-band minted golden set (the #194 named follow-up — would flip kernel.status); the CommandSpec modal facet (slash-opens-modal ingress); review-channel poster (parks with NL arming); live-NL leg BLOCKED on OWNER-ACTION 5 (unchanged); testing-report row 9 pending the band's own live-testing pass.
- CONTINUATION SESSION COMPLETE (folded at the 19:50Z WRAP-UP heartbeat — the OWNER WRAP-UP DIRECTIVE, archive-prep, received and EXECUTED: enders only, no new slices; anything uncommitted is lost at archive): ONE more slice merged on green first. #208 (merge `0e7cacd0`, main HEAD) — btd6 resolver maps/modes matching, the first #144 parked DOMAIN item retires its oracle_cards docstring ledger line in the same diff (typed MapEntry/ModeEntry, the shipped `_match_terms` discipline incl. the common-word quirk, deterministic_answer's shipped precedence, test-intent Maps/Modes rows live, golden-pinned empty state byte-identical; oracle refs d647b2e9→7349c8a7, trap 24; ZERO manifest/golden/ratchet movement); codex zero-findings review at the final head — the lane's precedence-hijack question (comment 4948465613: "hard" token-matching can outrank a round question, carried as shipped) drew only the boilerplate no-issues reply. FINAL COUNTS (measured by the #208 worker at the merged tree `0e7cacd0`, local serial ladder on real Postgres — trap 25): units 1458 passed / 2 skipped; gate GREEN 258/258 across the 37 ported; report 295/467 green, 467/467 replayable, 37/49 ported; corpus 467 = 465 imported + 2 minted; gate/report movement 253→258 / 291→295 is the parallel lane's #207 (this lane's #199/#204/#208 minted zero goldens). THE 8 CONTINUATION PRs, all API-verified merged at wrap-up: #185 `168ef808` (D-0071) · #187 `d9c6b35a` (D-0072) · #192 `14e5037` (heartbeat) · #194 `4024624d` (D-0073) · #199 `029b4001` (hygiene) · #204 `f82ac804` (D-0074) · #205 `81b04bc` (heartbeat) · #208 `0e7cacd0`; no open PRs, no stray remote branches, no control/claims/ files from this lane (the open #196/#206 are codex doc-review PRs, another actor). ENDERS shipped on the wrap-up PR: lane session card `.sessions/2026-07-11-band7-continuation-session.md` + telemetry row (Q-0194; the 6 per-PR cards already existed and were NOT duplicated), retro lessons doc `docs/retro/lessons-2026-07-11-band7-continuation.md` (mergeable-dirty-first diagnosis, trap-24 churn ledgering, codex calibration, worker-stall resume, the #199 tool-fix-retires-workaround lesson, the A-16-clause-3 minted-golden kernel-strip doctrine), the OWNER-ACTION 3 ruleset wall appended to docs/CAPABILITIES.md (the #199-surfaced `owner-ask-wall-unrecorded` advisory CLOSED — `bootstrap.py check --strict` green with zero advisories at the wrap-up head), the stale ux-lab housekeeping flag groomed (card complete at HEAD). BAND-7 REMAINING MAP FOR A FRESH SUCCESSOR (post-#208): freeplay MOAB scaling (the next-smallest parked domain item — needs the oracle `bloon_rbe_at_round` spawn-tree reconstruction) · boss estimator · CT-team guided-set (NK-live-gated) · `!btd6 ops seed-data` terminals · the kernel-band minted golden set (#194's named follow-up; would flip kernel.status) · the CommandSpec modal facet (slash-opens-modal ingress) · review-channel poster (parks with NL arming) · live-NL leg BLOCKED on OWNER-ACTION 5 (ANTHROPIC_API_KEY + AI_ENABLED absent, unchanged) · testing-report row 9 pending the band's own live-testing pass · the oracle maps/modes `btd6_facts` DB-grounding pass + `!btd6 map|mode` command surfaces (no corpus capture exists, trap-28 check in the #208 card) ride D-0046. Oddities carried for the successor: the pre-existing `_run_op` latent bug (sb/domain/btd6/service.py — guard recipe in the #194 card), trap-24 oracle default-branch churn (latest LEDGERED ref 7349c8a7; a post-#208 probe reported `b2b7fe0c`, carried as reported — the oracle repo is outside the wrap-up seat's repo scope), the codex calibration ledger (line-anchored findings real all session: 3 fixed + 1 declined #187, 1 verified-declined #194, 3/3 fixed #199, zero #204/#208; top-level claims phantom-prone — Q-0120 stands; one mid-evening usage-limit window, recovered).

live-drive grants (§0.4 readiness — LIVE-VERIFIED via the #109 live leg):
- test app token: PRESENT in session env (`DISCORD_BOT_TOKEN_PRODUCTION` — connects as the test bot; env var NAME only, no value recorded)
- privileged-intent env flags: `SB_INTENT_MSGCONTENT_OK` / `SB_INTENT_MEMBERS_OK` ARE present (=true) in env — zero degrade markers at gateway READY
- prefix conflict: RESOLVED owner-side (flag 15) — old SuperBot removed from test guild 1350952413737259151 (REST 404 Unknown Member); `!` prefix now uncontested
- app-command sync: `SB_APPCMD_SYNC_GUILD_ID=1522099141671653417` now targets the new owner guild "Superbot Admin" (12 commands synced there; test guild retains the earlier 13) — deserves an owner confirm-of-intent line but is NOT a blocker
- env fact: `HEALTH_HOST` default `::` cannot bind in the build container (no IPv6); `HEALTH_HOST=127.0.0.1` required
- test guild id: present in docs (1350952413737259151)
- live-drive leg: DONE (#109, merge `b813324`)

⚑ OWNER-ACTION 2 — create the hello-plugin repo (flag 18a)
WHAT: Create one new empty GitHub repository named superbot-plugin-hello.
WHERE: https://github.com/new (owner account menno420).
HOW: name `superbot-plugin-hello`, public, no template — agents then move `examples/superbot-plugin-hello/` verbatim (pin hashes the manifest, not the repo — no re-pin needed).
WHY-IT-MATTERS: it proves a game plugin can live in its own repo, the architecture the mining/exploration game Projects will copy.
UNBLOCKS: ORDER 002 → done; the game Projects' reference pattern.
VERIFIED-NEEDED: attempted repo-create with the integration token — GitHub returns 403 on repo creation for this token class (recorded in docs/retro/project-review-2026-07-09.md §2 item 2); only the owner account can create repos.

⚑ OWNER-ACTION 3 — kill the branch-update merge dance
WHAT: Change the repo merge settings so PRs stop needing a manual "update branch" click before merging.
WHERE: github.com/menno420/superbot-next → Settings → Rules/Rulesets (or Settings → General → merge queue).
HOW: enable the merge queue, or drop the require-up-to-date rule for `docs/**` + `control/**` paths.
WHY-IT-MATTERS: every session lost time to the update-branch dance and one session's tail was stranded on it (PRs #86/#87), and the same dance triggered a rate-limit stall.
UNBLOCKS: unattended session wrap-ups; less API traffic.
VERIFIED-NEEDED: repo Settings/Rulesets are admin-only — agent tokens can read but not modify rulesets (the #86/#87 stranding is the captured evidence of the wall in effect); an agent re-verified it cannot edit the ruleset when un-stranding those PRs.

⚑ OWNER-ACTION 4 — RETIRED 2026-07-11 (was: configure Codex for this repo — environment + code-review capacity)
RESOLVED owner-side, both halves: the connector produced a FULL substantive review on #138 — comment 4941074976 (line-anchored walkthrough of all four changes; it ran the PR's 4 new tests itself, ✅) + verdict comment 4941104857 "Codex Review: Didn't find any major issues. Nice work!" (reviewed commit `77f9345`) — so the environment exists AND code-review capacity is restored. Historical evidence trail (kept for the record): "create an environment for this repo" on #117 (comment 4939224869) and #120 (comment 4939692286); "reached your Codex usage limits" on #124 (comment 4940004903), #125 (comment 4940095408) and #130 (comment 4940441924); no text reply on #114 (comment 4938835129) and #133 (comment 4940819831). The Q-0259/Q-0120 return path is now LIVE; the standing rule continues unchanged — @codex question on every substantive PR's final head, merge on green without waiting (Q-0258). REGRESSION NOTE (2026-07-11 late-night, folded at the 05:01Z heartbeat): the CAPACITY half is back at the wall — the connector answered BOTH band-7 questions with usage-limit replies: #148 comment 4942100526 ("You have reached your Codex usage limits") and #151 comment 4942514698 ("…usage limits for code reviews"). The environment half stays resolved (the #138 full review proved it exists), so this item stays RETIRED rather than reopened — but the owner may want to know the credit pool drained again the same day it was declared restored; until it refills, substantive questions keep landing per the standing rule and simply queue unanswered (the #148/#151 questions are in the open-questions note below). FLAP UPDATE (2026-07-11 morning, folded at the 06:35Z heartbeat): the pool REFILLED within the hour — one more usage-limit reply on #152 (comment 4942577962, 05:08Z), then FULL substantive P2 reviews on #154 (review 4676723779, 05:54Z, 2 P2s, final head `43b9b44`) and #157 (review 4676836079, 06:31Z, 1 P2, final head `f8532ef`). Item stays RETIRED; the capacity half is best described as FLAPPING — reviews arrive post-merge when the pool happens to be up (findings ledgered in the wave-5 records above).

⚑ OWNER-ACTION 5 — provide the band-7 AI key envelope (live-NL leg)
WHAT: Put a real `ANTHROPIC_API_KEY` in the agents' session environment and turn `AI_ENABLED` on for the live-drive guild.
WHERE: the Claude Code session environment the builder lanes run in (same place `DISCORD_BOT_TOKEN_PRODUCTION` already lives) — config seam sb/spec/config.py:166 (`ANTHROPIC_API_KEY`, SecretSpec, activation_link `ai.on_when_keyed`) and :148 (`AI_ENABLED`, default False, dormant posture).
HOW: export both vars into the session env (key value never recorded in-repo — env var NAME only, the §0.4 grant convention); optionally scope `AI_ENABLED=true` to the Superbot Admin live-drive guild posture.
WHY-IT-MATTERS: band 7's remaining legs — the live NL shell (mention→answer), `verify_and_regenerate_once`, and live routing — need a real model call to produce live-drive evidence; deterministic surfaces shipped regardless (#151), so this gates EVIDENCE, not code.
UNBLOCKS: the band-7 live-NL slice's ORDER 004 live-drive leg; until provided, those legs can only ship deterministic-provider tests (A-17 posture), not live-drive proof.
VERIFIED-NEEDED: verified during #151's ORDER 004 live drive — `ANTHROPIC_API_KEY` absent and `AI_ENABLED` off in the session env (PR #151 body, key-gap honesty section); the deterministic operator surface went live regardless (six real posts). An `OPENAI_API_KEY` exists in the sandbox env but arming NL on it was explicitly out of scope and the shipped default provider posture is deterministic.

⚑ OWNER-ACTION 6 — re-arm the fleet's dead failsafe routines (2026-07-11T16:31Z env-teardown fallout)
WHAT: Check every lane's scheduled Routines and re-create the ones the platform teardown auto-disabled.
WHERE: the Claude Routines/schedules surface for each Project (or ask each lane's next fired session to self-check via list_triggers).
HOW: for each affected lane (substrate-kit, trading-strategy, sim-lab, idea-engine, product-forge; venture-lab/superbot-games/superbot-idle also touched) look for triggers with ended_reason "auto_disabled_env_deleted" and re-arm per that lane's recorded routine text; THIS lane needs nothing until un-archived (its loop was deliberately DISARMED at close-out — re-arm recipe in docs/retro/q0265-routine-loop-2026-07-11.md).
WHY-IT-MATTERS: a lane whose failsafe is dead never wakes on its own — it goes dark silently, not red.
UNBLOCKS: the standing autonomous core's sibling lanes resuming their loops.
VERIFIED-NEEDED: a platform ENV TEARDOWN at 2026-07-11T16:31Z auto-disabled scheduled triggers fleet-wide (ended_reason "auto_disabled_env_deleted" observed via list_triggers from the coordinator session); this lane self-healed its own failsafe but has no tool path to another lane's triggers — cross-lane trigger repair is owner/manager-only; owner was alerted ~2026-07-11T17:31Z; no repo evidence shows the sibling failsafes re-armed since.

carried (unchanged, full detail in the retro §2): sacrificial member (flag 9), settings-EDIT/setup-FLOW boundary (flags 14/17a), hub topology (flag 21), band-7 AI key envelope (GRADUATED to OWNER-ACTION 5 above at the 05:01Z heartbeat), owner hand-pass of presentation surfaces — incl. the band-4 human keystroke check (a real message should earn 15-25 XP on a 60s cooldown via !rank)
notes: (1) the prior session's parked blackjack/rps ensure-only follow-up is CLOSED by #114 — both register at import now, and the composition-parity invariant kills the whole class going forward; what remains of that thread is the 99-ref burn-down ledger (docs/ideas/ensure-only-registration-gaps-2026-07-10.md), each ref a live RefUnresolved envelope until its subsystem's port slice registers it at import and shrinks `_KNOWN_ENSURE_ONLY`. (2) codex return path OPENED at #138 but capacity REGRESSED same-day (usage-limit replies on #148/#151 — regression note in the OWNER-ACTION 4 record above); TWO new band-7 questions now queue unanswered: #148 comment 4942099573 (head `27b6333`, split-audience-by-surface — can a PanelSpec keep the slash response ephemeral while leaving the prefix panel click-open, or is that a ledger row) and #151 comment 4942514348 (head `16f9151`, the two replay-harness semantics changes — is there a third process-lifetime in-memory carrier besides the K10 conversation buffer that curated-first ordering or the unknown-slash drop could false-green). The SEVEN earlier questions predate the capacity restore and remain codex-unanswered: #112 comment 4938620657 (ref renumbering), #114 comment 4938835129 (head `9afa7c4`, composition-probe blind spots), #117 comment 4939223062 (session-view ingress bypass), #120 comment 4939690779 (head `e18a7f9`, refresh-seam wrong-message edit), #124 comment 4940003762 (head `645955a`, PvP double-settle race), #130 comment 4940441314 (head `f6a7796`, tournament free-branch double-payout race — SELF-ANSWERED: #133's check-and-set settle-once guard closed exactly that race in both games, double-call tests pin it), #133 comment 4940819831 (head `d9a27bd`, duplicate results-frame render — its cosmetic half CLOSED by #138's render-once guard; the teardown interleaving question folded into #138's own open question). #138's question (comment 4941075583, the rps settled-guard ALTITUDE — should the claim move into `advance_round`'s champion return to make stage decision + claim one atomic step) is the lane's one OPEN question: cheap successor hardening, not answered by the codex review (which predates it). Historical: #111's post-merge codex reply ("Didn't find any major issues", reviewed head `8faa5aca`, comment 4938542412) remains INPUT-not-verification; the `_unlock_at` stash-path check rides with the answers above. Earlier follow-up state unchanged: the #105/#108 compensator lineage is fully closed by #111's delete-if-match fold-in. (3) NEW hygiene flag from the games lane (phase-7 lane notes): a PRE-EXISTING test-order flake at CLEAN main — `pytest tests/unit/app tests/unit/authority tests/unit/band6/test_band6_rps_tournament.py` in that non-canonical order fails 3 pre-#138 band6 tests (earlier suites clear the spec-refs registries and `Harness._load_manifests` re-imports without re-arming ENSURE_REFS; the canonical full-suite order is green, CI unaffected). Not a #138 regression — verified at clean main before the slice; flag for a successor hygiene slice (the #138 tests already self-arm via `ensure_ops_refs() + register_ops()`, the band6 fixture idiom). CLOSED 2026-07-11 by #141 (merge `78509df`, band-7 lane record above): `register_ops()` in both games now ends with `ensure_ops_refs()`, all pytest orders green.
