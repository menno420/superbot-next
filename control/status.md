# superbot-next · status
updated: 2026-07-09T12:26Z
phase: live-testing ledger step 2 (band 1: settings + help + diagnostic + setup) — in progress; replay + live hub exercise done, now building app-command registration + the on_message prefix feed adapter, then band 2 slice 1 (moderation + logging)
health: red-by-design (golden-parity `report` leg red while parity rows are `pending`: 0/465 corpus-wide, band-1 0/53 — ledgered structural boundary D-0050, not a build break). Everything else green: unit suite 1009 passed / 2 skipped; `bootstrap.py check --strict` GREEN on main (red cleared by #56); live boot proven (`python3 -m sb` → RUNNING on the test token, all 24 migrations, audit canary delivered, clean SIGTERM exit 0)
last-shipped: #59 — testing-report band-1 row (replay 0/53 ledgered-red per D-0050; live hubs PASS; real bugs fixed in #56 manifest PanelSpec registration + #58 live dispatch target index, 514 targets). #57 (this control plane) merged after it.
blockers: on_message feed adapter not armed in main() — human-typed prefix commands get silence; the single blocker for human-driven live testing (being built this session). Leg C stays compare-only until app-command registration lands (78 stale old-bot slash commands on the remote tree).
orders: acked=001 done=
⚑ needs-owner: (1) band-1 goldens CANNOT flip green until the deferred successor slices land (D-0025/D-0026/D-0027: settings component hub, help categories, platform_* diagnostic fleet, setup wizard — itemized in D-0050); ORDER 001's "band 1 passes" acceptance is blocked on that boundary — schedule the slices or accept band-1 parity rows staying pending. (2) Stale 78-command remote tree (leg C): leave until app-command registration lands (recommended) or clear deliberately. (3) Optional: relax the "require branches up to date" rule / enable merge queue for concurrent doc PRs.
notes: ORDER 001 acked, NOT done — band-1 golden replay ran (0/53 green, matches CI report leg exactly; zero parity flips, zero verified_live records minted — one-way door respected) but live exercise PASSED: all four band-1 hubs posted real panels through the full dispatch pipeline on the test guild, K7 settings declare/read/bind audited on the live DB. Privileged intents RESOLVED portal-side: live boot with both intents accepted, intent_degradations=[] (seeded blocker cleared). check --strict red on main also already cleared (#56). App-command registration + on_message feed adapter are in flight this session; moderation+logging testing follows. Full evidence: docs/status/testing-report-2026-07-09.md.
