# superbot-next · status
updated: 2026-07-11T07:38Z
phase: band-5 COMPLETE — live-bug fix lane DONE via #111 (merge `569beea`; CI https://github.com/menno420/superbot-next/actions/runs/29116022101 — all 6 checks in that run green, `report` red-by-design/non-required): the 3 live-only bugs from #109's ledger fixed test-first — (1) role pending terminals now register at MODULE IMPORT (`sb/domain/role/handlers.py` calls `_register_pending()` top-level; the live root never ran ENSURE_REFS with zero plugins), (2) setrole/unsetrole/removereactrole acks read the engine's REAL `after` keys (rollup keys by step target_name, never `after["record"]`) with copy pinned to the oracle (disbot/cogs/role_cog.py:534/:565/:705), (3) `!temprole` failure copy no longer leaks the raw WorkflowResult repr — plus the #108 symmetric twin folded in: `_compensate_lock` deletes IF-MATCH (winner_id + unlock_at via `store.delete_lock_if_match`; a permanent grant compensates nothing) + the extended compensator-invariant test (every declared compensator ref resolves to a registered leg). AND the first parity flip landed: #112 (merge `c0f0783`) — `help: ported` in parity/parity.yml through the A-16 door: 3/3 help goldens byte-green (help.panel_open, sweep.help, sweep.slash_help), 100% declared-surface coverage (R2 vacuously — help declares none), zero exemptions, ratchet row `help: {events: 1, tables: 3, settings: 0}`. Dispositions that bit: kernel-surface-drift, xp-coins-alias, invoking-message-deletion, plus symmetric `<msg:N>` ref renumbering added to `apply_dispositions` (docs/parity/flag-13-disposition-2026-07-10.md § Ref renumbering). ▶ LANES (2026-07-10/11, parallel): (a) parity-flips lane — SESSION END: two waves shipped (wave 1 #118/#119/#121/#123/#125, wave 2 #128/#129/#131/#132) PLUS post-wrap wave-3 sibling flips #137 (settings pending→ported, 4/4 goldens) + #140 (cleanup pending→ported, 3/3 goldens) + wave-4 sibling flips #143 (xp pending→ported, 3/3 goldens) + #145 (proof_channel pending→ported, 3/3 goldens) + wave-5 flips #152 (economy pending→ported, 6/6 goldens) + #154 (ticket pending→ported, 6/6 goldens) + #157 (karma pending→ported, 8/8 goldens — wave-5 records below), quicksetup attempted and honestly parked at a verbatim wall (record below); (b) band-6 games lane — SESSION END: SEVEN slices merged in continuous mode per Q-0265 (#114/#117/#120/#124/#130/#133/#138, lane record below) — the composition-parity class-killer, both game ports, PvP, both tournaments + the reaction-ingress seam, the settle-once + render-once guards, and the last red game-family golden flipped by #138; both games playable solo + PvP + tournament, ALL game-family goldens green (rps_tournament 6/6, blackjack 5/5); remaining surface is unpinned polish only (ledgered in the two docs/ideas remaining-surface files) — NO golden-pinned work left in this lane; next wake either starts band-7 per the canonical order or takes owner direction; (c) kit lane — vendored substrate-kit v1.6.0→v1.7.0 (#116, merge `a700975`) →v1.7.1 (#122, merge `1ba8607`) →v1.8.0 (#135, merge `3dfc194`; new plants control/claims/README.md + scripts/env-setup.sh, auto-merge enabler STAGED only, v1.7.1 dist banked); (d) band-7 builder lane — OPENED per the Q-0265 honesty-guard handoff (record below): #141 test-order-flake fix (merge `78509df`) + #144 btd6 pending→ported (merge `596fb2d`, 39/39 goldens) + #148 project_moon+projectmoon pending→ported (merge `f2dcf0d`, 11/11 goldens) + #151 ai pending→ported (merge `1bea65d`, 20/20 goldens fixed-in-PR, deterministic operator surface; the live-NL leg stays owner-key-gated, OWNER-ACTION 5 below) + #155 aireview sweep family re-homed `_unmapped`→ai (merge `4c8c5b0`, 11/11 goldens — the #151 parked item 4 CLOSED) + #156 k10 test-order-flake fix (merge `179dfb2` — AI-registry isolation in both directions) + #160 (merge `0a29d37` — main HEAD, ai policy/settings-mutation surface: the shipped chooser PAGES + the S6/S7 settings edit/reset widgets over the audited `settings.set_scalar` lane, band-7 slice 5 — the #151 parked item 3 CLOSED; lane record below); (e) codex-P2-triage lane — #161 (merge `8ad243f`, card `.sessions/2026-07-11-codex-p2-triage.md`): the three post-merge Codex P2s from #154/#157 all verified REAL and fixed (group-scoped command aliases via one shared `sb/spec/commands.py command_dispatch_keys()` truth, the ticket post-panel staff gate with the shipped refusal byte, the live `/karma` acked-interaction followup send — live-adapter only). Still queued: role/proof_channel live EFFECT action ports (GuildRoleActions, ChannelPermActions still unarmed)
health: green with the standing red-by-design (`report` dashboard red until full parity — the golden-parity GATE is non-vacuous and GREEN). Parity: 22/49 subsystems ported — ai, blackjack, btd6, channel, cleanup, economy, general, help, hermes, karma, leaderboard, project_moon, projectmoon, proof_channel, rps_tournament, servermanagement, settings, ticket, utility, ux_lab, uxlab, xp (counted by hand in parity/parity.yml at HEAD `0a29d37`, #160's merge = main HEAD; same 22 as at `9053311` — #160 flipped no row, it armed the ai mutation surface); gate GREEN — 143/143 goldens across the 22 ported subsystems replay clean at main HEAD `0a29d37` (gate job 86523808594 in run 29144580286 — job log verified: "gate: GREEN — all 143 golden(s) across 22 ported subsystem(s) replay clean", check_parity_depth "OK — 49 subsystems (22 ported), 465 goldens" same job; earlier waypoints: 143/143 across 22 at `9053311` [gate job 86518931866 run 29142780549], 112/112 across 19 at `1bea65d` [gate job 86512108907 run 29140344074], 92/92 across 18 at `531f14ee` [gate job 86510003976 run 29139525710], 78/78 across 15 at `596fb2d` [gate job 86504916507 run 29137655334], 36/36 at `3bbe93e` [gate job 86500434884 run 29136042436], 33/33 at `9ea90e9`); report leg 182/465 goldens green, replayable 465/465 (read from main's own CI report job 86523808591 in run 29144580286 at `0a29d37`: "green: 182/465", ai 31/31 [ported] in the per-subsystem table — the #155 re-home moved the 11 aireview goldens ai-side, `_unmapped` now 34/220; earlier: 182/465 at `9053311` [report job 86518931887 run 29142780549], economy 6/6 + ticket 6/6 + karma 8/8 [ported] in that table; the job stays EXPECTED RED, not required; earlier waypoints: 152/465 at `1bea65d` [job 86512108915 run 29140344074], 101/465 at `531f14ee` [job 86510003975 run 29139525710], 87/465 at `596fb2d` [job 86504916495 run 29137655334], 48/465 at `3bbe93e`, 45/465 local at the #138 head, 40/465 at `9923151`, 38/465 at `12886b9`, 30/465 at `7c819b1` run 29128810545, help 3/3 first verified in #112's run 29116678733). Unit suite 1334 passed / 4 skipped in CI at `0a29d37` (run 29144580270 job 86523808535 — "1334 passed, 4 skipped"; was 1313/4 at `9053311` [run 29142780551 job 86518931877], 1303/4 at `1bea65d` [run 29140344073 job 86512108932], 1293/4 at `531f14ee` [run 29139525706 job 86510003965], 1288/4 at `596fb2d`, 1283/4 at `3bbe93e`, 1272/4 at `9ea90e9`; local canonical-order ladders run with deps and differ by the guarded-import skip delta — the standing note: #160's local ladder measured 1336/2 at its head `e8bbb66`). Main-push CI at `0a29d37` green on the required set: ci run 29144580270, named-gates run 29144580269, golden-parity gate job green in run 29144580286 (that run's overall red is only its red-by-design `report` job, not required; prior waypoint `9053311`: runs 29142780551/29142780559/29142780549 same shape). Every merge this session — all lanes plus the heartbeats — went in green on the 6-check ruleset (`report` red-by-design, not required), as were #111 and #112 before them
kit: v1.10.0 · check: green (#159, merge `72db87b` 2026-07-11T07:01Z, card `.sessions/2026-07-11-kit-upgrade-v1.10.0.md`: asset sha256 `ba69fc5c…` three-way verified against tag v1.10.0 @ kit commit `1b5db16` release run 29142780212; v1.9.0 banked byte-identical as `.substrate/backup/bootstrap-1.9.0.py`; staged gate regen now carries the v1.10.0 session-card-hold payload — still STAGED only, no live substrate-gate by design; `.sessions/README.md` model doctrine correctly no-oped, byte-identical since the v1.9.0 regen; prior: v1.9.0 #150 merge `d653ba1`, v1.8.0 #135 merge `3dfc194`) · engaged: yes
last-shipped: band-7 lane #160 (ai policy/settings-mutation surface, merge `0a29d37` — main HEAD; lane record below) after #161 (codex P2 triage fix, merge `8ad243f`), kit lane #159 (substrate-kit v1.9.0→v1.10.0, merge `72db87b`) and parity-lane heartbeat #158 (merge `b1cda9c`). Earlier same session: parity lane wave 5 — #157 (karma pending→ported, merge `9053311`, 8/8 goldens) after #152 (economy flip, merge `b2ec7d9`, 6/6 goldens) + #154 (ticket flip, merge `5b750c8`, 6/6 goldens; wave-5 records below); band-7 lane #155 (aireview family re-homed `_unmapped`→ai, merge `4c8c5b0`, 11/11 goldens) + #156 (k10 test-order-flake fix, merge `179dfb2`) landed between them, plus band-7 heartbeat #153 (merge `119ecce`). Earlier same session: band-7 lane #141 (test-order-flake fix, merge `78509df`) + #144 (btd6 flip, merge `596fb2d`, 39/39 goldens) + #148 (project_moon+projectmoon flip, merge `f2dcf0d`, 11/11 goldens) + #151 (ai flip, merge `1bea65d`, 20/20 goldens); kit lane #150 (substrate-kit v1.8.0→v1.9.0, merge `d653ba1`); parity lane wave 4 — #143 (xp flip, merge `f97a4c7`, 3/3 goldens) + #145 (proof_channel flip, merge `531f14ee`, 3/3 goldens); heartbeats #142 (merge `aef3135`) + #147 (merge `9f83ea1` — carried D-0063) + #149 (merge `dccac62` — parity wave 4) and fm relay #146 (merge `928e212`). Earlier same session: band-6 lane SESSION COMPLETE #114/#117/#120/#124/#130/#133/#138 (class-killer, rps_tournament port, blackjack port, PvP, rps tournament + reaction seam, blackjack tournament + settle-once, rpssettings + render-once; latest merge `9ea90e9`; session card `.sessions/2026-07-11-band6-games-session.md` + telemetry row ride THIS heartbeat PR — Q-0194); parity lane #118/#119/#121/#123/#125 + wave 2 #128/#129/#131/#132 + post-wrap wave 3 #137 (settings flip, merge `77ae8b4`, card `.sessions/2026-07-11-settings-parity-flip.md`) + #140 (cleanup flip, merge `3bbe93e`, card `.sessions/2026-07-11-cleanup-parity-flip.md`); kit lane #116/#122/#135 (v1.8.0; latest merge `3dfc194`). Earlier: #111 (merge `569beea`) + #112 (merge `c0f0783`; session card `.sessions/2026-07-10-help-parity-flip.md`)
blockers: ORDER 002 done-when still hangs ONLY on the owner-created separate repo (OWNER-ACTION 2 below). Codex capacity is FLAPPING, not hard-walled: after the late-night usage-limit replies on #148 (comment 4942100526) and #151 (comment 4942514698) and one more on #152 (comment 4942577962, 05:08Z), the connector returned FULL P2 reviews on #154 (review 4676723779, 05:54Z), #157 (review 4676836079, 06:31Z) and #160 (review 4676953047, 07:10Z — 1 real P2 fixed in-PR, 1 P3 taken; but its "committed changes" reply claimed a PHANTOM commit/PR, documented in the #160 lane record) — OWNER-ACTION 4 stays retired, flap evidence in the item 4 record below; questions still posted on every substantive PR per the standing Q-0259/Q-0258 rule, and every returned finding gets Q-0120 source verification before action (the #160 phantom is the newest evidence for why). Band-7 live-NL leg owner-key-gated (OWNER-ACTION 5 below); no band blockers otherwise
orders: acked=001,002,003,004,005,006,007,008,009,010,011,012 done=003,005,006,007,008,009,010,011,012 — ORDER 012 (P3 fleet standing rule, model-attribution ground truth) ACKED at the 06:35Z heartbeat and its done-when now MET, re-verified item-by-item at THIS heartbeat (`0a29d37`): (1) the session-card template half was already DONE by the kit lane (already-done-by-other-lane, nothing added here) — the template lives in the kit's own draft/enforce path, verified at HEAD: bootstrap.py v1.10.0 `_default_session_markers` (line ~244) requires the `📊 Model:` needle on every card, `_marker_line`/`draft_card` auto-draft it (`- **📊 Model:** [[fill:model]] · …`), the strict session-log guard blocks a card without it (guard-fire evidence in `.substrate/guard-fires.jsonl`), and `.sessions/README.md` carries the family-level-names-only doctrine (planted at the v1.9.0 regeneration #150, byte-identical through v1.10.0); (2) fired sessions' committed cards carry real family-level lines (`📊 Model: Claude Fable 5` in the wave-5 flip cards, `.sessions/2026-07-11-kit-upgrade-v1.10.0.md` and `.sessions/2026-07-11-codex-p2-triage.md`, verified at HEAD; this heartbeat's own card too); (3) n/a — the standing rule binds every future fired session's card — ORDER 004: items 1 (#80), 2 (#112) and 4 (#83) are DONE, and band-6 now demonstrably carries BOTH bindings (item 3: walking-skeleton CI tests in tests/unit/band6/ ride every band-6 port PR #117/#120/#124/#130/#133/#138 + classify-or-fix in each testing-report); still NOT marking done=004 — its done-when reads "bands 3-9 orders carry the two binding steps" and item 5's demo rule binds every future demo; bands 7-9 haven't run yet
⚑ needs-owner: THREE live OWNER-ACTION items below (six-field format per control/README.md) — items 2 and 3 unchanged; NEW item 5 (band-7 AI key envelope — the live-NL leg is blocked on it); item 4 stays RETIRED, its capacity half now FLAPPING rather than walled (usage-limit replies on #148/#151/#152 but full P2 reviews on #154/#157 the same morning; flap update in the item 4 record)

ORDER 008 record — AMENDED by owner directive Q-0265 (CONTINUOUS-MODE AMENDMENT, 2026-07-10). Q-0265 supersedes the one-slice-per-wake throttle: the Builder loop now runs continuously — slice after slice, each slice still its own merged-on-green PR; backpressure comes from queue saturation, not time; honesty guard: out of useful work → say so and idle until the failsafe. Cutover record (exact calls + outcomes, replacing the old builder-wake arming record):
- DELETED: delete_trigger {"trigger_id":"trig_01VYZQ7GHxYq3ecSw8UNZek8"} → "deleted trigger trig_01VYZQ7GHxYq3ecSw8UNZek8" (old builder-wake cron, verified absent from list_triggers).
- CREATED failsafe: create_trigger {"name":"Builder failsafe wake","cron_expression":"0 */2 * * *","persistent_session_id":"session_01HRfuSKiQSnGHXKne3yzadg","prompt":"FAILSAFE WAKE (Builder, Q-0265): if your send_later continuation chain is alive, verify that in one line and end. If it stalled, resume the work loop (sync HEAD → inbox → slice after slice, each merged-on-green) and re-arm the chain (~15 min) before ending."} → SUCCESS, trigger trig_01L5JBefGSCM1fUdwm4SRQnY, enabled, next_run_at 2026-07-10T20:05:55Z, verified via list_triggers.
- CHAIN MECHANISM: send_later cannot target another session (schema: message/at/delay_minutes only, fires into the calling session), so the continuation chain runs on ONE-SHOT create_trigger calls into the coordinator session instead — first link trig_01KedTiCKYMbB3oaNZL5rHmf ("Builder continuation chain (one-shot)", run_once_at 2026-07-10T20:03:00Z); the coordinator re-arms a fresh one-shot each turn (a one-shot self-disables after firing, ended_reason=run_once_fired). Failsafe cron catches any stalled chain.
- Q-0265 honesty guard INVOKED by the games lane at this wrap-up: the band-6 lane is out of golden-pinned work (record below) — next wake starts band-7 per the canonical order or takes owner direction.

parity-flips lane record (session 2026-07-10/11) — SESSION COMPLETE: two waves, nine lane PRs (8 flips + the #118 groundwork) plus post-wrap wave-3 sibling flips (#137 + #140), wave-4 sibling flips (#143 + #145), wave-5 flips (#152 + #154 + #157) and heartbeats #126 + #134 + #142 + #149, ALL squash-merged on green with the 6-check ruleset. Wave 1:
- #118 leaderboard groundwork — board panel oracle fidelity + games-lane-granted 3-string providers fix; merge `9757755c61034edad4b5dee5ab715783da18f1a6`
- #119 leaderboard flip pending→ported (1/1 goldens); merge `7d1cf9a2133032f9f930189762eed8b80a49f35b`
- #121 hermes flip (2/2 goldens; webhook-shaped followups in the parity transport; sim-gate overlay machinery per seed `a627153`); merge `9e4180d48dac515b200a06d35344a8640adb541f`
- #123 general flip (1/1 goldens; shipped GeneralMenuView as a session-lifecycle panel); merge `4a32f618838e430ca71fe2c241a23236cb614fe3`
- #125 utility flip (7/7 goldens; profile-card placeholder, capture-world guild directory, DeferMode.NONE trap documented); merge `7c819b197b969d8dc1bcdaffa6c7bf7a6f68d74c`
Wave 2 (all squash-merged on green 6-check CI):
- #128 ux_lab flip pending→ported (1/1 goldens); merge `71eee908fa2795012efca6461ec45b4e30038530`
- #129 uxlab flip (1/1 goldens; the corpus's only get_original_response call ported); merge `038992cd4078583a0e76507aa538b9d2af7808fc`
- #131 channel flip (1/1 goldens; shipped ChannelManagerView hub — sub-panels are pending terminals per D-0030); merge `287566584ba7e3b15ede9106cb00e47742ae07f7`
- #132 servermanagement flip (1/1 goldens; slash row only — the sibling server_management prefix row stays pending, 2 diffs from anchored-persistent-panel semantics); merge `12886b9be94aae76dd81a4d68db888fa03bf51e4`
Post-wrap wave 3 (folded into this record at the 2026-07-11 heartbeats — both landed after the lane's own LANE END entry):
- #137 settings flip pending→ported (4/4 goldens — settings_hub_open, sweep_settings, sweep_slash_settings, sweep_settings_access: the shipped ⚙️ Settings Manager hub verbatim on both surfaces — a DeferMode.NONE hub — + the 🔍 Access Policy Explorer behind the NEW `settings access` grouped prefix front door, the corpus's first; the FIRST R2-non-vacuous flip — three depth-exemption rows under the existing `covered-elsewhere` reason class; ratchet row `settings: {events: 1, tables: 2, settings: 0}`); merge `77ae8b4`; card `.sessions/2026-07-11-settings-parity-flip.md`
- #140 cleanup flip pending→ported (3/3 goldens — the shipped 🧹 Cleanup Hub + Prohibited Words Manager + `!cleanuphistory` scan; the FIRST Discord-READ capture port — the goldens' `logs_from` wire verb gets a twin in the parity transport and the live history reader is armed in boot; the sweep_wordmenu golden pins the oracle's process-memory `_word_cache` leak as a literal (`` `test` `` — the capture's alphabetical sweep order left `!word add test` in the cog's in-memory cache; in-code under-port note), ONE `covered-elsewhere` depth exemption (`prohibited_words`, written only by `_unmapped/sweep_word_add`); codex P2 fixed on the PR head `c510830` — honest refusal for unported scan-mode matchers; ratchet row `cleanup: {events: 1, tables: 2, settings: 0}`); merge `3bbe93e`; card `.sessions/2026-07-11-cleanup-parity-flip.md`
Post-wrap wave 4 (folded into this record at the 2026-07-11T04:45Z heartbeat — both squash-merged on green 6-check CI):
- #143 xp flip pending→ported (3/3 goldens — the rank-card session panel + the avatar READ port/capture twin; 2 depth exemptions under the existing `covered-elsewhere` reason class: xp.reset, xp.level_up; codex question on the final head confirmed the arg-walk abort behaves as intended — no change); merge `f97a4c7`; card `.sessions/2026-07-11-xp-parity-flip.md`
- #145 proof_channel flip pending→ported (3/3 goldens — the `_PrizeManagerView` port; ALL THREE codex P2s were real and fixed on the PR: the locks depth exemption reclassified `covered-elsewhere`→`env-keyed-integration`, DeferMode.MODAL on the modal-backed actions, the `+prize` wire literal; the end_access compensator needed NO new work — already fixed pre-flip by the #105/#108/#111 lineage; the compensator allowlist stays EMPTY); merge `531f14ee`; card `.sessions/2026-07-11-proof-channel-parity-flip.md`
Post-wrap wave 5 (folded into this record at the 2026-07-11T06:35Z heartbeat — all three squash-merged on green 6-check CI; every fact below re-verified against parity.yml + the PR threads at HEAD `9053311`):
- #152 economy flip pending→ported (6/6 goldens — the wallet card, the 💰 Economy Menu hub and the Job Center work panels; the `/economy` slash twin rides the AUTO+EPHEMERAL defer inverse; THREE depth exemptions in parity.yml: `table:economy_balances` under the existing `env-keyed-integration` class — the ledgered-coins boundary's NEW home, no old-bot capture can carry the row — and `table:job_progress` + `table:inventory` under the NEW `select-driven` reason class, decision record D-0064 in docs/decisions.md: a write behind a shipped run-minted select-menu pick is structurally outside the imported corpus's input vocabulary — the click schema carries no `values` field and the run-minted select ids normalize to `<cid:N>`; ratchet row `economy: {events: 2, tables: 5, settings: 0}`; @codex question on the head `522df24` [comment 4942577422] got only the usage-limit reply [comment 4942577962] — non-review); merge `b2ec7d9`; card `.sessions/2026-07-11-economy-parity-flip.md`
- #154 ticket flip pending→ported (6/6 goldens on the FIRST local replay — the all-guard-byte row: the `!ticket` hub panel with the three shipped buttons carrying label + emoji as SEPARATE wire fields, plus five guard bytes [empty-subject usage, REASON_NOT_CONFIGURED, the "This isn't an open ticket channel." in-channel guard, the TicketOpenModal not-configured refusal]; the first fully-from-zero NEW-SUBSYSTEM birth kit — no prior sb/domain/ticket existed; ZERO declared surfaces and ZERO depth exemptions BY DESIGN: the shipped ticket config/row tables are structurally absent from the v1 schema epoch, so the slice declares only what it fully carries instead of minting dead tables — no D-0065 needed, the under-port boundary centralized in sb/domain/ticket/service.py; ratchet row `ticket: {events: 1, tables: 2, settings: 0}`; @codex question on the A-16 zero-declaration reading [comment 4942796411] answered POST-merge with a real review [4676723779, final head `43b9b44`]: TWO open P2s — bare `open`/`create` subcommand aliases indexed unqualified in the live prefix index, and `ticket_post_panel` declared `user`-tier without the shipped staff gate — both are follow-up rows for the next ticket slice, neither golden-pinned); merge `5b750c8`; card `.sessions/2026-07-11-ticket-parity-flip.md`
- #157 karma flip pending→ported (8/8 goldens — the shipped thanks/karma surfaces incl. `karma.card` AND the corpus's first RED `error_card` envelope port [`karma.error_card`, the shipped utils/embeds.error refusal — sb/domain/karma/panels.py]; `karma_audit_log.mutation_id` added to the EXISTING kernel-surface-drift `columns` disposition [parity/parity.yml:149, the economy_audit_log precedent under the Q-0262.3 delegation — the pinned-list test grew with it]; the K7 engine-swallows-typed-errors problem solved via a `ctx.params["_karma_refusal"]` side-channel [sb/domain/karma/ops.py `_mark_refusal` → handlers.py — leg exceptions classify into frozen-five results, so the class identity never reaches the cog otherwise]; ZERO depth exemptions; ratchet row `karma: {events: 2, tables: 4, settings: 0}`; @codex question on the final head `f8532ef` — is there a cleaner sanctioned lane than the shared-params marker — [comment 4943018975] answered POST-merge with a real review [4676836079]: ONE open P2 — live slash `/karma` panel-present lands on the acked-interaction `edit_original_response` branch instead of the shipped followup send, a live-wire drift the parity presenter can't see — follow-up row, not golden-pinned); merge `9053311` — main HEAD; card `.sessions/2026-07-11-karma-parity-flip.md`
- Codex-capacity flap note for this wave: usage-limited at #152 (05:08Z) yet FULL P2 reviews on #154 (05:54Z) and #157 (06:31Z) — the pool refilled the same morning it walled #148/#151/#152; all three wave-5 questions plus the three open P2s above stay ledgered here since post-merge reviews can't block the merged heads (Q-0258 merge-on-green).
- UPDATE folded at the 07:38Z heartbeat: all THREE open wave-5 P2s (the #154 pair + the #157 followup drift) are CLOSED by #161 (merge `8ad243f`, card `.sessions/2026-07-11-codex-p2-triage.md`) — each verified REAL against oracle source before fixing (Q-0120): (1) group-scoped subcommand aliases (one shared `command_dispatch_keys()` truth in sb/spec/commands.py, both indexers; also corrects treasury's group-scoped aliases and un-shadows the shipped `!create` channel op), (2) the `ticket_post_panel` staff gate with the shipped `"Only staff can post the ticket panel."` refusal byte (gate = ActorRef.is_guild_operator at the v1 config-absent epoch), (3) the live `/karma` acked-interaction path now sends a webhook FOLLOWUP (live-adapter only, parity replay untouched). Zero golden-byte movement; that PR's local ladder: 1319/5 units, gate 143/143 GREEN.
quicksetup: attempted and SKIPPED at a verbatim wall — its golden requires `/setup` to provision a private setup channel (a create-channel EFFECT with no capture twin and no compensator ruling; D-0030 names channel-ops as the successor slice; cross-lane sb/domain/setup files). The lane parked it — no PR, no exemption rows. This is the lane's honest stop, not a regression.
LANE END: this session's slices are complete. Remaining pending non-game rows are either settings-heavy R2 singletons (welcome, automod, security, image_moderation, counters), multi-golden mid-size (diagnostic 37, moderation 8, setup 8 [IN FLIGHT this session — the setup slice was dispatched; still `setup: pending` in parity.yml at `9053311`], logging 7, chain 7, counting 3 — settings left this list via #137, cleanup via #140, xp via #143, proof_channel via #145, ai via the BAND-7 lane's #151, and wave 5 removed economy via #152, ticket via #154, karma via #157; golden counts re-verified against the gate job's PENDING table at HEAD `9053311` [job 86518931866]), the server_management prefix sibling, or blocked (quicksetup, unchanged); the next parity session should start from those with the playbook in the .sessions/ cards. Band-6 games lane wrapped separately (record below).
A-16 latent-defect note (surfaced at the #140 flip): the R2 SETTINGS dimension is dead code — `check_parity_depth.declared_surfaces` reads a `key` field per declared setting but the compiler emits `settings_key`/`name`, so the settings leg of every ratchet row is vacuously satisfied today; arming it is its own future sweep PR (several already-ported rows would red at once), not a per-flip fix (#140).
Disposition note: the session-lifecycle custom_id class (`<cid:N>`) was resolved by fidelity fix #117 `_mint_ephemeral` — no new disposition class needed (D-0061 analysis: ORDER 009 acceptance is scoped to the three ruled classes).
Housekeeping flag FOR THIS LANE (raised by the games lane, 2026-07-11; RE-VERIFIED at this heartbeat): `.sessions/2026-07-10-ux-lab-parity-flip.md` still fails the bootstrap strict session-card check — missing the `💡` Session-idea and Previous-session-review sections (grep at HEAD `531f14ee`: zero matches for both markers; advisory, CI unaffected). Two games-lane workers tripped over it; the parity lane (or its next session) should complete the card rather than anyone else guessing its content.

band-6 games lane record (session 2026-07-10/11, continuous mode Q-0265) — SESSION COMPLETE: SEVEN PRs shipped, ALL merged on green with the 6-check ruleset; lane heartbeats #127 + #136 + this wrap-up:
- #114 the composition-parity invariant test (`tests/unit/invariants/test_composition_parity.py`) — BUG A CLASS-killer: both roots' registered ref sets are diffed in a clean subprocess, so unregistered pending terminals now fail CI for EVERY plugin; plus the blackjack/rps register-at-import fix (closing the prior session's parked note). The sweep exposed a 99-ref ensure-only burn-down across 8 more subsystems (ai 20, btd6 23, projmoon 10, mining 28, fishing 15, creature 1, proof_channel 1, role 1), ledgered in docs/ideas/ensure-only-registration-gaps-2026-07-10.md and gated by the test's `_KNOWN_ENSURE_ONLY` list (may only shrink); merge `385df11`
- #117 rps_tournament pending→ported — `!rps` quick-play playable end-to-end on a NEW session-view kernel seam (minted 32-hex component ids + invoker-lock in `sb/kernel/panels/engine.py` / `component.py`); merge `ec2bcf2`
- #120 blackjack pending→ported — `!blackjack` solo playable (hit/stand/double on an edit-in-place refresh seam); carried the ⚑ flag-13 ENCODING COMPLETION for `economy_audit_log.mutation_id` + `economy_balances` new-home (Q-0262.3 delegated class, decision record docs/parity/flag-13-disposition-2026-07-10.md amended, revert path recorded — reversible on paper, Q-0240); merge `4c1882b`
- #124 PvP on the wire for BOTH games — challenge → accept (escrow-in-one-txn) → play → resolve on one staged message; merge `2958224`
- #130 rps tournament orchestration + THE REACTION SEAM (slice 5) — the reaction/live-adapter seam decision landed as three plugin-agnostic kernel seams: reaction ingress (`sb/kernel/interaction/reactions.py` named-consumer registry + never-raise dispatch, live twin `sb/adapters/discord/reaction_feed.py` armed in main(), intent non-privileged; starboard/reaction-roles are now pure-domain successors), `RenderedPanel.self_reactions` (the shipped ✅ primer as a render-model field), and click-driven CHANNEL_ANCHOR opens as fresh sends. Full rps tournament on the wire: `!rpsregister` (golden-pinned embed + Join button + ✅ primer + the shipped `active_tournament=rps` flag row, migration 0027) → sign-up via button AND ✅ reaction on one audited body (`rps.tournament_enter`) → `!rpsstart` bracket (byes advance) → best-of match views → champion through `rps.tournament_payout` (flag cleared same txn) + "🏆" copy. 4 sweeps re-homed `_unmapped→rps_tournament` (rpsregister/rpsstart/rpsbot/rpsmatchup; sweep.rpsstart is corpus-order green by construction — the golden captured the oracle's own in-memory cross-case leak, ledgered). Merge `c3f7a02`
- #133 blackjack tournament orchestration (slice 6) — the #130 shape pure-domain (no new kernel seams): `!bjtournament` registration embed + Join/✅ sign-up on the reaction seam → `!bjstart` (per-player fee debits at launch, broke player silently skipped) → chips-space rounds (1000 start / flat 200 per round) on in-place-edit views → ranked 🏆 results + audited champion payout, flag row cleared same txn. PLUS SETTLE-ONCE for BOTH games: the free branch had a double-payout race (no escrow rows to consume — two racing champion resolutions could both pay the consolation, the #130 review question's race); the champion-payout leg's flag-row delete now runs FIRST as a check-and-set keyed on the atomic row-deletion count, retrofit onto `rps.tournament_payout` too, with direct-leg double-call tests pinning settle-once in both games. sweep.bjtournament + sweep.bjstart flipped honestly and re-homed `_unmapped→blackjack` with sweep.bjstatus. Merge `9923151`
- #138 copy-parity polish sweep (slice 7) — flipped `sweep.rpssettings`, the LAST red game-family golden (class settings-view-copy-deviation): the shipped `rps_settings` command verbatim (invalid-setting / invalid-mode / odd-positive-int guards + the "Setting `{setting}` updated to `{value}`." success copy, oracle-pinned via search_code fragments), the write riding the band-1 `settings.set_scalar` op onto declared persisted keys `rps_default_mode`/`rps_default_best_of` (§4.1 one-write-path, ZERO new kernel seams), golden re-homed `_unmapped→rps_tournament` (gating dir 6/6), verified green in ISOLATION and in corpus order. PLUS the RENDER-ONCE settle guard in BOTH games' tournament finish paths — an in-memory `state.settled` check-and-set (no await between) gates blackjack `_finish_tournament` and the rps champion branch, closing the #133 codex question's cosmetic half: the racing loser renders no second results/champion frame and never re-runs the payout op or the teardown (money was already settle-once by the flag-row delete count since #133). 4 new walking-skeleton tests; classify tables updated in both ideas ledgers. Merge `9ea90e9`
LANE END (SESSION COMPLETE): both games playable solo + PvP + tournament; ALL game-family goldens GREEN — rps_tournament 6/6, blackjack 5/5. Counts at main `9ea90e9`, verified from the main-push CI job logs: check_parity_depth OK — 49 subsystems (12 ported), 465 goldens; gate GREEN 33/33 across the 12 ported (gate job 86496962174, run 29134820004); report 45/465 green / 465/465 replayable (local serial run at the #138 head; CI report job 86496962191 red-by-design, non-required); units 1272 passed / 4 skipped in CI (run 29134819997 job 86496962169). Remaining game surface is UNPINNED polish only, ledgered with classes in docs/ideas/rps-tournament-remaining-surface-2026-07-10.md + docs/ideas/blackjack-remaining-surface-2026-07-10.md: rps `!rpsbot` deep flow, rps time-driven depth (reminder loop / timed close), rps solo edit-in-place, private round/match channels, blackjack natural-at-deal exact wire shape, blackjack PvP double-down, hub-button solo flow, autostart timer. NO golden-pinned work left in this lane — per the Q-0265 honesty guard the lane says so and stops: next wake either starts band-7 per the canonical order or takes owner direction. Session card `.sessions/2026-07-11-band6-games-session.md` + its telemetry/model-usage.jsonl row ship WITH this heartbeat (Q-0194: no card without its telemetry row).

band-7 builder lane record (session 2026-07-11, continuous mode Q-0265) — OPENED: Band 7 = Knowledge + AI, deterministic-first per docs/status/rebuild-completion-report-2026-07-09.md §5 step 9 (goldens ai 20, btd6 39, project_moon 10+1; live NL needs `ANTHROPIC_API_KEY` + `AI_ENABLED` — sb/spec/config.py:148/:166; deterministic provider in CI). The lane executed the band-6 wrap-up's next-wake ruling (Q-0265 honesty guard above: no golden-pinned game work left → canonical order says band 7). Four PRs merged on green so far — all three of the band's golden-pinned deterministic rows (btd6, project_moon+projectmoon, ai) are now PORTED — plus two heartbeats:
- #141 (merge `78509df`) — the pre-existing test-order flake from note (3) below KILLED: root cause was `register_ops()` in rps + blackjack re-arming only the CompoundOpSpecs + op markers after the sanctioned `clear_ref_table` seam — a manifest re-import re-fired decorators only for freshly-imported domain modules, so the CACHED ops modules' `@workflow` leg refs stayed unregistered and the first engine leg resolution died in `RefUnresolved: workflow:rps.record_tournament_open…` under non-canonical pytest selection (canonical order was green only because band5's teardown happened to re-arm every manifest). Fix per the ensure-only doctrine: `register_ops()` now ends with the idempotent `ensure_ops_refs()` (two files, one line of logic each, no allowlist touched); the PR body's ladder shows all permutation families green (app+authority+band6 in every order) and canonical `pytest tests/` unchanged.
- #144 (merge `596fb2d`, main HEAD) — btd6 pending→ported at BYTE parity: 39/39 goldens green (the shipped unified `!btd6` tree — 🐵 hub with persistent `btd6:*` ids, flat lookups, strat/ops/events groups incl. the bare-group silence and the dataset-derived round economy numbers); parity.yml `btd6: ported` through the A-16 door, ratchet row `btd6: {events: 1, tables: 3, settings: 1}`; gate 78/78 across 15 ported at HEAD `596fb2d` (health line above, gate job 86504916507 — the 15 includes the parity lane's sibling #143 xp flip). ENSURE-ONLY BURN-DOWN: all 23 btd6 handler refs register at import now — `_KNOWN_ENSURE_ONLY` measured 99 at `f97a4c7` (pre-#144) → 76 at `596fb2d` (remaining: ai 20, mining 28, fishing 15, projmoon 10, creature 1, proof_channel 1, role 1; NOTE the #144 PR body says "122 → 99" — off by the same 23; the file counts are the measured truth, matching the #114 ledger's original 99). ORDER 004 walking-skeleton LIVE-DRIVE done for real: real composition root booted (27 migrations, health 127.0.0.1:18099), gateway READY as Galaxy Bot#6724, four real `!btd6` posts in Superbot Admin #general through the live prefix entrypoint (message links in the PR body — round-3 card 1525336889748361300, status, hub with the 8 `btd6:*` component ids + nav:help, ops refusal) + the CI-runnable skeleton tests/unit/band7/test_band7_btd6_walking_skeleton.py. NEW DEPTH REASON CLASS `modal-driven` minted for `table:btd6_strategies` (the write sits behind the shipped `/btd6 strat submit` Discord modal — an input kind the corpus schema structurally lacks; cases reconstruct command/slash/click only): shipped flagged-for-review but record-less — decision record D-0063 rides THIS heartbeat and is the required ledger entry. @codex question posted on the final head (comments 4941993818 + 4941996704, head `94f59c1` — the §3.4 scheme-token fence relaxation ordering hole); codex answered with a verdict ("Didn't find any major issues", comment 4942006230, reviewed `b0377b6` — the pre-compat-pin head) and comment 4942002321 claiming a regression-test PR that is NOT visible in this repo; the ordering-hole question itself stays the lane's open question.
- PARKED from #144 (ledgered unpinned deviations, no golden drives them): freeplay MOAB scaling (effective RBE rounds 81+ — RBE renders the wiki base), the resolver's maps/modes matching, the boss-fight estimator (`!btd6 estimate`), the CT-team guided-set flow, `!btd6 ops seed-data` — all honest pending/successor terminals. HYGIENE-FIX CANDIDATE: `tools/check_parity_depth.py --write-ratchet` rewrites parity.yml via `yaml.safe_dump` and destroys every comment incl. the depth-block rules (the tool itself prints "comments are lost"; flip PRs hand-restore the header — a comment-preserving writer is the fix).
- Testing-report row 9 (band 7) deliberately NOT touched by this heartbeat: the convention check shows rows update at the band's own TESTING pass (replay + live legs per the 9-step order), not at port slices — band 6 shipped its entire seven-slice port lane with row 8 still "— pending".
- #148 (merge `f2dcf0d`) — project_moon + projectmoon pending→ported at BYTE parity, BOTH rows flipped in one slice: 11/11 goldens green (10 prefix sweeps + the 1 `sweep_slash_pm` slash golden — the shipped `!pm` LimbusBrowseView with the 8 session-minted buttons in rows 5+3, the GAME_COLOR 10181046 overview/kind/entry/origins cards with the verify-at-ingest provenance footer, the two greyple 10070709 FOOTER-LESS miss cards incl. the shipped `rstrip("s")` "No statuse matches" byte, and `/pm` as a real ephemeral type-4/flags-64 slash twin); parity.yml both rows through the A-16 door, ratchet rows `project_moon: {events: 1, tables: 2, settings: 0}` + `projectmoon: {events: 0, tables: 0, settings: 0}`, no new exemption row or reason class; the invented 7-button hub + lookup modal deleted (compat pin sheds `projmoon.lookup_form`, + slash `pm`, same-PR). Ensure-only burn-down: all 10 projmoon refs register at import — `_KNOWN_ENSURE_ONLY` measured 76 at `596fb2d` → 66 at `f2dcf0d` (the PR body says "99 → 89" — the same +23 offset the #144 body carried; the file counts are the measured truth). ORDER 004 live drive: real root booted, gateway READY as Galaxy Bot#6724, four real `!pm` posts in Superbot Admin #general (message links in the PR body, incl. the footer-less greyple miss live) + tests/unit/band7/test_band7_projmoon_walking_skeleton.py. Ledgered unpinned deviations: shipped buttons edited in place vs result-lane cards; shipped view was public/180s vs the port's INVOKER audience — the #148 codex question (comment 4942099573, split-audience-by-surface) got only a usage-limits reply (comment 4942100526).
- #151 (merge `1bea65d`, main HEAD) — ai pending→ported at BYTE parity, the band's largest row: 20/20 goldens green, ALL fixed-in-PR (the shipped AIPanelView hub on verbatim persistent `ai:*` custom_ids + standard nav row, the status/diagnostics/providers/routing/readiness/policy/support-report embeds — 17-task routing table in shipped enum order, 7-finding readiness chain, dual dry-run policy trace with the restored `no ai_guild_policy row → deny GUILD_NOT_CONFIGURED` byte —, the shipped SubsystemSettingsView ai page, the ✅ forget byte over the K10 conversation buffer, the ephemeral `/aimenu` slash twin, and the 7 EMPTY grouped-slash goldens green by the harness drop rule below); ratchet row `ai: {events: 1, tables: 2, settings: 0}`, TWO depth exemptions under EXISTING classes (`table:ai_review_log` env-keyed-integration, `table:ai_answer_presets` covered-elsewhere). TWO REPLAY-HARNESS SEMANTICS CHANGES rode this PR — they affect EVERY subsystem's replay and are the flagged-to-codex risk (question comment 4942514348; only a usage-limits reply, comment 4942514698): (1) `sb/adapters/parity/boot.py` now DROPS unknown-name slash interactions exactly like the old harness's discord.py ConnectionState did (boot.py:479-485 — an unregistered app command never reached dispatch; the empty grouped `sweep_slash_ai_*` goldens pin zero calls), (2) `sb/adapters/parity/cases.py` `load_replay_cases` restored to the capture's CURATED-FIRST order (the trailing id-sort replayed curated `xp.chat_award` AFTER the ai sweeps, starving `sweep_ai_forget`'s conversation buffer). Ensure-only burn-down: all 20 ai refs register at import — `_KNOWN_ENSURE_ONLY` measured 66 at `f2dcf0d` → 65 at `531f14ee` (the parity lane's #145 pruned proof_channel) → 45 at `1bea65d` (verified by hand at each sha; remaining: mining 28, fishing 15, creature 1, role 1). Gate 112/112 across 19 ported in CI on the PR head `16f9151` (run 29140265573 gate job 86511895348) and again at main `1bea65d` (run 29140344074 gate job 86512108907). ORDER 004 live drive: real root + gateway READY as Galaxy Bot#6724, six real posts in Superbot Admin #general (`!aimenu` hub with the live `ai:*` ids, `!ai status/policy/settings/forget/support-report` — message links in the PR body) + tests/unit/band7/test_band7_ai_walking_skeleton.py. KEY-GAP HONESTY: `ANTHROPIC_API_KEY` absent + `AI_ENABLED` off in the agents' environment (verified during the live drive) — no real NL model call attempted; every shipped surface is the deterministic operator surface (OWNER-ACTION 5 below).
- PARKED from #151 (ledgered, no golden drives them): (1) CLI-order artifact — a band7 walking-skeleton Harness boot pollutes `tools_catalogue` for a later-listed `tests/unit/ai/test_k10_orchestration.py` under non-canonical pytest selection (reproduces with the merged projmoon skeleton alone; canonical `pytest tests/` green — successor hygiene slice, the #141 family); (2) the diagnostics card's `setup_advisor_provider` serves the shipped FALLBACK (`ai_default_provider`) instead of the legacy `SETUP_ADVISOR_PROVIDER` env override — NOTE the module docstring says the override "is not yet an RC-10 Config field" but `sb/spec/config.py:170` HAS declared `SETUP_ADVISOR_PROVIDER` since ~#90; the real parked work is wiring the card read to it (byte-identical wherever the var was never set, i.e. the goldens' state); (3) the shipped ai policy/behavior/tools chooser PAGES + the settings page's edit/reset select widgets land on declared pending terminals — pending the policy/settings-mutation slice; (4) the `!aireview` sweep family (11 goldens counted in parity/goldens/_unmapped/sweep_aireview*.json) stays in `_unmapped` until a re-home slice.
- NEXT per the lane plan: the ai review-loop/presets/aireview-re-home deterministic slice (dispatched in parallel — no PR/branch visible at that heartbeat; UPDATE folded at the 06:35Z heartbeat: LANDED as #155, merge `4c8c5b0`, 11/11 sweep_aireview* goldens re-homed `_unmapped`→ai), then the policy/settings-mutation slice, then the live-NL leg (verify_and_regenerate_once / NL shell / routing — owner-key-gated, OWNER-ACTION 5). Sibling merges this window: parity lane's #145 (proof_channel, `531f14ee`) + kit lane's #150 (v1.9.0, `d653ba1` — NOT a parity flip; the kit-line record above). Health-line counts are measured at `1bea65d`; future counts must re-verify against parity.yml at their own HEAD.
- #155 (merge `4c8c5b0`) — the `!aireview` review-loop + presets surface at BYTE parity, the sweep_aireview family re-homed `_unmapped`→ai: 11/11 goldens green, ai now 31 goldens total (verified at `0a29d37`: `parity/goldens/ai/` counts 31 with 11 aireview files, zero sweep_aireview* left in `_unmapped`); the `table:ai_answer_presets` covered-elsewhere depth exemption REMOVED (parity.yml at HEAD carries ONLY `table:ai_review_log` under `env-keyed-integration`, its reason rewritten to name the now-pinned empty-state read goldens); ratchet row grew `ai: {events: 2, tables: 4, settings: 1}` (was {1,2,0} at #151 — read from parity.yml at HEAD). The #151 parked item 4 CLOSED.
- #156 (merge `179dfb2`) — k10 test-order-flake fix, TEST-ONLY (the #141 doctrine family): the `tests/unit/ai/test_k10_orchestration.py` suite now isolates the AI registries in BOTH directions (tools catalogue + profiles + answer workflows) — the #151 parked item 1 (band7-skeleton-boot pollutes `tools_catalogue` under non-canonical pytest selection) CLOSED.
- #160 (merge `0a29d37`, main HEAD) — ai policy/settings-mutation surface (band 7 slice 5): the three shipped chooser PAGES land (`ai:policy` / `ai:behavior` / `ai:tools` → the AI Policy / AI Behavior / AI Tools & Workflows embeds at oracle bytes, incl. the Behavior→Advanced punt to the raw policy page and the tools page's fresh-guild `Current` decoration) and the `!ai settings` page's S6/S7 edit/reset selects RUN the shipped dispatch — bool single-click toggle, numeric-presets page (the shipped rosters RESTORED onto the four int scalars, an omission vs @7f7628e1), enum select page, reset-to-default — EVERY write one audited `settings.set_scalar` op (the §4.1 sole-writer lane, upsert + central audit in one txn, `settings.changed` after commit; compensator allowlist stays EMPTY; K6 floor DB-verified refusing a plain user). No golden drives any click on this surface (the #151 ledger) — gate GREEN 143/143 across 22 at the final head `e8bbb66` (CI runs 29144436250/29144436275/29144436272, all 11 required-class checks green) AND at main `0a29d37` (health line above); ratchet + compat pin untouched; unit 1336/2 local at `e8bbb66`, 1334/4 in CI at `0a29d37`. Ensure-only allowlist 45 measured at HEAD (counted by hand: mining 28, fishing 15, creature 1, role 1 — zero ai rows; the #151 note's `role` ref persists). ORDER 004 live drive: real root + gateway READY as Galaxy Bot#6724, chooser clicks editing IN PLACE + real `settings` rows written/reset/cleaned in Superbot Admin #general (message links + DB reads in the PR body) + the 14-test CI skeleton. CODEX RETURN (capacity UP again this window — review 4676953047 on head `0c95e97` at 07:10Z): ONE REAL P2 — widget-written scalar raws (e.g. `'15'` for ai_memory_window_minutes) rendered `**invalid**` on the settings page because explicit rows were compared against `allowed_values` uncoerced — FIXED IN-PR (coerce-before-validity, the shipped `resolve_setting` posture, + regression test at `e8bbb66`; the all-defaults golden never reaches the branch, `sweep_ai_settings` byte-identical); ONE P3 TAKEN — the presets reply's `(was …)` prior now derives from the write leg's IN-TRANSACTION `LegOutcome.before` (concurrent-click accuracy); the toggle-fold suggestion DECLINED + ledgered (would change the band-1 generic op contract; the shipped pipeline had the same read-then-transact shape). Q-0120 PHANTOM documented (PR comment 4943535922): the codex reply (comment 4943407864) claimed it "committed changes … with commit `64d607a` and created a follow-up PR" — the commit exists NOWHERE in the repo (git cat-file across all refs — re-verified at this heartbeat) and no such PR exists; no action taken on the claimed artifacts. Parked honestly (declared pending terminals, named successors): the free-form text modal editors (`ai_default_model`, `ai_guild_instruction_profile`) + the presets Override… number modal — the live root's modal-submit lane is dormant by design (sb/app/main.py step 10c), so they port with the MODAL-ARMING slice; the chooser scope pickers (policy channel/category/role/preview/list, behavior scope+preset, tools profiles) — the policy/orchestration-mutation slices.
- BAND-7 POSITION at this heartbeat: remaining deterministic surface = (1) the modal-arming slice (dispatched in parallel at this heartbeat — no PR/branch visible yet; unblocks the #160-parked text/number modals), (2) the chooser scope pickers / policy-orchestration-mutation slice, (3) the review-channel poster (parks with NL arming). The live-NL leg (NL shell, verify_and_regenerate_once, live routing) stays owner-key-gated — OWNER-ACTION 5 stands.

live-drive grants (§0.4 readiness — LIVE-VERIFIED via the #109 live leg):
- test app token: PRESENT in session env (`DISCORD_BOT_TOKEN_PRODUCTION` — connects as the test bot; env var NAME only, no value recorded)
- privileged-intent env flags: `SB_INTENT_MSGCONTENT_OK` / `SB_INTENT_MEMBERS_OK` ARE present (=true) in env — zero degrade markers at gateway READY
- prefix conflict: RESOLVED owner-side (flag 15) — old SuperBot removed from test guild 1350952413737259151 (REST 404 Unknown Member); `!` prefix now uncontested
- app-command sync: `SB_APPCMD_SYNC_GUILD_ID=1522099141671653417` now targets the new owner guild "Superbot Admin" (12 commands synced there; test guild retains the earlier 13) — deserves an owner confirm-of-intent line but is NOT a blocker
- env fact: `HEALTH_HOST` default `::` cannot bind in the build container (no IPv6); `HEALTH_HOST=127.0.0.1` required
- test guild id: present in docs (1350952413737259151)
- live-drive leg: DONE (#109, merge `b813324`)

⚑ OWNER-ACTION 2 — create the hello-plugin repo (flag 18a)
WHAT: Create one new empty GitHub repository named superbot-plugin-hello.
WHERE: https://github.com/new (owner account menno420).
HOW: name `superbot-plugin-hello`, public, no template — agents then move `examples/superbot-plugin-hello/` verbatim (pin hashes the manifest, not the repo — no re-pin needed).
WHY-IT-MATTERS: it proves a game plugin can live in its own repo, the architecture the mining/exploration game Projects will copy.
UNBLOCKS: ORDER 002 → done; the game Projects' reference pattern.
VERIFIED-NEEDED: attempted repo-create with the integration token — GitHub returns 403 on repo creation for this token class (recorded in docs/retro/project-review-2026-07-09.md §2 item 2); only the owner account can create repos.

⚑ OWNER-ACTION 3 — kill the branch-update merge dance
WHAT: Change the repo merge settings so PRs stop needing a manual "update branch" click before merging.
WHERE: github.com/menno420/superbot-next → Settings → Rules/Rulesets (or Settings → General → merge queue).
HOW: enable the merge queue, or drop the require-up-to-date rule for `docs/**` + `control/**` paths.
WHY-IT-MATTERS: every session lost time to the update-branch dance and one session's tail was stranded on it (PRs #86/#87), and the same dance triggered a rate-limit stall.
UNBLOCKS: unattended session wrap-ups; less API traffic.
VERIFIED-NEEDED: repo Settings/Rulesets are admin-only — agent tokens can read but not modify rulesets (the #86/#87 stranding is the captured evidence of the wall in effect); an agent re-verified it cannot edit the ruleset when un-stranding those PRs.

⚑ OWNER-ACTION 4 — RETIRED 2026-07-11 (was: configure Codex for this repo — environment + code-review capacity)
RESOLVED owner-side, both halves: the connector produced a FULL substantive review on #138 — comment 4941074976 (line-anchored walkthrough of all four changes; it ran the PR's 4 new tests itself, ✅) + verdict comment 4941104857 "Codex Review: Didn't find any major issues. Nice work!" (reviewed commit `77f9345`) — so the environment exists AND code-review capacity is restored. Historical evidence trail (kept for the record): "create an environment for this repo" on #117 (comment 4939224869) and #120 (comment 4939692286); "reached your Codex usage limits" on #124 (comment 4940004903), #125 (comment 4940095408) and #130 (comment 4940441924); no text reply on #114 (comment 4938835129) and #133 (comment 4940819831). The Q-0259/Q-0120 return path is now LIVE; the standing rule continues unchanged — @codex question on every substantive PR's final head, merge on green without waiting (Q-0258). REGRESSION NOTE (2026-07-11 late-night, folded at the 05:01Z heartbeat): the CAPACITY half is back at the wall — the connector answered BOTH band-7 questions with usage-limit replies: #148 comment 4942100526 ("You have reached your Codex usage limits") and #151 comment 4942514698 ("…usage limits for code reviews"). The environment half stays resolved (the #138 full review proved it exists), so this item stays RETIRED rather than reopened — but the owner may want to know the credit pool drained again the same day it was declared restored; until it refills, substantive questions keep landing per the standing rule and simply queue unanswered (the #148/#151 questions are in the open-questions note below). FLAP UPDATE (2026-07-11 morning, folded at the 06:35Z heartbeat): the pool REFILLED within the hour — one more usage-limit reply on #152 (comment 4942577962, 05:08Z), then FULL substantive P2 reviews on #154 (review 4676723779, 05:54Z, 2 P2s, final head `43b9b44`) and #157 (review 4676836079, 06:31Z, 1 P2, final head `f8532ef`). Item stays RETIRED; the capacity half is best described as FLAPPING — reviews arrive post-merge when the pool happens to be up (findings ledgered in the wave-5 records above).

⚑ OWNER-ACTION 5 — provide the band-7 AI key envelope (live-NL leg)
WHAT: Put a real `ANTHROPIC_API_KEY` in the agents' session environment and turn `AI_ENABLED` on for the live-drive guild.
WHERE: the Claude Code session environment the builder lanes run in (same place `DISCORD_BOT_TOKEN_PRODUCTION` already lives) — config seam sb/spec/config.py:166 (`ANTHROPIC_API_KEY`, SecretSpec, activation_link `ai.on_when_keyed`) and :148 (`AI_ENABLED`, default False, dormant posture).
HOW: export both vars into the session env (key value never recorded in-repo — env var NAME only, the §0.4 grant convention); optionally scope `AI_ENABLED=true` to the Superbot Admin live-drive guild posture.
WHY-IT-MATTERS: band 7's remaining legs — the live NL shell (mention→answer), `verify_and_regenerate_once`, and live routing — need a real model call to produce live-drive evidence; deterministic surfaces shipped regardless (#151), so this gates EVIDENCE, not code.
UNBLOCKS: the band-7 live-NL slice's ORDER 004 live-drive leg; until provided, those legs can only ship deterministic-provider tests (A-17 posture), not live-drive proof.
VERIFIED-NEEDED: verified during #151's ORDER 004 live drive — `ANTHROPIC_API_KEY` absent and `AI_ENABLED` off in the session env (PR #151 body, key-gap honesty section); the deterministic operator surface went live regardless (six real posts). An `OPENAI_API_KEY` exists in the sandbox env but arming NL on it was explicitly out of scope and the shipped default provider posture is deterministic.

carried (unchanged, full detail in the retro §2): sacrificial member (flag 9), settings-EDIT/setup-FLOW boundary (flags 14/17a), hub topology (flag 21), band-7 AI key envelope (GRADUATED to OWNER-ACTION 5 above at the 05:01Z heartbeat), owner hand-pass of presentation surfaces — incl. the band-4 human keystroke check (a real message should earn 15-25 XP on a 60s cooldown via !rank)
notes: (1) the prior session's parked blackjack/rps ensure-only follow-up is CLOSED by #114 — both register at import now, and the composition-parity invariant kills the whole class going forward; what remains of that thread is the 99-ref burn-down ledger (docs/ideas/ensure-only-registration-gaps-2026-07-10.md), each ref a live RefUnresolved envelope until its subsystem's port slice registers it at import and shrinks `_KNOWN_ENSURE_ONLY`. (2) codex return path OPENED at #138 but capacity REGRESSED same-day (usage-limit replies on #148/#151 — regression note in the OWNER-ACTION 4 record above); TWO new band-7 questions now queue unanswered: #148 comment 4942099573 (head `27b6333`, split-audience-by-surface — can a PanelSpec keep the slash response ephemeral while leaving the prefix panel click-open, or is that a ledger row) and #151 comment 4942514348 (head `16f9151`, the two replay-harness semantics changes — is there a third process-lifetime in-memory carrier besides the K10 conversation buffer that curated-first ordering or the unknown-slash drop could false-green). The SEVEN earlier questions predate the capacity restore and remain codex-unanswered: #112 comment 4938620657 (ref renumbering), #114 comment 4938835129 (head `9afa7c4`, composition-probe blind spots), #117 comment 4939223062 (session-view ingress bypass), #120 comment 4939690779 (head `e18a7f9`, refresh-seam wrong-message edit), #124 comment 4940003762 (head `645955a`, PvP double-settle race), #130 comment 4940441314 (head `f6a7796`, tournament free-branch double-payout race — SELF-ANSWERED: #133's check-and-set settle-once guard closed exactly that race in both games, double-call tests pin it), #133 comment 4940819831 (head `d9a27bd`, duplicate results-frame render — its cosmetic half CLOSED by #138's render-once guard; the teardown interleaving question folded into #138's own open question). #138's question (comment 4941075583, the rps settled-guard ALTITUDE — should the claim move into `advance_round`'s champion return to make stage decision + claim one atomic step) is the lane's one OPEN question: cheap successor hardening, not answered by the codex review (which predates it). Historical: #111's post-merge codex reply ("Didn't find any major issues", reviewed head `8faa5aca`, comment 4938542412) remains INPUT-not-verification; the `_unlock_at` stash-path check rides with the answers above. Earlier follow-up state unchanged: the #105/#108 compensator lineage is fully closed by #111's delete-if-match fold-in. (3) NEW hygiene flag from the games lane (phase-7 lane notes): a PRE-EXISTING test-order flake at CLEAN main — `pytest tests/unit/app tests/unit/authority tests/unit/band6/test_band6_rps_tournament.py` in that non-canonical order fails 3 pre-#138 band6 tests (earlier suites clear the spec-refs registries and `Harness._load_manifests` re-imports without re-arming ENSURE_REFS; the canonical full-suite order is green, CI unaffected). Not a #138 regression — verified at clean main before the slice; flag for a successor hygiene slice (the #138 tests already self-arm via `ensure_ops_refs() + register_ops()`, the band6 fixture idiom). CLOSED 2026-07-11 by #141 (merge `78509df`, band-7 lane record above): `register_ops()` in both games now ends with `ensure_ops_refs()`, all pytest orders green.
