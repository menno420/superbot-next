"""Role domain (band 5) — the shipped role-management family compiled
onto the kernel: feasibility → automation/xp_sync (pure planners) →
store → ops → service (ports + runtime cores) → panels/handlers.
"""
