"""Platform-control domain (band 5) — command-access policy (the K8
admission truth), the guild-teardown registry (guild_lifecycle.py
compiled), the consistency report, guild introspection/snapshot reads,
and the band-5 K10 task claims. The /control/* HTTP bridge (control_api
.py routes) is NAMED SUCCESSOR WORK — the CONTROL_API_TOKEN SecretSpec +
credential row landed with this band so the surface stays dormant-by-
default per Q-0156/Q-0159.
"""
