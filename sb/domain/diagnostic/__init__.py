"""Band 1 — the DIAGNOSTIC subsystem: the operator status/findings surface
(platform proves itself on itself). service.py aggregates the KERNEL truth
sources (lifecycle phase, operator findings ring, settings declarations,
the K10 AI diagnostics collector + decision-audit reads); panels.py renders
the hub. The shipped deep-diagnostic subcommand fleet (platform_group.py:
consistency/caches/backfill/anchors/...) re-homes surface-by-surface as
successor slices (D-0026)."""
