"""LOGGING subsystem (band 2, operator spine) — the server-logging routing
config + event fan-out engine (shipped `services/server_logging*`); module
named server_logging (stdlib-shadow discipline), manifest key = "logging"."""
