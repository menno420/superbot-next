"""ADMIN subsystem (band 2) — operator/process surfaces re-homed onto the
kernel truth sources (manifests replace cogs; lifecycle replaces process
kill). Extension management (cog/loadall/unloadall) and slash-tree push
(syncslash) are deploy-ops with no analog in the compiled architecture —
deliberately NOT ported (the sweep's excluded-with-reason class, D-0030)."""
