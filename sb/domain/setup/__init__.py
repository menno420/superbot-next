"""Band 1 — the SETUP subsystem skeleton (A-9: the wizard promoted to a
first-class roster line).

This slice lands the G-19 seam: the section REGISTRY (shipped validation
ported) + ALL 10 live registrants as WizardSectionSpec declarations
(labels/orders/op_kinds/emoji harvested VERBATIM from
disbot/views/setup/sections/* @7f7628e1) + the hub panel projected from the
registry + the setup.suggest/setup.explain legacy task claims.

The wizard LIFECYCLE (sections' interactive flows, recommended-ops,
customize -> preview -> apply over the K9 draft lane with
Producer.HUMAN_SETUP reserved-but-unconsumed per A-9(3), the quick/
essential presets, the AI advisor services) is successor work — each
section's `route` arms as its flow ports (None = declared, not yet routed;
the hub renders it as coming-soon rather than stranding)."""
