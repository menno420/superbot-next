"""Governance domain (band 5) — the shipped disbot/governance/ package
compiled onto the kernel: models → registry → cache → resolver →
cleanup → execution → ops/service/templates (the shipped layer order,
no circular imports). Tier taxonomy + role templates live in
sb/domain/governance/{tiers,role_templates}.py.
"""
