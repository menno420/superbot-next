# sim/records/ — deterministic run records (`sim/run.py --space <sim_id>`):
# winner, per-term breakdown, top-5 alternatives, input hashes, seed — the
# auditable "why it won". Committed per ratification; empty until port bands
# register spaces.
